<?xml version="1.0" encoding="UTF-8"?>
<!--
  This XML is a negative example because it uses a response processing template that prevents feedback from being displayed.

  REASON FOR BEING A NEGATIVE EXAMPLE:

  1.  CRITICAL: Ineffective Feedback Mechanism - The item uses `<qti-response-processing template=".../match_correct"/>`.
      While this template correctly scores the user's response (1 for correct, 0 for incorrect), it does NOT set the value of the `FEEDBACK`
      outcome variable that has been declared.
  2.  CRITICAL: Unused Feedback Blocks - The `<qti-feedback-block>` elements are designed to be shown or hidden based on the value
      of the `FEEDBACK` outcome variable (e.g., show when `FEEDBACK` is 'CORRECT'). Because the `match_correct` template never sets this variable,
      the feedback blocks will never be displayed to the user, regardless of whether their answer is right or wrong. This creates a poor
      learning experience as the authored feedback is inaccessible.
  3.  The correct implementation requires a full `<qti-response-processing>` block with a `<qti-response-condition>` that explicitly sets the `FEEDBACK`
      outcome variable to 'CORRECT' or 'INCORRECT'.
-->
<qti-assessment-item
    xmlns="http://www.imsglobal.org/xsd/imsqtiasi_v3p0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.imsglobal.org/xsd/imsqtiasi_v3p0 https://purl.imsglobal.org/spec/qti/v3p0/schema/xsd/imsqti_asiv3p0p1_v1p0.xsd http://www.w3.org/1998/Math/MathML https://purl.imsglobal.org/spec/mathml/v3p0/schema/xsd/mathml3.xsd"
    identifier="nice-tmp_x07b9a5265c40ba3a"
    title="Add 3 numbers"
    time-dependent="false"
    xml:lang="en-US">

    <qti-response-declaration identifier="RESPONSE" cardinality="single" base-type="integer">
        <qti-correct-response>
            <qti-value>15</qti-value>
        </qti-correct-response>
    </qti-response-declaration>

    <qti-outcome-declaration identifier="FEEDBACK" cardinality="single" base-type="identifier"/>

    <qti-item-body>
        <p><strong>How much money would you need to buy a bathing suit, a shovel, and a pail?</strong></p>
        <p>$ <qti-text-entry-interaction response-identifier="RESPONSE" expected-length="3"/></p>

        <div id="reference_text">
            <table>
                <thead>
                    <tr>
                        <th>Item</th>
                        <th>Price</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>Bathing suit</td>
                        <td>$12</td>
                    </tr>
                    <tr>
                        <td>Shovel</td>
                        <td>$1</td>
                    </tr>
                    <tr>
                        <td>Pail</td>
                        <td>$2</td>
                    </tr>
                </tbody>
            </table>
        </div>

        <qti-feedback-block outcome-identifier="FEEDBACK" identifier="CORRECT" show-hide="show">
            <qti-content-body>
                <p><strong>Correct!</strong> Adding $12 + $1 + $2 gives a total of $15.</p>
            </qti-content-body>
        </qti-feedback-block>
        <qti-feedback-block outcome-identifier="FEEDBACK" identifier="INCORRECT" show-hide="show">
            <qti-content-body>
                <p><strong>Not quite.</strong> Add the three prices: $12 for the bathing suit, $1 for the shovel, and $2 for the pail.</p>
            </qti-content-body>
        </qti-feedback-block>
    </qti-item-body>

    <qti-response-processing template="https://purl.imsglobal.org/spec/qti/v3p0/rptemplates/match_correct"/>

</qti-assessment-item>