// ============================================================================
// EXTRACTED TEST FILE - AUTO-GENERATED
// ============================================================================
// This file was automatically generated from database structured_json data
// Generated on: 2025-09-18T05:17:02.170Z
// Widget Type: integerChipLegend
// Source: Real questions from database
//
// These tests use actual widget configurations extracted from production data
// to ensure comprehensive test coverage with realistic scenarios.
//
// Note: Database data may not perfectly match current widget schemas, so these
// tests rely on runtime validation within generateWidget() for flexibility.
//
// DO NOT EDIT THIS FILE DIRECTLY - it will be overwritten on regeneration
//
// Run tests: bun test tests/widgets/integer-chip-legend.extracted.test.ts
// Update snapshots: bun test tests/widgets/integer-chip-legend.extracted.test.ts --update-snapshots
// ============================================================================

import { describe, test, expect } from "bun:test"
import { generateWidget } from "@superbuilders/qti-assessment-item-generator/widgets/widget-generator"
import type { Widget, WidgetInput } from "@superbuilders/qti-assessment-item-generator/widgets/registry"
import * as errors from "@superbuilders/errors"
import * as logger from "@superbuilders/slog"

describe("integerChipLegend widget tests", () => {
	// Extracted from question: x7aa00adbc102c655
	// Question: Complete the description using integer chips
	// Widget key: image_2
	test("integer-chip-legend - [x7aa00adbc102c655] Complete the description using integer chips", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= -1",
		  "positiveChipLabel": "= +1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x67c799a0f8d166e0
	// Question: Select two addition expressions for an integer-chip model
	// Widget key: image_1
	test("integer-chip-legend - [x67c799a0f8d166e0] Select two addition expressions for an integer-chip model", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= −1",
		  "positiveChipLabel": "= +1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2fba643be6fd9a4f
	// Question: Describe zero pairs with integer chips
	// Widget key: image_2
	test("integer-chip-legend - [x2fba643be6fd9a4f] Describe zero pairs with integer chips", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= -1",
		  "positiveChipLabel": "= +1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xea722bf49a523afb
	// Question: Count zero pairs with integer chips
	// Widget key: image_2
	test("integer-chip-legend - [xea722bf49a523afb] Count zero pairs with integer chips", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= -1",
		  "positiveChipLabel": "= +1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x272e8236dace3feb
	// Question: Identify two addition expressions from integer chips
	// Widget key: image_1
	test("integer-chip-legend - [x272e8236dace3feb] Identify two addition expressions from integer chips", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= -1",
		  "positiveChipLabel": "= +1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x9d8db87b91e88b8c
	// Question: Identify representations of 4 with integer chips
	// Widget key: image_1
	test("integer-chip-legend - [x9d8db87b91e88b8c] Identify representations of 4 with integer chips", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= -1",
		  "positiveChipLabel": "= +1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xce6b6daba4bd70f2
	// Question: Identify images representing −5 with integer chips
	// Widget key: image_1
	test("integer-chip-legend - [xce6b6daba4bd70f2] Identify images representing −5 with integer chips", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= -1",
		  "positiveChipLabel": "= +1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x12bc21f992922f93
	// Question: Select two addition expressions for integer chips
	// Widget key: image_1
	test("integer-chip-legend - [x12bc21f992922f93] Select two addition expressions for integer chips", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= -1",
		  "positiveChipLabel": "= +1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x44e48c43a1428bbb
	// Question: Identify images that represent −4 with integer chips
	// Widget key: image_1
	test("integer-chip-legend - [x44e48c43a1428bbb] Identify images that represent −4 with integer chips", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= -1",
		  "positiveChipLabel": "= +1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xbc4c331f3105dde6
	// Question: Identify images representing 3 with integer chips
	// Widget key: image_1
	test("integer-chip-legend - [xbc4c331f3105dde6] Identify images representing 3 with integer chips", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= -1",
		  "positiveChipLabel": "= +1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xcbde5d0699438e02
	// Question: Select two addition expressions for integer chips
	// Widget key: image_1
	test("integer-chip-legend - [xcbde5d0699438e02] Select two addition expressions for integer chips", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= -1",
		  "positiveChipLabel": "= 1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x9d9353ee8e346f8b
	// Question: Describe integer chips using zero pairs
	// Widget key: image_2
	test("integer-chip-legend - [x9d9353ee8e346f8b] Describe integer chips using zero pairs", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= -1",
		  "positiveChipLabel": "= +1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x90fc3f1e0d4d7df5
	// Question: Complete Sadio's subtraction equation
	// Widget key: image_1
	test("integer-chip-legend - [x90fc3f1e0d4d7df5] Complete Sadio's subtraction equation", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= -1",
		  "positiveChipLabel": "= 1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x64d89f0e4439fb60
	// Question: Complete a subtraction equation using integer chips
	// Widget key: image_1
	test("integer-chip-legend - [x64d89f0e4439fb60] Complete a subtraction equation using integer chips", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "-1",
		  "positiveChipLabel": "1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x3d4691f41a65dd92
	// Question: Complete a subtraction equation with integer chips
	// Widget key: image_1
	test("integer-chip-legend - [x3d4691f41a65dd92] Complete a subtraction equation with integer chips", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= -1",
		  "positiveChipLabel": "= +1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x1e243c8fd876e549
	// Question: Complete a subtraction equation using integer chips
	// Widget key: image_2
	test("integer-chip-legend - [x1e243c8fd876e549] Complete a subtraction equation using integer chips", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= -1",
		  "positiveChipLabel": "= +1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x430e12f4669434a9
	// Question: Complete a subtraction equation from integer chips
	// Widget key: image_2
	test("integer-chip-legend - [x430e12f4669434a9] Complete a subtraction equation from integer chips", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= -1",
		  "positiveChipLabel": "= 1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xc35b5bc8f3c70b52
	// Question: Complete a subtraction equation using integer chips
	// Widget key: image_2
	test("integer-chip-legend - [xc35b5bc8f3c70b52] Complete a subtraction equation using integer chips", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= -1",
		  "positiveChipLabel": "= +1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x70e0a638cd207a9a
	// Question: Complete a subtraction equation with integer chips
	// Widget key: image_2
	test("integer-chip-legend - [x70e0a638cd207a9a] Complete a subtraction equation with integer chips", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= -1",
		  "positiveChipLabel": "= +1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x79c70ee1b89ca5b7
	// Question: Complete a subtraction equation from integer chips
	// Widget key: image_2
	test("integer-chip-legend - [x79c70ee1b89ca5b7] Complete a subtraction equation from integer chips", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= -1",
		  "positiveChipLabel": "= +1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x73ef0a898e35d01b
	// Question: Complete a subtraction equation from integer chips
	// Widget key: image_2
	test("integer-chip-legend - [x73ef0a898e35d01b] Complete a subtraction equation from integer chips", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "-1",
		  "positiveChipLabel": "1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xc7e216e925e15870
	// Question: Complete a subtraction equation with integer chips
	// Widget key: image_1
	test("integer-chip-legend - [xc7e216e925e15870] Complete a subtraction equation with integer chips", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= -1",
		  "positiveChipLabel": "= +1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xc7cbbb65f42f202f
	// Question: Complete Mark's subtraction equation
	// Widget key: image_1
	test("integer-chip-legend - [xc7cbbb65f42f202f] Complete Mark's subtraction equation", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= -1",
		  "positiveChipLabel": "= +1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x25a33d41a493d308
	// Question: Complete a subtraction equation using integer chips
	// Widget key: image_2
	test("integer-chip-legend - [x25a33d41a493d308] Complete a subtraction equation using integer chips", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= -1",
		  "positiveChipLabel": "= +1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x0a960fe3aa62c90b
	// Question: Complete Tegan's subtraction equation
	// Widget key: image_1
	test("integer-chip-legend - [x0a960fe3aa62c90b] Complete Tegan's subtraction equation", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= -1",
		  "positiveChipLabel": "= 1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xb148748f6fa7aa4a
	// Question: Complete Talia's subtraction equation with integer chips
	// Widget key: image_1
	test("integer-chip-legend - [xb148748f6fa7aa4a] Complete Talia's subtraction equation with integer chips", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= -1",
		  "positiveChipLabel": "= +1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x1c3306d25afbe895
	// Question: Complete Keala's subtraction equation
	// Widget key: image_1
	test("integer-chip-legend - [x1c3306d25afbe895] Complete Keala's subtraction equation", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= -1",
		  "positiveChipLabel": "= +1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x0dd0a6268dda5727
	// Question: Complete a subtraction equation using integer chips
	// Widget key: image_2
	test("integer-chip-legend - [x0dd0a6268dda5727] Complete a subtraction equation using integer chips", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= -1",
		  "positiveChipLabel": "= +1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x5843c3c943826513
	// Question: Complete a subtraction equation using integer chips
	// Widget key: image_2
	test("integer-chip-legend - [x5843c3c943826513] Complete a subtraction equation using integer chips", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= -1",
		  "positiveChipLabel": "= +1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x67ed821473e88c42
	// Question: Complete a subtraction equation from integer chips
	// Widget key: image_2
	test("integer-chip-legend - [x67ed821473e88c42] Complete a subtraction equation from integer chips", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= -1",
		  "positiveChipLabel": "= +1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x41f7fa28a8955027
	// Question: Complete a subtraction equation with integer chips
	// Widget key: image_1
	test("integer-chip-legend - [x41f7fa28a8955027] Complete a subtraction equation with integer chips", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= -1",
		  "positiveChipLabel": "= +1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x8e714f7af63bbbcc
	// Question: Complete a subtraction equation with integer chips
	// Widget key: image_1
	test("integer-chip-legend - [x8e714f7af63bbbcc] Complete a subtraction equation with integer chips", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "integerChipLegend",
		  "negativeChipLabel": "= -1",
		  "positiveChipLabel": "= 1"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for integerChipLegend", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

})
