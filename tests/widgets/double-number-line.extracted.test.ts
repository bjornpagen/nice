// ============================================================================
// EXTRACTED TEST FILE - AUTO-GENERATED
// ============================================================================
// This file was automatically generated from database structured_json data
// Generated on: 2025-09-01T22:43:40.166Z
// Widget Type: doubleNumberLine
// Source: Real questions from database
//
// These tests use actual widget configurations extracted from production data
// to ensure comprehensive test coverage with realistic scenarios.
//
// DO NOT EDIT THIS FILE DIRECTLY - it will be overwritten on regeneration
//
// Run tests: bun test tests/widgets/double-number-line.extracted.test.ts
// Update snapshots: bun test tests/widgets/double-number-line.extracted.test.ts --update-snapshots
// ============================================================================

import { expect, test } from "bun:test"
import type { z } from "zod"
import { generateDoubleNumberLine, DoubleNumberLinePropsSchema } from "@/lib/widgets/generators"

type DoubleNumberLineInput = z.input<typeof DoubleNumberLinePropsSchema>


// Extracted from question: x6b9cc22db794915a
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: image_1
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Cherries, kilograms",
		"ticks": [
			0,
			"",
			"",
			"",
			4
		]
	},
	"bottomLine": {
		"label": "Cost, dollars",
		"ticks": [
			0,
			"",
			"",
			"",
			22
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x6b9cc22db794915a
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: choice_a_dnl
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Cherries, kilograms",
		"ticks": [
			0,
			1,
			2,
			3,
			4
		]
	},
	"bottomLine": {
		"label": "Cost, dollars",
		"ticks": [
			0,
			5.5,
			11,
			16.5,
			22
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x6b9cc22db794915a
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: choice_b_dnl
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Cherries, kilograms",
		"ticks": [
			0,
			1,
			2,
			3,
			4
		]
	},
	"bottomLine": {
		"label": "Cost, dollars",
		"ticks": [
			0,
			6,
			11.5,
			17,
			22
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x5fa5b5e029e54e5e
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: image_1
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Candy, pieces",
		"ticks": [
			0,
			"",
			"",
			"",
			""
		]
	},
	"bottomLine": {
		"label": "Toys",
		"ticks": [
			0,
			"",
			"",
			"",
			""
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x5fa5b5e029e54e5e
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: choice_a_dnl
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Candy, pieces",
		"ticks": [
			0,
			2,
			4,
			6,
			8
		]
	},
	"bottomLine": {
		"label": "Toys",
		"ticks": [
			0,
			3,
			6,
			9,
			12
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x5fa5b5e029e54e5e
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: choice_b_dnl
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Candy, pieces",
		"ticks": [
			0,
			3,
			6,
			9,
			12
		]
	},
	"bottomLine": {
		"label": "Toys",
		"ticks": [
			0,
			2,
			4,
			6,
			8
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xfde66648c76fee89
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: image_1
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Fat, grams",
		"ticks": [
			"0",
			"",
			"",
			"",
			""
		]
	},
	"bottomLine": {
		"label": "Protein, grams",
		"ticks": [
			"0",
			"",
			"",
			"",
			""
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xfde66648c76fee89
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: choice_a_dnl
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Fat, grams",
		"ticks": [
			0,
			2,
			4,
			6,
			8
		]
	},
	"bottomLine": {
		"label": "Protein, grams",
		"ticks": [
			0,
			11,
			22,
			33,
			44
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xfde66648c76fee89
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: choice_b_dnl
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Fat, grams",
		"ticks": [
			0,
			2,
			4,
			6,
			8
		]
	},
	"bottomLine": {
		"label": "Protein, grams",
		"ticks": [
			0,
			11,
			24,
			37,
			50
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x927b8bcd7c6456a5
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: image_1
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Pies",
		"ticks": [
			0,
			"",
			"",
			"",
			4
		]
	},
	"bottomLine": {
		"label": "Apples, kilograms",
		"ticks": [
			0,
			"",
			"",
			"",
			14
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x927b8bcd7c6456a5
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: choice_a_dnl
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Pies",
		"ticks": [
			0,
			1,
			2,
			3,
			4
		]
	},
	"bottomLine": {
		"label": "Apples, kilograms",
		"ticks": [
			0,
			3.5,
			7,
			10.5,
			14
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x927b8bcd7c6456a5
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: choice_b_dnl
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Pies",
		"ticks": [
			0,
			1,
			2,
			3,
			4
		]
	},
	"bottomLine": {
		"label": "Apples, kilograms",
		"ticks": [
			0,
			3,
			6,
			9,
			14
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe41317a0ad0df775
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: image_1
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Hours",
		"ticks": [
			0,
			"",
			"",
			"",
			4
		]
	},
	"bottomLine": {
		"label": "Cranes",
		"ticks": [
			0,
			"",
			"",
			"",
			28
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe41317a0ad0df775
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: choice_a_dnl
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Hours",
		"ticks": [
			0,
			1,
			2,
			3,
			4
		]
	},
	"bottomLine": {
		"label": "Cranes",
		"ticks": [
			0,
			7,
			13,
			20,
			28
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe41317a0ad0df775
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: choice_b_dnl
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Hours",
		"ticks": [
			0,
			1,
			2,
			3,
			4
		]
	},
	"bottomLine": {
		"label": "Cranes",
		"ticks": [
			0,
			7,
			14,
			21,
			28
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xc57d751010264518
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: image_1
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Food coupons",
		"ticks": [
			"0",
			"",
			"",
			"",
			""
		]
	},
	"bottomLine": {
		"label": "Raffle tickets",
		"ticks": [
			"0",
			"",
			"",
			"",
			""
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xc57d751010264518
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: choice_a_dnl
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Food coupons",
		"ticks": [
			0,
			3,
			6,
			9,
			12
		]
	},
	"bottomLine": {
		"label": "Raffle tickets",
		"ticks": [
			0,
			5,
			10,
			15,
			20
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xc57d751010264518
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: choice_b_dnl
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Food coupons",
		"ticks": [
			0,
			3,
			6,
			9,
			12
		]
	},
	"bottomLine": {
		"label": "Raffle tickets",
		"ticks": [
			0,
			5,
			13,
			21,
			29
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x574891a3db7a916f
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: image_1
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Beads",
		"ticks": [
			0,
			"",
			"",
			"",
			""
		]
	},
	"bottomLine": {
		"label": "Ribbons",
		"ticks": [
			0,
			"",
			"",
			"",
			""
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x574891a3db7a916f
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: choice_a_dnl
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Beads",
		"ticks": [
			0,
			6,
			12,
			18,
			24
		]
	},
	"bottomLine": {
		"label": "Ribbons",
		"ticks": [
			0,
			7,
			14,
			21,
			28
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x574891a3db7a916f
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: choice_b_dnl
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Beads",
		"ticks": [
			0,
			7,
			14,
			21,
			28
		]
	},
	"bottomLine": {
		"label": "Ribbons",
		"ticks": [
			0,
			6,
			12,
			18,
			24
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x3fd9ae08b0f9c4b8
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: image_1
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Time, minutes",
		"ticks": [
			0,
			"",
			"",
			3,
			""
		]
	},
	"bottomLine": {
		"label": "People",
		"ticks": [
			0,
			"",
			"",
			18,
			""
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x3fd9ae08b0f9c4b8
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: choice_a_dnl
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Time, minutes",
		"ticks": [
			0,
			1,
			2,
			3,
			4
		]
	},
	"bottomLine": {
		"label": "People",
		"ticks": [
			0,
			6,
			14,
			18,
			26
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x3fd9ae08b0f9c4b8
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: choice_b_dnl
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Time, minutes",
		"ticks": [
			0,
			1,
			2,
			3,
			4
		]
	},
	"bottomLine": {
		"label": "People",
		"ticks": [
			0,
			6,
			12,
			18,
			24
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe3cd3120e416ae1e
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: image_1
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Almonds, grams",
		"ticks": [
			0,
			"",
			"",
			"",
			""
		]
	},
	"bottomLine": {
		"label": "Raisins, grams",
		"ticks": [
			0,
			"",
			"",
			"",
			""
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe3cd3120e416ae1e
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: choice_a_dnl
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Almonds, grams",
		"ticks": [
			0,
			40,
			70,
			100,
			130
		]
	},
	"bottomLine": {
		"label": "Raisins, grams",
		"ticks": [
			0,
			30,
			60,
			90,
			120
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe3cd3120e416ae1e
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: choice_b_dnl
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Almonds, grams",
		"ticks": [
			0,
			40,
			80,
			120,
			160
		]
	},
	"bottomLine": {
		"label": "Raisins, grams",
		"ticks": [
			0,
			30,
			60,
			90,
			120
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x22009b1c4482a866
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: image_1
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Side dishes",
		"ticks": [
			0,
			"",
			"",
			"",
			""
		]
	},
	"bottomLine": {
		"label": "Porg, pieces",
		"ticks": [
			0,
			"",
			"",
			"",
			""
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x22009b1c4482a866
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: choice_a_dnl
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Side dishes",
		"ticks": [
			0,
			3,
			6,
			9,
			12
		]
	},
	"bottomLine": {
		"label": "Porg, pieces",
		"ticks": [
			0,
			1,
			2,
			3,
			4
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x22009b1c4482a866
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: choice_b_dnl
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Side dishes",
		"ticks": [
			0,
			3,
			6,
			9,
			12
		]
	},
	"bottomLine": {
		"label": "Porg, pieces",
		"ticks": [
			0,
			10,
			20,
			30,
			40
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x38761d74178ec71b
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: image_1
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Packages",
		"ticks": [
			0,
			"",
			"",
			3,
			""
		]
	},
	"bottomLine": {
		"label": "Pencils",
		"ticks": [
			0,
			"",
			"",
			36,
			""
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x38761d74178ec71b
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: choice_a_dnl
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Packages",
		"ticks": [
			0,
			1,
			2,
			3,
			4
		]
	},
	"bottomLine": {
		"label": "Pencils",
		"ticks": [
			0,
			12,
			24,
			36,
			48
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x38761d74178ec71b
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: choice_b_dnl
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Packages",
		"ticks": [
			0,
			1,
			2,
			3,
			4
		]
	},
	"bottomLine": {
		"label": "Pencils",
		"ticks": [
			0,
			10,
			22,
			36,
			52
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x9772709f09b87c69
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: image_1
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Distance, kilometers",
		"ticks": [
			0,
			"",
			"",
			3,
			""
		]
	},
	"bottomLine": {
		"label": "Elevation, meters",
		"ticks": [
			0,
			"",
			"",
			120,
			""
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x9772709f09b87c69
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: choice_a_dnl
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Distance, kilometers",
		"ticks": [
			0,
			1,
			2,
			3,
			4
		]
	},
	"bottomLine": {
		"label": "Elevation, meters",
		"ticks": [
			0,
			40,
			80,
			120,
			160
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x9772709f09b87c69
// Course: 6th grade math
// Exercise: Create double number lines
// Widget key: choice_b_dnl
test("double-number-line - Create double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Distance, kilometers",
		"ticks": [
			0,
			1,
			2,
			3,
			4
		]
	},
	"bottomLine": {
		"label": "Elevation, meters",
		"ticks": [
			0,
			80,
			100,
			120,
			140
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x54239abcf23f600e
// Course: 6th grade math
// Exercise: Ratios with double number lines
// Widget key: image_1
test("double-number-line - Ratios with double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Time, seconds",
		"ticks": [
			"0",
			"",
			"",
			"3",
			"",
			""
		]
	},
	"bottomLine": {
		"label": "Distance, meters",
		"ticks": [
			"0",
			"",
			"",
			"63",
			"",
			""
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xf4cbfb52eba71ff8
// Course: 6th grade math
// Exercise: Ratios with double number lines
// Widget key: image_1
test("double-number-line - Ratios with double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Almonds, kilograms",
		"ticks": [
			0,
			"",
			"",
			"",
			4,
			""
		]
	},
	"bottomLine": {
		"label": "Cost, dollars",
		"ticks": [
			0,
			"",
			"",
			"",
			34,
			""
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2f3f556a6429f6af
// Course: 6th grade math
// Exercise: Ratios with double number lines
// Widget key: image_1
test("double-number-line - Ratios with double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Ore cards",
		"ticks": [
			0,
			"",
			"",
			3,
			"",
			""
		]
	},
	"bottomLine": {
		"label": "Wool cards",
		"ticks": [
			0,
			"",
			"",
			6,
			"",
			""
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe44e04923c702180
// Course: 6th grade math
// Exercise: Ratios with double number lines
// Widget key: image_1
test("double-number-line - Ratios with double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Time, seconds",
		"ticks": [
			0,
			"",
			"",
			"",
			4
		]
	},
	"bottomLine": {
		"label": "Distance, meters",
		"ticks": [
			0,
			"",
			"",
			"",
			36
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x07f195bee2c0bda7
// Course: 6th grade math
// Exercise: Ratios with double number lines
// Widget key: image_1
test("double-number-line - Ratios with double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Bottles",
		"ticks": [
			0,
			"",
			"",
			"",
			4,
			""
		]
	},
	"bottomLine": {
		"label": "Cost (dollars)",
		"ticks": [
			0,
			"",
			"",
			"",
			10,
			""
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x138ae523177de3a9
// Course: 6th grade math
// Exercise: Ratios with double number lines
// Widget key: image_1
test("double-number-line - Ratios with double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Meat, kilograms",
		"ticks": [
			0,
			"",
			"",
			"",
			"",
			5
		]
	},
	"bottomLine": {
		"label": "Time, hours",
		"ticks": [
			0,
			"",
			"",
			"",
			"",
			12
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x7844686c0457981c
// Course: 6th grade math
// Exercise: Ratios with double number lines
// Widget key: image_1
test("double-number-line - Ratios with double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Avocados, kilograms",
		"ticks": [
			0,
			"",
			"",
			"",
			"",
			5
		]
	},
	"bottomLine": {
		"label": "Cost, dollars",
		"ticks": [
			0,
			"",
			"",
			"",
			"",
			9
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe19151859974d3e4
// Course: 6th grade math
// Exercise: Ratios with double number lines
// Widget key: image_1
test("double-number-line - Ratios with double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Time, minutes",
		"ticks": [
			0,
			"",
			"",
			"",
			4,
			""
		]
	},
	"bottomLine": {
		"label": "Length, centimeters",
		"ticks": [
			0,
			"",
			"",
			"",
			7,
			""
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xbab7adf7ced6d186
// Course: 6th grade math
// Exercise: Ratios with double number lines
// Widget key: image_1
test("double-number-line - Ratios with double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Time, minutes",
		"ticks": [
			0,
			"",
			2,
			"",
			""
		]
	},
	"bottomLine": {
		"label": "Fetches",
		"ticks": [
			0,
			"",
			6,
			"",
			""
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x0d90ea044261bf41
// Course: 6th grade math
// Exercise: Ratios with double number lines
// Widget key: image_1
test("double-number-line - Ratios with double number lines", async () => {
	const input = {
	"width": 600,
	"height": 200,
	"topLine": {
		"label": "Time, minutes",
		"ticks": [
			0,
			"",
			"",
			3,
			"",
			""
		]
	},
	"bottomLine": {
		"label": "Items",
		"ticks": [
			0,
			"",
			"",
			150,
			"",
			""
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x4bc31ddb064e19fc
// Course: 6th grade math
// Exercise: Ratios with double number lines
// Widget key: image_1
test("double-number-line - Ratios with double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Shirts",
		"ticks": [
			0,
			"",
			2,
			"",
			"",
			""
		]
	},
	"bottomLine": {
		"label": "Time, seconds",
		"ticks": [
			0,
			"",
			28,
			"",
			"",
			""
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x45121d74cc801fc1
// Course: 6th grade math
// Exercise: Ratios with double number lines
// Widget key: image_1
test("double-number-line - Ratios with double number lines", async () => {
	const input = {
	"width": 280,
	"height": 120,
	"topLine": {
		"label": "Time, minutes",
		"ticks": [
			0,
			"",
			2,
			"",
			"",
			""
		]
	},
	"bottomLine": {
		"label": "Words",
		"ticks": [
			0,
			"",
			180,
			"",
			"",
			""
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xcb698f8548e176bc
// Course: 6th grade math
// Exercise: Relate double number lines and ratio tables
// Widget key: image_1
test("double-number-line - Relate double number lines and ratio tables", async () => {
	const input = {
	"width": 350,
	"height": 120,
	"topLine": {
		"label": "Cupcakes",
		"ticks": [
			0,
			1
		]
	},
	"bottomLine": {
		"label": "Price (dollars)",
		"ticks": [
			0,
			1.5
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x473b036aa45f6284
// Course: 6th grade math
// Exercise: Relate double number lines and ratio tables
// Widget key: image_1
test("double-number-line - Relate double number lines and ratio tables", async () => {
	const input = {
	"width": 330,
	"height": 120,
	"topLine": {
		"label": "Time (days)",
		"ticks": [
			0,
			1
		]
	},
	"bottomLine": {
		"label": "Butter (kg)",
		"ticks": [
			0,
			20
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xf01dc76ff2ba461f
// Course: 6th grade math
// Exercise: Relate double number lines and ratio tables
// Widget key: image_1
test("double-number-line - Relate double number lines and ratio tables", async () => {
	const input = {
	"width": 350,
	"height": 120,
	"topLine": {
		"label": "Time (minutes)",
		"ticks": [
			0,
			1
		]
	},
	"bottomLine": {
		"label": "Snowballs",
		"ticks": [
			0,
			12
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x7e43407413526d13
// Course: 6th grade math
// Exercise: Relate double number lines and ratio tables
// Widget key: image_1
test("double-number-line - Relate double number lines and ratio tables", async () => {
	const input = {
	"width": 350,
	"height": 120,
	"topLine": {
		"label": "Adults",
		"ticks": [
			0,
			1
		]
	},
	"bottomLine": {
		"label": "Children",
		"ticks": [
			0,
			6
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x3c0d5acf2d120eb5
// Course: 6th grade math
// Exercise: Relate double number lines and ratio tables
// Widget key: image_1
test("double-number-line - Relate double number lines and ratio tables", async () => {
	const input = {
	"width": 350,
	"height": 200,
	"topLine": {
		"label": "Loaves",
		"ticks": [
			0,
			1,
			2,
			4
		]
	},
	"bottomLine": {
		"label": "Bananas",
		"ticks": [
			0,
			"2 1/2",
			5,
			10
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xaa634690def2dac3
// Course: 6th grade math
// Exercise: Relate double number lines and ratio tables
// Widget key: image_1
test("double-number-line - Relate double number lines and ratio tables", async () => {
	const input = {
	"width": 350,
	"height": 120,
	"topLine": {
		"label": "Time (seconds)",
		"ticks": [
			0,
			1
		]
	},
	"bottomLine": {
		"label": "Distance (meters)",
		"ticks": [
			0,
			13
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x638d18acba0e3b22
// Course: 6th grade math
// Exercise: Relate double number lines and ratio tables
// Widget key: image_1
test("double-number-line - Relate double number lines and ratio tables", async () => {
	const input = {
	"width": 350,
	"height": 120,
	"topLine": {
		"label": "Games",
		"ticks": [
			0,
			1
		]
	},
	"bottomLine": {
		"label": "Cost (dollars)",
		"ticks": [
			0,
			14
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x14ac2f13d6151c6d
// Course: 6th grade math
// Exercise: Relate double number lines and ratio tables
// Widget key: image_1
test("double-number-line - Relate double number lines and ratio tables", async () => {
	const input = {
	"width": 350,
	"height": 120,
	"topLine": {
		"label": "Time (seconds)",
		"ticks": [
			0,
			1
		]
	},
	"bottomLine": {
		"label": "Distance (meters)",
		"ticks": [
			0,
			25
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x88026e2b66de923c
// Course: 6th grade math
// Exercise: Relate double number lines and ratio tables
// Widget key: image_1
test("double-number-line - Relate double number lines and ratio tables", async () => {
	const input = {
	"width": 330,
	"height": 120,
	"topLine": {
		"label": "Weeks",
		"ticks": [
			0,
			1
		]
	},
	"bottomLine": {
		"label": "Model trains",
		"ticks": [
			0,
			9
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x6cc2d446ffdea7fc
// Course: 6th grade math
// Exercise: Relate double number lines and ratio tables
// Widget key: image_1
test("double-number-line - Relate double number lines and ratio tables", async () => {
	const input = {
	"width": 350,
	"height": 120,
	"topLine": {
		"label": "Eggs",
		"ticks": [
			0,
			1
		]
	},
	"bottomLine": {
		"label": "Time (hours)",
		"ticks": [
			0,
			25
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x04735dbf7012c762
// Course: 6th grade math
// Exercise: Relate double number lines and ratio tables
// Widget key: image_1
test("double-number-line - Relate double number lines and ratio tables", async () => {
	const input = {
	"width": 350,
	"height": 120,
	"topLine": {
		"label": "Rows",
		"ticks": [
			0,
			1
		]
	},
	"bottomLine": {
		"label": "Tomato plants",
		"ticks": [
			0,
			8
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x572c5bfe04734de1
// Course: 6th grade math
// Exercise: Relate double number lines and ratio tables
// Widget key: image_1
test("double-number-line - Relate double number lines and ratio tables", async () => {
	const input = {
	"width": 330,
	"height": 120,
	"topLine": {
		"label": "Chocolate (kg)",
		"ticks": [
			0,
			1
		]
	},
	"bottomLine": {
		"label": "Cost (dollars)",
		"ticks": [
			0,
			4
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x88dee6d57c0f1dc0
// Course: 6th grade math
// Exercise: Ratios and units of measurement
// Widget key: image_1
test("double-number-line - Ratios and units of measurement", async () => {
	const input = {
	"width": 330,
	"height": 120,
	"topLine": {
		"label": "Feet",
		"ticks": [
			"0",
			"",
			"10,560",
			""
		]
	},
	"bottomLine": {
		"label": "Miles",
		"ticks": [
			"0",
			"",
			"2",
			""
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xbff764969a70b964
// Course: 6th grade math
// Exercise: Ratios and units of measurement
// Widget key: image_1
test("double-number-line - Ratios and units of measurement", async () => {
	const input = {
	"width": 330,
	"height": 120,
	"topLine": {
		"label": "Cups",
		"ticks": [
			0,
			"",
			"",
			"",
			64
		]
	},
	"bottomLine": {
		"label": "Gallons",
		"ticks": [
			0,
			"",
			"",
			"",
			4
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xbcea461ece156510
// Course: 6th grade math
// Exercise: Ratios and units of measurement
// Widget key: image_1
test("double-number-line - Ratios and units of measurement", async () => {
	const input = {
	"width": 330,
	"height": 120,
	"topLine": {
		"label": "Yards",
		"ticks": [
			"0",
			"",
			"",
			"",
			"",
			"8,800"
		]
	},
	"bottomLine": {
		"label": "Miles",
		"ticks": [
			"0",
			"",
			"",
			"",
			"",
			"5"
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x665684bb4edf0685
// Course: 6th grade math
// Exercise: Ratios and units of measurement
// Widget key: image_1
test("double-number-line - Ratios and units of measurement", async () => {
	const input = {
	"width": 330,
	"height": 120,
	"topLine": {
		"label": "Minutes",
		"ticks": [
			"0",
			"",
			"",
			"4,320",
			""
		]
	},
	"bottomLine": {
		"label": "Days",
		"ticks": [
			"0",
			"",
			"",
			"3",
			""
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xb924926a93d7095a
// Course: 6th grade math
// Exercise: Find percents visually
// Widget key: image_1
test("double-number-line - Find percents visually", async () => {
	const input = {
	"width": 350,
	"height": 120,
	"topLine": {
		"label": "Banjos",
		"ticks": [
			0,
			15
		]
	},
	"bottomLine": {
		"label": "Percentage",
		"ticks": [
			"0 percent",
			"100 percent"
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x47ae021cf3a00c6a
// Course: 6th grade math
// Exercise: Find percents visually
// Widget key: image_1
test("double-number-line - Find percents visually", async () => {
	const input = {
	"width": 350,
	"height": 120,
	"topLine": {
		"label": "Distance, meters",
		"ticks": [
			0,
			700
		]
	},
	"bottomLine": {
		"label": "Percentage",
		"ticks": [
			"0 percent",
			"100 percent"
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x8d6317e3903a3e5f
// Course: 6th grade math
// Exercise: Find percents visually
// Widget key: image_1
test("double-number-line - Find percents visually", async () => {
	const input = {
	"width": 350,
	"height": 120,
	"topLine": {
		"label": "Time, seconds",
		"ticks": [
			0,
			36
		]
	},
	"bottomLine": {
		"label": "Percentage",
		"ticks": [
			"0 percent",
			"100 percent"
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xc185efc8b0fda786
// Course: 6th grade math
// Exercise: Find percents visually
// Widget key: image_1
test("double-number-line - Find percents visually", async () => {
	const input = {
	"width": 350,
	"height": 120,
	"topLine": {
		"label": "Time, days",
		"ticks": [
			0,
			350
		]
	},
	"bottomLine": {
		"label": "Percentage",
		"ticks": [
			"0 percent",
			"100 percent"
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xb23c55e46078e804
// Course: 6th grade math
// Exercise: Find percents visually
// Widget key: image_1
test("double-number-line - Find percents visually", async () => {
	const input = {
	"width": 350,
	"height": 120,
	"topLine": {
		"label": "People",
		"ticks": [
			0,
			42
		]
	},
	"bottomLine": {
		"label": "Percentage",
		"ticks": [
			"0 percent",
			"100 percent"
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x94a2f9d2a9675051
// Course: 6th grade math
// Exercise: Find percents visually
// Widget key: image_1
test("double-number-line - Find percents visually", async () => {
	const input = {
	"width": 350,
	"height": 120,
	"topLine": {
		"label": "Age, years",
		"ticks": [
			0,
			24
		]
	},
	"bottomLine": {
		"label": "Percentage",
		"ticks": [
			"0 percent",
			"75 percent"
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x17d286c5fd268ced
// Course: 6th grade math
// Exercise: Find percents visually
// Widget key: image_1
test("double-number-line - Find percents visually", async () => {
	const input = {
	"width": 350,
	"height": 120,
	"topLine": {
		"label": "People",
		"ticks": [
			0,
			270
		]
	},
	"bottomLine": {
		"label": "Percentage",
		"ticks": [
			"0 percent",
			"125 percent"
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xda750be488d145af
// Course: 6th grade math
// Exercise: Find percents visually
// Widget key: image_1
test("double-number-line - Find percents visually", async () => {
	const input = {
	"width": 350,
	"height": 120,
	"topLine": {
		"label": "Distance, meters",
		"ticks": [
			0,
			28
		]
	},
	"bottomLine": {
		"label": "Percentage",
		"ticks": [
			"0 percent",
			"80 percent"
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x97905bb1dc74d640
// Course: 6th grade math
// Exercise: Find percents visually
// Widget key: image_1
test("double-number-line - Find percents visually", async () => {
	const input = {
	"width": 350,
	"height": 120,
	"topLine": {
		"label": "Mass, kilograms",
		"ticks": [
			0,
			60
		]
	},
	"bottomLine": {
		"label": "Percentage",
		"ticks": [
			"0 percent",
			"100 percent"
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xb59c6a60c50f9d85
// Course: 6th grade math
// Exercise: Find percents visually
// Widget key: image_1
test("double-number-line - Find percents visually", async () => {
	const input = {
	"width": 350,
	"height": 120,
	"topLine": {
		"label": "Games",
		"ticks": [
			0,
			250
		]
	},
	"bottomLine": {
		"label": "Percentage",
		"ticks": [
			"0%",
			"100%"
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xfe723c069c0378ec
// Course: 6th grade math
// Exercise: Find percents visually
// Widget key: image_1
test("double-number-line - Find percents visually", async () => {
	const input = {
	"width": 350,
	"height": 120,
	"topLine": {
		"label": "Distance, kilometers",
		"ticks": [
			0,
			48
		]
	},
	"bottomLine": {
		"label": "Percentage",
		"ticks": [
			0,
			100
		]
	},
	"type": "doubleNumberLine"
} satisfies DoubleNumberLineInput

	// Validate the input
	const parseResult = DoubleNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted doubleNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateDoubleNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})

