// ============================================================================
// EXTRACTED TEST FILE - AUTO-GENERATED
// ============================================================================
// This file was automatically generated from database structured_json data
// Generated on: 2025-09-01T22:43:40.168Z
// Widget Type: unitBlockDiagram
// Source: Real questions from database
//
// These tests use actual widget configurations extracted from production data
// to ensure comprehensive test coverage with realistic scenarios.
//
// DO NOT EDIT THIS FILE DIRECTLY - it will be overwritten on regeneration
//
// Run tests: bun test tests/widgets/unit-block-diagram.extracted.test.ts
// Update snapshots: bun test tests/widgets/unit-block-diagram.extracted.test.ts --update-snapshots
// ============================================================================

import { expect, test } from "bun:test"
import type { z } from "zod"
import { generateUnitBlockDiagram, UnitBlockDiagramPropsSchema } from "@/lib/widgets/generators"

type UnitBlockDiagramInput = z.input<typeof UnitBlockDiagramPropsSchema>


// Extracted from question: xe30a4e0b83bed388
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_1
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 100,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe30a4e0b83bed388
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_2
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 19,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xff61678fdd7a224b
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_1
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 100,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xff61678fdd7a224b
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_2
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 59,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x170c3eba94557b70
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_1
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 43,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x71bff6f5768df1ed
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_1
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 34,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x8b07a9ab977c8d0b
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_1
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 13,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x56e5a06aab650f37
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_1
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 29,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x9b0d7176a0ca687e
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_1
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 100,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x9b0d7176a0ca687e
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_2
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 86,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x06f5f54750bd654a
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_1
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 100,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x06f5f54750bd654a
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_2
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 3,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe44d5b4b20892535
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_1
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 6,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x934e10cf2ea0e7e4
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_1
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 100,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x934e10cf2ea0e7e4
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_2
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 91,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe2b51550b67a62ed
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_1
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 100,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe2b51550b67a62ed
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_2
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 60,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x5f3a91a610f1c06f
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_1
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 77,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x0d023dd4ef520ad2
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_1
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 52,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x0c8c705933998816
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_1
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 85,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xb6aab3241baa8b86
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_1
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 100,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xb6aab3241baa8b86
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_2
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 45,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xd91d79a5ab20adab
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_1
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 61,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xa4aed2989c7f5264
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_1
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 100,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xa4aed2989c7f5264
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_2
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 38,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xec8730c0b56d0b04
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_1
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 92,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xa5217acb87f7dc6b
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_1
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 100,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xa5217acb87f7dc6b
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_2
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 73,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x8a48eef7716ece01
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_1
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 100,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x8a48eef7716ece01
// Course: 6th grade math
// Exercise: Intro to percents
// Widget key: image_2
test("unit-block-diagram - Intro to percents", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 22,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x95e0733a6b5c27cc
// Course: 6th grade math
// Exercise: Percents from fraction models
// Widget key: image_1
test("unit-block-diagram - Percents from fraction models", async () => {
	const input = {
	"blockWidth": 200,
	"shadeColor": "#11accd",
	"blockHeight": 200,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 40,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x3575372603bc68a0
// Course: 6th grade math
// Exercise: Percents from fraction models
// Widget key: image_1
test("unit-block-diagram - Percents from fraction models", async () => {
	const input = {
	"blockWidth": 130,
	"shadeColor": "#11accd",
	"blockHeight": 130,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 100,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x3575372603bc68a0
// Course: 6th grade math
// Exercise: Percents from fraction models
// Widget key: image_2
test("unit-block-diagram - Percents from fraction models", async () => {
	const input = {
	"blockWidth": 130,
	"shadeColor": "#11accd",
	"blockHeight": 130,
	"totalBlocks": 1,
	"blocksPerRow": 1,
	"shadedUnitsPerBlock": 10,
	"type": "unitBlockDiagram"
} satisfies UnitBlockDiagramInput

	// Validate the input
	const parseResult = UnitBlockDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted unitBlockDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateUnitBlockDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})

