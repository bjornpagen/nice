// ============================================================================
// EXTRACTED TEST FILE - AUTO-GENERATED
// ============================================================================
// This file was automatically generated from database structured_json data
// Generated on: 2025-09-01T22:43:40.169Z
// Widget Type: inequalityNumberLine
// Source: Real questions from database
//
// These tests use actual widget configurations extracted from production data
// to ensure comprehensive test coverage with realistic scenarios.
//
// DO NOT EDIT THIS FILE DIRECTLY - it will be overwritten on regeneration
//
// Run tests: bun test tests/widgets/inequality-number-line.extracted.test.ts
// Update snapshots: bun test tests/widgets/inequality-number-line.extracted.test.ts --update-snapshots
// ============================================================================

import { expect, test } from "bun:test"
import type { z } from "zod"
import { generateInequalityNumberLine, InequalityNumberLinePropsSchema } from "@/lib/widgets/generators"

type InequalityNumberLineInput = z.input<typeof InequalityNumberLinePropsSchema>


// Extracted from question: x077c573f15ceb702
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: image_1
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 1,
	"min": -9,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#11accd",
			"start": {
				"at": {
					"type": "open",
					"value": -8
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x3b765e8b65de4317
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: image_1
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 1,
	"min": -9,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": -6
				},
				"type": "bounded"
			},
			"color": "#11accd",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x4bbf24717593bd38
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: image_1
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": 3
				},
				"type": "bounded"
			},
			"color": "#11accd",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x4bbf24717593bd38
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_a_image
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#11accd",
			"start": {
				"at": {
					"type": "open",
					"value": -3
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x4bbf24717593bd38
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_b_image
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#11accd",
			"start": {
				"at": {
					"type": "open",
					"value": 2
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x4bbf24717593bd38
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_d_image
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": 4
				},
				"type": "bounded"
			},
			"color": "#11accd",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x81b7b8d0d9d5d130
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: image_1
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": 4
				},
				"type": "bounded"
			},
			"color": "#11accd",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xd52e7a5a3a11c500
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_a_graph
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 360,
	"height": 48,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#7854ab",
			"start": {
				"at": {
					"type": "open",
					"value": 2
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xd52e7a5a3a11c500
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_b_graph
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 360,
	"height": 48,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#7854ab",
			"start": {
				"at": {
					"type": "open",
					"value": 1
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xd52e7a5a3a11c500
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_c_graph
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 360,
	"height": 48,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": 1
				},
				"type": "bounded"
			},
			"color": "#7854ab",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xd52e7a5a3a11c500
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_d_graph
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 360,
	"height": 48,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": -1
				},
				"type": "bounded"
			},
			"color": "#7854ab",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe6564ee186a5ed75
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_a_graph
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 9,
	"min": -1,
	"width": 360,
	"height": 48,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": 5
				},
				"type": "bounded"
			},
			"color": "#7854ab",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe6564ee186a5ed75
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_b_graph
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 9,
	"min": -1,
	"width": 360,
	"height": 48,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": 6
				},
				"type": "bounded"
			},
			"color": "#7854ab",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe6564ee186a5ed75
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_c_graph
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 9,
	"min": -1,
	"width": 360,
	"height": 48,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": 7
				},
				"type": "bounded"
			},
			"color": "#7854ab",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe6564ee186a5ed75
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_d_graph
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 9,
	"min": -1,
	"width": 360,
	"height": 48,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": 8
				},
				"type": "bounded"
			},
			"color": "#7854ab",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe6ce0ba1a0c0daa5
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_a_number_line
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 600,
	"height": 120,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#7854AB80",
			"start": {
				"at": {
					"type": "open",
					"value": 2
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe6ce0ba1a0c0daa5
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_b_number_line
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 600,
	"height": 120,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#7854AB80",
			"start": {
				"at": {
					"type": "open",
					"value": -2
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe6ce0ba1a0c0daa5
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_d_number_line
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 600,
	"height": 120,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": -2
				},
				"type": "bounded"
			},
			"color": "#7854AB80",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xf329f86aa4c05206
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_a_number_line
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 600,
	"height": 120,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#11accd80",
			"start": {
				"at": {
					"type": "open",
					"value": 3
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xf329f86aa4c05206
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_b_number_line
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 600,
	"height": 120,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#11accd80",
			"start": {
				"at": {
					"type": "open",
					"value": -3
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xf329f86aa4c05206
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_c_number_line
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 600,
	"height": 120,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": 3
				},
				"type": "bounded"
			},
			"color": "#11accd80",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xf329f86aa4c05206
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_d_number_line
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 600,
	"height": 120,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": -3
				},
				"type": "bounded"
			},
			"color": "#11accd80",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x0ea6ddf2623b5753
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_a_number_line
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 600,
	"height": 100,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": -4
				},
				"type": "bounded"
			},
			"color": "#1E90FF80",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x0ea6ddf2623b5753
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_c_number_line
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 600,
	"height": 100,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": 4
				},
				"type": "bounded"
			},
			"color": "#1E90FF80",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x0ea6ddf2623b5753
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_d_number_line
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 600,
	"height": 100,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#1E90FF80",
			"start": {
				"at": {
					"type": "open",
					"value": -4
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xac4daf3ec3b769cb
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: image_1
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#29abca",
			"start": {
				"at": {
					"type": "open",
					"value": 2
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x139df1be864d99ef
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: image_1
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#11accd",
			"start": {
				"at": {
					"type": "open",
					"value": 1
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x5c5000061a808edf
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: image_1
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#11accd80",
			"start": {
				"at": {
					"type": "open",
					"value": -5
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xfa0a483560f82a8e
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: image_1
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 1,
	"min": -9,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": -7
				},
				"type": "bounded"
			},
			"color": "#11accd",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x43fd7d94d1049daf
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_a_numberline
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 7,
	"min": -3,
	"width": 360,
	"height": 48,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#7854AB",
			"start": {
				"at": {
					"type": "open",
					"value": 5
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x43fd7d94d1049daf
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_b_numberline
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 7,
	"min": -3,
	"width": 360,
	"height": 48,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": 5
				},
				"type": "bounded"
			},
			"color": "#7854AB",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x43fd7d94d1049daf
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_c_numberline
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 7,
	"min": -3,
	"width": 360,
	"height": 48,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#7854AB",
			"start": {
				"at": {
					"type": "open",
					"value": 3
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x43fd7d94d1049daf
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_d_numberline
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 7,
	"min": -3,
	"width": 360,
	"height": 48,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": 0
				},
				"type": "bounded"
			},
			"color": "#7854AB",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x0e14c3dd69960a1d
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_a_number_line
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 600,
	"height": 100,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#4287F54D",
			"start": {
				"at": {
					"type": "open",
					"value": 1
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x0e14c3dd69960a1d
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_b_number_line
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 600,
	"height": 100,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#4287F54D",
			"start": {
				"at": {
					"type": "open",
					"value": -1
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x0e14c3dd69960a1d
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_c_number_line
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 600,
	"height": 100,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": 1
				},
				"type": "bounded"
			},
			"color": "#4287F54D",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x0e14c3dd69960a1d
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_d_number_line
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 600,
	"height": 100,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": -1
				},
				"type": "bounded"
			},
			"color": "#4287F54D",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x23a8d3f0f1cb6f3b
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_a_graph
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 9,
	"min": -1,
	"width": 360,
	"height": 48,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#7854ab",
			"start": {
				"at": {
					"type": "open",
					"value": 6
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x23a8d3f0f1cb6f3b
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_b_graph
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 9,
	"min": -1,
	"width": 360,
	"height": 48,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": 7
				},
				"type": "bounded"
			},
			"color": "#7854ab",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x23a8d3f0f1cb6f3b
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_c_graph
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 9,
	"min": -1,
	"width": 360,
	"height": 48,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": 6
				},
				"type": "bounded"
			},
			"color": "#7854ab",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x23a8d3f0f1cb6f3b
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_d_graph
test("inequality-number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 9,
	"min": -1,
	"width": 360,
	"height": 48,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": 5
				},
				"type": "bounded"
			},
			"color": "#7854ab",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x6f4b476483794106
// Course: 6th grade math
// Exercise: Inequality from graph
// Widget key: image_1
test("inequality-number-line - Inequality from graph", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#29abca",
			"start": {
				"at": {
					"type": "closed",
					"value": -4
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x10106859a7a53c23
// Course: 6th grade math
// Exercise: Inequality from graph
// Widget key: image_1
test("inequality-number-line - Inequality from graph", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "closed",
					"value": 1
				},
				"type": "bounded"
			},
			"color": "#29abca",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xecf287982e61a26d
// Course: 6th grade math
// Exercise: Inequality from graph
// Widget key: image_1
test("inequality-number-line - Inequality from graph", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": 0
				},
				"type": "bounded"
			},
			"color": "#29abca",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x581c2c1aba2c1894
// Course: 6th grade math
// Exercise: Inequality from graph
// Widget key: image_1
test("inequality-number-line - Inequality from graph", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#29abca",
			"start": {
				"at": {
					"type": "closed",
					"value": 3
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xece2cb8aeb81fb3e
// Course: 6th grade math
// Exercise: Inequality from graph
// Widget key: image_1
test("inequality-number-line - Inequality from graph", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "closed",
					"value": 5
				},
				"type": "bounded"
			},
			"color": "#29abca",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xece2cb8aeb81fb3e
// Course: 6th grade math
// Exercise: Inequality from graph
// Widget key: choice_a_image
test("inequality-number-line - Inequality from graph", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": 5
				},
				"type": "bounded"
			},
			"color": "#29abca",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xece2cb8aeb81fb3e
// Course: 6th grade math
// Exercise: Inequality from graph
// Widget key: choice_c_image
test("inequality-number-line - Inequality from graph", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#29abca",
			"start": {
				"at": {
					"type": "open",
					"value": 5
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xece2cb8aeb81fb3e
// Course: 6th grade math
// Exercise: Inequality from graph
// Widget key: choice_d_image
test("inequality-number-line - Inequality from graph", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#29abca",
			"start": {
				"at": {
					"type": "closed",
					"value": 5
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x3dd0f420fdc2cf97
// Course: 6th grade math
// Exercise: Inequality from graph
// Widget key: image_1
test("inequality-number-line - Inequality from graph", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#29ABCA",
			"start": {
				"at": {
					"type": "open",
					"value": 0
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x0e197546025a0d4a
// Course: 6th grade math
// Exercise: Inequality from graph
// Widget key: image_1
test("inequality-number-line - Inequality from graph", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "closed",
					"value": -4
				},
				"type": "bounded"
			},
			"color": "#29abca",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xcb5b7eeed2cd45f8
// Course: 6th grade math
// Exercise: Inequality from graph
// Widget key: image_1
test("inequality-number-line - Inequality from graph", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#29abca",
			"start": {
				"at": {
					"type": "closed",
					"value": 1
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x4e1d9b6ab74b86c2
// Course: 6th grade math
// Exercise: Inequality from graph
// Widget key: image_1
test("inequality-number-line - Inequality from graph", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "closed",
					"value": 0
				},
				"type": "bounded"
			},
			"color": "#29ABCA",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x82d60f7df25dc683
// Course: 6th grade math
// Exercise: Inequality from graph
// Widget key: image_1
test("inequality-number-line - Inequality from graph", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": -1
				},
				"type": "bounded"
			},
			"color": "#29abca",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x56a1c7f1059f80a3
// Course: 6th grade math
// Exercise: Inequality from graph
// Widget key: image_1
test("inequality-number-line - Inequality from graph", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#29abca",
			"start": {
				"at": {
					"type": "closed",
					"value": 0
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x9b1664e34d681ebd
// Course: 6th grade math
// Exercise: Inequality from graph
// Widget key: image_1
test("inequality-number-line - Inequality from graph", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#11accd",
			"start": {
				"at": {
					"type": "open",
					"value": -1
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xbe07c6e040c037ef
// Course: 6th grade math
// Exercise: Inequality from graph
// Widget key: image_1
test("inequality-number-line - Inequality from graph", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#29abca",
			"start": {
				"at": {
					"type": "open",
					"value": 2
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe969544a429a3b61
// Course: 6th grade math
// Exercise: Inequality from graph
// Widget key: image_1
test("inequality-number-line - Inequality from graph", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#29ABCA80",
			"start": {
				"at": {
					"type": "open",
					"value": 4
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe969544a429a3b61
// Course: 6th grade math
// Exercise: Inequality from graph
// Widget key: choice_a_visual
test("inequality-number-line - Inequality from graph", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": 4
				},
				"type": "bounded"
			},
			"color": "#29ABCA80",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe969544a429a3b61
// Course: 6th grade math
// Exercise: Inequality from graph
// Widget key: choice_b_visual
test("inequality-number-line - Inequality from graph", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "closed",
					"value": 4
				},
				"type": "bounded"
			},
			"color": "#29ABCA80",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe969544a429a3b61
// Course: 6th grade math
// Exercise: Inequality from graph
// Widget key: choice_d_visual
test("inequality-number-line - Inequality from graph", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#29ABCA80",
			"start": {
				"at": {
					"type": "closed",
					"value": 4
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xea5cbb67dfc6b1da
// Course: 6th grade math
// Exercise: Inequality from graph
// Widget key: image_1
test("inequality-number-line - Inequality from graph", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": -2
				},
				"type": "bounded"
			},
			"color": "#29abca",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x64359e0f84d62ded
// Course: 6th grade math
// Exercise: Inequality from graph
// Widget key: image_1
test("inequality-number-line - Inequality from graph", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": 3
				},
				"type": "bounded"
			},
			"color": "#29abca80",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xc1df064f30796edc
// Course: 6th grade math
// Exercise: Inequality from graph
// Widget key: image_1
test("inequality-number-line - Inequality from graph", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "closed",
					"value": -3
				},
				"type": "bounded"
			},
			"color": "#29ABCA",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xc1df064f30796edc
// Course: 6th grade math
// Exercise: Inequality from graph
// Widget key: choice_a_visual
test("inequality-number-line - Inequality from graph", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": -3
				},
				"type": "bounded"
			},
			"color": "#29ABCA",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xc1df064f30796edc
// Course: 6th grade math
// Exercise: Inequality from graph
// Widget key: choice_c_visual
test("inequality-number-line - Inequality from graph", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#29ABCA",
			"start": {
				"at": {
					"type": "open",
					"value": -3
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xc1df064f30796edc
// Course: 6th grade math
// Exercise: Inequality from graph
// Widget key: choice_d_visual
test("inequality-number-line - Inequality from graph", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#29ABCA",
			"start": {
				"at": {
					"type": "closed",
					"value": -3
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x57b85ba0b3c58ec1
// Course: 6th grade math
// Exercise: Inequality from graph
// Widget key: image_1
test("inequality-number-line - Inequality from graph", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": 5
				},
				"type": "bounded"
			},
			"color": "#29abca",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xb9ee5e5d1733d8cb
// Course: 6th grade math
// Exercise: Inequality from graph
// Widget key: image_1
test("inequality-number-line - Inequality from graph", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#11accd",
			"start": {
				"at": {
					"type": "open",
					"value": -5
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe3ffda4262cba1e9
// Course: 6th grade math
// Exercise: Inequality from graph
// Widget key: image_1
test("inequality-number-line - Inequality from graph", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#29abca",
			"start": {
				"at": {
					"type": "closed",
					"value": -2
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x93f8cb0ca90b7a13
// Course: 6th grade math
// Exercise: Plotting inequalities
// Widget key: number_line_initial
test("inequality-number-line - Plotting inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x93f8cb0ca90b7a13
// Course: 6th grade math
// Exercise: Plotting inequalities
// Widget key: number_line_choice_a
test("inequality-number-line - Plotting inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "closed",
					"value": -2
				},
				"type": "bounded"
			},
			"color": "#29ABCA66",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x93f8cb0ca90b7a13
// Course: 6th grade math
// Exercise: Plotting inequalities
// Widget key: number_line_choice_b
test("inequality-number-line - Plotting inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#29ABCA66",
			"start": {
				"at": {
					"type": "open",
					"value": -2
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x93f8cb0ca90b7a13
// Course: 6th grade math
// Exercise: Plotting inequalities
// Widget key: number_line_choice_c
test("inequality-number-line - Plotting inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#29ABCA66",
			"start": {
				"at": {
					"type": "closed",
					"value": -2
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x5a50ef47021aeb97
// Course: 6th grade math
// Exercise: Plotting inequalities
// Widget key: number_line_choice_a
test("inequality-number-line - Plotting inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "closed",
					"value": 5
				},
				"type": "bounded"
			},
			"color": "#29ABCA66",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x5a50ef47021aeb97
// Course: 6th grade math
// Exercise: Plotting inequalities
// Widget key: number_line_choice_b
test("inequality-number-line - Plotting inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#29ABCA66",
			"start": {
				"at": {
					"type": "closed",
					"value": 5
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x5a50ef47021aeb97
// Course: 6th grade math
// Exercise: Plotting inequalities
// Widget key: number_line_choice_c
test("inequality-number-line - Plotting inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": 5
				},
				"type": "bounded"
			},
			"color": "#29ABCA66",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x7470e9413e6afac0
// Course: 6th grade math
// Exercise: Plotting inequalities
// Widget key: number_line_choice_a
test("inequality-number-line - Plotting inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#29ABCA80",
			"start": {
				"at": {
					"type": "closed",
					"value": 0
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x7470e9413e6afac0
// Course: 6th grade math
// Exercise: Plotting inequalities
// Widget key: number_line_choice_b
test("inequality-number-line - Plotting inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": 0
				},
				"type": "bounded"
			},
			"color": "#29ABCA80",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x7470e9413e6afac0
// Course: 6th grade math
// Exercise: Plotting inequalities
// Widget key: number_line_choice_c
test("inequality-number-line - Plotting inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "closed",
					"value": 0
				},
				"type": "bounded"
			},
			"color": "#29ABCA80",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x7af81be6b2c580c8
// Course: 6th grade math
// Exercise: Plotting inequalities
// Widget key: inequality_choice_a
test("inequality-number-line - Plotting inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 100,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": 1
				},
				"type": "bounded"
			},
			"color": "#11accd66",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x7af81be6b2c580c8
// Course: 6th grade math
// Exercise: Plotting inequalities
// Widget key: inequality_choice_b
test("inequality-number-line - Plotting inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 100,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "closed",
					"value": 1
				},
				"type": "bounded"
			},
			"color": "#11accd66",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x7af81be6b2c580c8
// Course: 6th grade math
// Exercise: Plotting inequalities
// Widget key: inequality_choice_c
test("inequality-number-line - Plotting inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 100,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#11accd66",
			"start": {
				"at": {
					"type": "open",
					"value": 1
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x04511e49f389e608
// Course: 6th grade math
// Exercise: Plotting inequalities
// Widget key: inequality_choice_a
test("inequality-number-line - Plotting inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 100,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "closed",
					"value": 4
				},
				"type": "bounded"
			},
			"color": "#29ABCA66",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x04511e49f389e608
// Course: 6th grade math
// Exercise: Plotting inequalities
// Widget key: inequality_choice_b
test("inequality-number-line - Plotting inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 100,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": 4
				},
				"type": "bounded"
			},
			"color": "#29ABCA66",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x04511e49f389e608
// Course: 6th grade math
// Exercise: Plotting inequalities
// Widget key: inequality_choice_c
test("inequality-number-line - Plotting inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 100,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#29ABCA66",
			"start": {
				"at": {
					"type": "closed",
					"value": 4
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x94239005dd3f6fa5
// Course: 6th grade math
// Exercise: Plotting inequalities
// Widget key: nl_choice_a
test("inequality-number-line - Plotting inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#29ABCA66",
			"start": {
				"at": {
					"type": "closed",
					"value": 1
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x94239005dd3f6fa5
// Course: 6th grade math
// Exercise: Plotting inequalities
// Widget key: nl_choice_b
test("inequality-number-line - Plotting inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "open",
					"value": 1
				},
				"type": "bounded"
			},
			"color": "#29ABCA66",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x94239005dd3f6fa5
// Course: 6th grade math
// Exercise: Plotting inequalities
// Widget key: nl_choice_c
test("inequality-number-line - Plotting inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#29ABCA66",
			"start": {
				"at": {
					"type": "open",
					"value": 1
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x154446a20bf0a4c4
// Course: 6th grade math
// Exercise: Plotting inequalities
// Widget key: number_line_choice_a
test("inequality-number-line - Plotting inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#29ABCA80",
			"start": {
				"at": {
					"type": "closed",
					"value": 0
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x154446a20bf0a4c4
// Course: 6th grade math
// Exercise: Plotting inequalities
// Widget key: number_line_choice_b
test("inequality-number-line - Plotting inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"type": "unbounded"
			},
			"color": "#29ABCA80",
			"start": {
				"at": {
					"type": "open",
					"value": 0
				},
				"type": "bounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x154446a20bf0a4c4
// Course: 6th grade math
// Exercise: Plotting inequalities
// Widget key: number_line_choice_c
test("inequality-number-line - Plotting inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"ranges": [
		{
			"end": {
				"at": {
					"type": "closed",
					"value": 0
				},
				"type": "bounded"
			},
			"color": "#29ABCA80",
			"start": {
				"type": "unbounded"
			}
		}
	],
	"tickInterval": 1,
	"type": "inequalityNumberLine"
} satisfies InequalityNumberLineInput

	// Validate the input
	const parseResult = InequalityNumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted inequalityNumberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateInequalityNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})

