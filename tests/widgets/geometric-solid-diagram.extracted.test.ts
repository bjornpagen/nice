// ============================================================================
// EXTRACTED TEST FILE - AUTO-GENERATED
// ============================================================================
// This file was automatically generated from database structured_json data
// Generated on: 2025-09-17T15:38:03.959Z
// Widget Type: geometricSolidDiagram
// Source: Real questions from database
//
// These tests use actual widget configurations extracted from production data
// to ensure comprehensive test coverage with realistic scenarios.
//
// Note: Database data may not perfectly match current widget schemas, so these
// tests rely on runtime validation within generateWidget() for flexibility.
//
// DO NOT EDIT THIS FILE DIRECTLY - it will be overwritten on regeneration
//
// Run tests: bun test tests/widgets/geometric-solid-diagram.extracted.test.ts
// Update snapshots: bun test tests/widgets/geometric-solid-diagram.extracted.test.ts --update-snapshots
// ============================================================================

import { describe, test, expect } from "bun:test"
import { generateWidget } from "@superbuilders/qti-assessment-item-generator/widgets/widget-generator"
import type { Widget, WidgetInput } from "@superbuilders/qti-assessment-item-generator/widgets/registry"
import * as errors from "@superbuilders/errors"
import * as logger from "@superbuilders/slog"

describe("geometricSolidDiagram widget tests", () => {
	// Extracted from question: x72dc3d0e7b62efba
	// Question: Find the volume of a sphere with radius 1/2 unit
	// Widget key: image_1
	test("geometric-solid-diagram - [x72dc3d0e7b62efba] Find the volume of a sphere with radius 1/2 unit", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "sphere",
		    "radius": 0.5
		  },
		  "width": 240,
		  "height": 240,
		  "labels": [
		    {
		      "text": "r = 1/2 unit",
		      "target": "radius"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x7bb3c2db5acf9797
	// Question: Find the volume of a sphere
	// Widget key: image_1
	test("geometric-solid-diagram - [x7bb3c2db5acf9797] Find the volume of a sphere", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "sphere",
		    "radius": 9
		  },
		  "width": 180,
		  "height": 180,
		  "labels": [
		    {
		      "text": "9 units",
		      "target": "radius"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x3fa89dec0e79ffcf
	// Question: Find the volume of a sphere
	// Widget key: image_1
	test("geometric-solid-diagram - [x3fa89dec0e79ffcf] Find the volume of a sphere", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "sphere",
		    "radius": 4
		  },
		  "width": 180,
		  "height": 180,
		  "labels": [
		    {
		      "text": "4 units",
		      "target": "radius"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xe1d438f18f46c848
	// Question: Find the volume of a sphere
	// Widget key: image_1
	test("geometric-solid-diagram - [xe1d438f18f46c848] Find the volume of a sphere", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "sphere",
		    "radius": 3
		  },
		  "width": 180,
		  "height": 180,
		  "labels": [
		    {
		      "text": "3 units",
		      "target": "radius"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x4cb611880d500097
	// Question: Find the volume of a sphere from its radius
	// Widget key: image_1
	test("geometric-solid-diagram - [x4cb611880d500097] Find the volume of a sphere from its radius", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "sphere",
		    "radius": 10
		  },
		  "width": 180,
		  "height": 180,
		  "labels": [
		    {
		      "text": "10 units",
		      "target": "radius"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x9c18eb9bb29697f1
	// Question: Volume of a sphere
	// Widget key: image_1
	test("geometric-solid-diagram - [x9c18eb9bb29697f1] Volume of a sphere", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "sphere",
		    "radius": 0.25
		  },
		  "width": 240,
		  "height": 240,
		  "labels": [
		    {
		      "text": "r = 1/4 unit",
		      "target": "radius"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x7cd6ac5126417fea
	// Question: Find the volume of the sphere (radius 2)
	// Widget key: image_1
	test("geometric-solid-diagram - [x7cd6ac5126417fea] Find the volume of the sphere (radius 2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "sphere",
		    "radius": 2
		  },
		  "width": 180,
		  "height": 180,
		  "labels": [
		    {
		      "text": "2 units",
		      "target": "radius"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x35c03635a82e3af9
	// Question: Find the volume of a sphere
	// Widget key: image_1
	test("geometric-solid-diagram - [x35c03635a82e3af9] Find the volume of a sphere", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "sphere",
		    "radius": 8
		  },
		  "width": 180,
		  "height": 180,
		  "labels": [
		    {
		      "text": "8 units",
		      "target": "radius"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x316454d167fb81d4
	// Question: Find the volume of a sphere
	// Widget key: image_1
	test("geometric-solid-diagram - [x316454d167fb81d4] Find the volume of a sphere", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "sphere",
		    "radius": 7
		  },
		  "width": 180,
		  "height": 180,
		  "labels": [
		    {
		      "text": "7 units",
		      "target": "radius"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xa6e384defcba290a
	// Question: Find the volume of the sphere
	// Widget key: image_1
	test("geometric-solid-diagram - [xa6e384defcba290a] Find the volume of the sphere", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "sphere",
		    "radius": 1
		  },
		  "width": 180,
		  "height": 180,
		  "labels": [
		    {
		      "text": "1 unit",
		      "target": "radius"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x71e776db609db5ad
	// Question: Find the volume of a sphere (radius 5)
	// Widget key: image_1
	test("geometric-solid-diagram - [x71e776db609db5ad] Find the volume of a sphere (radius 5)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "sphere",
		    "radius": 5
		  },
		  "width": 180,
		  "height": 180,
		  "labels": [
		    {
		      "text": "5 units",
		      "target": "radius"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x3fc5b28254055d8a
	// Question: Find the volume of the sphere
	// Widget key: image_1
	test("geometric-solid-diagram - [x3fc5b28254055d8a] Find the volume of the sphere", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "sphere",
		    "radius": 6
		  },
		  "width": 180,
		  "height": 180,
		  "labels": [
		    {
		      "text": "6 units",
		      "target": "radius"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xd2523669f0c90b0e
	// Question: Find the volume of a cone
	// Widget key: image_1
	test("geometric-solid-diagram - [xd2523669f0c90b0e] Find the volume of a cone", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cone",
		    "height": 9,
		    "radius": 2
		  },
		  "width": 300,
		  "height": 320,
		  "labels": [
		    {
		      "text": "2 units",
		      "target": "radius"
		    },
		    {
		      "text": "9 units",
		      "target": "height"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x75ef3b80886fcf7b
	// Question: Find the volume of a cone
	// Widget key: image_1
	test("geometric-solid-diagram - [x75ef3b80886fcf7b] Find the volume of a cone", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cone",
		    "height": 6,
		    "radius": 10
		  },
		  "width": 240,
		  "height": 90,
		  "labels": [
		    {
		      "text": "10 units",
		      "target": "radius"
		    },
		    {
		      "text": "6 units",
		      "target": "height"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x07dee815cde5bc2a
	// Question: Find the volume of a cone
	// Widget key: image_1
	test("geometric-solid-diagram - [x07dee815cde5bc2a] Find the volume of a cone", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cone",
		    "height": 10,
		    "radius": 4
		  },
		  "width": 134.23728813559322,
		  "height": 180,
		  "labels": [
		    {
		      "text": "r = 4 units",
		      "target": "radius"
		    },
		    {
		      "text": "h = 10 units",
		      "target": "height"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xae1c703950392d27
	// Question: Find the volume of the cone
	// Widget key: image_1
	test("geometric-solid-diagram - [xae1c703950392d27] Find the volume of the cone", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cone",
		    "height": 8,
		    "radius": 6
		  },
		  "width": 180,
		  "height": 138,
		  "labels": [
		    {
		      "text": "6 units",
		      "target": "radius"
		    },
		    {
		      "text": "8 units",
		      "target": "height"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xaf4773c52980cc4d
	// Question: Find the volume of a cone
	// Widget key: image_1
	test("geometric-solid-diagram - [xaf4773c52980cc4d] Find the volume of a cone", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cone",
		    "height": 6,
		    "radius": 5
		  },
		  "width": 240,
		  "height": 138,
		  "labels": [
		    {
		      "text": "5 units",
		      "target": "radius"
		    },
		    {
		      "text": "6 units",
		      "target": "height"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x1a99f30d2fb4ab35
	// Question: Find the volume of a cone
	// Widget key: image_1
	test("geometric-solid-diagram - [x1a99f30d2fb4ab35] Find the volume of a cone", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cone",
		    "height": 5,
		    "radius": 5
		  },
		  "width": 180,
		  "height": 105,
		  "labels": [
		    {
		      "text": "5 units",
		      "target": "radius"
		    },
		    {
		      "text": "5 units",
		      "target": "height"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x9b0935fd5d35903e
	// Question: Find the volume of a cone
	// Widget key: image_1
	test("geometric-solid-diagram - [x9b0935fd5d35903e] Find the volume of a cone", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cone",
		    "height": 3,
		    "radius": 5
		  },
		  "width": 240,
		  "height": 90,
		  "labels": [
		    {
		      "text": "5 units",
		      "target": "radius"
		    },
		    {
		      "text": "3 units",
		      "target": "height"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x5ea278c96a8b9685
	// Question: Find the volume of a cone (r = 7, h = 7)
	// Widget key: image_1
	test("geometric-solid-diagram - [x5ea278c96a8b9685] Find the volume of a cone (r = 7, h = 7)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cone",
		    "height": 7,
		    "radius": 7
		  },
		  "width": 180,
		  "height": 105,
		  "labels": [
		    {
		      "text": "7 units",
		      "target": "radius"
		    },
		    {
		      "text": "7 units",
		      "target": "height"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xfdd6ec9efadc5fa2
	// Question: Find the volume of a cone
	// Widget key: image_1
	test("geometric-solid-diagram - [xfdd6ec9efadc5fa2] Find the volume of a cone", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cone",
		    "height": 5,
		    "radius": 2
		  },
		  "width": 134.23728813559322,
		  "height": 180,
		  "labels": [
		    {
		      "text": "2 units",
		      "target": "radius"
		    },
		    {
		      "text": "5 units",
		      "target": "height"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x47283ee3cc27d5a3
	// Question: Find the volume of a cone
	// Widget key: image_1
	test("geometric-solid-diagram - [x47283ee3cc27d5a3] Find the volume of a cone", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cone",
		    "height": 4,
		    "radius": 6
		  },
		  "width": 240,
		  "height": 106.45,
		  "labels": [
		    {
		      "text": "6 units",
		      "target": "radius"
		    },
		    {
		      "text": "4 units",
		      "target": "height"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x13b348a260cd9d6f
	// Question: Find the volume of a cone (exact or decimal)
	// Widget key: image_1
	test("geometric-solid-diagram - [x13b348a260cd9d6f] Find the volume of a cone (exact or decimal)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cone",
		    "height": 2,
		    "radius": 3
		  },
		  "width": 240,
		  "height": 106,
		  "labels": [
		    {
		      "text": "3 units",
		      "target": "radius"
		    },
		    {
		      "text": "2 units",
		      "target": "height"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x5acd8ae84f530e66
	// Question: Find the volume of a cone
	// Widget key: image_1
	test("geometric-solid-diagram - [x5acd8ae84f530e66] Find the volume of a cone", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cone",
		    "height": 4,
		    "radius": 3
		  },
		  "width": 180,
		  "height": 138,
		  "labels": [
		    {
		      "text": "3 units",
		      "target": "radius"
		    },
		    {
		      "text": "4 units",
		      "target": "height"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xde6ec880165a5aec
	// Question: Find the volume of a cylinder
	// Widget key: image_1
	test("geometric-solid-diagram - [xde6ec880165a5aec] Find the volume of a cylinder", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cylinder",
		    "height": 2,
		    "radius": 3
		  },
		  "width": 220,
		  "height": 94,
		  "labels": [
		    {
		      "text": "3 units",
		      "target": "radius"
		    },
		    {
		      "text": "2 units",
		      "target": "height"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x87ea94bac8422390
	// Question: Find the volume of a cylinder
	// Widget key: image_1
	test("geometric-solid-diagram - [x87ea94bac8422390] Find the volume of a cylinder", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cylinder",
		    "height": 6,
		    "radius": 8
		  },
		  "width": 180,
		  "height": 103,
		  "labels": [
		    {
		      "text": "8 units",
		      "target": "radius"
		    },
		    {
		      "text": "6 units",
		      "target": "height"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x1fa26ca62e6933f3
	// Question: Find the volume of a cylinder
	// Widget key: image_1
	test("geometric-solid-diagram - [x1fa26ca62e6933f3] Find the volume of a cylinder", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cylinder",
		    "height": 8,
		    "radius": 4
		  },
		  "width": 175,
		  "height": 180,
		  "labels": [
		    {
		      "text": "4 units",
		      "target": "radius"
		    },
		    {
		      "text": "8 units",
		      "target": "height"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x6169461922c6f1d4
	// Question: Find the volume of a cylinder
	// Widget key: image_1
	test("geometric-solid-diagram - [x6169461922c6f1d4] Find the volume of a cylinder", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cylinder",
		    "height": 4,
		    "radius": 6
		  },
		  "width": 220,
		  "height": 94,
		  "labels": [
		    {
		      "text": "6 units",
		      "target": "radius"
		    },
		    {
		      "text": "4 units",
		      "target": "height"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xaead43c05c37ee59
	// Question: Find the volume of a cylinder
	// Widget key: image_1
	test("geometric-solid-diagram - [xaead43c05c37ee59] Find the volume of a cylinder", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cylinder",
		    "height": 3,
		    "radius": 5
		  },
		  "width": 240,
		  "height": 96,
		  "labels": [
		    {
		      "text": "5 units",
		      "target": "radius"
		    },
		    {
		      "text": "3 units",
		      "target": "height"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xba9aa73a34f2def5
	// Question: Find the volume of a cylinder
	// Widget key: image_1
	test("geometric-solid-diagram - [xba9aa73a34f2def5] Find the volume of a cylinder", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cylinder",
		    "height": 8,
		    "radius": 2
		  },
		  "width": 107,
		  "height": 180,
		  "labels": [
		    {
		      "text": "2 units",
		      "target": "radius"
		    },
		    {
		      "text": "8 units",
		      "target": "height"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xc77ffbd9e312fe1e
	// Question: Find the volume of a cylinder
	// Widget key: image_1
	test("geometric-solid-diagram - [xc77ffbd9e312fe1e] Find the volume of a cylinder", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cylinder",
		    "height": 10,
		    "radius": 4
		  },
		  "width": 146,
		  "height": 180,
		  "labels": [
		    {
		      "text": "4 units",
		      "target": "radius"
		    },
		    {
		      "text": "10 units",
		      "target": "height"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x68ce34a39f63b75b
	// Question: Find the volume of a cylinder
	// Widget key: image_1
	test("geometric-solid-diagram - [x68ce34a39f63b75b] Find the volume of a cylinder", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cylinder",
		    "height": 6,
		    "radius": 3
		  },
		  "width": 175,
		  "height": 180,
		  "labels": [
		    {
		      "text": "3 units",
		      "target": "radius"
		    },
		    {
		      "text": "6 units",
		      "target": "height"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x90385f1429f2a59b
	// Question: Find the volume of a cylinder
	// Widget key: image_1
	test("geometric-solid-diagram - [x90385f1429f2a59b] Find the volume of a cylinder", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cylinder",
		    "height": 5,
		    "radius": 2
		  },
		  "width": 146,
		  "height": 180,
		  "labels": [
		    {
		      "text": "r = 2 units",
		      "target": "radius"
		    },
		    {
		      "text": "h = 5 units",
		      "target": "height"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x07f186bf343b3364
	// Question: Find the volume of a cylinder
	// Widget key: image_1
	test("geometric-solid-diagram - [x07f186bf343b3364] Find the volume of a cylinder", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cylinder",
		    "height": 10,
		    "radius": 4
		  },
		  "width": 240,
		  "height": 236,
		  "labels": [
		    {
		      "text": "4 units",
		      "target": "radius"
		    },
		    {
		      "text": "10 units",
		      "target": "height"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xd42c8f5c09f49763
	// Question: Find the volume of a cylinder (exact or approximation)
	// Widget key: image_1
	test("geometric-solid-diagram - [xd42c8f5c09f49763] Find the volume of a cylinder (exact or approximation)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cylinder",
		    "height": 6,
		    "radius": 5
		  },
		  "width": 240,
		  "height": 156,
		  "labels": [
		    {
		      "text": "5 units",
		      "target": "radius"
		    },
		    {
		      "text": "6 units",
		      "target": "height"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xa69e9a8cb1f3f6aa
	// Question: Find the volume of a cylinder
	// Widget key: image_1
	test("geometric-solid-diagram - [xa69e9a8cb1f3f6aa] Find the volume of a cylinder", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cylinder",
		    "height": 3,
		    "radius": 4
		  },
		  "width": 180,
		  "height": 103,
		  "labels": [
		    {
		      "text": "4 units",
		      "target": "radius"
		    },
		    {
		      "text": "3 units",
		      "target": "height"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xbb68cc21fed872d0
	// Question: Volume of a cylindrical container
	// Widget key: image_1
	test("geometric-solid-diagram - [xbb68cc21fed872d0] Volume of a cylindrical container", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cylinder",
		    "height": 20,
		    "radius": 10
		  },
		  "width": 180,
		  "height": 173,
		  "labels": [
		    {
		      "text": "10 inches",
		      "target": "radius"
		    },
		    {
		      "text": "20 inches",
		      "target": "height"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2d6345571874145e
	// Question: Volume of a cylinder (water bottle)
	// Widget key: image_1
	test("geometric-solid-diagram - [x2d6345571874145e] Volume of a cylinder (water bottle)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cylinder",
		    "height": 20,
		    "radius": 4
		  },
		  "width": 145.463,
		  "height": 260,
		  "labels": [
		    {
		      "text": "4 cm",
		      "target": "radius"
		    },
		    {
		      "text": "20 cm",
		      "target": "height"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x00dff9034f2ddb48
	// Question: Volume of a hemispherical bowl
	// Widget key: image_1
	test("geometric-solid-diagram - [x00dff9034f2ddb48] Volume of a hemispherical bowl", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "sphere",
		    "radius": 15
		  },
		  "width": 180,
		  "height": 180,
		  "labels": [
		    {
		      "text": "15 cm",
		      "target": "radius"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xa9a5cb4c040e5824
	// Question: Volume of water remaining in a cone-shaped cup
	// Widget key: image_1
	test("geometric-solid-diagram - [xa9a5cb4c040e5824] Volume of water remaining in a cone-shaped cup", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cone",
		    "height": 8,
		    "radius": 3
		  },
		  "width": 129.438,
		  "height": 180,
		  "labels": [
		    {
		      "text": "3 cm",
		      "target": "radius"
		    },
		    {
		      "text": "8 cm",
		      "target": "height"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x273f0f1d3d234c1c
	// Question: Volume of water remaining in a half-full cylindrical pool
	// Widget key: image_1
	test("geometric-solid-diagram - [x273f0f1d3d234c1c] Volume of water remaining in a half-full cylindrical pool", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cylinder",
		    "height": 2,
		    "radius": 4
		  },
		  "width": 180,
		  "height": 59,
		  "labels": [
		    {
		      "text": "4 feet",
		      "target": "radius"
		    },
		    {
		      "text": "2 feet",
		      "target": "height"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2d009ffa46ca3820
	// Question: Identify the shape from a net of squares
	// Widget key: choice_c_shape
	test("geometric-solid-diagram - [x2d009ffa46ca3820] Identify the shape from a net of squares", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "sphere",
		    "radius": 5
		  },
		  "width": 120,
		  "height": 120,
		  "labels": []
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2b03516c0180b352
	// Question: Identify the solid formed by a net
	// Widget key: choice_c_shape
	test("geometric-solid-diagram - [x2b03516c0180b352] Identify the solid formed by a net", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "sphere",
		    "radius": 5
		  },
		  "width": 200,
		  "height": 180,
		  "labels": []
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x8191439a8ce20320
	// Question: Identify the 3D shape
	// Widget key: image_1
	test("geometric-solid-diagram - [x8191439a8ce20320] Identify the 3D shape", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cylinder",
		    "height": 12,
		    "radius": 4
		  },
		  "width": 128,
		  "height": 150,
		  "labels": []
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xadf4c6388526ed73
	// Question: Identify the 3D shape
	// Widget key: image_1
	test("geometric-solid-diagram - [xadf4c6388526ed73] Identify the 3D shape", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "sphere",
		    "radius": 45.455
		  },
		  "width": 100,
		  "height": 100,
		  "labels": []
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x5216f19f9da4aa51
	// Question: Identify the three-dimensional shape
	// Widget key: image_1
	test("geometric-solid-diagram - [x5216f19f9da4aa51] Identify the three-dimensional shape", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cylinder",
		    "height": 24,
		    "radius": 8
		  },
		  "width": 56,
		  "height": 180,
		  "labels": []
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x473ed90494934b15
	// Question: Identify the 3D shape
	// Widget key: image_1
	test("geometric-solid-diagram - [x473ed90494934b15] Identify the 3D shape", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cone",
		    "height": 15,
		    "radius": 6
		  },
		  "width": 134,
		  "height": 180,
		  "labels": []
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xb031ee96abb9e104
	// Question: Identify the 3D shape
	// Widget key: image_1
	test("geometric-solid-diagram - [xb031ee96abb9e104] Identify the 3D shape", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cylinder",
		    "height": 3,
		    "radius": 6
		  },
		  "width": 180,
		  "height": 74,
		  "labels": []
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x4109ee185e4bee91
	// Question: Identify the 3D shape
	// Widget key: image_1
	test("geometric-solid-diagram - [x4109ee185e4bee91] Identify the 3D shape", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "cone",
		    "height": 5,
		    "radius": 8
		  },
		  "width": 180,
		  "height": 68,
		  "labels": []
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x5bdf2332dc5f9e15
	// Question: Identify the 3D shape
	// Widget key: image_1
	test("geometric-solid-diagram - [x5bdf2332dc5f9e15] Identify the 3D shape", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "geometricSolidDiagram",
		  "shape": {
		    "type": "sphere",
		    "radius": 5
		  },
		  "width": 180,
		  "height": 180,
		  "labels": []
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for geometricSolidDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

})
