// ============================================================================
// EXTRACTED TEST FILE - AUTO-GENERATED
// ============================================================================
// This file was automatically generated from database structured_json data
// Generated on: 2025-09-01T22:43:40.170Z
// Widget Type: geometricSolidDiagram
// Source: Real questions from database
//
// These tests use actual widget configurations extracted from production data
// to ensure comprehensive test coverage with realistic scenarios.
//
// DO NOT EDIT THIS FILE DIRECTLY - it will be overwritten on regeneration
//
// Run tests: bun test tests/widgets/geometric-solid-diagram.extracted.test.ts
// Update snapshots: bun test tests/widgets/geometric-solid-diagram.extracted.test.ts --update-snapshots
// ============================================================================

import { expect, test } from "bun:test"
import type { z } from "zod"
import { generateGeometricSolidDiagram, GeometricSolidDiagramPropsSchema } from "@/lib/widgets/generators"

type GeometricSolidDiagramInput = z.input<typeof GeometricSolidDiagramPropsSchema>


// Extracted from question: x8191439a8ce20320
// Course: 6th grade math
// Exercise: Identify geometric solids (3D shapes)
// Widget key: image_1
test("geometric-solid-diagram - Identify geometric solids 3D shapes", () => {
	const input = {
	"shape": {
		"type": "cylinder",
		"height": 12,
		"radius": 5
	},
	"width": 128,
	"height": 150,
	"labels": [],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x5216f19f9da4aa51
// Course: 6th grade math
// Exercise: Identify geometric solids (3D shapes)
// Widget key: image_1
test("geometric-solid-diagram - Identify geometric solids 3D shapes", () => {
	const input = {
	"shape": {
		"type": "cylinder",
		"height": 36,
		"radius": 12
	},
	"width": 300,
	"height": 400,
	"labels": [],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x473ed90494934b15
// Course: 6th grade math
// Exercise: Identify geometric solids (3D shapes)
// Widget key: image_1
test("geometric-solid-diagram - Identify geometric solids 3D shapes", () => {
	const input = {
	"shape": {
		"type": "cone",
		"height": 8,
		"radius": 3
	},
	"width": 134,
	"height": 180,
	"labels": [],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xb031ee96abb9e104
// Course: 6th grade math
// Exercise: Identify geometric solids (3D shapes)
// Widget key: image_1
test("geometric-solid-diagram - Identify geometric solids 3D shapes", () => {
	const input = {
	"shape": {
		"type": "cylinder",
		"height": 6,
		"radius": 5
	},
	"width": 180,
	"height": 74,
	"labels": [],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x4109ee185e4bee91
// Course: 6th grade math
// Exercise: Identify geometric solids (3D shapes)
// Widget key: image_1
test("geometric-solid-diagram - Identify geometric solids 3D shapes", () => {
	const input = {
	"shape": {
		"type": "cone",
		"height": 6,
		"radius": 5
	},
	"width": 180,
	"height": 68,
	"labels": [],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x5bdf2332dc5f9e15
// Course: 6th grade math
// Exercise: Identify geometric solids (3D shapes)
// Widget key: image_1
test("geometric-solid-diagram - Identify geometric solids 3D shapes", () => {
	const input = {
	"shape": {
		"type": "sphere",
		"radius": 5
	},
	"width": 180,
	"height": 180,
	"labels": [],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2d009ffa46ca3820
// Course: 6th grade math
// Exercise: Nets of polyhedra
// Widget key: choice_c_shape
test("geometric-solid-diagram - Nets of polyhedra", () => {
	const input = {
	"shape": {
		"type": "sphere",
		"radius": 5
	},
	"width": 300,
	"height": 300,
	"labels": [],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x29a33987cc70cc98
// Course: 6th grade math
// Exercise: Nets of polyhedra
// Widget key: choice_b_shape
test("geometric-solid-diagram - Nets of polyhedra", () => {
	const input = {
	"shape": {
		"type": "sphere",
		"radius": 5
	},
	"width": 300,
	"height": 300,
	"labels": [],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2b03516c0180b352
// Course: 6th grade math
// Exercise: Nets of polyhedra
// Widget key: choice_c_shape
test("geometric-solid-diagram - Nets of polyhedra", () => {
	const input = {
	"shape": {
		"type": "sphere",
		"radius": 5
	},
	"width": 320,
	"height": 320,
	"labels": [],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})

