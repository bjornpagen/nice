// ============================================================================
// EXTRACTED TEST FILE - AUTO-GENERATED
// ============================================================================
// This file was automatically generated from database structured_json data
// Generated on: 2025-09-01T22:27:08.778Z
// Widget Type: geometricSolidDiagram
// Source: Real questions from database
//
// These tests use actual widget configurations extracted from production data
// to ensure comprehensive test coverage with realistic scenarios.
//
// DO NOT EDIT THIS FILE DIRECTLY - it will be overwritten on regeneration
//
// Run tests: bun test tests/widgets/geometric-solid-diagram.extracted.test.ts
// Update snapshots: bun test tests/widgets/geometric-solid-diagram.extracted.test.ts --update-snapshots
// ============================================================================

import { expect, test } from "bun:test"
import type { z } from "zod"
import { generateGeometricSolidDiagram, GeometricSolidDiagramPropsSchema } from "@/lib/widgets/generators"

type GeometricSolidDiagramInput = z.input<typeof GeometricSolidDiagramPropsSchema>


// Extracted from question: x87ea94bac8422390
// Course: 8th grade math
// Exercise: Volume of cylinders
// Widget key: image_1
test("geometric-solid-diagram - Volume of cylinders", () => {
	const input = {
	"shape": {
		"type": "cylinder",
		"height": 6,
		"radius": 8
	},
	"width": 180,
	"height": 103,
	"labels": [
		{
			"text": "8 units",
			"target": "radius"
		},
		{
			"text": "6 units",
			"target": "height"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xde6ec880165a5aec
// Course: 8th grade math
// Exercise: Volume of cylinders
// Widget key: image_1
test("geometric-solid-diagram - Volume of cylinders", () => {
	const input = {
	"shape": {
		"type": "cylinder",
		"height": 2,
		"radius": 3
	},
	"width": 220,
	"height": 94,
	"labels": [
		{
			"text": "3 units",
			"target": "radius"
		},
		{
			"text": "2 units",
			"target": "height"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x1fa26ca62e6933f3
// Course: 8th grade math
// Exercise: Volume of cylinders
// Widget key: image_1
test("geometric-solid-diagram - Volume of cylinders", () => {
	const input = {
	"shape": {
		"type": "cylinder",
		"height": 8,
		"radius": 4
	},
	"width": 175,
	"height": 180,
	"labels": [
		{
			"text": "4 units",
			"target": "radius"
		},
		{
			"text": "8 units",
			"target": "height"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x6169461922c6f1d4
// Course: 8th grade math
// Exercise: Volume of cylinders
// Widget key: image_1
test("geometric-solid-diagram - Volume of cylinders", () => {
	const input = {
	"shape": {
		"type": "cylinder",
		"height": 4,
		"radius": 6
	},
	"width": 220,
	"height": 94,
	"labels": [
		{
			"text": "6 units",
			"target": "radius"
		},
		{
			"text": "4 units",
			"target": "height"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xaead43c05c37ee59
// Course: 8th grade math
// Exercise: Volume of cylinders
// Widget key: image_1
test("geometric-solid-diagram - Volume of cylinders", () => {
	const input = {
	"shape": {
		"type": "cylinder",
		"height": 3,
		"radius": 5
	},
	"width": 240,
	"height": 96,
	"labels": [
		{
			"text": "5 units",
			"target": "radius"
		},
		{
			"text": "3 units",
			"target": "height"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xba9aa73a34f2def5
// Course: 8th grade math
// Exercise: Volume of cylinders
// Widget key: image_1
test("geometric-solid-diagram - Volume of cylinders", () => {
	const input = {
	"shape": {
		"type": "cylinder",
		"height": 8,
		"radius": 2
	},
	"width": 107,
	"height": 180,
	"labels": [
		{
			"text": "2 units",
			"target": "radius"
		},
		{
			"text": "8 units",
			"target": "height"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xc77ffbd9e312fe1e
// Course: 8th grade math
// Exercise: Volume of cylinders
// Widget key: image_1
test("geometric-solid-diagram - Volume of cylinders", () => {
	const input = {
	"shape": {
		"type": "cylinder",
		"height": 10,
		"radius": 4
	},
	"width": 146,
	"height": 180,
	"labels": [
		{
			"text": "4 units",
			"target": "radius"
		},
		{
			"text": "10 units",
			"target": "height"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x68ce34a39f63b75b
// Course: 8th grade math
// Exercise: Volume of cylinders
// Widget key: image_1
test("geometric-solid-diagram - Volume of cylinders", () => {
	const input = {
	"shape": {
		"type": "cylinder",
		"height": 6,
		"radius": 3
	},
	"width": 175,
	"height": 180,
	"labels": [
		{
			"text": "3 units",
			"target": "radius"
		},
		{
			"text": "6 units",
			"target": "height"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x90385f1429f2a59b
// Course: 8th grade math
// Exercise: Volume of cylinders
// Widget key: image_1
test("geometric-solid-diagram - Volume of cylinders", () => {
	const input = {
	"shape": {
		"type": "cylinder",
		"height": 5,
		"radius": 2
	},
	"width": 146,
	"height": 180,
	"labels": [
		{
			"text": "2 units",
			"target": "radius"
		},
		{
			"text": "5 units",
			"target": "height"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x07f186bf343b3364
// Course: 8th grade math
// Exercise: Volume of cylinders
// Widget key: image_1
test("geometric-solid-diagram - Volume of cylinders", () => {
	const input = {
	"shape": {
		"type": "cylinder",
		"height": 10,
		"radius": 4
	},
	"width": 240,
	"height": 236,
	"labels": [
		{
			"text": "4 units",
			"target": "radius"
		},
		{
			"text": "10 units",
			"target": "height"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xd42c8f5c09f49763
// Course: 8th grade math
// Exercise: Volume of cylinders
// Widget key: image_1
test("geometric-solid-diagram - Volume of cylinders", () => {
	const input = {
	"shape": {
		"type": "cylinder",
		"height": 6,
		"radius": 5
	},
	"width": 240,
	"height": 156,
	"labels": [
		{
			"text": "5 units",
			"target": "radius"
		},
		{
			"text": "6 units",
			"target": "height"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xa69e9a8cb1f3f6aa
// Course: 8th grade math
// Exercise: Volume of cylinders
// Widget key: image_1
test("geometric-solid-diagram - Volume of cylinders", () => {
	const input = {
	"shape": {
		"type": "cylinder",
		"height": 3,
		"radius": 4
	},
	"width": 180,
	"height": 103,
	"labels": [
		{
			"text": "4 units",
			"target": "radius"
		},
		{
			"text": "3 units",
			"target": "height"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x7bb3c2db5acf9797
// Course: 8th grade math
// Exercise: Volume of spheres
// Widget key: image_1
test("geometric-solid-diagram - Volume of spheres", () => {
	const input = {
	"shape": {
		"type": "sphere",
		"radius": 9
	},
	"width": 180,
	"height": 180,
	"labels": [
		{
			"text": "9 units",
			"target": "radius"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x72dc3d0e7b62efba
// Course: 8th grade math
// Exercise: Volume of spheres
// Widget key: image_1
test("geometric-solid-diagram - Volume of spheres", () => {
	const input = {
	"shape": {
		"type": "sphere",
		"radius": 0.5
	},
	"width": 240,
	"height": 240,
	"labels": [
		{
			"text": "r = 1/2 unit",
			"target": "radius"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x3fa89dec0e79ffcf
// Course: 8th grade math
// Exercise: Volume of spheres
// Widget key: image_1
test("geometric-solid-diagram - Volume of spheres", () => {
	const input = {
	"shape": {
		"type": "sphere",
		"radius": 4
	},
	"width": 180,
	"height": 180,
	"labels": [
		{
			"text": "4 units",
			"target": "radius"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x4cb611880d500097
// Course: 8th grade math
// Exercise: Volume of spheres
// Widget key: image_1
test("geometric-solid-diagram - Volume of spheres", () => {
	const input = {
	"shape": {
		"type": "sphere",
		"radius": 10
	},
	"width": 180,
	"height": 180,
	"labels": [
		{
			"text": "10 units",
			"target": "radius"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x9c18eb9bb29697f1
// Course: 8th grade math
// Exercise: Volume of spheres
// Widget key: image_1
test("geometric-solid-diagram - Volume of spheres", () => {
	const input = {
	"shape": {
		"type": "sphere",
		"radius": 0.25
	},
	"width": 240,
	"height": 240,
	"labels": [
		{
			"text": "r = 1/4 unit",
			"target": "radius"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe1d438f18f46c848
// Course: 8th grade math
// Exercise: Volume of spheres
// Widget key: image_1
test("geometric-solid-diagram - Volume of spheres", () => {
	const input = {
	"shape": {
		"type": "sphere",
		"radius": 3
	},
	"width": 180,
	"height": 180,
	"labels": [
		{
			"text": "3 units",
			"target": "radius"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x7cd6ac5126417fea
// Course: 8th grade math
// Exercise: Volume of spheres
// Widget key: image_1
test("geometric-solid-diagram - Volume of spheres", () => {
	const input = {
	"shape": {
		"type": "sphere",
		"radius": 2
	},
	"width": 180,
	"height": 180,
	"labels": [
		{
			"text": "2 units",
			"target": "radius"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x35c03635a82e3af9
// Course: 8th grade math
// Exercise: Volume of spheres
// Widget key: image_1
test("geometric-solid-diagram - Volume of spheres", () => {
	const input = {
	"shape": {
		"type": "sphere",
		"radius": 8
	},
	"width": 180,
	"height": 180,
	"labels": [
		{
			"text": "8 units",
			"target": "radius"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x316454d167fb81d4
// Course: 8th grade math
// Exercise: Volume of spheres
// Widget key: image_1
test("geometric-solid-diagram - Volume of spheres", () => {
	const input = {
	"shape": {
		"type": "sphere",
		"radius": 7
	},
	"width": 180,
	"height": 180,
	"labels": [
		{
			"text": "7 units",
			"target": "radius"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xa6e384defcba290a
// Course: 8th grade math
// Exercise: Volume of spheres
// Widget key: image_1
test("geometric-solid-diagram - Volume of spheres", () => {
	const input = {
	"shape": {
		"type": "sphere",
		"radius": 1
	},
	"width": 180,
	"height": 180,
	"labels": [
		{
			"text": "1 unit",
			"target": "radius"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x71e776db609db5ad
// Course: 8th grade math
// Exercise: Volume of spheres
// Widget key: image_1
test("geometric-solid-diagram - Volume of spheres", () => {
	const input = {
	"shape": {
		"type": "sphere",
		"radius": 5
	},
	"width": 180,
	"height": 180,
	"labels": [
		{
			"text": "5 units",
			"target": "radius"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x3fc5b28254055d8a
// Course: 8th grade math
// Exercise: Volume of spheres
// Widget key: image_1
test("geometric-solid-diagram - Volume of spheres", () => {
	const input = {
	"shape": {
		"type": "sphere",
		"radius": 6
	},
	"width": 180,
	"height": 180,
	"labels": [
		{
			"text": "6 units",
			"target": "radius"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x07dee815cde5bc2a
// Course: 8th grade math
// Exercise: Volume of cones
// Widget key: image_1
test("geometric-solid-diagram - Volume of cones", () => {
	const input = {
	"shape": {
		"type": "cone",
		"height": 10,
		"radius": 4
	},
	"width": 134,
	"height": 180,
	"labels": [
		{
			"text": "4 units",
			"target": "radius"
		},
		{
			"text": "10 units",
			"target": "height"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xd2523669f0c90b0e
// Course: 8th grade math
// Exercise: Volume of cones
// Widget key: image_1
test("geometric-solid-diagram - Volume of cones", () => {
	const input = {
	"shape": {
		"type": "cone",
		"height": 9,
		"radius": 2
	},
	"width": 112,
	"height": 240,
	"labels": [
		{
			"text": "2 units",
			"target": "radius"
		},
		{
			"text": "9 units",
			"target": "height"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xae1c703950392d27
// Course: 8th grade math
// Exercise: Volume of cones
// Widget key: image_1
test("geometric-solid-diagram - Volume of cones", () => {
	const input = {
	"shape": {
		"type": "cone",
		"height": 8,
		"radius": 6
	},
	"width": 180,
	"height": 138,
	"labels": [
		{
			"text": "6 units",
			"target": "radius"
		},
		{
			"text": "8 units",
			"target": "height"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xaf4773c52980cc4d
// Course: 8th grade math
// Exercise: Volume of cones
// Widget key: image_1
test("geometric-solid-diagram - Volume of cones", () => {
	const input = {
	"shape": {
		"type": "cone",
		"height": 6,
		"radius": 5
	},
	"width": 240,
	"height": 138,
	"labels": [
		{
			"text": "5 units",
			"target": "radius"
		},
		{
			"text": "6 units",
			"target": "height"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x1a99f30d2fb4ab35
// Course: 8th grade math
// Exercise: Volume of cones
// Widget key: image_1
test("geometric-solid-diagram - Volume of cones", () => {
	const input = {
	"shape": {
		"type": "cone",
		"height": 5,
		"radius": 5
	},
	"width": 180,
	"height": 105,
	"labels": [
		{
			"text": "5 units",
			"target": "radius"
		},
		{
			"text": "5 units",
			"target": "height"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x9b0935fd5d35903e
// Course: 8th grade math
// Exercise: Volume of cones
// Widget key: image_1
test("geometric-solid-diagram - Volume of cones", () => {
	const input = {
	"shape": {
		"type": "cone",
		"height": 3,
		"radius": 5
	},
	"width": 240,
	"height": 90,
	"labels": [
		{
			"text": "5 units",
			"target": "radius"
		},
		{
			"text": "3 units",
			"target": "height"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x5ea278c96a8b9685
// Course: 8th grade math
// Exercise: Volume of cones
// Widget key: image_1
test("geometric-solid-diagram - Volume of cones", () => {
	const input = {
	"shape": {
		"type": "cone",
		"height": 7,
		"radius": 7
	},
	"width": 180,
	"height": 105,
	"labels": [
		{
			"text": "7 units",
			"target": "radius"
		},
		{
			"text": "7 units",
			"target": "height"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xfdd6ec9efadc5fa2
// Course: 8th grade math
// Exercise: Volume of cones
// Widget key: image_1
test("geometric-solid-diagram - Volume of cones", () => {
	const input = {
	"shape": {
		"type": "cone",
		"height": 5,
		"radius": 2
	},
	"width": 134,
	"height": 180,
	"labels": [
		{
			"text": "2 units",
			"target": "radius"
		},
		{
			"text": "5 units",
			"target": "height"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x75ef3b80886fcf7b
// Course: 8th grade math
// Exercise: Volume of cones
// Widget key: image_1
test("geometric-solid-diagram - Volume of cones", () => {
	const input = {
	"shape": {
		"type": "cone",
		"height": 6,
		"radius": 10
	},
	"width": 240,
	"height": 90,
	"labels": [
		{
			"text": "10 units",
			"target": "radius"
		},
		{
			"text": "6 units",
			"target": "height"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x47283ee3cc27d5a3
// Course: 8th grade math
// Exercise: Volume of cones
// Widget key: image_1
test("geometric-solid-diagram - Volume of cones", () => {
	const input = {
	"shape": {
		"type": "cone",
		"height": 4,
		"radius": 6
	},
	"width": 240,
	"height": 106,
	"labels": [
		{
			"text": "6 units",
			"target": "radius"
		},
		{
			"text": "4 units",
			"target": "height"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x13b348a260cd9d6f
// Course: 8th grade math
// Exercise: Volume of cones
// Widget key: image_1
test("geometric-solid-diagram - Volume of cones", () => {
	const input = {
	"shape": {
		"type": "cone",
		"height": 2,
		"radius": 3
	},
	"width": 240,
	"height": 106,
	"labels": [
		{
			"text": "3 units",
			"target": "radius"
		},
		{
			"text": "2 units",
			"target": "height"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x5acd8ae84f530e66
// Course: 8th grade math
// Exercise: Volume of cones
// Widget key: image_1
test("geometric-solid-diagram - Volume of cones", () => {
	const input = {
	"shape": {
		"type": "cone",
		"height": 4,
		"radius": 3
	},
	"width": 180,
	"height": 138,
	"labels": [
		{
			"text": "3 units",
			"target": "radius"
		},
		{
			"text": "4 units",
			"target": "height"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x273f0f1d3d234c1c
// Course: 8th grade math
// Exercise: Volume of cylinders, spheres, and cones word problems
// Widget key: image_1
test("geometric-solid-diagram - Volume of cylinders spheres and cones word pr", () => {
	const input = {
	"shape": {
		"type": "cylinder",
		"height": 2,
		"radius": 4
	},
	"width": 300,
	"height": 180,
	"labels": [
		{
			"text": "4 feet",
			"target": "radius"
		},
		{
			"text": "2 feet",
			"target": "height"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xbb68cc21fed872d0
// Course: 8th grade math
// Exercise: Volume of cylinders, spheres, and cones word problems
// Widget key: image_1
test("geometric-solid-diagram - Volume of cylinders spheres and cones word pr", () => {
	const input = {
	"shape": {
		"type": "cylinder",
		"height": 20,
		"radius": 10
	},
	"width": 180,
	"height": 173,
	"labels": [
		{
			"text": "10 inches",
			"target": "radius"
		},
		{
			"text": "20 inches",
			"target": "height"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xa9a5cb4c040e5824
// Course: 8th grade math
// Exercise: Volume of cylinders, spheres, and cones word problems
// Widget key: image_1
test("geometric-solid-diagram - Volume of cylinders spheres and cones word pr", () => {
	const input = {
	"shape": {
		"type": "cone",
		"height": 8,
		"radius": 3
	},
	"width": 129,
	"height": 180,
	"labels": [
		{
			"text": "3 cm",
			"target": "radius"
		},
		{
			"text": "8 cm",
			"target": "height"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2d6345571874145e
// Course: 8th grade math
// Exercise: Volume of cylinders, spheres, and cones word problems
// Widget key: image_1
test("geometric-solid-diagram - Volume of cylinders spheres and cones word pr", () => {
	const input = {
	"shape": {
		"type": "cylinder",
		"height": 20,
		"radius": 4
	},
	"width": 145,
	"height": 260,
	"labels": [
		{
			"text": "4 centimeters",
			"target": "radius"
		},
		{
			"text": "20 centimeters",
			"target": "height"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x00dff9034f2ddb48
// Course: 8th grade math
// Exercise: Volume of cylinders, spheres, and cones word problems
// Widget key: image_1
test("geometric-solid-diagram - Volume of cylinders spheres and cones word pr", () => {
	const input = {
	"shape": {
		"type": "sphere",
		"radius": 15
	},
	"width": 180,
	"height": 180,
	"labels": [
		{
			"text": "15 cm",
			"target": "radius"
		}
	],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x8191439a8ce20320
// Course: 6th grade math
// Exercise: Identify geometric solids (3D shapes)
// Widget key: image_1
test("geometric-solid-diagram - Identify geometric solids 3D shapes", () => {
	const input = {
	"shape": {
		"type": "cylinder",
		"height": 12,
		"radius": 5
	},
	"width": 128,
	"height": 150,
	"labels": [],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x5216f19f9da4aa51
// Course: 6th grade math
// Exercise: Identify geometric solids (3D shapes)
// Widget key: image_1
test("geometric-solid-diagram - Identify geometric solids 3D shapes", () => {
	const input = {
	"shape": {
		"type": "cylinder",
		"height": 36,
		"radius": 12
	},
	"width": 300,
	"height": 400,
	"labels": [],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x473ed90494934b15
// Course: 6th grade math
// Exercise: Identify geometric solids (3D shapes)
// Widget key: image_1
test("geometric-solid-diagram - Identify geometric solids 3D shapes", () => {
	const input = {
	"shape": {
		"type": "cone",
		"height": 8,
		"radius": 3
	},
	"width": 134,
	"height": 180,
	"labels": [],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xb031ee96abb9e104
// Course: 6th grade math
// Exercise: Identify geometric solids (3D shapes)
// Widget key: image_1
test("geometric-solid-diagram - Identify geometric solids 3D shapes", () => {
	const input = {
	"shape": {
		"type": "cylinder",
		"height": 6,
		"radius": 5
	},
	"width": 180,
	"height": 74,
	"labels": [],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x4109ee185e4bee91
// Course: 6th grade math
// Exercise: Identify geometric solids (3D shapes)
// Widget key: image_1
test("geometric-solid-diagram - Identify geometric solids 3D shapes", () => {
	const input = {
	"shape": {
		"type": "cone",
		"height": 6,
		"radius": 5
	},
	"width": 180,
	"height": 68,
	"labels": [],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x5bdf2332dc5f9e15
// Course: 6th grade math
// Exercise: Identify geometric solids (3D shapes)
// Widget key: image_1
test("geometric-solid-diagram - Identify geometric solids 3D shapes", () => {
	const input = {
	"shape": {
		"type": "sphere",
		"radius": 5
	},
	"width": 180,
	"height": 180,
	"labels": [],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2d009ffa46ca3820
// Course: 6th grade math
// Exercise: Nets of polyhedra
// Widget key: choice_c_shape
test("geometric-solid-diagram - Nets of polyhedra", () => {
	const input = {
	"shape": {
		"type": "sphere",
		"radius": 5
	},
	"width": 300,
	"height": 300,
	"labels": [],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x29a33987cc70cc98
// Course: 6th grade math
// Exercise: Nets of polyhedra
// Widget key: choice_b_shape
test("geometric-solid-diagram - Nets of polyhedra", () => {
	const input = {
	"shape": {
		"type": "sphere",
		"radius": 5
	},
	"width": 300,
	"height": 300,
	"labels": [],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2b03516c0180b352
// Course: 6th grade math
// Exercise: Nets of polyhedra
// Widget key: choice_c_shape
test("geometric-solid-diagram - Nets of polyhedra", () => {
	const input = {
	"shape": {
		"type": "sphere",
		"radius": 5
	},
	"width": 320,
	"height": 320,
	"labels": [],
	"type": "geometricSolidDiagram"
} satisfies GeometricSolidDiagramInput

	// Validate the input
	const parseResult = GeometricSolidDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted geometricSolidDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateGeometricSolidDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})

