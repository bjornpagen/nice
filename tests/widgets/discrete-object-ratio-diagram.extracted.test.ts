// ============================================================================
// EXTRACTED TEST FILE - AUTO-GENERATED
// ============================================================================
// This file was automatically generated from database structured_json data
// Generated on: 2025-09-18T05:17:02.122Z
// Widget Type: discreteObjectRatioDiagram
// Source: Real questions from database
//
// These tests use actual widget configurations extracted from production data
// to ensure comprehensive test coverage with realistic scenarios.
//
// Note: Database data may not perfectly match current widget schemas, so these
// tests rely on runtime validation within generateWidget() for flexibility.
//
// DO NOT EDIT THIS FILE DIRECTLY - it will be overwritten on regeneration
//
// Run tests: bun test tests/widgets/discrete-object-ratio-diagram.extracted.test.ts
// Update snapshots: bun test tests/widgets/discrete-object-ratio-diagram.extracted.test.ts --update-snapshots
// ============================================================================

import { describe, test, expect } from "bun:test"
import { generateWidget } from "@superbuilders/qti-assessment-item-generator/widgets/widget-generator"
import type { Widget, WidgetInput } from "@superbuilders/qti-assessment-item-generator/widgets/registry"
import * as errors from "@superbuilders/errors"
import * as logger from "@superbuilders/slog"

describe("discreteObjectRatioDiagram widget tests", () => {
	// Extracted from question: x0edaeaa7d07c9f64
	// Question: Probability of selecting an apple
	// Widget key: image_1
	test("discrete-object-ratio-diagram - [x0edaeaa7d07c9f64] Probability of selecting an apple", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "discreteObjectRatioDiagram",
		  "title": "Four apples and five bananas.",
		  "width": 360,
		  "height": 320,
		  "objects": [
		    {
		      "count": 4,
		      "emoji": "🍎"
		    },
		    {
		      "count": 5,
		      "emoji": "🍌"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for discreteObjectRatioDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x23980263d7f9cc69
	// Question: Write an equation to model reading time
	// Widget key: image_1
	test("discrete-object-ratio-diagram - [x23980263d7f9cc69] Write an equation to model reading time", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "discreteObjectRatioDiagram",
		  "title": "Image of 5 books.",
		  "width": 400,
		  "height": 300,
		  "objects": [
		    {
		      "count": 5,
		      "emoji": "📘"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for discreteObjectRatioDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xb893bf1f0e48d890
	// Question: Select the equation that matches a reading scenario
	// Widget key: image_1
	test("discrete-object-ratio-diagram - [xb893bf1f0e48d890] Select the equation that matches a reading scenario", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "discreteObjectRatioDiagram",
		  "title": "Image of 8 books.",
		  "width": 400,
		  "height": 300,
		  "objects": [
		    {
		      "count": 8,
		      "emoji": "📘"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for discreteObjectRatioDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xcb5a887c55e85365
	// Question: Write an equation for pages read at a constant rate
	// Widget key: image_1
	test("discrete-object-ratio-diagram - [xcb5a887c55e85365] Write an equation for pages read at a constant rate", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "discreteObjectRatioDiagram",
		  "title": "Image of 5 books.",
		  "width": 320,
		  "height": 320,
		  "objects": [
		    {
		      "count": 5,
		      "emoji": "📘"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for discreteObjectRatioDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x890b80a083e430a8
	// Question: Complete ratio statements from an image
	// Widget key: image_1
	test("discrete-object-ratio-diagram - [x890b80a083e430a8] Complete ratio statements from an image", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "discreteObjectRatioDiagram",
		  "title": "A group of 6 large soccer balls and 9 small baseballs.",
		  "width": 320,
		  "height": 320,
		  "objects": [
		    {
		      "count": 9,
		      "emoji": "⚾"
		    },
		    {
		      "count": 6,
		      "emoji": "⚽"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for discreteObjectRatioDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x3b23ab4479e78201
	// Question: Mean from a pictograph
	// Widget key: image_2
	test("discrete-object-ratio-diagram - [x3b23ab4479e78201] Mean from a pictograph", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "discreteObjectRatioDiagram",
		  "title": "Home runs hit by each team in a baseball league.",
		  "width": 350,
		  "height": 413,
		  "objects": [
		    {
		      "count": 7,
		      "emoji": "⚾"
		    },
		    {
		      "count": 2,
		      "emoji": "⚾"
		    },
		    {
		      "count": 4,
		      "emoji": "⚾"
		    },
		    {
		      "count": 3,
		      "emoji": "⚾"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for discreteObjectRatioDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xf8498b902f74bfd6
	// Question: Ratio of chicks to eggs
	// Widget key: image_1
	test("discrete-object-ratio-diagram - [xf8498b902f74bfd6] Ratio of chicks to eggs", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "discreteObjectRatioDiagram",
		  "title": "5 eggs and 3 chicks.",
		  "width": 320,
		  "height": 320,
		  "objects": [
		    {
		      "count": 5,
		      "emoji": "🥚"
		    },
		    {
		      "count": 3,
		      "emoji": "🐥"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for discreteObjectRatioDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x8382bb4365a08be2
	// Question: Ratio of apples to bananas
	// Widget key: image_1
	test("discrete-object-ratio-diagram - [x8382bb4365a08be2] Ratio of apples to bananas", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "discreteObjectRatioDiagram",
		  "title": "6 bananas and 2 apples.",
		  "width": 320,
		  "height": 300,
		  "objects": [
		    {
		      "count": 2,
		      "emoji": "🍎"
		    },
		    {
		      "count": 6,
		      "emoji": "🍌"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for discreteObjectRatioDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x70d4ff682334d643
	// Question: Ratio of turtles to total animals
	// Widget key: animals_image
	test("discrete-object-ratio-diagram - [x70d4ff682334d643] Ratio of turtles to total animals", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "discreteObjectRatioDiagram",
		  "title": "5 turtles and 3 seals.",
		  "width": 400,
		  "height": 320,
		  "objects": [
		    {
		      "count": 5,
		      "emoji": "🐢"
		    },
		    {
		      "count": 3,
		      "emoji": "🦭"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for discreteObjectRatioDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x6ef8f4eac2e3f75f
	// Question: Ratio of cookies to ice cream cones
	// Widget key: image_1
	test("discrete-object-ratio-diagram - [x6ef8f4eac2e3f75f] Ratio of cookies to ice cream cones", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "discreteObjectRatioDiagram",
		  "title": "Cookies and ice cream cones",
		  "width": 320,
		  "height": 320,
		  "objects": [
		    {
		      "count": 3,
		      "emoji": "🍪"
		    },
		    {
		      "count": 1,
		      "emoji": "🍦"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for discreteObjectRatioDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x94428d0ee089a38d
	// Question: Ratio of sharks to total animals
	// Widget key: image_1
	test("discrete-object-ratio-diagram - [x94428d0ee089a38d] Ratio of sharks to total animals", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "discreteObjectRatioDiagram",
		  "title": "2 sharks and 8 whales.",
		  "width": 320,
		  "height": 320,
		  "objects": [
		    {
		      "count": 2,
		      "emoji": "🦈"
		    },
		    {
		      "count": 8,
		      "emoji": "🐋"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for discreteObjectRatioDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xe572120d52dde32b
	// Question: Ratio of cows to chunks of cheese
	// Widget key: image_1
	test("discrete-object-ratio-diagram - [xe572120d52dde32b] Ratio of cows to chunks of cheese", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "discreteObjectRatioDiagram",
		  "title": "Cows and chunks of cheese",
		  "width": 400,
		  "height": 320,
		  "objects": [
		    {
		      "count": 5,
		      "emoji": "🐄"
		    },
		    {
		      "count": 2,
		      "emoji": "🧀"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for discreteObjectRatioDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x8c833e327631f33c
	// Question: Ratio of raccoons to total animals
	// Widget key: image_1
	test("discrete-object-ratio-diagram - [x8c833e327631f33c] Ratio of raccoons to total animals", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "discreteObjectRatioDiagram",
		  "title": "2 rabbits and 4 raccoons",
		  "width": 355,
		  "height": 300,
		  "objects": [
		    {
		      "count": 2,
		      "emoji": "🐇"
		    },
		    {
		      "count": 4,
		      "emoji": "🦝"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for discreteObjectRatioDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x675601e0d938dab4
	// Question: Ratio of penguins to total birds
	// Widget key: image_1
	test("discrete-object-ratio-diagram - [x675601e0d938dab4] Ratio of penguins to total birds", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "discreteObjectRatioDiagram",
		  "title": "4 chicks and 5 penguins",
		  "width": 360,
		  "height": 360,
		  "objects": [
		    {
		      "count": 4,
		      "emoji": "🐥"
		    },
		    {
		      "count": 5,
		      "emoji": "🐧"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for discreteObjectRatioDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

})
