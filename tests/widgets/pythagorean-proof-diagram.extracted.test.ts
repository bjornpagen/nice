// ============================================================================
// EXTRACTED TEST FILE - AUTO-GENERATED
// ============================================================================
// This file was automatically generated from database structured_json data
// Generated on: 2025-09-17T15:38:03.973Z
// Widget Type: pythagoreanProofDiagram
// Source: Real questions from database
//
// These tests use actual widget configurations extracted from production data
// to ensure comprehensive test coverage with realistic scenarios.
//
// Note: Database data may not perfectly match current widget schemas, so these
// tests rely on runtime validation within generateWidget() for flexibility.
//
// DO NOT EDIT THIS FILE DIRECTLY - it will be overwritten on regeneration
//
// Run tests: bun test tests/widgets/pythagorean-proof-diagram.extracted.test.ts
// Update snapshots: bun test tests/widgets/pythagorean-proof-diagram.extracted.test.ts --update-snapshots
// ============================================================================

import { describe, test, expect } from "bun:test"
import { generateWidget } from "@superbuilders/qti-assessment-item-generator/widgets/widget-generator"
import type { Widget, WidgetInput } from "@superbuilders/qti-assessment-item-generator/widgets/registry"
import * as errors from "@superbuilders/errors"
import * as logger from "@superbuilders/slog"

describe("pythagoreanProofDiagram widget tests", () => {
	// Extracted from question: x5771b36c95aef1a7
	// Question: Area of the square adjacent to the third side
	// Widget key: image_1
	test("pythagorean-proof-diagram - [x5771b36c95aef1a7] Area of the square adjacent to the third side", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "pythagoreanProofDiagram",
		  "sideA": {
		    "label": "a",
		    "square": {
		      "area": 33,
		      "type": "value",
		      "color": "#ca337c"
		    }
		  },
		  "sideB": {
		    "label": "b",
		    "square": {
		      "type": "unknown",
		      "color": "#11accd"
		    }
		  },
		  "sideC": {
		    "label": "c",
		    "square": {
		      "area": 44,
		      "type": "value",
		      "color": "#1fab54"
		    }
		  },
		  "width": 240,
		  "height": 193
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for pythagoreanProofDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x1ecf301bf3e57e34
	// Question: Determine whether a triangle is right using areas of attached squares
	// Widget key: image_1
	test("pythagorean-proof-diagram - [x1ecf301bf3e57e34] Determine whether a triangle is right using areas of attached squares", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "pythagoreanProofDiagram",
		  "sideA": {
		    "label": "a",
		    "square": {
		      "area": 58,
		      "type": "value",
		      "color": "#e3f2fd"
		    }
		  },
		  "sideB": {
		    "label": "b",
		    "square": {
		      "area": 10,
		      "type": "value",
		      "color": "#fff3e0"
		    }
		  },
		  "sideC": {
		    "label": "c",
		    "square": {
		      "area": 64,
		      "type": "value",
		      "color": "#e8f5e9"
		    }
		  },
		  "width": 281,
		  "height": 400
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for pythagoreanProofDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x576c8d2a8fa0de16
	// Question: Area of the square on the triangle's third side
	// Widget key: image_1
	test("pythagorean-proof-diagram - [x576c8d2a8fa0de16] Area of the square on the triangle's third side", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "pythagoreanProofDiagram",
		  "sideA": {
		    "label": "2",
		    "square": null
		  },
		  "sideB": {
		    "label": "4",
		    "square": null
		  },
		  "sideC": {
		    "label": " ",
		    "square": {
		      "type": "unknown",
		      "color": "#11accd"
		    }
		  },
		  "width": 300,
		  "height": 263
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for pythagoreanProofDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xffe4fe69c7ada6c6
	// Question: Areas of squares on the sides of a right triangle
	// Widget key: image_1
	test("pythagorean-proof-diagram - [xffe4fe69c7ada6c6] Areas of squares on the sides of a right triangle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "pythagoreanProofDiagram",
		  "sideA": {
		    "label": "a",
		    "square": {
		      "type": "unknown",
		      "color": "#ffbde0"
		    }
		  },
		  "sideB": {
		    "label": "b",
		    "square": {
		      "type": "unknown",
		      "color": "#ffbde0"
		    }
		  },
		  "sideC": {
		    "label": "c",
		    "square": {
		      "area": 55,
		      "type": "value",
		      "color": "#ffbde0"
		    }
		  },
		  "width": 224,
		  "height": 240
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for pythagoreanProofDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x8d9bf2e7361a83d4
	// Question: Areas of smaller squares on a right triangle
	// Widget key: image_1
	test("pythagorean-proof-diagram - [x8d9bf2e7361a83d4] Areas of smaller squares on a right triangle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "pythagoreanProofDiagram",
		  "sideA": {
		    "label": "a",
		    "square": {
		      "type": "unknown",
		      "color": "#ffbde0"
		    }
		  },
		  "sideB": {
		    "label": "b",
		    "square": {
		      "type": "unknown",
		      "color": "#ffbde0"
		    }
		  },
		  "sideC": {
		    "label": "c",
		    "square": {
		      "area": 67,
		      "type": "value",
		      "color": "#ffbde0"
		    }
		  },
		  "width": 240,
		  "height": 197
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for pythagoreanProofDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2629e6f0882108f6
	// Question: Areas of squares on the sides of a right triangle
	// Widget key: image_1
	test("pythagorean-proof-diagram - [x2629e6f0882108f6] Areas of squares on the sides of a right triangle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "pythagoreanProofDiagram",
		  "sideA": {
		    "label": "a",
		    "square": {
		      "type": "unknown",
		      "color": "#ffbde0"
		    }
		  },
		  "sideB": {
		    "label": "b",
		    "square": {
		      "type": "unknown",
		      "color": "#ffbde0"
		    }
		  },
		  "sideC": {
		    "label": "c",
		    "square": {
		      "area": 36,
		      "type": "value",
		      "color": "#ffbde0"
		    }
		  },
		  "width": 218.571,
		  "height": 240
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for pythagoreanProofDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x3950c0fe656bd0d1
	// Question: Find the length of the third side from square areas
	// Widget key: image_1
	test("pythagorean-proof-diagram - [x3950c0fe656bd0d1] Find the length of the third side from square areas", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "pythagoreanProofDiagram",
		  "sideA": {
		    "label": " ",
		    "square": {
		      "area": 32,
		      "type": "value",
		      "color": "#11accd"
		    }
		  },
		  "sideB": {
		    "label": " ",
		    "square": {
		      "area": 32,
		      "type": "value",
		      "color": "#ca337c"
		    }
		  },
		  "sideC": {
		    "label": "x",
		    "square": null
		  },
		  "width": 300,
		  "height": 170
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for pythagoreanProofDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xd6373945f7c29ea6
	// Question: Area of the square on the third side
	// Widget key: image_1
	test("pythagorean-proof-diagram - [xd6373945f7c29ea6] Area of the square on the third side", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "pythagoreanProofDiagram",
		  "sideA": {
		    "label": "a",
		    "square": {
		      "area": 35,
		      "type": "value",
		      "color": "#ca337c"
		    }
		  },
		  "sideB": {
		    "label": "b",
		    "square": {
		      "area": 50,
		      "type": "value",
		      "color": "#1fab54"
		    }
		  },
		  "sideC": {
		    "label": "c",
		    "square": {
		      "type": "unknown",
		      "color": "#11accd"
		    }
		  },
		  "width": 271.698,
		  "height": 300
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for pythagoreanProofDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x03f872387a8cca62
	// Question: Determine whether a triangle is right using square areas
	// Widget key: image_1
	test("pythagorean-proof-diagram - [x03f872387a8cca62] Determine whether a triangle is right using square areas", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "pythagoreanProofDiagram",
		  "sideA": {
		    "label": "a",
		    "square": {
		      "area": 18,
		      "type": "value",
		      "color": "#f0f0f0"
		    }
		  },
		  "sideB": {
		    "label": "b",
		    "square": {
		      "area": 7,
		      "type": "value",
		      "color": "#f0f0f0"
		    }
		  },
		  "sideC": {
		    "label": "c",
		    "square": {
		      "area": 27,
		      "type": "value",
		      "color": "#f0f0f0"
		    }
		  },
		  "width": 300,
		  "height": 300
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for pythagoreanProofDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x5bac99795acaee26
	// Question: Area of the square on the hypotenuse
	// Widget key: image_1
	test("pythagorean-proof-diagram - [x5bac99795acaee26] Area of the square on the hypotenuse", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "pythagoreanProofDiagram",
		  "sideA": {
		    "label": "3",
		    "square": null
		  },
		  "sideB": {
		    "label": "7",
		    "square": null
		  },
		  "sideC": {
		    "label": " ",
		    "square": {
		      "type": "unknown",
		      "color": "#e0e0e0"
		    }
		  },
		  "width": 300,
		  "height": 255
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for pythagoreanProofDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xcf1d0c79357900b1
	// Question: Find the hypotenuse from areas of attached squares
	// Widget key: image_1
	test("pythagorean-proof-diagram - [xcf1d0c79357900b1] Find the hypotenuse from areas of attached squares", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "pythagoreanProofDiagram",
		  "sideA": {
		    "label": " ",
		    "square": {
		      "area": 13,
		      "type": "value",
		      "color": "#ca337c"
		    }
		  },
		  "sideB": {
		    "label": " ",
		    "square": {
		      "area": 29.25,
		      "type": "value",
		      "color": "#11accd"
		    }
		  },
		  "sideC": {
		    "label": "x",
		    "square": null
		  },
		  "width": 300,
		  "height": 209
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for pythagoreanProofDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x5f138893ed28d3c3
	// Question: Extra distance via Dayton
	// Widget key: image_1
	test("pythagorean-proof-diagram - [x5f138893ed28d3c3] Extra distance via Dayton", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "pythagoreanProofDiagram",
		  "sideA": {
		    "label": "165",
		    "square": null
		  },
		  "sideB": {
		    "label": "x",
		    "square": null
		  },
		  "sideC": {
		    "label": "173",
		    "square": null
		  },
		  "width": 396,
		  "height": 162
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for pythagoreanProofDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

})
