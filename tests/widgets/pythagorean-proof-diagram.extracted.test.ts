// ============================================================================
// EXTRACTED TEST FILE - AUTO-GENERATED
// ============================================================================
// This file was automatically generated from database structured_json data
// Generated on: 2025-09-01T22:27:08.777Z
// Widget Type: pythagoreanProofDiagram
// Source: Real questions from database
//
// These tests use actual widget configurations extracted from production data
// to ensure comprehensive test coverage with realistic scenarios.
//
// DO NOT EDIT THIS FILE DIRECTLY - it will be overwritten on regeneration
//
// Run tests: bun test tests/widgets/pythagorean-proof-diagram.extracted.test.ts
// Update snapshots: bun test tests/widgets/pythagorean-proof-diagram.extracted.test.ts --update-snapshots
// ============================================================================

import { expect, test } from "bun:test"
import type { z } from "zod"
import { generatePythagoreanProofDiagram, PythagoreanProofDiagramPropsSchema } from "@/lib/widgets/generators"

type PythagoreanProofDiagramInput = z.input<typeof PythagoreanProofDiagramPropsSchema>


// Extracted from question: x1ecf301bf3e57e34
// Course: 8th grade math
// Exercise: Use area of squares to visualize Pythagorean theorem
// Widget key: image_1
test("pythagorean-proof-diagram - Use area of squares to visualize Pythagorean th", async () => {
	const input = {
	"width": 281,
	"height": 400,
	"sideA": { "label": "a", "square": { "type": "value", "area": 58, "color": "#FFE082" } },
	"sideB": { "label": "b", "square": { "type": "value", "area": 10, "color": "#90CAF9" } },
	"sideC": { "label": "c", "square": { "type": "value", "area": 64, "color": "#A5D6A7" } },
	"type": "pythagoreanProofDiagram"
	} satisfies PythagoreanProofDiagramInput

	// Validate the input
	const parseResult = PythagoreanProofDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted pythagoreanProofDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generatePythagoreanProofDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x5771b36c95aef1a7
// Course: 8th grade math
// Exercise: Use area of squares to visualize Pythagorean theorem
// Widget key: image_1
test("pythagorean-proof-diagram - Use area of squares to visualize Pythagorean th", async () => {
	const input = {
	"width": 240,
	"height": 193,
	"sideA": { "label": null, "square": { "type": "value", "area": 33, "color": "#ca337c" } },
	"sideB": { "label": null, "square": { "type": "unknown", "color": "#11accd" } },
	"sideC": { "label": null, "square": { "type": "value", "area": 44, "color": "#1fab54" } },
	"type": "pythagoreanProofDiagram"
	} satisfies PythagoreanProofDiagramInput

	// Validate the input
	const parseResult = PythagoreanProofDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted pythagoreanProofDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generatePythagoreanProofDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x576c8d2a8fa0de16
// Course: 8th grade math
// Exercise: Use area of squares to visualize Pythagorean theorem
// Widget key: image_1
test("pythagorean-proof-diagram - Use area of squares to visualize Pythagorean th", async () => {
	const input = {
	"width": 300,
	"height": 263,
	"sideA": { "label": "2", "square": { "type": "value", "area": 4, "color": "#FFE082" } },
	"sideB": { "label": "4", "square": { "type": "value", "area": 16, "color": "#90CAF9" } },
	"sideC": { "label": "x", "square": { "type": "unknown", "color": "#FFAB91" } },
	"type": "pythagoreanProofDiagram"
	} satisfies PythagoreanProofDiagramInput

	// Validate the input
	const parseResult = PythagoreanProofDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted pythagoreanProofDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generatePythagoreanProofDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xffe4fe69c7ada6c6
// Course: 8th grade math
// Exercise: Use area of squares to visualize Pythagorean theorem
// Widget key: image_1
test("pythagorean-proof-diagram - Use area of squares to visualize Pythagorean th", async () => {
	const input = {
	"width": 224,
	"height": 240,
	"sideA": { "label": "a", "square": { "type": "unknown", "color": "#ffbde0" } },
	"sideB": { "label": "b", "square": { "type": "unknown", "color": "#ffbde0" } },
	"sideC": { "label": "c", "square": { "type": "value", "area": 55, "color": "#ffbde0" } },
	"type": "pythagoreanProofDiagram"
	} satisfies PythagoreanProofDiagramInput

	// Validate the input
	const parseResult = PythagoreanProofDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted pythagoreanProofDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generatePythagoreanProofDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x8d9bf2e7361a83d4
// Course: 8th grade math
// Exercise: Use area of squares to visualize Pythagorean theorem
// Widget key: image_1
test("pythagorean-proof-diagram - Use area of squares to visualize Pythagorean th", async () => {
	const input = {
	"width": 240,
	"height": 197,
	"sideA": { "label": " ", "square": { "type": "unknown", "color": "#ffbde0" } },
	"sideB": { "label": " ", "square": { "type": "unknown", "color": "#ffbde0" } },
	"sideC": { "label": " ", "square": { "type": "value", "area": 67, "color": "#ffbde0" } },
	"type": "pythagoreanProofDiagram"
} satisfies PythagoreanProofDiagramInput

	// Validate the input
	const parseResult = PythagoreanProofDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted pythagoreanProofDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generatePythagoreanProofDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2629e6f0882108f6
// Course: 8th grade math
// Exercise: Use area of squares to visualize Pythagorean theorem
// Widget key: image_1
test("pythagorean-proof-diagram - Use area of squares to visualize Pythagorean th", async () => {
	const input = {
	"width": 219,
	"height": 240,
	"sideA": { "label": "a", "square": { "type": "value", "area": 12, "color": "#FFBDE0" } },
	"sideB": { "label": "b", "square": { "type": "value", "area": 24, "color": "#FFBDE0" } },
	"sideC": { "label": "c", "square": { "type": "value", "area": 36, "color": "#FFBDE0" } },
	"type": "pythagoreanProofDiagram"
} satisfies PythagoreanProofDiagramInput

	// Validate the input
	const parseResult = PythagoreanProofDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted pythagoreanProofDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generatePythagoreanProofDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x3950c0fe656bd0d1
// Course: 8th grade math
// Exercise: Use area of squares to visualize Pythagorean theorem
// Widget key: image_1
test("pythagorean-proof-diagram - Use area of squares to visualize Pythagorean th", async () => {
	const input = {
	"width": 300,
	"height": 170,
	"sideA": { "label": " ", "square": { "type": "value", "area": 32, "color": "#11accd" } },
	"sideB": { "label": " ", "square": { "type": "value", "area": 32, "color": "#ca337c" } },
	"sideC": { "label": "x", "square": { "type": "value", "area": 64, "color": "#FFE082" } },
	"type": "pythagoreanProofDiagram"
} satisfies PythagoreanProofDiagramInput

	// Validate the input
	const parseResult = PythagoreanProofDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted pythagoreanProofDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generatePythagoreanProofDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xd6373945f7c29ea6
// Course: 8th grade math
// Exercise: Use area of squares to visualize Pythagorean theorem
// Widget key: image_1
test("pythagorean-proof-diagram - Use area of squares to visualize Pythagorean th", async () => {
	const input = {
	"width": 272,
	"height": 300,
	"sideA": { "label": " ", "square": { "type": "value", "area": 35, "color": "#ca337c" } },
	"sideB": { "label": " ", "square": { "type": "value", "area": 50, "color": "#1fab54" } },
	"sideC": { "label": " ", "square": { "type": "unknown", "color": "#11accd" } },
	"type": "pythagoreanProofDiagram"
} satisfies PythagoreanProofDiagramInput

	// Validate the input
	const parseResult = PythagoreanProofDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted pythagoreanProofDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generatePythagoreanProofDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x03f872387a8cca62
// Course: 8th grade math
// Exercise: Use area of squares to visualize Pythagorean theorem
// Widget key: image_1
test("pythagorean-proof-diagram - Use area of squares to visualize Pythagorean th", async () => {
	const input = {
	"width": 300,
	"height": 300,
	"sideA": { "label": "a", "square": { "type": "value", "area": 18, "color": "#FFE082" } },
	"sideB": { "label": "b", "square": { "type": "value", "area": 7, "color": "#C5E1A5" } },
	"sideC": { "label": "c", "square": { "type": "value", "area": 27, "color": "#80DEEA" } },
	"type": "pythagoreanProofDiagram"
} satisfies PythagoreanProofDiagramInput

	// Validate the input
	const parseResult = PythagoreanProofDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted pythagoreanProofDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generatePythagoreanProofDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x5bac99795acaee26
// Course: 8th grade math
// Exercise: Use area of squares to visualize Pythagorean theorem
// Widget key: image_1
test("pythagorean-proof-diagram - Use area of squares to visualize Pythagorean th", async () => {
	const input = {
	"width": 300,
	"height": 255,
	"sideA": { "label": "3", "square": { "type": "value", "area": 9, "color": "#FFE082" } },
	"sideB": { "label": "7", "square": { "type": "value", "area": 49, "color": "#90CAF9" } },
	"sideC": { "label": " ", "square": { "type": "unknown", "color": "#B39DDB" } },
	"type": "pythagoreanProofDiagram"
} satisfies PythagoreanProofDiagramInput

	// Validate the input
	const parseResult = PythagoreanProofDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted pythagoreanProofDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generatePythagoreanProofDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xcf1d0c79357900b1
// Course: 8th grade math
// Exercise: Use area of squares to visualize Pythagorean theorem
// Widget key: image_1
test("pythagorean-proof-diagram - Use area of squares to visualize Pythagorean th", async () => {
	const input = {
	"width": 300,
	"height": 209,
	"sideA": { "label": "a", "square": { "type": "value", "area": 13, "color": "#ca337c" } },
	"sideB": { "label": "b", "square": { "type": "value", "area": 29.25, "color": "#11accd" } },
	"sideC": { "label": "x", "square": { "type": "unknown", "color": "#00000000" } },
	"type": "pythagoreanProofDiagram"
} satisfies PythagoreanProofDiagramInput

	// Validate the input
	const parseResult = PythagoreanProofDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted pythagoreanProofDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generatePythagoreanProofDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})

test("pythagorean-proof-diagram - renders a side label without a square", async () => {
	const input = {
		width: 300,
		height: 300,
		sideA: { label: "5" },
		sideB: { square: { type: "value", area: 144, color: "#90CAF9" } },
		sideC: { square: { type: "value", area: 169, color: "#A5D6A7" } },
		type: "pythagoreanProofDiagram"
	} satisfies PythagoreanProofDiagramInput

	const parseResult = PythagoreanProofDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		return
	}
	const svg = await generatePythagoreanProofDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})

test("pythagorean-proof-diagram - renders a square without a side label", async () => {
	const input = {
		width: 300,
		height: 300,
		sideA: { square: { type: "value", area: 25, color: "#FFE082" } },
		sideB: { square: { type: "value", area: 144, color: "#90CAF9" } },
		sideC: { label: "13" },
		type: "pythagoreanProofDiagram"
	} satisfies PythagoreanProofDiagramInput

	const parseResult = PythagoreanProofDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		return
	}
	const svg = await generatePythagoreanProofDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})

test("pythagorean-proof-diagram - renders a side with both a square and a label", async () => {
	const input = {
		width: 600,
		height: 600,
		sideA: { label: "a", square: { type: "value", area: 49, color: "#FFE082" } },
		sideB: { label: "b", square: { type: "value", area: 576, color: "#90CAF9" } },
		sideC: { label: "c", square: { type: "value", area: 625, color: "#A5D6A7" } },
		type: "pythagoreanProofDiagram"
	} satisfies PythagoreanProofDiagramInput

	const parseResult = PythagoreanProofDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		return
	}
	const svg = await generatePythagoreanProofDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})

test("pythagorean-proof-diagram - renders a diagram with a bare side", async () => {
	const input = {
		width: 300,
		height: 300,
		sideA: {},
		sideB: { square: { type: "value", area: 144, color: "#90CAF9" } },
		sideC: { square: { type: "value", area: 169, color: "#A5D6A7" } },
		type: "pythagoreanProofDiagram"
	} satisfies PythagoreanProofDiagramInput

	const parseResult = PythagoreanProofDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		return
	}
	const svg = await generatePythagoreanProofDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})

test("pythagorean-proof-diagram - correctly calculates a missing numeric area with the new schema", async () => {
	const input = {
		width: 300,
		height: 300,
		sideA: { square: { type: "value", area: 8, color: "#FFE082" } },
		sideB: { square: { type: "value", area: 15, color: "#90CAF9" } },
		sideC: { square: { type: "unknown", color: "#A5D6A7" } },
		type: "pythagoreanProofDiagram"
	} satisfies PythagoreanProofDiagramInput

	const parseResult = PythagoreanProofDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		return
	}

	const svg = await generatePythagoreanProofDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})

test("pythagorean-proof-diagram - renders string square label for B", async () => {
	const input = {
		width: 300,
		height: 240,
		sideA: { square: { type: "value", area: 9, color: "#FFE082" } },
		sideB: { square: { type: "unknown", color: "#90CAF9" } },
		sideC: { square: { type: "value", area: 13, color: "#A5D6A7" } },
		type: "pythagoreanProofDiagram"
	} satisfies PythagoreanProofDiagramInput

	const parseResult = PythagoreanProofDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		return
	}
	const svg = await generatePythagoreanProofDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})

