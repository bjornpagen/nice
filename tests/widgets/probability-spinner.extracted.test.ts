// ============================================================================
// EXTRACTED TEST FILE - AUTO-GENERATED
// ============================================================================
// This file was automatically generated from database structured_json data
// Generated on: 2025-09-17T15:38:03.935Z
// Widget Type: probabilitySpinner
// Source: Real questions from database
//
// These tests use actual widget configurations extracted from production data
// to ensure comprehensive test coverage with realistic scenarios.
//
// Note: Database data may not perfectly match current widget schemas, so these
// tests rely on runtime validation within generateWidget() for flexibility.
//
// DO NOT EDIT THIS FILE DIRECTLY - it will be overwritten on regeneration
//
// Run tests: bun test tests/widgets/probability-spinner.extracted.test.ts
// Update snapshots: bun test tests/widgets/probability-spinner.extracted.test.ts --update-snapshots
// ============================================================================

import { describe, test, expect } from "bun:test"
import { generateWidget } from "@superbuilders/qti-assessment-item-generator/widgets/widget-generator"
import type { Widget, WidgetInput } from "@superbuilders/qti-assessment-item-generator/widgets/registry"
import * as errors from "@superbuilders/errors"
import * as logger from "@superbuilders/slog"

describe("probabilitySpinner widget tests", () => {
	// Extracted from question: x891978ec319a0b50
	// Question: Probability of landing on the beaver
	// Widget key: image_1
	test("probability-spinner - [x891978ec319a0b50] Probability of landing on the beaver", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "probabilitySpinner",
		  "title": "Animal Spinner",
		  "width": 265,
		  "groups": [
		    {
		      "color": "#8B4513",
		      "count": 1,
		      "emoji": "🦫"
		    },
		    {
		      "color": "#6D4C41",
		      "count": 1,
		      "emoji": "🦦"
		    },
		    {
		      "color": "#2E7D32",
		      "count": 1,
		      "emoji": "🐢"
		    }
		  ],
		  "height": 265,
		  "pointerAngle": 60
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for probabilitySpinner", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x9700f4a6390b2913
	// Question: Probability of not purple on a spinner
	// Widget key: image_1
	test("probability-spinner - [x9700f4a6390b2913] Probability of not purple on a spinner", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "probabilitySpinner",
		  "title": null,
		  "width": 200,
		  "groups": [
		    {
		      "color": "#28ae7b",
		      "count": 1,
		      "emoji": ":"
		    },
		    {
		      "color": "#6495ed",
		      "count": 1,
		      "emoji": ":"
		    },
		    {
		      "color": "#9d38bd",
		      "count": 1,
		      "emoji": ":"
		    },
		    {
		      "color": "#ff00af",
		      "count": 1,
		      "emoji": ":"
		    }
		  ],
		  "height": 200,
		  "pointerAngle": 80
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for probabilitySpinner", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xddee92f1f80973a2
	// Question: Probability of landing on a shaded sector
	// Widget key: image_1
	test("probability-spinner - [xddee92f1f80973a2] Probability of landing on a shaded sector", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "probabilitySpinner",
		  "title": null,
		  "width": 280,
		  "groups": [
		    {
		      "color": "#4A90E2",
		      "count": 2,
		      "emoji": null
		    },
		    {
		      "color": "#FFFFFF",
		      "count": 3,
		      "emoji": null
		    }
		  ],
		  "height": 280,
		  "pointerAngle": 90
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for probabilitySpinner", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x14436e9c61b55100
	// Question: Probability of landing on green
	// Widget key: image_1
	test("probability-spinner - [x14436e9c61b55100] Probability of landing on green", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "probabilitySpinner",
		  "title": "Color Spinner",
		  "width": 200,
		  "groups": [
		    {
		      "color": "#6495ed",
		      "count": 1,
		      "emoji": "🎯"
		    },
		    {
		      "color": "#9d38bd",
		      "count": 1,
		      "emoji": "🎯"
		    },
		    {
		      "color": "#ff00af",
		      "count": 1,
		      "emoji": "🎯"
		    },
		    {
		      "color": "#28ae7b",
		      "count": 1,
		      "emoji": "🎯"
		    }
		  ],
		  "height": 200,
		  "pointerAngle": 80
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for probabilitySpinner", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xcf77f5eab0b7b91c
	// Question: Probability of landing on a beaver
	// Widget key: image_1
	test("probability-spinner - [xcf77f5eab0b7b91c] Probability of landing on a beaver", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "probabilitySpinner",
		  "title": "Animal Spinner",
		  "width": 265,
		  "groups": [
		    {
		      "color": "#2E8B57",
		      "count": 1,
		      "emoji": "🐢"
		    },
		    {
		      "color": "#8B4513",
		      "count": 4,
		      "emoji": "🦫"
		    }
		  ],
		  "height": 265,
		  "pointerAngle": 10
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for probabilitySpinner", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x337e68855d74fec2
	// Question: Probability of not shaded sector on a spinner
	// Widget key: image_1
	test("probability-spinner - [x337e68855d74fec2] Probability of not shaded sector on a spinner", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "probabilitySpinner",
		  "title": null,
		  "width": 280,
		  "groups": [
		    {
		      "color": "#D0021B",
		      "count": 1,
		      "emoji": ":"
		    },
		    {
		      "color": "#FFFFFF",
		      "count": 1,
		      "emoji": ":"
		    },
		    {
		      "color": "#D0021B",
		      "count": 1,
		      "emoji": ":"
		    },
		    {
		      "color": "#FFFFFF",
		      "count": 1,
		      "emoji": ":"
		    },
		    {
		      "color": "#D0021B",
		      "count": 1,
		      "emoji": ":"
		    },
		    {
		      "color": "#FFFFFF",
		      "count": 1,
		      "emoji": ":"
		    },
		    {
		      "color": "#D0021B",
		      "count": 1,
		      "emoji": ":"
		    },
		    {
		      "color": "#FFFFFF",
		      "count": 1,
		      "emoji": ":"
		    },
		    {
		      "color": "#D0021B",
		      "count": 1,
		      "emoji": ":"
		    },
		    {
		      "color": "#FFFFFF",
		      "count": 1,
		      "emoji": ":"
		    },
		    {
		      "color": "#D0021B",
		      "count": 1,
		      "emoji": ":"
		    },
		    {
		      "color": "#FFFFFF",
		      "count": 1,
		      "emoji": ":"
		    }
		  ],
		  "height": 280,
		  "pointerAngle": 15
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for probabilitySpinner", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xcf163a33a66504d4
	// Question: Probability of not landing on a squirrel
	// Widget key: image_1
	test("probability-spinner - [xcf163a33a66504d4] Probability of not landing on a squirrel", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "probabilitySpinner",
		  "title": "Animal Spinner",
		  "width": 265,
		  "groups": [
		    {
		      "color": "#F94144",
		      "count": 1,
		      "emoji": "🐿️"
		    },
		    {
		      "color": "#F3722C",
		      "count": 1,
		      "emoji": "🐛"
		    },
		    {
		      "color": "#F9C74F",
		      "count": 1,
		      "emoji": "🐰"
		    },
		    {
		      "color": "#90BE6D",
		      "count": 1,
		      "emoji": "🦝"
		    },
		    {
		      "color": "#577590",
		      "count": 1,
		      "emoji": "🐞"
		    }
		  ],
		  "height": 265,
		  "pointerAngle": 288
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for probabilitySpinner", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x9547925ec463e26e
	// Question: Probability of blue on a spinner
	// Widget key: image_1
	test("probability-spinner - [x9547925ec463e26e] Probability of blue on a spinner", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "probabilitySpinner",
		  "title": "Color Spinner",
		  "width": 200,
		  "groups": [
		    {
		      "color": "#6495ed",
		      "count": 1,
		      "emoji": "🔵"
		    },
		    {
		      "color": "#9d38bd",
		      "count": 1,
		      "emoji": "🟣"
		    },
		    {
		      "color": "#ff00af",
		      "count": 1,
		      "emoji": "🩷"
		    },
		    {
		      "color": "#28ae7b",
		      "count": 1,
		      "emoji": "🟢"
		    }
		  ],
		  "height": 200,
		  "pointerAngle": 135
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for probabilitySpinner", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x549f87579a87b182
	// Question: Best prediction for spinner outcomes
	// Widget key: image_1
	test("probability-spinner - [x549f87579a87b182] Best prediction for spinner outcomes", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "probabilitySpinner",
		  "title": "Animal Spinner",
		  "width": 265,
		  "groups": [
		    {
		      "color": "#6A5ACD",
		      "count": 1,
		      "emoji": "🐘"
		    },
		    {
		      "color": "#FFB347",
		      "count": 2,
		      "emoji": "🐵"
		    },
		    {
		      "color": "#6EC6FF",
		      "count": 2,
		      "emoji": "🐭"
		    }
		  ],
		  "height": 265,
		  "pointerAngle": 36
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for probabilitySpinner", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x26c9cadb446b3736
	// Question: Best prediction for not landing on an elephant
	// Widget key: image_1
	test("probability-spinner - [x26c9cadb446b3736] Best prediction for not landing on an elephant", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "probabilitySpinner",
		  "title": "Animal Spinner",
		  "width": 265,
		  "groups": [
		    {
		      "color": "#B0BEC5",
		      "count": 4,
		      "emoji": "🐘"
		    },
		    {
		      "color": "#FFD166",
		      "count": 2,
		      "emoji": "🐭"
		    },
		    {
		      "color": "#8BC34A",
		      "count": 1,
		      "emoji": "🐒"
		    }
		  ],
		  "height": 265,
		  "pointerAngle": 250
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for probabilitySpinner", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xe8d4841dc9a44ca7
	// Question: Best prediction for spinner outcomes
	// Widget key: image_1
	test("probability-spinner - [xe8d4841dc9a44ca7] Best prediction for spinner outcomes", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "probabilitySpinner",
		  "title": null,
		  "width": 280,
		  "groups": [
		    {
		      "color": "#5B87E5",
		      "count": 4,
		      "emoji": null
		    },
		    {
		      "color": "#FFFFFF",
		      "count": 1,
		      "emoji": null
		    }
		  ],
		  "height": 280,
		  "pointerAngle": 20
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for probabilitySpinner", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

})
