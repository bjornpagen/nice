// ============================================================================
// EXTRACTED TEST FILE - AUTO-GENERATED
// ============================================================================
// This file was automatically generated from database structured_json data
// Generated on: 2025-09-18T05:17:02.170Z
// Widget Type: hangerDiagram
// Source: Real questions from database
//
// These tests use actual widget configurations extracted from production data
// to ensure comprehensive test coverage with realistic scenarios.
//
// Note: Database data may not perfectly match current widget schemas, so these
// tests rely on runtime validation within generateWidget() for flexibility.
//
// DO NOT EDIT THIS FILE DIRECTLY - it will be overwritten on regeneration
//
// Run tests: bun test tests/widgets/hanger-diagram.extracted.test.ts
// Update snapshots: bun test tests/widgets/hanger-diagram.extracted.test.ts --update-snapshots
// ============================================================================

import { describe, test, expect } from "bun:test"
import { generateWidget } from "@superbuilders/qti-assessment-item-generator/widgets/widget-generator"
import type { Widget, WidgetInput } from "@superbuilders/qti-assessment-item-generator/widgets/registry"
import * as errors from "@superbuilders/errors"
import * as logger from "@superbuilders/slog"

describe("hangerDiagram widget tests", () => {
	// Extracted from question: xba981b30cbe68354
	// Question: Find the value of h from a hanger diagram
	// Widget key: image_1
	test("hanger-diagram - [xba981b30cbe68354] Find the value of h from a hanger diagram", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "hangerDiagram",
		  "width": 400,
		  "height": 320,
		  "leftSide": [
		    {
		      "label": "h",
		      "shape": "pentagon",
		      "backgroundColor": "#F4F8FF"
		    },
		    {
		      "label": 9,
		      "shape": "square",
		      "backgroundColor": "#FFF5CC"
		    }
		  ],
		  "rightSide": [
		    {
		      "label": 14,
		      "shape": "square",
		      "backgroundColor": "#E6FFEE"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for hangerDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xd9de1d2596f1641b
	// Question: Find the value of b from a balanced hanger
	// Widget key: image_1
	test("hanger-diagram - [xd9de1d2596f1641b] Find the value of b from a balanced hanger", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "hangerDiagram",
		  "width": 400,
		  "height": 300,
		  "leftSide": [
		    {
		      "label": 15,
		      "shape": "square",
		      "backgroundColor": "#FFF5CC"
		    }
		  ],
		  "rightSide": [
		    {
		      "label": "b",
		      "shape": "triangle",
		      "backgroundColor": "#E6FFEE"
		    },
		    {
		      "label": 8,
		      "shape": "square",
		      "backgroundColor": "#F4F8FF"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for hangerDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xc8fb56456e01507c
	// Question: Solve for g from a balanced hanger
	// Widget key: image_1
	test("hanger-diagram - [xc8fb56456e01507c] Solve for g from a balanced hanger", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "hangerDiagram",
		  "width": 400,
		  "height": 320,
		  "leftSide": [
		    {
		      "label": 30,
		      "shape": "square",
		      "backgroundColor": "#FFF5CC"
		    }
		  ],
		  "rightSide": [
		    {
		      "label": 26,
		      "shape": "square",
		      "backgroundColor": "#F4F8FF"
		    },
		    {
		      "label": "g",
		      "shape": "square",
		      "backgroundColor": "#E6FFEE"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for hangerDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x9834712212681367
	// Question: Find the value of d from a balanced hanger
	// Widget key: image_1
	test("hanger-diagram - [x9834712212681367] Find the value of d from a balanced hanger", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "hangerDiagram",
		  "width": 400,
		  "height": 320,
		  "leftSide": [
		    {
		      "label": 6,
		      "shape": "square",
		      "backgroundColor": "#F4F8FF"
		    },
		    {
		      "label": "d",
		      "shape": "hexagon",
		      "backgroundColor": "#FFF5CC"
		    }
		  ],
		  "rightSide": [
		    {
		      "label": 21,
		      "shape": "square",
		      "backgroundColor": "#E6FFEE"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for hangerDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xe0956d0ca8d9bdeb
	// Question: Find the value of j from a balanced hanger
	// Widget key: image_1
	test("hanger-diagram - [xe0956d0ca8d9bdeb] Find the value of j from a balanced hanger", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "hangerDiagram",
		  "width": 400,
		  "height": 300,
		  "leftSide": [
		    {
		      "label": 5,
		      "shape": "square",
		      "backgroundColor": "#F4F8FF"
		    },
		    {
		      "label": "j",
		      "shape": "hexagon",
		      "backgroundColor": "#E6FFEE"
		    }
		  ],
		  "rightSide": [
		    {
		      "label": 24,
		      "shape": "square",
		      "backgroundColor": "#F4F8FF"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for hangerDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xee48001aac0416bf
	// Question: Find the value of n from a balanced hanger
	// Widget key: image_1
	test("hanger-diagram - [xee48001aac0416bf] Find the value of n from a balanced hanger", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "hangerDiagram",
		  "width": 400,
		  "height": 300,
		  "leftSide": [
		    {
		      "label": 16,
		      "shape": "square",
		      "backgroundColor": "#FFF5CC"
		    }
		  ],
		  "rightSide": [
		    {
		      "label": "n",
		      "shape": "triangle",
		      "backgroundColor": "#F4F8FF"
		    },
		    {
		      "label": 10,
		      "shape": "square",
		      "backgroundColor": "#FFF5CC"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for hangerDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xf798c55875e14677
	// Question: Find the value of u from a balanced hanger
	// Widget key: image_1
	test("hanger-diagram - [xf798c55875e14677] Find the value of u from a balanced hanger", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "hangerDiagram",
		  "width": 400,
		  "height": 320,
		  "leftSide": [
		    {
		      "label": "u",
		      "shape": "hexagon",
		      "backgroundColor": "#FFF5CC"
		    },
		    {
		      "label": "u",
		      "shape": "hexagon",
		      "backgroundColor": "#FFF5CC"
		    },
		    {
		      "label": "u",
		      "shape": "hexagon",
		      "backgroundColor": "#FFF5CC"
		    },
		    {
		      "label": "u",
		      "shape": "hexagon",
		      "backgroundColor": "#FFF5CC"
		    },
		    {
		      "label": "u",
		      "shape": "hexagon",
		      "backgroundColor": "#FFF5CC"
		    }
		  ],
		  "rightSide": [
		    {
		      "label": 60,
		      "shape": "square",
		      "backgroundColor": "#F4F8FF"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for hangerDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xb137180f3ee3aff2
	// Question: Solve for r using a balanced hanger
	// Widget key: image_1
	test("hanger-diagram - [xb137180f3ee3aff2] Solve for r using a balanced hanger", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "hangerDiagram",
		  "width": 400,
		  "height": 340,
		  "leftSide": [
		    {
		      "label": 32,
		      "shape": "square",
		      "backgroundColor": "#FFF5CC"
		    }
		  ],
		  "rightSide": [
		    {
		      "label": "r",
		      "shape": "hexagon",
		      "backgroundColor": "#E6FFEE"
		    },
		    {
		      "label": "r",
		      "shape": "hexagon",
		      "backgroundColor": "#E6FFEE"
		    },
		    {
		      "label": "r",
		      "shape": "hexagon",
		      "backgroundColor": "#E6FFEE"
		    },
		    {
		      "label": "r",
		      "shape": "hexagon",
		      "backgroundColor": "#E6FFEE"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for hangerDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x74d72de3124828aa
	// Question: Solve for v using a balanced hanger
	// Widget key: image_1
	test("hanger-diagram - [x74d72de3124828aa] Solve for v using a balanced hanger", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "hangerDiagram",
		  "width": 400,
		  "height": 300,
		  "leftSide": [
		    {
		      "label": 27,
		      "shape": "square",
		      "backgroundColor": "#E6F0FF"
		    }
		  ],
		  "rightSide": [
		    {
		      "label": "v",
		      "shape": "circle",
		      "backgroundColor": "#FFF5CC"
		    },
		    {
		      "label": "v",
		      "shape": "circle",
		      "backgroundColor": "#FFF5CC"
		    },
		    {
		      "label": "v",
		      "shape": "circle",
		      "backgroundColor": "#FFF5CC"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for hangerDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xe7c1c0f9f15d2432
	// Question: Solve for t from a balanced hanger
	// Widget key: image_1
	test("hanger-diagram - [xe7c1c0f9f15d2432] Solve for t from a balanced hanger", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "hangerDiagram",
		  "width": 400,
		  "height": 350,
		  "leftSide": [
		    {
		      "label": "t",
		      "shape": "hexagon",
		      "backgroundColor": "#E6FFEE"
		    },
		    {
		      "label": "t",
		      "shape": "hexagon",
		      "backgroundColor": "#E6FFEE"
		    },
		    {
		      "label": "t",
		      "shape": "hexagon",
		      "backgroundColor": "#E6FFEE"
		    },
		    {
		      "label": "t",
		      "shape": "hexagon",
		      "backgroundColor": "#E6FFEE"
		    },
		    {
		      "label": "t",
		      "shape": "hexagon",
		      "backgroundColor": "#E6FFEE"
		    },
		    {
		      "label": "t",
		      "shape": "hexagon",
		      "backgroundColor": "#E6FFEE"
		    },
		    {
		      "label": "t",
		      "shape": "hexagon",
		      "backgroundColor": "#E6FFEE"
		    }
		  ],
		  "rightSide": [
		    {
		      "label": 49,
		      "shape": "square",
		      "backgroundColor": "#F4F8FF"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for hangerDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xbb6d5288507ae03f
	// Question: Solve for c from a balanced hanger
	// Widget key: image_1
	test("hanger-diagram - [xbb6d5288507ae03f] Solve for c from a balanced hanger", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "hangerDiagram",
		  "width": 400,
		  "height": 320,
		  "leftSide": [
		    {
		      "label": 24,
		      "shape": "square",
		      "backgroundColor": "#FFF5CC"
		    }
		  ],
		  "rightSide": [
		    {
		      "label": "c",
		      "shape": "circle",
		      "backgroundColor": "#F4F8FF"
		    },
		    {
		      "label": "c",
		      "shape": "circle",
		      "backgroundColor": "#F4F8FF"
		    },
		    {
		      "label": "c",
		      "shape": "circle",
		      "backgroundColor": "#F4F8FF"
		    },
		    {
		      "label": "c",
		      "shape": "circle",
		      "backgroundColor": "#F4F8FF"
		    },
		    {
		      "label": "c",
		      "shape": "circle",
		      "backgroundColor": "#F4F8FF"
		    },
		    {
		      "label": "c",
		      "shape": "circle",
		      "backgroundColor": "#F4F8FF"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for hangerDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x5ff4153a982797c6
	// Question: Find the value of a from a balanced hanger
	// Widget key: image_1
	test("hanger-diagram - [x5ff4153a982797c6] Find the value of a from a balanced hanger", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "hangerDiagram",
		  "width": 400,
		  "height": 300,
		  "leftSide": [
		    {
		      "label": "a",
		      "shape": "triangle",
		      "backgroundColor": "#F4F8FF"
		    },
		    {
		      "label": "a",
		      "shape": "triangle",
		      "backgroundColor": "#F4F8FF"
		    }
		  ],
		  "rightSide": [
		    {
		      "label": 36,
		      "shape": "square",
		      "backgroundColor": "#E6FFEE"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for hangerDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x21b4eb70ae1dac31
	// Question: Balanced hanger: select an equation and solve for z
	// Widget key: image_1
	test("hanger-diagram - [x21b4eb70ae1dac31] Balanced hanger: select an equation and solve for z", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "hangerDiagram",
		  "width": 400,
		  "height": 300,
		  "leftSide": [
		    {
		      "label": 45,
		      "shape": "square",
		      "backgroundColor": "#F4F8FF"
		    }
		  ],
		  "rightSide": [
		    {
		      "label": "z",
		      "shape": "triangle",
		      "backgroundColor": "#FFECE6"
		    },
		    {
		      "label": "z",
		      "shape": "triangle",
		      "backgroundColor": "#FFECE6"
		    },
		    {
		      "label": "z",
		      "shape": "triangle",
		      "backgroundColor": "#FFECE6"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for hangerDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2c28c2860a5feef7
	// Question: Write an equation from a balanced hanger
	// Widget key: image_1
	test("hanger-diagram - [x2c28c2860a5feef7] Write an equation from a balanced hanger", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "hangerDiagram",
		  "width": 400,
		  "height": 300,
		  "leftSide": [
		    {
		      "label": "1/5",
		      "shape": "square",
		      "backgroundColor": "#F4F8FF"
		    },
		    {
		      "label": "z",
		      "shape": "triangle",
		      "backgroundColor": "#FFF5CC"
		    }
		  ],
		  "rightSide": [
		    {
		      "label": "3/5",
		      "shape": "square",
		      "backgroundColor": "#E6FFEE"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for hangerDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xafc71c7ecaf33d77
	// Question: Write an equation from a balanced hanger
	// Widget key: image_1
	test("hanger-diagram - [xafc71c7ecaf33d77] Write an equation from a balanced hanger", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "hangerDiagram",
		  "width": 400,
		  "height": 300,
		  "leftSide": [
		    {
		      "label": "p",
		      "shape": "hexagon",
		      "backgroundColor": "#F4F8FF"
		    },
		    {
		      "label": "p",
		      "shape": "hexagon",
		      "backgroundColor": "#F4F8FF"
		    },
		    {
		      "label": "p",
		      "shape": "hexagon",
		      "backgroundColor": "#F4F8FF"
		    }
		  ],
		  "rightSide": [
		    {
		      "label": "1/2",
		      "shape": "square",
		      "backgroundColor": "#FFF5CC"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for hangerDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x05bbc87fe429e2e9
	// Question: Write an equation from a balanced hanger
	// Widget key: image_1
	test("hanger-diagram - [x05bbc87fe429e2e9] Write an equation from a balanced hanger", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "hangerDiagram",
		  "width": 320,
		  "height": 300,
		  "leftSide": [
		    {
		      "label": 1,
		      "shape": "square",
		      "backgroundColor": "#F4F8FF"
		    },
		    {
		      "label": 1,
		      "shape": "square",
		      "backgroundColor": "#F4F8FF"
		    },
		    {
		      "label": 1,
		      "shape": "square",
		      "backgroundColor": "#F4F8FF"
		    },
		    {
		      "label": 1,
		      "shape": "square",
		      "backgroundColor": "#F4F8FF"
		    },
		    {
		      "label": 1,
		      "shape": "square",
		      "backgroundColor": "#F4F8FF"
		    },
		    {
		      "label": 1,
		      "shape": "square",
		      "backgroundColor": "#F4F8FF"
		    },
		    {
		      "label": 1,
		      "shape": "square",
		      "backgroundColor": "#F4F8FF"
		    }
		  ],
		  "rightSide": [
		    {
		      "label": 1,
		      "shape": "square",
		      "backgroundColor": "#F4F8FF"
		    },
		    {
		      "label": 1,
		      "shape": "square",
		      "backgroundColor": "#F4F8FF"
		    },
		    {
		      "label": "r",
		      "shape": "pentagon",
		      "backgroundColor": "#FFF5CC"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for hangerDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x506aef4743280d14
	// Question: Balanced hanger: Select an equation and solve for m
	// Widget key: image_1
	test("hanger-diagram - [x506aef4743280d14] Balanced hanger: Select an equation and solve for m", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "hangerDiagram",
		  "width": 400,
		  "height": 320,
		  "leftSide": [
		    {
		      "label": "m",
		      "shape": "pentagon",
		      "backgroundColor": "#E6FFEE"
		    },
		    {
		      "label": "m",
		      "shape": "pentagon",
		      "backgroundColor": "#E6FFEE"
		    },
		    {
		      "label": "m",
		      "shape": "pentagon",
		      "backgroundColor": "#E6FFEE"
		    },
		    {
		      "label": "m",
		      "shape": "pentagon",
		      "backgroundColor": "#E6FFEE"
		    },
		    {
		      "label": "m",
		      "shape": "pentagon",
		      "backgroundColor": "#E6FFEE"
		    }
		  ],
		  "rightSide": [
		    {
		      "label": 50,
		      "shape": "square",
		      "backgroundColor": "#FFF5CC"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for hangerDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xbfee2dfa3fea665a
	// Question: Select an equation from a balanced hanger and solve for k
	// Widget key: image_1
	test("hanger-diagram - [xbfee2dfa3fea665a] Select an equation from a balanced hanger and solve for k", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "hangerDiagram",
		  "width": 400,
		  "height": 300,
		  "leftSide": [
		    {
		      "label": "k",
		      "shape": "hexagon",
		      "backgroundColor": "#F4F8FF"
		    },
		    {
		      "label": "k",
		      "shape": "hexagon",
		      "backgroundColor": "#F4F8FF"
		    }
		  ],
		  "rightSide": [
		    {
		      "label": 18,
		      "shape": "square",
		      "backgroundColor": "#FFF5CC"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for hangerDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xbed56df7026ec360
	// Question: Balanced hanger: select an equation and solve for b
	// Widget key: image_1
	test("hanger-diagram - [xbed56df7026ec360] Balanced hanger: select an equation and solve for b", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "hangerDiagram",
		  "width": 400,
		  "height": 300,
		  "leftSide": [
		    {
		      "label": 27,
		      "shape": "square",
		      "backgroundColor": "#F4F8FF"
		    }
		  ],
		  "rightSide": [
		    {
		      "label": "b",
		      "shape": "circle",
		      "backgroundColor": "#FFEFD5"
		    },
		    {
		      "label": 9,
		      "shape": "square",
		      "backgroundColor": "#E6FFEE"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for hangerDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x19dbc69eea8f8d47
	// Question: Balanced hanger: choose an equation and solve for h
	// Widget key: image_1
	test("hanger-diagram - [x19dbc69eea8f8d47] Balanced hanger: choose an equation and solve for h", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "hangerDiagram",
		  "width": 400,
		  "height": 300,
		  "leftSide": [
		    {
		      "label": "h",
		      "shape": "hexagon",
		      "backgroundColor": "#F4F0FF"
		    },
		    {
		      "label": 7,
		      "shape": "square",
		      "backgroundColor": "#FFF5CC"
		    }
		  ],
		  "rightSide": [
		    {
		      "label": 20,
		      "shape": "square",
		      "backgroundColor": "#E6FFEE"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for hangerDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x3f4289200e75bd94
	// Question: Write an equation for a balanced hanger
	// Widget key: image_1
	test("hanger-diagram - [x3f4289200e75bd94] Write an equation for a balanced hanger", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "hangerDiagram",
		  "width": 400,
		  "height": 320,
		  "leftSide": [
		    {
		      "label": "x",
		      "shape": "circle",
		      "backgroundColor": "#E6F7FF"
		    },
		    {
		      "label": "x",
		      "shape": "circle",
		      "backgroundColor": "#E6F7FF"
		    }
		  ],
		  "rightSide": [
		    {
		      "label": 1,
		      "shape": "square",
		      "backgroundColor": "#FFF5CC"
		    },
		    {
		      "label": 1,
		      "shape": "square",
		      "backgroundColor": "#FFF5CC"
		    },
		    {
		      "label": 1,
		      "shape": "square",
		      "backgroundColor": "#FFF5CC"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for hangerDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xffb04c29985491e2
	// Question: Equation from a balanced hanger and find the value
	// Widget key: image_1
	test("hanger-diagram - [xffb04c29985491e2] Equation from a balanced hanger and find the value", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "hangerDiagram",
		  "width": 400,
		  "height": 300,
		  "leftSide": [
		    {
		      "label": 9,
		      "shape": "square",
		      "backgroundColor": "#FFF5CC"
		    }
		  ],
		  "rightSide": [
		    {
		      "label": 2,
		      "shape": "square",
		      "backgroundColor": "#FFF5CC"
		    },
		    {
		      "label": "c",
		      "shape": "hexagon",
		      "backgroundColor": "#E6FFEE"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for hangerDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xf8c87c9676b2ec68
	// Question: Write an equation from a balanced hanger
	// Widget key: image_1
	test("hanger-diagram - [xf8c87c9676b2ec68] Write an equation from a balanced hanger", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "hangerDiagram",
		  "width": 400,
		  "height": 300,
		  "leftSide": [
		    {
		      "label": 9.1,
		      "shape": "square",
		      "backgroundColor": "#F4F8FF"
		    }
		  ],
		  "rightSide": [
		    {
		      "label": "a",
		      "shape": "hexagon",
		      "backgroundColor": "#E6FFEE"
		    },
		    {
		      "label": 4,
		      "shape": "square",
		      "backgroundColor": "#FFF5CC"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for hangerDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x0f57b9ee12b30bdd
	// Question: Write an equation from a balanced hanger
	// Widget key: image_1
	test("hanger-diagram - [x0f57b9ee12b30bdd] Write an equation from a balanced hanger", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "type": "hangerDiagram",
		  "width": 320,
		  "height": 320,
		  "leftSide": [
		    {
		      "label": 12,
		      "shape": "square",
		      "backgroundColor": "#F4F8FF"
		    }
		  ],
		  "rightSide": [
		    {
		      "label": "c",
		      "shape": "pentagon",
		      "backgroundColor": "#E6FFEE"
		    },
		    {
		      "label": "c",
		      "shape": "pentagon",
		      "backgroundColor": "#E6FFEE"
		    },
		    {
		      "label": "c",
		      "shape": "pentagon",
		      "backgroundColor": "#E6FFEE"
		    },
		    {
		      "label": "c",
		      "shape": "pentagon",
		      "backgroundColor": "#E6FFEE"
		    }
		  ]
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for hangerDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

})
