// ============================================================================
// EXTRACTED TEST FILE - AUTO-GENERATED
// ============================================================================
// This file was automatically generated from database structured_json data
// Generated on: 2025-09-01T22:27:08.781Z
// Widget Type: vennDiagram
// Source: Real questions from database
//
// These tests use actual widget configurations extracted from production data
// to ensure comprehensive test coverage with realistic scenarios.
//
// DO NOT EDIT THIS FILE DIRECTLY - it will be overwritten on regeneration
//
// Run tests: bun test tests/widgets/venn-diagram.extracted.test.ts
// Update snapshots: bun test tests/widgets/venn-diagram.extracted.test.ts --update-snapshots
// ============================================================================

import { expect, test } from "bun:test"
import type { z } from "zod"
import { generateVennDiagram, VennDiagramPropsSchema } from "@/lib/widgets/generators"

type VennDiagramInput = z.input<typeof VennDiagramPropsSchema>


// Extracted from question: x7bfa8efd005608e3
// Course: 8th grade math
// Exercise: Create two-way frequency tables
// Widget key: image_1
test("venn-diagram - Create two-way frequency tables", async () => {
	const input = {
	"width": 350,
	"height": 262,
	"circleA": {
		"color": "#1E90FF80",
		"count": 7,
		"label": "Wore a Tie"
	},
	"circleB": {
		"color": "#FF6B6B80",
		"count": 5,
		"label": "Received a Compliment"
	},
	"outsideCount": 10,
	"intersectionCount": 8,
	"type": "vennDiagram"
} satisfies VennDiagramInput

	// Validate the input
	const parseResult = VennDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted vennDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateVennDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x9987e96f3253973f
// Course: 8th grade math
// Exercise: Create two-way frequency tables
// Widget key: image_1
test("venn-diagram - Create two-way frequency tables", async () => {
	const input = {
	"width": 350,
	"height": 262,
	"circleA": {
		"color": "#1E90FF80",
		"count": 5,
		"label": "Have a Dog"
	},
	"circleB": {
		"color": "#FF6B6B80",
		"count": 6,
		"label": "Have a Cat"
	},
	"outsideCount": 0,
	"intersectionCount": 7,
	"type": "vennDiagram"
} satisfies VennDiagramInput

	// Validate the input
	const parseResult = VennDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted vennDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateVennDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x47fe1dcc0683e6b8
// Course: 8th grade math
// Exercise: Create two-way frequency tables
// Widget key: image_1
test("venn-diagram - Create two-way frequency tables", async () => {
	const input = {
	"width": 350,
	"height": 262,
	"circleA": {
		"color": "#8B451380",
		"count": 6,
		"label": "Chocolate"
	},
	"circleB": {
		"color": "#2E8B5780",
		"count": 1,
		"label": "Coconut"
	},
	"outsideCount": 2,
	"intersectionCount": 3,
	"type": "vennDiagram"
} satisfies VennDiagramInput

	// Validate the input
	const parseResult = VennDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted vennDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateVennDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2da876528f5927a0
// Course: 8th grade math
// Exercise: Create two-way frequency tables
// Widget key: venn_image
test("venn-diagram - Create two-way frequency tables", async () => {
	const input = {
	"width": 350,
	"height": 262,
	"circleA": {
		"color": "#11accd80",
		"count": 14,
		"label": "10,000 hours"
	},
	"circleB": {
		"color": "#ffb62780",
		"count": 8,
		"label": "Won a major"
	},
	"outsideCount": 6,
	"intersectionCount": 37,
	"type": "vennDiagram"
} satisfies VennDiagramInput

	// Validate the input
	const parseResult = VennDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted vennDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateVennDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xfa8c23e9a8954056
// Course: 8th grade math
// Exercise: Create two-way frequency tables
// Widget key: image_1
test("venn-diagram - Create two-way frequency tables", async () => {
	const input = {
	"width": 350,
	"height": 262,
	"circleA": {
		"color": "#1E90FF80",
		"count": 0,
		"label": "Studied For"
	},
	"circleB": {
		"color": "#FF6B6B80",
		"count": 2,
		"label": "Passed"
	},
	"outsideCount": 11,
	"intersectionCount": 12,
	"type": "vennDiagram"
} satisfies VennDiagramInput

	// Validate the input
	const parseResult = VennDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted vennDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateVennDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x316af7c25381119c
// Course: 8th grade math
// Exercise: Create two-way frequency tables
// Widget key: image_1
test("venn-diagram - Create two-way frequency tables", async () => {
	const input = {
	"width": 350,
	"height": 262,
	"circleA": {
		"color": "#1E90FF80",
		"count": 27,
		"label": "Cold Medicine"
	},
	"circleB": {
		"color": "#FF6B6B80",
		"count": 20,
		"label": "Cold Longer than 7 Days"
	},
	"outsideCount": 30,
	"intersectionCount": 23,
	"type": "vennDiagram"
} satisfies VennDiagramInput

	// Validate the input
	const parseResult = VennDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted vennDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateVennDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x9534f2e4ae832609
// Course: 8th grade math
// Exercise: Create two-way frequency tables
// Widget key: venn_image
test("venn-diagram - Create two-way frequency tables", async () => {
	const input = {
	"width": 350,
	"height": 262,
	"circleA": {
		"color": "#1E90FF80",
		"count": 17,
		"label": "Foreign Language"
	},
	"circleB": {
		"color": "#FF6B6B80",
		"count": 12,
		"label": "Sport"
	},
	"outsideCount": 4,
	"intersectionCount": 21,
	"type": "vennDiagram"
} satisfies VennDiagramInput

	// Validate the input
	const parseResult = VennDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted vennDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateVennDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xbce4e4db20a1f148
// Course: 8th grade math
// Exercise: Create two-way frequency tables
// Widget key: image_1
test("venn-diagram - Create two-way frequency tables", async () => {
	const input = {
	"width": 350,
	"height": 262,
	"circleA": {
		"color": "#1E90FF80",
		"count": 5,
		"label": "Australia"
	},
	"circleB": {
		"color": "#FF6B6B80",
		"count": 6,
		"label": "United States"
	},
	"outsideCount": 4,
	"intersectionCount": 2,
	"type": "vennDiagram"
} satisfies VennDiagramInput

	// Validate the input
	const parseResult = VennDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted vennDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateVennDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x544f8e3a8ef6c493
// Course: 8th grade math
// Exercise: Create two-way frequency tables
// Widget key: image_1
test("venn-diagram - Create two-way frequency tables", async () => {
	const input = {
	"width": 350,
	"height": 262,
	"circleA": {
		"color": "#1E90FF80",
		"count": 4,
		"label": "Like Vegetables"
	},
	"circleB": {
		"color": "#FF6B6B80",
		"count": 8,
		"label": "Like Fruit"
	},
	"outsideCount": 5,
	"intersectionCount": 23,
	"type": "vennDiagram"
} satisfies VennDiagramInput

	// Validate the input
	const parseResult = VennDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted vennDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateVennDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x9b3992fe3e1009c3
// Course: 8th grade math
// Exercise: Create two-way frequency tables
// Widget key: venn_image
test("venn-diagram - Create two-way frequency tables", async () => {
	const input = {
	"width": 350,
	"height": 262,
	"circleA": {
		"color": "#1E90FF80",
		"count": 5,
		"label": "Multiple of 3"
	},
	"circleB": {
		"color": "#FF6B6B80",
		"count": 2,
		"label": "Multiple of 4"
	},
	"outsideCount": 5,
	"intersectionCount": 4,
	"type": "vennDiagram"
} satisfies VennDiagramInput

	// Validate the input
	const parseResult = VennDiagramPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted vennDiagram:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = await generateVennDiagram(parseResult.data)
	expect(svg).toMatchSnapshot()
})

