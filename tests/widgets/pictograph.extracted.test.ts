// ============================================================================
// EXTRACTED TEST FILE - AUTO-GENERATED
// ============================================================================
// This file was automatically generated from database structured_json data
// Generated on: 2025-09-01T22:27:08.787Z
// Widget Type: pictograph
// Source: Real questions from database
//
// These tests use actual widget configurations extracted from production data
// to ensure comprehensive test coverage with realistic scenarios.
//
// DO NOT EDIT THIS FILE DIRECTLY - it will be overwritten on regeneration
//
// Run tests: bun test tests/widgets/pictograph.extracted.test.ts
// Update snapshots: bun test tests/widgets/pictograph.extracted.test.ts --update-snapshots
// ============================================================================

import { expect, test } from "bun:test"
import type { z } from "zod"
import { generatePictograph, PictographPropsSchema } from "@/lib/widgets/generators"

type PictographInput = z.input<typeof PictographPropsSchema>


// Extracted from question: x3b23ab4479e78201
// Course: 6th grade math
// Exercise: Calculating the mean: data displays
// Widget key: image_2
test("pictograph - Calculating the mean data displays", () => {
	const input = {
	"key": {
		"icon": "⚾",
		"label": "= 1 home run"
	},
	"data": [],
	"title": "Key",
	"type": "pictograph"
} satisfies PictographInput

	// Validate the input
	const parseResult = PictographPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted pictograph:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generatePictograph(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x3b23ab4479e78201
// Course: 6th grade math
// Exercise: Calculating the mean: data displays
// Widget key: image_3
test("pictograph - Calculating the mean data displays", () => {
	const input = {
	"key": {
		"icon": "⚾",
		"label": "= 1 home run"
	},
	"data": [
		{
			"category": "Lancers",
			"iconCount": 7
		},
		{
			"category": "Huskies",
			"iconCount": 2
		},
		{
			"category": "Grizzlies",
			"iconCount": 4
		},
		{
			"category": "Raiders",
			"iconCount": 3
		}
	],
	"title": "Home runs hit by each team in a baseball league",
	"type": "pictograph"
} satisfies PictographInput

	// Validate the input
	const parseResult = PictographPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted pictograph:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generatePictograph(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xbb618582cbf1434e
// Course: 6th grade math
// Exercise: Calculating the mean: data displays
// Widget key: image_2
test("pictograph - Calculating the mean data displays", () => {
	const input = {
	"key": {
		"icon": "🍨",
		"label": "= 3 sundaes"
	},
	"data": [
		{
			"category": "Two Scoops",
			"iconCount": 4
		},
		{
			"category": "I Scream",
			"iconCount": 6
		},
		{
			"category": "Dream Cream",
			"iconCount": 2
		},
		{
			"category": "Sundays",
			"iconCount": 4
		}
	],
	"title": "Sundaes each store made on Monday",
	"type": "pictograph"
} satisfies PictographInput

	// Validate the input
	const parseResult = PictographPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted pictograph:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generatePictograph(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xbb618582cbf1434e
// Course: 6th grade math
// Exercise: Calculating the mean: data displays
// Widget key: image_3
test("pictograph - Calculating the mean data displays", () => {
	const input = {
	"key": {
		"icon": "🍨",
		"label": "= 3 sundaes"
	},
	"data": [
		{
			"category": "Two Scoops",
			"iconCount": 4
		},
		{
			"category": "I Scream",
			"iconCount": 6
		},
		{
			"category": "Dream Cream",
			"iconCount": 2
		},
		{
			"category": "Sundays",
			"iconCount": 4
		}
	],
	"title": "Sundaes each store made on Monday",
	"type": "pictograph"
} satisfies PictographInput

	// Validate the input
	const parseResult = PictographPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted pictograph:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generatePictograph(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe1cca2942efaee77
// Course: 6th grade math
// Exercise: Calculating the mean: data displays
// Widget key: image_2
test("pictograph - Calculating the mean data displays", () => {
	const input = {
	"key": {
		"icon": "🐳",
		"label": "= 1 toy whale"
	},
	"data": [
		{
			"category": "Luca",
			"iconCount": 3
		},
		{
			"category": "Niklas",
			"iconCount": 3
		},
		{
			"category": "Finn",
			"iconCount": 5
		},
		{
			"category": "Lena",
			"iconCount": 3
		}
	],
	"title": "Toy whales each swimmer held in the pool.",
	"type": "pictograph"
} satisfies PictographInput

	// Validate the input
	const parseResult = PictographPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted pictograph:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generatePictograph(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe1cca2942efaee77
// Course: 6th grade math
// Exercise: Calculating the mean: data displays
// Widget key: image_3
test("pictograph - Calculating the mean data displays", () => {
	const input = {
	"key": {
		"icon": "🐳",
		"label": "= 1 toy whale"
	},
	"data": [],
	"title": "Key",
	"type": "pictograph"
} satisfies PictographInput

	// Validate the input
	const parseResult = PictographPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted pictograph:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generatePictograph(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x4cddb3b060f1c05e
// Course: 6th grade math
// Exercise: Mean absolute deviation (MAD)
// Widget key: image_1
test("pictograph - Mean absolute deviation MAD", () => {
	const input = {
	"key": {
		"icon": "🪄",
		"label": "= 1 wand"
	},
	"data": [
		{
			"category": "Glenda",
			"iconCount": 5
		},
		{
			"category": "Sprinkle",
			"iconCount": 3
		},
		{
			"category": "Godmother",
			"iconCount": 3
		},
		{
			"category": "Rosebud",
			"iconCount": 5
		}
	],
	"title": "Wands held by fairies in Fairyland",
	"type": "pictograph"
} satisfies PictographInput

	// Validate the input
	const parseResult = PictographPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted pictograph:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generatePictograph(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x4cddb3b060f1c05e
// Course: 6th grade math
// Exercise: Mean absolute deviation (MAD)
// Widget key: image_2
test("pictograph - Mean absolute deviation MAD", () => {
	const input = {
	"key": {
		"icon": "🪄",
		"label": "= 1 wand"
	},
	"data": [],
	"title": "Key",
	"type": "pictograph"
} satisfies PictographInput

	// Validate the input
	const parseResult = PictographPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted pictograph:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generatePictograph(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x001a52d916889012
// Course: 6th grade math
// Exercise: Mean absolute deviation (MAD)
// Widget key: image_1
test("pictograph - Mean absolute deviation MAD", () => {
	const input = {
	"key": {
		"icon": "⚾",
		"label": "= 1 home run"
	},
	"data": [
		{
			"category": "Lancers",
			"iconCount": 7
		},
		{
			"category": "Huskies",
			"iconCount": 2
		},
		{
			"category": "Grizzlies",
			"iconCount": 4
		},
		{
			"category": "Raiders",
			"iconCount": 3
		}
	],
	"title": "Home runs hit by each team in a baseball league.",
	"type": "pictograph"
} satisfies PictographInput

	// Validate the input
	const parseResult = PictographPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted pictograph:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generatePictograph(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x001a52d916889012
// Course: 6th grade math
// Exercise: Mean absolute deviation (MAD)
// Widget key: image_2
test("pictograph - Mean absolute deviation MAD", () => {
	const input = {
	"key": {
		"icon": "⚾",
		"label": "= 1 home run"
	},
	"data": [],
	"title": "Key",
	"type": "pictograph"
} satisfies PictographInput

	// Validate the input
	const parseResult = PictographPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted pictograph:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generatePictograph(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x014e5b2f8efff0f1
// Course: 6th grade math
// Exercise: Mean absolute deviation (MAD)
// Widget key: image_2
test("pictograph - Mean absolute deviation MAD", () => {
	const input = {
	"key": {
		"icon": "🍨",
		"label": "= 2 sundaes"
	},
	"data": [
		{
			"category": "Two Scoops",
			"iconCount": 4
		},
		{
			"category": "I Scream",
			"iconCount": 6
		},
		{
			"category": "Dream Cream",
			"iconCount": 2
		},
		{
			"category": "Sundays",
			"iconCount": 4
		}
	],
	"title": "Sundaes each store made on Monday",
	"type": "pictograph"
} satisfies PictographInput

	// Validate the input
	const parseResult = PictographPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted pictograph:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generatePictograph(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x014e5b2f8efff0f1
// Course: 6th grade math
// Exercise: Mean absolute deviation (MAD)
// Widget key: image_3
test("pictograph - Mean absolute deviation MAD", () => {
	const input = {
	"key": {
		"icon": "🍨",
		"label": "= 2 sundaes"
	},
	"data": [],
	"title": "/",
	"type": "pictograph"
} satisfies PictographInput

	// Validate the input
	const parseResult = PictographPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted pictograph:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generatePictograph(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x37831c3f8c0f1dab
// Course: 6th grade math
// Exercise: Mean absolute deviation (MAD)
// Widget key: image_2
test("pictograph - Mean absolute deviation MAD", () => {
	const input = {
	"key": {
		"icon": "🪄",
		"label": "= 2 wands"
	},
	"data": [
		{
			"category": "Glenda",
			"iconCount": 2
		},
		{
			"category": "Tink",
			"iconCount": 3
		},
		{
			"category": "Merry",
			"iconCount": 4
		},
		{
			"category": "Gracie",
			"iconCount": 5
		}
	],
	"title": "Wands each fairy owns",
	"type": "pictograph"
} satisfies PictographInput

	// Validate the input
	const parseResult = PictographPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted pictograph:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generatePictograph(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x37831c3f8c0f1dab
// Course: 6th grade math
// Exercise: Mean absolute deviation (MAD)
// Widget key: image_3
test("pictograph - Mean absolute deviation MAD", () => {
	const input = {
	"key": {
		"icon": "🪄",
		"label": "= 2 wands"
	},
	"data": [],
	"title": "Key",
	"type": "pictograph"
} satisfies PictographInput

	// Validate the input
	const parseResult = PictographPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted pictograph:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generatePictograph(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xa6ac2285fb5d1ce1
// Course: 6th grade math
// Exercise: Mean absolute deviation (MAD)
// Widget key: image_2
test("pictograph - Mean absolute deviation MAD", () => {
	const input = {
	"key": {
		"icon": "🧀",
		"label": "= 1 piece of cheese"
	},
	"data": [
		{
			"category": "Caroline",
			"iconCount": 2
		},
		{
			"category": "Samuel",
			"iconCount": 4
		},
		{
			"category": "Abby",
			"iconCount": 4
		},
		{
			"category": "Jason",
			"iconCount": 2
		}
	],
	"title": "Pieces of stinky cheese in each student's lunch box.",
	"type": "pictograph"
} satisfies PictographInput

	// Validate the input
	const parseResult = PictographPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted pictograph:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generatePictograph(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xa6ac2285fb5d1ce1
// Course: 6th grade math
// Exercise: Mean absolute deviation (MAD)
// Widget key: image_3
test("pictograph - Mean absolute deviation MAD", () => {
	const input = {
	"key": {
		"icon": "🧀",
		"label": "= 1 piece of cheese"
	},
	"data": [],
	"title": "Key",
	"type": "pictograph"
} satisfies PictographInput

	// Validate the input
	const parseResult = PictographPropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted pictograph:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generatePictograph(parseResult.data)
	expect(svg).toMatchSnapshot()
})

