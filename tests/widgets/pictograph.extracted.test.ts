// ============================================================================
// EXTRACTED TEST FILE - AUTO-GENERATED
// ============================================================================
// This file was automatically generated from database structured_json data
// Generated on: 2025-09-18T05:17:02.170Z
// Widget Type: pictograph
// Source: Real questions from database
//
// These tests use actual widget configurations extracted from production data
// to ensure comprehensive test coverage with realistic scenarios.
//
// Note: Database data may not perfectly match current widget schemas, so these
// tests rely on runtime validation within generateWidget() for flexibility.
//
// DO NOT EDIT THIS FILE DIRECTLY - it will be overwritten on regeneration
//
// Run tests: bun test tests/widgets/pictograph.extracted.test.ts
// Update snapshots: bun test tests/widgets/pictograph.extracted.test.ts --update-snapshots
// ============================================================================

import { describe, test, expect } from "bun:test"
import { generateWidget } from "@superbuilders/qti-assessment-item-generator/widgets/widget-generator"
import type { Widget, WidgetInput } from "@superbuilders/qti-assessment-item-generator/widgets/registry"
import * as errors from "@superbuilders/errors"
import * as logger from "@superbuilders/slog"

describe("pictograph widget tests", () => {
	// Extracted from question: x4cddb3b060f1c05e
	// Question: Mean absolute deviation from a pictograph
	// Widget key: image_1
	test("pictograph - [x4cddb3b060f1c05e] Mean absolute deviation from a pictograph", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "key": {
		    "icon": "🪄",
		    "label": "= 1 wand"
		  },
		  "data": [
		    {
		      "category": "Glenda",
		      "iconCount": 5
		    },
		    {
		      "category": "Sprinkle",
		      "iconCount": 3
		    },
		    {
		      "category": "Godmother",
		      "iconCount": 3
		    },
		    {
		      "category": "Rosebud",
		      "iconCount": 5
		    }
		  ],
		  "type": "pictograph",
		  "title": "Wands held by fairies in Fairyland"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for pictograph", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x001a52d916889012
	// Question: Mean absolute deviation from a pictograph
	// Widget key: image_1
	test("pictograph - [x001a52d916889012] Mean absolute deviation from a pictograph", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "key": {
		    "icon": "⚾",
		    "label": "= 1 home run"
		  },
		  "data": [
		    {
		      "category": "Lancers",
		      "iconCount": 7
		    },
		    {
		      "category": "Huskies",
		      "iconCount": 2
		    },
		    {
		      "category": "Grizzlies",
		      "iconCount": 4
		    },
		    {
		      "category": "Raiders",
		      "iconCount": 3
		    }
		  ],
		  "type": "pictograph",
		  "title": "Home runs hit by each team in a baseball league."
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for pictograph", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x6f6997d1de52c08b
	// Question: Mean absolute deviation from a pictograph
	// Widget key: image_2
	test("pictograph - [x6f6997d1de52c08b] Mean absolute deviation from a pictograph", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "key": {
		    "icon": "🏀",
		    "label": "= 2 baskets"
		  },
		  "data": [
		    {
		      "category": "Reynaldo",
		      "iconCount": 2
		    },
		    {
		      "category": "Marcelle",
		      "iconCount": 4
		    },
		    {
		      "category": "Allie",
		      "iconCount": 3
		    },
		    {
		      "category": "Fernando",
		      "iconCount": 5
		    }
		  ],
		  "type": "pictograph",
		  "title": "Baskets each student made"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for pictograph", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x014e5b2f8efff0f1
	// Question: Mean absolute deviation from a pictograph
	// Widget key: image_2
	test("pictograph - [x014e5b2f8efff0f1] Mean absolute deviation from a pictograph", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "key": {
		    "icon": "🍨",
		    "label": "= 2 sundaes"
		  },
		  "data": [
		    {
		      "category": "Two Scoops",
		      "iconCount": 4
		    },
		    {
		      "category": "I Scream",
		      "iconCount": 6
		    },
		    {
		      "category": "Dream Cream",
		      "iconCount": 2
		    },
		    {
		      "category": "Sundays",
		      "iconCount": 4
		    }
		  ],
		  "type": "pictograph",
		  "title": "Sundaes each store made on Monday"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for pictograph", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x37831c3f8c0f1dab
	// Question: Find the mean absolute deviation from a pictograph
	// Widget key: image_2
	test("pictograph - [x37831c3f8c0f1dab] Find the mean absolute deviation from a pictograph", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "key": {
		    "icon": "🪄",
		    "label": "= 2 wands"
		  },
		  "data": [
		    {
		      "category": "Glenda",
		      "iconCount": 2
		    },
		    {
		      "category": "Tink",
		      "iconCount": 3
		    },
		    {
		      "category": "Merry",
		      "iconCount": 4
		    },
		    {
		      "category": "Gracie",
		      "iconCount": 5
		    }
		  ],
		  "type": "pictograph",
		  "title": "Wands each fairy owns"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for pictograph", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xa6ac2285fb5d1ce1
	// Question: Mean absolute deviation from a pictograph
	// Widget key: image_2
	test("pictograph - [xa6ac2285fb5d1ce1] Mean absolute deviation from a pictograph", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "key": {
		    "icon": "🧀",
		    "label": "= 1 piece of cheese"
		  },
		  "data": [
		    {
		      "category": "Caroline",
		      "iconCount": 2
		    },
		    {
		      "category": "Samuel",
		      "iconCount": 4
		    },
		    {
		      "category": "Abby",
		      "iconCount": 4
		    },
		    {
		      "category": "Jason",
		      "iconCount": 2
		    }
		  ],
		  "type": "pictograph",
		  "title": "Pieces of stinky cheese in each student's lunch box."
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for pictograph", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xca6637292cbef860
	// Question: Find the mean from a pictograph
	// Widget key: image_2
	test("pictograph - [xca6637292cbef860] Find the mean from a pictograph", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "key": {
		    "icon": "🪄",
		    "label": "= 1 wand"
		  },
		  "data": [
		    {
		      "category": "Glenda",
		      "iconCount": 5
		    },
		    {
		      "category": "Sprinkle",
		      "iconCount": 3
		    },
		    {
		      "category": "Godmother",
		      "iconCount": 3
		    },
		    {
		      "category": "Rosebud",
		      "iconCount": 5
		    }
		  ],
		  "type": "pictograph",
		  "title": "Wands each fairy owns"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for pictograph", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x849d322199a11644
	// Question: Find the mean from a pictograph
	// Widget key: image_2
	test("pictograph - [x849d322199a11644] Find the mean from a pictograph", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "key": {
		    "icon": "🧀",
		    "label": "= 1 piece of cheese"
		  },
		  "data": [
		    {
		      "category": "Caroline",
		      "iconCount": 2
		    },
		    {
		      "category": "Samuel",
		      "iconCount": 4
		    },
		    {
		      "category": "Abby",
		      "iconCount": 4
		    },
		    {
		      "category": "Jason",
		      "iconCount": 2
		    }
		  ],
		  "type": "pictograph",
		  "title": "Pieces of stinky cheese in each student's lunch box."
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for pictograph", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x5dc099ff03f52b9e
	// Question: Find the mean from a pictograph
	// Widget key: image_2
	test("pictograph - [x5dc099ff03f52b9e] Find the mean from a pictograph", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "key": {
		    "icon": "🪄",
		    "label": "= 2 wands"
		  },
		  "data": [
		    {
		      "category": "Glenda",
		      "iconCount": 2
		    },
		    {
		      "category": "Tink",
		      "iconCount": 3
		    },
		    {
		      "category": "Merry",
		      "iconCount": 4
		    },
		    {
		      "category": "Gracie",
		      "iconCount": 5
		    }
		  ],
		  "type": "pictograph",
		  "title": "Wands each fairy owns"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for pictograph", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xbb618582cbf1434e
	// Question: Find the mean from a pictograph
	// Widget key: image_2
	test("pictograph - [xbb618582cbf1434e] Find the mean from a pictograph", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "key": {
		    "icon": "🍨",
		    "label": "= 3 sundaes"
		  },
		  "data": [
		    {
		      "category": "Two Scoops",
		      "iconCount": 4
		    },
		    {
		      "category": "I Scream",
		      "iconCount": 6
		    },
		    {
		      "category": "Dream Cream",
		      "iconCount": 2
		    },
		    {
		      "category": "Sundays",
		      "iconCount": 4
		    }
		  ],
		  "type": "pictograph",
		  "title": "Sundaes each store made on Monday"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for pictograph", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xe1cca2942efaee77
	// Question: Find the mean from a pictograph
	// Widget key: image_2
	test("pictograph - [xe1cca2942efaee77] Find the mean from a pictograph", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "key": {
		    "icon": "🐳",
		    "label": "= 1 toy whale"
		  },
		  "data": [
		    {
		      "category": "Luca",
		      "iconCount": 3
		    },
		    {
		      "category": "Niklas",
		      "iconCount": 3
		    },
		    {
		      "category": "Finn",
		      "iconCount": 5
		    },
		    {
		      "category": "Lena",
		      "iconCount": 3
		    }
		  ],
		  "type": "pictograph",
		  "title": "Toy whales each swimmer held in the pool"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for pictograph", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x7e3812e476c39b21
	// Question: Find the mean from a pictograph
	// Widget key: image_2
	test("pictograph - [x7e3812e476c39b21] Find the mean from a pictograph", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "key": {
		    "icon": "🏀",
		    "label": "= 3 baskets"
		  },
		  "data": [
		    {
		      "category": "Reynaldo",
		      "iconCount": 2
		    },
		    {
		      "category": "Marcelle",
		      "iconCount": 4
		    },
		    {
		      "category": "Allie",
		      "iconCount": 3
		    },
		    {
		      "category": "Fernando",
		      "iconCount": 5
		    }
		  ],
		  "type": "pictograph",
		  "title": "Baskets each student made"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for pictograph", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

})
