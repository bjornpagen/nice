// ============================================================================
// EXTRACTED TEST FILE - AUTO-GENERATED
// ============================================================================
// This file was automatically generated from database structured_json data
// Generated on: 2025-09-01T22:27:08.765Z
// Widget Type: numberLine
// Source: Real questions from database
//
// These tests use actual widget configurations extracted from production data
// to ensure comprehensive test coverage with realistic scenarios.
//
// DO NOT EDIT THIS FILE DIRECTLY - it will be overwritten on regeneration
//
// Run tests: bun test tests/widgets/number-line.extracted.test.ts
// Update snapshots: bun test tests/widgets/number-line.extracted.test.ts --update-snapshots
// ============================================================================

import { expect, test } from "bun:test"
import type { z } from "zod"
import { generateNumberLine, NumberLinePropsSchema } from "@/lib/widgets/generators"

type NumberLineInput = z.input<typeof NumberLinePropsSchema>


// Extracted from question: x2793302cc08c9d81
// Course: 8th grade math
// Exercise: Comparing irrational numbers with a calculator
// Widget key: image_1
test("number-line - Comparing irrational numbers with a calculator", () => {
	const input = {
	"max": 10,
	"min": 9.5,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ED",
			"label": "d",
			"value": 9.7502,
			"labelPosition": "above"
		},
		{
			"color": "#6495ED",
			"label": "e",
			"value": 9.81,
			"labelPosition": "above"
		},
		{
			"color": "#6495ED",
			"label": "f",
			"value": 9.8489,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x414b8220aec34836
// Course: 8th grade math
// Exercise: Comparing irrational numbers with a calculator
// Widget key: image_1
test("number-line - Comparing irrational numbers with a calculator", () => {
	const input = {
	"max": -8.5,
	"min": -9,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ed",
			"label": "m",
			"value": -8.774,
			"labelPosition": "above"
		},
		{
			"color": "#6495ed",
			"label": "n",
			"value": -8.726,
			"labelPosition": "above"
		},
		{
			"color": "#6495ed",
			"label": "o",
			"value": -8.69,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x69935621f216bc8c
// Course: 8th grade math
// Exercise: Comparing irrational numbers with a calculator
// Widget key: image_1
test("number-line - Comparing irrational numbers with a calculator", () => {
	const input = {
	"max": -7,
	"min": -7.5,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ed",
			"label": "j",
			"value": -7.31115,
			"labelPosition": "above"
		},
		{
			"color": "#6495ed",
			"label": "k",
			"value": -7.26284,
			"labelPosition": "above"
		},
		{
			"color": "#6495ed",
			"label": "l",
			"value": -7.2111,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x654e1ffb45d6f82c
// Course: 8th grade math
// Exercise: Comparing irrational numbers with a calculator
// Widget key: image_1
test("number-line - Comparing irrational numbers with a calculator", () => {
	const input = {
	"max": 9,
	"min": 8.5,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ed",
			"label": "d",
			"value": 8.6602,
			"labelPosition": "above"
		},
		{
			"color": "#6495ed",
			"label": "e",
			"value": 8.705,
			"labelPosition": "above"
		},
		{
			"color": "#6495ed",
			"label": "f",
			"value": 8.76,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xdf9b279db19b86d3
// Course: 8th grade math
// Exercise: Comparing irrational numbers with a calculator
// Widget key: image_1
test("number-line - Comparing irrational numbers with a calculator", () => {
	const input = {
	"max": 5.5,
	"min": 5,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ED",
			"label": "a",
			"value": 5.11,
			"labelPosition": "above"
		},
		{
			"color": "#6495ED",
			"label": "b",
			"value": 5.1961,
			"labelPosition": "above"
		},
		{
			"color": "#6495ED",
			"label": "c",
			"value": 5.23,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x4d8dbe91bd4466f1
// Course: 8th grade math
// Exercise: Comparing irrational numbers with a calculator
// Widget key: image_1
test("number-line - Comparing irrational numbers with a calculator", () => {
	const input = {
	"max": -2.5,
	"min": -3,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ed",
			"label": "j",
			"value": -2.88502,
			"labelPosition": "above"
		},
		{
			"color": "#6495ed",
			"label": "k",
			"value": -2.8284,
			"labelPosition": "above"
		},
		{
			"color": "#6495ed",
			"label": "l",
			"value": -2.7489,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x75206aad4d824373
// Course: 8th grade math
// Exercise: Comparing irrational numbers with a calculator
// Widget key: image_1
test("number-line - Comparing irrational numbers with a calculator", () => {
	const input = {
	"max": -9,
	"min": -9.5,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ed",
			"label": "m",
			"value": -9.214,
			"labelPosition": "above"
		},
		{
			"color": "#6495ed",
			"label": "n",
			"value": -9.166,
			"labelPosition": "above"
		},
		{
			"color": "#6495ed",
			"label": "o",
			"value": -9.091,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x8801e1b9a9c6f18a
// Course: 8th grade math
// Exercise: Comparing irrational numbers with a calculator
// Widget key: image_1
test("number-line - Comparing irrational numbers with a calculator", () => {
	const input = {
	"max": 5,
	"min": 4.5,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ed",
			"label": "a",
			"value": 4.68,
			"labelPosition": "above"
		},
		{
			"color": "#6495ed",
			"label": "b",
			"value": 4.72,
			"labelPosition": "above"
		},
		{
			"color": "#6495ed",
			"label": "c",
			"value": 4.7958,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x14283a167a550768
// Course: 8th grade math
// Exercise: Signs of expressions challenge
// Widget key: image_1
test("number-line - Signs of expressions challenge", () => {
	const input = {
	"max": 3,
	"min": -3,
	"width": 125,
	"height": 240,
	"points": [
		{
			"color": "#6495ed",
			"label": "c",
			"value": -2,
			"labelPosition": "right"
		},
		{
			"color": "#6495ed",
			"label": "d",
			"value": 2,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x9ed54989cf2d075f
// Course: 8th grade math
// Exercise: Signs of expressions challenge
// Widget key: image_1
test("number-line - Signs of expressions challenge", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 460,
	"height": 69,
	"points": [
		{
			"color": "#6495ED",
			"label": "m",
			"value": -1.6,
			"labelPosition": "above"
		},
		{
			"color": "#6495ED",
			"label": "n",
			"value": 0.7,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [
		{
			"label": "",
			"value": -2
		},
		{
			"label": "",
			"value": -1
		},
		{
			"label": "0",
			"value": 0
		},
		{
			"label": "",
			"value": 1
		},
		{
			"label": "",
			"value": 2
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x18d8a36dc05bc413
// Course: 8th grade math
// Exercise: Signs of expressions challenge
// Widget key: image_1
test("number-line - Signs of expressions challenge", () => {
	const input = {
	"max": 2,
	"min": 0,
	"width": 125,
	"height": 240,
	"points": [
		{
			"color": "#6495ed",
			"label": "g",
			"value": 0.3,
			"labelPosition": "right"
		},
		{
			"color": "#6495ed",
			"label": "f",
			"value": 1.8,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x9122705c9e43fac7
// Course: 8th grade math
// Exercise: Signs of expressions challenge
// Widget key: image_1
test("number-line - Signs of expressions challenge", () => {
	const input = {
	"max": 2.65,
	"min": -1.35,
	"width": 460,
	"height": 69,
	"points": [
		{
			"color": "#6495ed",
			"label": "m",
			"value": -1.3,
			"labelPosition": "above"
		},
		{
			"color": "#6495ed",
			"label": "n",
			"value": -0.5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2ac97a878f373aac
// Course: 8th grade math
// Exercise: Signs of expressions challenge
// Widget key: image_1
test("number-line - Signs of expressions challenge", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 125,
	"height": 240,
	"points": [
		{
			"color": "#6495ed",
			"label": "c",
			"value": -1.8,
			"labelPosition": "right"
		},
		{
			"color": "#6495ed",
			"label": "b",
			"value": 0.7,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x06c9ebc30b95bc5c
// Course: 8th grade math
// Exercise: Signs of expressions challenge
// Widget key: image_1
test("number-line - Signs of expressions challenge", () => {
	const input = {
	"max": 2,
	"min": -7,
	"width": 460,
	"height": 68.51,
	"points": [
		{
			"color": "#6495ed",
			"label": "a",
			"value": 0,
			"labelPosition": "above"
		},
		{
			"color": "#6495ed",
			"label": "b",
			"value": -5.76,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [
		{
			"label": "0",
			"value": 0
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xd52bf71ae9367c1d
// Course: 7th grade math
// Exercise: Signs of sums
// Widget key: image_1
test("number-line - Signs of sums", () => {
	const input = {
	"max": 4,
	"min": -14,
	"width": 460,
	"height": 69,
	"points": [
		{
			"color": "#6495ed",
			"label": "A",
			"value": -4.5,
			"labelPosition": "above"
		},
		{
			"color": "#6495ed",
			"label": "B",
			"value": -9,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [
		{
			"label": "0",
			"value": 0
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xa17526c1388ca227
// Course: 7th grade math
// Exercise: Signs of sums
// Widget key: image_1
test("number-line - Signs of sums", () => {
	const input = {
	"max": 9,
	"min": -9,
	"width": 460,
	"height": 70,
	"points": [
		{
			"color": "#6495ed",
			"label": "A",
			"value": -8,
			"labelPosition": "above"
		},
		{
			"color": "#6495ed",
			"label": "B",
			"value": 3,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xb34bd9efe7f7b167
// Course: 7th grade math
// Exercise: Signs of sums
// Widget key: image_1
test("number-line - Signs of sums", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 102,
	"height": 240,
	"points": [
		{
			"color": "#6495ed",
			"label": "A",
			"value": -5,
			"labelPosition": "right"
		},
		{
			"color": "#6495ed",
			"label": "B",
			"value": 9,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 1,
	"specialTickLabels": [
		{
			"label": "0",
			"value": 0
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xb68760aaf6f3ffeb
// Course: 7th grade math
// Exercise: Signs of sums
// Widget key: image_1
test("number-line - Signs of sums", () => {
	const input = {
	"max": 7,
	"min": -8,
	"width": 104,
	"height": 240,
	"points": [
		{
			"color": "#6495ed",
			"label": "A",
			"value": -8,
			"labelPosition": "right"
		},
		{
			"color": "#6495ed",
			"label": "B",
			"value": 5,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 1,
	"specialTickLabels": [
		{
			"label": "0",
			"value": 0
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2a21cc51e5afb7b3
// Course: 7th grade math
// Exercise: Signs of sums
// Widget key: image_1
test("number-line - Signs of sums", () => {
	const input = {
	"max": 7,
	"min": -7,
	"width": 460,
	"height": 94,
	"points": [
		{
			"color": "#6495ED",
			"label": "A",
			"value": 5,
			"labelPosition": "above"
		},
		{
			"color": "#6495ED",
			"label": "B",
			"value": -3,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xa2d63caf8b0024eb
// Course: 7th grade math
// Exercise: Signs of sums
// Widget key: image_1
test("number-line - Signs of sums", () => {
	const input = {
	"max": 4,
	"min": -7,
	"width": 460,
	"height": 69,
	"points": [
		{
			"color": "#6495ed",
			"label": "A",
			"value": 3,
			"labelPosition": "above"
		},
		{
			"color": "#6495ed",
			"label": "B",
			"value": -4,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [
		{
			"label": "0",
			"value": 0
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x6c3fca6f50a7886e
// Course: 7th grade math
// Exercise: Signs of sums
// Widget key: image_1
test("number-line - Signs of sums", () => {
	const input = {
	"max": 3,
	"min": -12,
	"width": 104,
	"height": 240,
	"points": [
		{
			"color": "#6495ED",
			"label": "A",
			"value": -2,
			"labelPosition": "right"
		},
		{
			"color": "#6495ED",
			"label": "B",
			"value": -8.5,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 1,
	"specialTickLabels": [
		{
			"label": "0",
			"value": 0
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x3a70b38ca758b5a2
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x67e44f95cf30cb93
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x67ac6202a9d4f087
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 10,
	"specialTickLabels": [
		{
			"label": "",
			"value": -10
		},
		{
			"label": "0",
			"value": 0
		},
		{
			"label": "",
			"value": 10
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xd96eb217b7c9a450
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x550561183fbc7144
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [
		{
			"label": "",
			"value": -10
		},
		{
			"label": "",
			"value": -9
		},
		{
			"label": "",
			"value": -8
		},
		{
			"label": "",
			"value": -7
		},
		{
			"label": "",
			"value": -6
		},
		{
			"label": "",
			"value": -5
		},
		{
			"label": "",
			"value": -4
		},
		{
			"label": "",
			"value": -3
		},
		{
			"label": "",
			"value": -2
		},
		{
			"label": "",
			"value": -1
		},
		{
			"label": "0",
			"value": 0
		},
		{
			"label": "",
			"value": 1
		},
		{
			"label": "",
			"value": 2
		},
		{
			"label": "",
			"value": 3
		},
		{
			"label": "",
			"value": 4
		},
		{
			"label": "",
			"value": 5
		},
		{
			"label": "",
			"value": 6
		},
		{
			"label": "",
			"value": 7
		},
		{
			"label": "",
			"value": 8
		},
		{
			"label": "",
			"value": 9
		},
		{
			"label": "",
			"value": 10
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x0e6bd1df203c7886
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x7cc0baac5d140007
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xd973b123bab53a84
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2436d2f7d2b2dc6d
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [
		{
			"label": "0",
			"value": 0
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x503e4bb8de69fa12
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x3f9d8f1bc1edf16f
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x85e43baa483fc14d
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xc68972fee29782e4
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xbd6be7269a700394
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x22654291a8e6cdba
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xc5b1a83429ca90fc
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [
		{
			"label": "0",
			"value": 0
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xaf3c2b6396663afc
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xbebd2f72cd59eb7f
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xb406d46142c425d9
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [
		{
			"label": "0",
			"value": 0
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x45364903b6bb25c3
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x804ff13c5e0a3b0d
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe88afd2e5e99c2eb
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [
		{
			"label": "0",
			"value": 0
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x8817b46c17165809
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 10,
	"specialTickLabels": [
		{
			"label": "0",
			"value": 0
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x68f5deef1c423e40
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 10,
	"specialTickLabels": [
		{
			"label": "0",
			"value": 0
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x428f83cf6fa27a30
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x04f5bb2056f5a942
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [
		{
			"label": "0",
			"value": 0
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xd1c7929d24a2f00f
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x458e3358458ed022
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 20,
	"min": -20,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 4,
	"specialTickLabels": [
		{
			"label": "0",
			"value": 0
		}
	],
	"minorTicksPerInterval": 3,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xf97688185047bb0e
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [
		{
			"label": "0",
			"value": 0
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x6c115eefe7cc2360
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 2,
	"specialTickLabels": [
		{
			"label": "0",
			"value": 0
		}
	],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x873f699dbd384dfc
// Course: 7th grade math
// Exercise: Interpret negative number addition and subtraction expressions
// Widget key: image_1
test("number-line - Interpret negative number addition and subtract", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 10,
	"specialTickLabels": [
		{
			"label": "",
			"value": -10
		},
		{
			"label": "0",
			"value": 0
		},
		{
			"label": "",
			"value": 10
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x7b27f9336e6b6272
// Course: 7th grade math
// Exercise: Addition & subtraction: find the missing value
// Widget key: image_1
test("number-line - Addition subtraction find the missing value", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 380,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x0120f9f9574a64c2
// Course: 7th grade math
// Exercise: Addition & subtraction: find the missing value
// Widget key: image_1
test("number-line - Addition subtraction find the missing value", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 380,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x807c968f5dc02bad
// Course: 7th grade math
// Exercise: Addition & subtraction: find the missing value
// Widget key: image_1
test("number-line - Addition subtraction find the missing value", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 380,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x8628be484c010df2
// Course: 7th grade math
// Exercise: Addition & subtraction: find the missing value
// Widget key: image_1
test("number-line - Addition subtraction find the missing value", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 380,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xcc20326bcb5bf91d
// Course: 7th grade math
// Exercise: Addition & subtraction: find the missing value
// Widget key: image_1
test("number-line - Addition subtraction find the missing value", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 380,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x160604a98b8fb38c
// Course: 7th grade math
// Exercise: Addition & subtraction: find the missing value
// Widget key: image_1
test("number-line - Addition subtraction find the missing value", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 380,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x1be90394c94041d0
// Course: 7th grade math
// Exercise: Addition & subtraction: find the missing value
// Widget key: image_1
test("number-line - Addition subtraction find the missing value", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 380,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xd5151fab9c4f9048
// Course: 7th grade math
// Exercise: Addition & subtraction: find the missing value
// Widget key: image_1
test("number-line - Addition subtraction find the missing value", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 380,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x6a9ec443b339eca6
// Course: 7th grade math
// Exercise: Addition & subtraction: find the missing value
// Widget key: image_1
test("number-line - Addition subtraction find the missing value", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 380,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x681b1cd156c59e80
// Course: 7th grade math
// Exercise: Addition & subtraction: find the missing value
// Widget key: image_1
test("number-line - Addition subtraction find the missing value", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 380,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [
		{
			"label": "-15",
			"value": -15
		},
		{
			"label": "-10",
			"value": -10
		},
		{
			"label": "-5",
			"value": -5
		},
		{
			"label": "0",
			"value": 0
		},
		{
			"label": "5",
			"value": 5
		},
		{
			"label": "10",
			"value": 10
		},
		{
			"label": "15",
			"value": 15
		}
	],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xc87eedc76dcf9888
// Course: 7th grade math
// Exercise: Addition & subtraction: find the missing value
// Widget key: image_1
test("number-line - Addition subtraction find the missing value", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 380,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x8e53cd0658445816
// Course: 7th grade math
// Exercise: Addition & subtraction: find the missing value
// Widget key: image_1
test("number-line - Addition subtraction find the missing value", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 380,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x1cbe88cf7d63a3c5
// Course: 7th grade math
// Exercise: Addition & subtraction: find the missing value
// Widget key: image_1
test("number-line - Addition subtraction find the missing value", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 380,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [
		{
			"label": "-15",
			"value": -15
		},
		{
			"label": "-10",
			"value": -10
		},
		{
			"label": "-5",
			"value": -5
		},
		{
			"label": "0",
			"value": 0
		},
		{
			"label": "5",
			"value": 5
		},
		{
			"label": "10",
			"value": 10
		},
		{
			"label": "15",
			"value": 15
		}
	],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x7b189f09cb6c89d1
// Course: 7th grade math
// Exercise: Addition & subtraction: find the missing value
// Widget key: image_1
test("number-line - Addition subtraction find the missing value", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 380,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xbd3b34293fb591a2
// Course: 7th grade math
// Exercise: Addition & subtraction: find the missing value
// Widget key: image_1
test("number-line - Addition subtraction find the missing value", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 380,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x146cb2ef734d4012
// Course: 7th grade math
// Exercise: Addition & subtraction: find the missing value
// Widget key: image_1
test("number-line - Addition subtraction find the missing value", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 380,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [
		{
			"label": "-15",
			"value": -15
		},
		{
			"label": "-10",
			"value": -10
		},
		{
			"label": "-5",
			"value": -5
		},
		{
			"label": "0",
			"value": 0
		},
		{
			"label": "5",
			"value": 5
		},
		{
			"label": "10",
			"value": 10
		},
		{
			"label": "15",
			"value": 15
		}
	],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xa88752a37218add6
// Course: 7th grade math
// Exercise: Addition & subtraction: find the missing value
// Widget key: image_1
test("number-line - Addition subtraction find the missing value", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 380,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xf77b08ed8d926109
// Course: 7th grade math
// Exercise: Addition & subtraction: find the missing value
// Widget key: image_1
test("number-line - Addition subtraction find the missing value", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 380,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x54b1b23af62ae203
// Course: 7th grade math
// Exercise: Addition & subtraction: find the missing value
// Widget key: image_1
test("number-line - Addition subtraction find the missing value", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 380,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x195497fa81db1cd0
// Course: 7th grade math
// Exercise: Addition & subtraction: find the missing value
// Widget key: image_1
test("number-line - Addition subtraction find the missing value", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 380,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xfec8bc26b6ada198
// Course: 7th grade math
// Exercise: Addition & subtraction: find the missing value
// Widget key: image_1
test("number-line - Addition subtraction find the missing value", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 380,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xa39914d9a6b44171
// Course: 7th grade math
// Exercise: Addition & subtraction: find the missing value
// Widget key: image_1
test("number-line - Addition subtraction find the missing value", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 380,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x5bf297396596bf9b
// Course: 7th grade math
// Exercise: Addition & subtraction: find the missing value
// Widget key: image_1
test("number-line - Addition subtraction find the missing value", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 380,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x6924bf3af0c393ef
// Course: 7th grade math
// Exercise: Addition & subtraction: find the missing value
// Widget key: image_1
test("number-line - Addition subtraction find the missing value", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 380,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x8abaa6aa369e1766
// Course: 7th grade math
// Exercise: Addition & subtraction: find the missing value
// Widget key: image_1
test("number-line - Addition subtraction find the missing value", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 380,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xda54389caa4f69ac
// Course: 7th grade math
// Exercise: Addition & subtraction: find the missing value
// Widget key: image_1
test("number-line - Addition subtraction find the missing value", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 380,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xb8ca93d297136904
// Course: 7th grade math
// Exercise: Addition & subtraction: find the missing value
// Widget key: image_1
test("number-line - Addition subtraction find the missing value", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 380,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x57b58e02397b35fd
// Course: 7th grade math
// Exercise: Addition & subtraction: find the missing value
// Widget key: image_1
test("number-line - Addition subtraction find the missing value", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 380,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2847e4fed7c1c79f
// Course: 7th grade math
// Exercise: Ordering negative number expressions
// Widget key: image_1
test("number-line - Ordering negative number expressions", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 460,
	"height": 69,
	"points": [
		{
			"color": "#11accd",
			"label": "a",
			"value": -1.6,
			"labelPosition": "above"
		},
		{
			"color": "#11accd",
			"label": "b",
			"value": 0.85,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x9762a96d6ed72ffb
// Course: 7th grade math
// Exercise: Ordering negative number expressions
// Widget key: image_1
test("number-line - Ordering negative number expressions", () => {
	const input = {
	"max": 3,
	"min": -3,
	"width": 460,
	"height": 69,
	"points": [
		{
			"color": "#11accd",
			"label": "n",
			"value": -2,
			"labelPosition": "above"
		},
		{
			"color": "#11accd",
			"label": "q",
			"value": 1,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x50acca2c9e26425c
// Course: 7th grade math
// Exercise: Ordering negative number expressions
// Widget key: image_1
test("number-line - Ordering negative number expressions", () => {
	const input = {
	"max": 3,
	"min": -3,
	"width": 460,
	"height": 69,
	"points": [
		{
			"color": "#11accd",
			"label": "a",
			"value": -1.5,
			"labelPosition": "above"
		},
		{
			"color": "#11accd",
			"label": "b",
			"value": 0.8,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2630a00bc0919ed9
// Course: 7th grade math
// Exercise: Ordering negative number expressions
// Widget key: image_1
test("number-line - Ordering negative number expressions", () => {
	const input = {
	"max": 6,
	"min": -6,
	"width": 460,
	"height": 69,
	"points": [
		{
			"color": "#11accd",
			"label": "a",
			"value": -1,
			"labelPosition": "above"
		},
		{
			"color": "#11accd",
			"label": "b",
			"value": 2,
			"labelPosition": "above"
		},
		{
			"color": "#11accd",
			"label": "c",
			"value": 3,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x7fdb16d094db88e8
// Course: 7th grade math
// Exercise: Ordering negative number expressions
// Widget key: image_1
test("number-line - Ordering negative number expressions", () => {
	const input = {
	"max": 2.6,
	"min": -3,
	"width": 460,
	"height": 69,
	"points": [
		{
			"color": "#11accd",
			"label": "a",
			"value": -1.7,
			"labelPosition": "above"
		},
		{
			"color": "#11accd",
			"label": "b",
			"value": 2.5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x000308979d33050b
// Course: 7th grade math
// Exercise: Ordering negative number expressions
// Widget key: image_1
test("number-line - Ordering negative number expressions", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 69,
	"points": [
		{
			"color": "#11accd",
			"label": "n",
			"value": -4,
			"labelPosition": "above"
		},
		{
			"color": "#11accd",
			"label": "q",
			"value": -1,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x0b7d83ae9dcba640
// Course: 7th grade math
// Exercise: Ordering negative number expressions
// Widget key: image_1
test("number-line - Ordering negative number expressions", () => {
	const input = {
	"max": 1,
	"min": -1,
	"width": 115,
	"height": 240,
	"points": [
		{
			"color": "#11accd",
			"label": "j",
			"value": 0.8,
			"labelPosition": "right"
		},
		{
			"color": "#11accd",
			"label": "k",
			"value": 0.3,
			"labelPosition": "right"
		},
		{
			"color": "#11accd",
			"label": "l",
			"value": -0.1,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 0.5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xd874b91d7e96e8e4
// Course: 7th grade math
// Exercise: Ordering negative number expressions
// Widget key: image_1
test("number-line - Ordering negative number expressions", () => {
	const input = {
	"max": 1,
	"min": -1,
	"width": 77,
	"height": 240,
	"points": [
		{
			"color": "#11accd",
			"label": "a",
			"value": -1,
			"labelPosition": "right"
		},
		{
			"color": "#11accd",
			"label": "b",
			"value": -0.5,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x9b7d923e5f168763
// Course: 7th grade math
// Exercise: Ordering negative number expressions
// Widget key: image_1
test("number-line - Ordering negative number expressions", () => {
	const input = {
	"max": 8,
	"min": -8,
	"width": 77,
	"height": 240,
	"points": [
		{
			"color": "#6495ed",
			"label": "x",
			"value": 4.5,
			"labelPosition": "right"
		},
		{
			"color": "#6495ed",
			"label": "y",
			"value": 1.5,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 8,
	"specialTickLabels": [],
	"minorTicksPerInterval": 3,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x89451955162e0708
// Course: 7th grade math
// Exercise: Ordering negative number expressions
// Widget key: image_1
test("number-line - Ordering negative number expressions", () => {
	const input = {
	"max": 4,
	"min": -4,
	"width": 460,
	"height": 69,
	"points": [
		{
			"color": "#6495ED",
			"label": "r",
			"value": -1.6,
			"labelPosition": "above"
		},
		{
			"color": "#6495ED",
			"label": "s",
			"value": 2.6,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x98cd49b5121e21de
// Course: 7th grade math
// Exercise: Ordering negative number expressions
// Widget key: image_1
test("number-line - Ordering negative number expressions", () => {
	const input = {
	"max": 1.9,
	"min": -1.9,
	"width": 460,
	"height": 69,
	"points": [
		{
			"color": "#6495ED",
			"label": "a",
			"value": -0.6,
			"labelPosition": "above"
		},
		{
			"color": "#6495ED",
			"label": "b",
			"value": 1.6,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x0ef6d2b4c68519d9
// Course: 7th grade math
// Exercise: Ordering negative number expressions
// Widget key: image_1
test("number-line - Ordering negative number expressions", () => {
	const input = {
	"max": 3,
	"min": -4,
	"width": 460,
	"height": 69,
	"points": [
		{
			"color": "#6495ed",
			"label": "a",
			"value": -3.3,
			"labelPosition": "above"
		},
		{
			"color": "#6495ed",
			"label": "b",
			"value": 1.6,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x33814e542c5816f3
// Course: 7th grade math
// Exercise: Ordering negative number expressions
// Widget key: image_1
test("number-line - Ordering negative number expressions", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 460,
	"height": 69,
	"points": [
		{
			"color": "#6495ed",
			"label": "a",
			"value": -1.2,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xea0a8e5dd711d5b9
// Course: 7th grade math
// Exercise: Ordering negative number expressions
// Widget key: image_1
test("number-line - Ordering negative number expressions", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 69,
	"points": [
		{
			"color": "#6495ed",
			"label": "a",
			"value": -0.8,
			"labelPosition": "above"
		},
		{
			"color": "#6495ed",
			"label": "b",
			"value": 2.6,
			"labelPosition": "above"
		},
		{
			"color": "#6495ed",
			"label": "c",
			"value": 4.4,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x5207f2836aa03b35
// Course: 7th grade math
// Exercise: Ordering negative number expressions
// Widget key: image_1
test("number-line - Ordering negative number expressions", () => {
	const input = {
	"max": 1.3,
	"min": -1.3,
	"width": 460,
	"height": 69,
	"points": [
		{
			"color": "#6495ed",
			"label": "b",
			"value": -1.2,
			"labelPosition": "above"
		},
		{
			"color": "#6495ed",
			"label": "a",
			"value": 0.5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xb441fe35e6768bcd
// Course: 7th grade math
// Exercise: Ordering negative number expressions
// Widget key: image_1
test("number-line - Ordering negative number expressions", () => {
	const input = {
	"max": 1,
	"min": -1,
	"width": 460,
	"height": 69,
	"points": [
		{
			"color": "#6495ed",
			"label": "b",
			"value": -0.4,
			"labelPosition": "above"
		},
		{
			"color": "#6495ed",
			"label": "a",
			"value": 0.9,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x56a633da4f62b8b3
// Course: 7th grade math
// Exercise: Ordering negative number expressions
// Widget key: image_1
test("number-line - Ordering negative number expressions", () => {
	const input = {
	"max": 8,
	"min": -4,
	"width": 460,
	"height": 69,
	"points": [
		{
			"color": "#6495ed",
			"label": "a",
			"value": 6.2,
			"labelPosition": "above"
		},
		{
			"color": "#6495ed",
			"label": "b",
			"value": -3.7,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xb698e113e751422f
// Course: 7th grade math
// Exercise: Ordering negative number expressions
// Widget key: image_1
test("number-line - Ordering negative number expressions", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 460,
	"height": 69,
	"points": [
		{
			"color": "#6495ED",
			"label": "-c",
			"value": -1.1,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2cb364c025caa4e5
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: choice_a_numberline
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 700,
	"height": 120,
	"points": [
		{
			"color": "#0c7f99",
			"label": " ",
			"value": 0.25,
			"labelPosition": "above"
		},
		{
			"color": "#0c7f99",
			"label": " ",
			"value": 1.25,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2cb364c025caa4e5
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: choice_b_numberline
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 700,
	"height": 120,
	"points": [
		{
			"color": "#0c7f99",
			"label": " ",
			"value": -0.25,
			"labelPosition": "above"
		},
		{
			"color": "#0c7f99",
			"label": " ",
			"value": 1.25,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2cb364c025caa4e5
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: choice_c_numberline
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 700,
	"height": 120,
	"points": [
		{
			"color": "#0c7f99",
			"label": " ",
			"value": -1.25,
			"labelPosition": "above"
		},
		{
			"color": "#0c7f99",
			"label": " ",
			"value": -0.25,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2cb364c025caa4e5
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: choice_d_numberline
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 700,
	"height": 120,
	"points": [
		{
			"color": "#0c7f99",
			"label": " ",
			"value": -1.25,
			"labelPosition": "above"
		},
		{
			"color": "#0c7f99",
			"label": " ",
			"value": 0.25,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xbc93ac7088d522c6
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: image_1
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": -3,
	"min": -5,
	"width": 80,
	"height": 325,
	"points": [
		{
			"color": "#0c7f99",
			"label": " ",
			"value": -4.7,
			"labelPosition": "left"
		},
		{
			"color": "#0c7f99",
			"label": " ",
			"value": -3.9,
			"labelPosition": "left"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 9,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x678e737d9d948dd2
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: choice_a_numberline
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#0c7f99",
			"label": " ",
			"value": -2,
			"labelPosition": "above"
		},
		{
			"color": "#0c7f99",
			"label": " ",
			"value": 7,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x678e737d9d948dd2
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: choice_b_numberline
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#0c7f99",
			"label": " ",
			"value": 2,
			"labelPosition": "above"
		},
		{
			"color": "#0c7f99",
			"label": " ",
			"value": 7,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x678e737d9d948dd2
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: choice_c_numberline
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#0c7f99",
			"label": " ",
			"value": -7,
			"labelPosition": "above"
		},
		{
			"color": "#0c7f99",
			"label": " ",
			"value": 2,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x678e737d9d948dd2
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: choice_d_numberline
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#0c7f99",
			"label": " ",
			"value": -7,
			"labelPosition": "above"
		},
		{
			"color": "#0c7f99",
			"label": " ",
			"value": -2,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2e27812b11dffe14
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: numberline_choice_a
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#0c7f99",
			"label": " ",
			"value": -1.6666666667,
			"labelPosition": "above"
		},
		{
			"color": "#0c7f99",
			"label": " ",
			"value": -0.6666666667,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 2,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2e27812b11dffe14
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: numberline_choice_b
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#0c7f99",
			"label": " ",
			"value": -1.6666666667,
			"labelPosition": "above"
		},
		{
			"color": "#0c7f99",
			"label": " ",
			"value": 0.6666666667,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 2,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2e27812b11dffe14
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: numberline_choice_c
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#0c7f99",
			"label": " ",
			"value": 0.6666666667,
			"labelPosition": "above"
		},
		{
			"color": "#0c7f99",
			"label": " ",
			"value": 1.6666666667,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 2,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2e27812b11dffe14
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: numberline_choice_d
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#0c7f99",
			"label": " ",
			"value": -0.6666666667,
			"labelPosition": "above"
		},
		{
			"color": "#0c7f99",
			"label": " ",
			"value": 1.6666666667,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 2,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x192b8af759e7cb42
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: image_1
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 460,
	"height": 69,
	"points": [
		{
			"color": "#11accd",
			"label": " ",
			"value": -2,
			"labelPosition": "above"
		},
		{
			"color": "#11accd",
			"label": " ",
			"value": 1,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x5afb43cd66f2bb42
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: image_1
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": 2,
	"min": -3,
	"width": 80,
	"height": 325,
	"points": [
		{
			"color": "#0c7f99",
			"label": null,
			"value": -2,
			"labelPosition": "right"
		},
		{
			"color": "#0c7f99",
			"label": null,
			"value": 1.5,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xf3035ac589940ba4
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: image_1
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": 1,
	"min": -0.5,
	"width": 80,
	"height": 325,
	"points": [
		{
			"color": "#11accd",
			"label": "−1/4",
			"value": -0.25,
			"labelPosition": "right"
		},
		{
			"color": "#11accd",
			"label": "3/4",
			"value": 0.75,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 0.25,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xa8ccf6741922eaaf
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: image_1
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": 3,
	"min": -6,
	"width": 77,
	"height": 240,
	"points": [
		{
			"color": "#11accd",
			"label": " ",
			"value": -4,
			"labelPosition": "right"
		},
		{
			"color": "#11accd",
			"label": " ",
			"value": 1,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x06454d11ce9a2807
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: choice_a_number_line
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": 6,
	"min": -6,
	"width": 230,
	"height": 80,
	"points": [
		{
			"color": "#0c7f99",
			"label": null,
			"value": -6,
			"labelPosition": "above"
		},
		{
			"color": "#0c7f99",
			"label": null,
			"value": -3,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x06454d11ce9a2807
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: choice_b_number_line
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": 6,
	"min": -6,
	"width": 230,
	"height": 80,
	"points": [
		{
			"color": "#0c7f99",
			"label": null,
			"value": -6,
			"labelPosition": "above"
		},
		{
			"color": "#0c7f99",
			"label": null,
			"value": 3,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x06454d11ce9a2807
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: choice_c_number_line
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": 6,
	"min": -6,
	"width": 230,
	"height": 80,
	"points": [
		{
			"color": "#0c7f99",
			"label": null,
			"value": 3,
			"labelPosition": "above"
		},
		{
			"color": "#0c7f99",
			"label": null,
			"value": 6,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x06454d11ce9a2807
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: choice_d_number_line
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": 6,
	"min": -6,
	"width": 230,
	"height": 80,
	"points": [
		{
			"color": "#0c7f99",
			"label": null,
			"value": -3,
			"labelPosition": "above"
		},
		{
			"color": "#0c7f99",
			"label": null,
			"value": 6,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x76d6e637a385ffc0
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: image_1
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": 1,
	"min": -1.6666666667,
	"width": 460,
	"height": 88,
	"points": [
		{
			"color": "#11accd",
			"label": " ",
			"value": -1.3333333333,
			"labelPosition": "above"
		},
		{
			"color": "#11accd",
			"label": " ",
			"value": 0.3333333333,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.3333333333,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2977b6e04d636e31
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: choice_a_number_line
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 230,
	"height": 80,
	"points": [
		{
			"color": "#0c7f99",
			"label": null,
			"value": -4,
			"labelPosition": "above"
		},
		{
			"color": "#0c7f99",
			"label": null,
			"value": 1,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2977b6e04d636e31
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: choice_b_number_line
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 230,
	"height": 80,
	"points": [
		{
			"color": "#0c7f99",
			"label": null,
			"value": -1,
			"labelPosition": "above"
		},
		{
			"color": "#0c7f99",
			"label": null,
			"value": 4,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2977b6e04d636e31
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: choice_c_number_line
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 230,
	"height": 80,
	"points": [
		{
			"color": "#0c7f99",
			"label": null,
			"value": 1,
			"labelPosition": "above"
		},
		{
			"color": "#0c7f99",
			"label": null,
			"value": 4,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2977b6e04d636e31
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: choice_d_number_line
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 230,
	"height": 80,
	"points": [
		{
			"color": "#0c7f99",
			"label": null,
			"value": -4,
			"labelPosition": "above"
		},
		{
			"color": "#0c7f99",
			"label": null,
			"value": -1,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x17b0d05150d27b19
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: numberline_choice_a
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#0C7F99",
			"label": "−1",
			"value": -1,
			"labelPosition": "above"
		},
		{
			"color": "#0C7F99",
			"label": "8",
			"value": 8,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x17b0d05150d27b19
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: numberline_choice_b
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": 10,
	"min": 0,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#0C7F99",
			"label": "1",
			"value": 1,
			"labelPosition": "above"
		},
		{
			"color": "#0C7F99",
			"label": "8",
			"value": 8,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x17b0d05150d27b19
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: numberline_choice_c
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#0C7F99",
			"label": "−8",
			"value": -8,
			"labelPosition": "above"
		},
		{
			"color": "#0C7F99",
			"label": "−1",
			"value": -1,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x17b0d05150d27b19
// Course: 7th grade math
// Exercise: Absolute value to find distance
// Widget key: numberline_choice_d
test("number-line - Absolute value to find distance", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#0C7F99",
			"label": "−8",
			"value": -8,
			"labelPosition": "above"
		},
		{
			"color": "#0C7F99",
			"label": "1",
			"value": 1,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xf74910c2ce997c62
// Course: 7th grade math
// Exercise: Negative number addition and subtraction: word problems
// Widget key: image_1
test("number-line - Negative number addition and subtraction word", () => {
	const input = {
	"max": -40,
	"min": -50,
	"width": 240,
	"height": 325,
	"points": [
		{
			"color": "#0c7f99",
			"label": "Bowman",
			"value": -47,
			"labelPosition": "right"
		},
		{
			"color": "#0c7f99",
			"label": "Williston",
			"value": -44,
			"labelPosition": "right"
		},
		{
			"color": "#0c7f99",
			"label": "Minot",
			"value": -42,
			"labelPosition": "right"
		},
		{
			"color": "#0c7f99",
			"label": "Bismarck",
			"value": -41,
			"labelPosition": "right"
		},
		{
			"color": "#0c7f99",
			"label": "Jamestown",
			"value": -40,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x0af75497ef49eb7f
// Course: 7th grade math
// Exercise: Negative number addition and subtraction: word problems
// Widget key: image_1
test("number-line - Negative number addition and subtraction word", () => {
	const input = {
	"max": 0,
	"min": -20,
	"width": 240,
	"height": 325,
	"points": [
		{
			"color": "#0c7f99",
			"label": "Friday: negative 18 degrees Celsius",
			"value": -18,
			"labelPosition": "right"
		},
		{
			"color": "#0c7f99",
			"label": "Wednesday: negative 13 degrees Celsius",
			"value": -13,
			"labelPosition": "right"
		},
		{
			"color": "#0c7f99",
			"label": "Tuesday: negative 11 degrees Celsius",
			"value": -11,
			"labelPosition": "right"
		},
		{
			"color": "#0c7f99",
			"label": "Thursday: negative 8 degrees Celsius",
			"value": -8,
			"labelPosition": "right"
		},
		{
			"color": "#0c7f99",
			"label": "Monday: negative 4 degrees Celsius",
			"value": -4,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xc52a5aa891289c50
// Course: 7th grade math
// Exercise: Negative number addition and subtraction: word problems
// Widget key: image_1
test("number-line - Negative number addition and subtraction word", () => {
	const input = {
	"max": 0,
	"min": -20,
	"width": 240,
	"height": 325,
	"points": [
		{
			"color": "#0c7f99",
			"label": "Friday",
			"value": -18,
			"labelPosition": "right"
		},
		{
			"color": "#0c7f99",
			"label": "Wednesday",
			"value": -13,
			"labelPosition": "right"
		},
		{
			"color": "#0c7f99",
			"label": "Tuesday",
			"value": -11,
			"labelPosition": "right"
		},
		{
			"color": "#0c7f99",
			"label": "Thursday",
			"value": -8,
			"labelPosition": "right"
		},
		{
			"color": "#0c7f99",
			"label": "Monday",
			"value": -4,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xb242080383738fde
// Course: 7th grade math
// Exercise: Negative number addition and subtraction: word problems
// Widget key: image_1
test("number-line - Negative number addition and subtraction word", () => {
	const input = {
	"max": -40,
	"min": -50,
	"width": 240,
	"height": 325,
	"points": [
		{
			"color": "#0C7F99",
			"label": "Bowman",
			"value": -47,
			"labelPosition": "right"
		},
		{
			"color": "#0C7F99",
			"label": "Williston",
			"value": -44,
			"labelPosition": "right"
		},
		{
			"color": "#0C7F99",
			"label": "Minot",
			"value": -42,
			"labelPosition": "right"
		},
		{
			"color": "#0C7F99",
			"label": "Bismarck",
			"value": -41,
			"labelPosition": "right"
		},
		{
			"color": "#0C7F99",
			"label": "Jamestown",
			"value": -40,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x45dbfd7b60c55ec8
// Course: 7th grade math
// Exercise: Negative number addition and subtraction: word problems
// Widget key: image_1
test("number-line - Negative number addition and subtraction word", () => {
	const input = {
	"max": 10,
	"min": -90,
	"width": 240,
	"height": 325,
	"points": [
		{
			"color": "#0c7f99",
			"label": "krill: negative 86 meters",
			"value": -86,
			"labelPosition": "right"
		},
		{
			"color": "#0c7f99",
			"label": "adult penguin: negative 59 meters",
			"value": -59,
			"labelPosition": "right"
		},
		{
			"color": "#0c7f99",
			"label": "sardine: negative 32 meters",
			"value": -32,
			"labelPosition": "right"
		},
		{
			"color": "#0c7f99",
			"label": "anchovy: negative 22 meters",
			"value": -22,
			"labelPosition": "right"
		},
		{
			"color": "#0c7f99",
			"label": "surface of water: 0 meters",
			"value": 0,
			"labelPosition": "right"
		},
		{
			"color": "#0c7f99",
			"label": "penguin chick: 5 meters",
			"value": 5,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 10,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2823d70029fc1298
// Course: 7th grade math
// Exercise: Negative number addition and subtraction: word problems
// Widget key: image_1
test("number-line - Negative number addition and subtraction word", () => {
	const input = {
	"max": 10,
	"min": -90,
	"width": 240,
	"height": 325,
	"points": [
		{
			"color": "#0C7F99",
			"label": "krill: negative 86 meters",
			"value": -86,
			"labelPosition": "right"
		},
		{
			"color": "#0C7F99",
			"label": "adult penguin: negative 59 meters",
			"value": -59,
			"labelPosition": "right"
		},
		{
			"color": "#0C7F99",
			"label": "sardine: negative 32 meters",
			"value": -32,
			"labelPosition": "right"
		},
		{
			"color": "#0C7F99",
			"label": "anchovy: negative 22 meters",
			"value": -22,
			"labelPosition": "right"
		},
		{
			"color": "#0C7F99",
			"label": "surface of water: 0 meters",
			"value": 0,
			"labelPosition": "right"
		},
		{
			"color": "#0C7F99",
			"label": "penguin chick: 5 meters",
			"value": 5,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 10,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x7eb0f597fff433a8
// Course: 7th grade math
// Exercise: Signs of expressions
// Widget key: image_1
test("number-line - Signs of expressions", () => {
	const input = {
	"max": 9,
	"min": -9,
	"width": 460,
	"height": 69,
	"points": [
		{
			"color": "#6495ed",
			"label": "y",
			"value": 1,
			"labelPosition": "above"
		},
		{
			"color": "#6495ed",
			"label": "x",
			"value": -8,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xd8f74c2ad290d41b
// Course: 7th grade math
// Exercise: Signs of expressions
// Widget key: image_1
test("number-line - Signs of expressions", () => {
	const input = {
	"max": 8,
	"min": -8,
	"width": 125,
	"height": 240,
	"points": [
		{
			"color": "#6495ed",
			"label": "a",
			"value": -8,
			"labelPosition": "right"
		},
		{
			"color": "#6495ed",
			"label": "b",
			"value": -3.5,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x946d1c1b5794c5d0
// Course: 7th grade math
// Exercise: Signs of expressions
// Widget key: image_1
test("number-line - Signs of expressions", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 125,
	"height": 240,
	"points": [
		{
			"color": "#6495ed",
			"label": "b",
			"value": 0.7,
			"labelPosition": "right"
		},
		{
			"color": "#6495ed",
			"label": "c",
			"value": -1.8,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xcc1780adf5a9f187
// Course: 7th grade math
// Exercise: Signs of expressions
// Widget key: image_1
test("number-line - Signs of expressions", () => {
	const input = {
	"max": 4,
	"min": -4,
	"width": 125,
	"height": 240,
	"points": [
		{
			"color": "#6495ed",
			"label": "b",
			"value": -2.5,
			"labelPosition": "right"
		},
		{
			"color": "#6495ed",
			"label": "a",
			"value": 0,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xf512502ffef902d8
// Course: 7th grade math
// Exercise: Signs of expressions
// Widget key: image_1
test("number-line - Signs of expressions", () => {
	const input = {
	"max": 9,
	"min": -11,
	"width": 460,
	"height": 69,
	"points": [
		{
			"color": "#6495ED",
			"label": "x",
			"value": -4.5,
			"labelPosition": "above"
		},
		{
			"color": "#6495ED",
			"label": "y",
			"value": 8.5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [
		{
			"label": "0",
			"value": 0
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xf3a72fc9244f5d0b
// Course: 7th grade math
// Exercise: Signs of expressions
// Widget key: image_1
test("number-line - Signs of expressions", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 460,
	"height": 69,
	"points": [
		{
			"color": "#6495ed",
			"label": "x",
			"value": -1.25,
			"labelPosition": "above"
		},
		{
			"color": "#6495ed",
			"label": "y",
			"value": 0,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x7af81be6b2c580c8
// Course: 7th grade math
// Exercise: Plotting inequalities
// Widget key: number_line_empty
test("number-line - Plotting inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xb3704fceda59f25f
// Course: 6th grade math
// Exercise: Negative numbers on the number line
// Widget key: image_2
test("number-line - Negative numbers on the number line", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 77,
	"height": 240,
	"points": [
		{
			"color": "#0C7F99",
			"label": "?",
			"value": -15,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 3,
	"specialTickLabels": [
		{
			"label": "-3",
			"value": -3
		},
		{
			"label": "0",
			"value": 0
		},
		{
			"label": "3",
			"value": 3
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xdae754517ed494ca
// Course: 6th grade math
// Exercise: Negative numbers on the number line
// Widget key: image_2
test("number-line - Negative numbers on the number line", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#0C7F99",
			"label": "?",
			"value": -12,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 3,
	"specialTickLabels": [
		{
			"label": "",
			"value": -15
		},
		{
			"label": "",
			"value": -12
		},
		{
			"label": "",
			"value": -9
		},
		{
			"label": "",
			"value": -6
		},
		{
			"label": "-3",
			"value": -3
		},
		{
			"label": "0",
			"value": 0
		},
		{
			"label": "3",
			"value": 3
		},
		{
			"label": "",
			"value": 6
		},
		{
			"label": "",
			"value": 9
		},
		{
			"label": "",
			"value": 12
		},
		{
			"label": "",
			"value": 15
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x34d24c3693e1d844
// Course: 6th grade math
// Exercise: Negative numbers on the number line
// Widget key: image_2
test("number-line - Negative numbers on the number line", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#0C7F99",
			"label": "?",
			"value": -6,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 10,
	"specialTickLabels": [
		{
			"label": "",
			"value": -10
		},
		{
			"label": "-2",
			"value": -2
		},
		{
			"label": "0",
			"value": 0
		},
		{
			"label": "2",
			"value": 2
		},
		{
			"label": "",
			"value": 10
		}
	],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xbe02b8fb6422f7af
// Course: 6th grade math
// Exercise: Negative numbers on the number line
// Widget key: image_2
test("number-line - Negative numbers on the number line", () => {
	const input = {
	"max": 25,
	"min": -25,
	"width": 77,
	"height": 240,
	"points": [
		{
			"color": "#0C7F99",
			"label": "?",
			"value": -20,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 5,
	"specialTickLabels": [
		{
			"label": "-5",
			"value": -5
		},
		{
			"label": "0",
			"value": 0
		},
		{
			"label": "5",
			"value": 5
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xc004713f8a564a33
// Course: 6th grade math
// Exercise: Negative numbers on the number line
// Widget key: image_2
test("number-line - Negative numbers on the number line", () => {
	const input = {
	"max": 20,
	"min": -20,
	"width": 77,
	"height": 240,
	"points": [
		{
			"color": "#0C7F99",
			"label": "?",
			"value": -16,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 4,
	"specialTickLabels": [
		{
			"label": "",
			"value": -20
		},
		{
			"label": "",
			"value": -16
		},
		{
			"label": "",
			"value": -12
		},
		{
			"label": "",
			"value": -8
		},
		{
			"label": "-4",
			"value": -4
		},
		{
			"label": "0",
			"value": 0
		},
		{
			"label": "4",
			"value": 4
		},
		{
			"label": "",
			"value": 8
		},
		{
			"label": "",
			"value": 12
		},
		{
			"label": "",
			"value": 16
		},
		{
			"label": "",
			"value": 20
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x49b3274645e4b5aa
// Course: 6th grade math
// Exercise: Negative numbers on the number line
// Widget key: number_line_initial
test("number-line - Negative numbers on the number line", () => {
	const input = {
	"max": 20,
	"min": -20,
	"width": 600,
	"height": 120,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 4,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x49b3274645e4b5aa
// Course: 6th grade math
// Exercise: Negative numbers on the number line
// Widget key: number_line_choice_a
test("number-line - Negative numbers on the number line", () => {
	const input = {
	"max": 20,
	"min": -20,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#1E90FF",
			"label": null,
			"value": 12,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 4,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x49b3274645e4b5aa
// Course: 6th grade math
// Exercise: Negative numbers on the number line
// Widget key: number_line_choice_b
test("number-line - Negative numbers on the number line", () => {
	const input = {
	"max": 20,
	"min": -20,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#1E90FF",
			"label": null,
			"value": 8,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 4,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x49b3274645e4b5aa
// Course: 6th grade math
// Exercise: Negative numbers on the number line
// Widget key: number_line_choice_c
test("number-line - Negative numbers on the number line", () => {
	const input = {
	"max": 20,
	"min": -20,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#1E90FF",
			"label": null,
			"value": 16,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 4,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x6e0f8f4f89d5d1f9
// Course: 6th grade math
// Exercise: Negative numbers on the number line
// Widget key: number_line_initial
test("number-line - Negative numbers on the number line", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 600,
	"height": 120,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x6e0f8f4f89d5d1f9
// Course: 6th grade math
// Exercise: Negative numbers on the number line
// Widget key: number_line_choice_a
test("number-line - Negative numbers on the number line", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#1E90FF",
			"label": " ",
			"value": 8,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x6e0f8f4f89d5d1f9
// Course: 6th grade math
// Exercise: Negative numbers on the number line
// Widget key: number_line_choice_b
test("number-line - Negative numbers on the number line", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#1E90FF",
			"label": " ",
			"value": 6,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x6e0f8f4f89d5d1f9
// Course: 6th grade math
// Exercise: Negative numbers on the number line
// Widget key: number_line_choice_c
test("number-line - Negative numbers on the number line", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#1E90FF",
			"label": " ",
			"value": 10,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x316c0770a8b9704f
// Course: 6th grade math
// Exercise: Negative numbers on the number line
// Widget key: image_2
test("number-line - Negative numbers on the number line", () => {
	const input = {
	"max": 20,
	"min": -20,
	"width": 77,
	"height": 240,
	"points": [
		{
			"color": "#0c7f99",
			"label": "?",
			"value": -12,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 20,
	"specialTickLabels": [
		{
			"label": "",
			"value": -20
		},
		{
			"label": "-4",
			"value": -4
		},
		{
			"label": "4",
			"value": 4
		},
		{
			"label": "",
			"value": 20
		}
	],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xbe60cd481ef15a99
// Course: 6th grade math
// Exercise: Negative numbers on the number line
// Widget key: image_2
test("number-line - Negative numbers on the number line", () => {
	const input = {
	"max": 20,
	"min": -20,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#0c7f99",
			"label": "?",
			"value": 16,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 4,
	"specialTickLabels": [
		{
			"label": "",
			"value": -20
		},
		{
			"label": "",
			"value": -16
		},
		{
			"label": "",
			"value": -12
		},
		{
			"label": "",
			"value": -8
		},
		{
			"label": "-4",
			"value": -4
		},
		{
			"label": "0",
			"value": 0
		},
		{
			"label": "4",
			"value": 4
		},
		{
			"label": "",
			"value": 8
		},
		{
			"label": "",
			"value": 12
		},
		{
			"label": "",
			"value": 16
		},
		{
			"label": "",
			"value": 20
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x07b6fc89523b9a35
// Course: 6th grade math
// Exercise: Negative numbers on the number line
// Widget key: image_2
test("number-line - Negative numbers on the number line", () => {
	const input = {
	"max": 15,
	"min": -15,
	"width": 77,
	"height": 240,
	"points": [
		{
			"color": "#29abca",
			"label": "?",
			"value": -6,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 3,
	"specialTickLabels": [
		{
			"label": "",
			"value": -15
		},
		{
			"label": "",
			"value": -12
		},
		{
			"label": "",
			"value": -9
		},
		{
			"label": "",
			"value": -6
		},
		{
			"label": "-3",
			"value": -3
		},
		{
			"label": "0",
			"value": 0
		},
		{
			"label": "3",
			"value": 3
		},
		{
			"label": "",
			"value": 6
		},
		{
			"label": "",
			"value": 9
		},
		{
			"label": "",
			"value": 12
		},
		{
			"label": "",
			"value": 15
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x5ac3d3b0638e144a
// Course: 6th grade math
// Exercise: Negative numbers on the number line
// Widget key: image_2
test("number-line - Negative numbers on the number line", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 77,
	"height": 240,
	"points": [
		{
			"color": "#0C7F99",
			"label": "?",
			"value": -10,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x98647957d1ad2fc1
// Course: 6th grade math
// Exercise: Negative numbers on the number line
// Widget key: image_2
test("number-line - Negative numbers on the number line", () => {
	const input = {
	"max": 25,
	"min": -25,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#0C7F99",
			"label": "?",
			"value": -15,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [
		{
			"label": "-5",
			"value": -5
		},
		{
			"label": "0",
			"value": 0
		},
		{
			"label": "5",
			"value": 5
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xd82a2829e11b6beb
// Course: 6th grade math
// Exercise: Negative numbers on the number line
// Widget key: image_2
test("number-line - Negative numbers on the number line", () => {
	const input = {
	"max": 30,
	"min": -30,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#0c7f99",
			"label": "?",
			"value": 18,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 6,
	"specialTickLabels": [
		{
			"label": "-6",
			"value": -6
		},
		{
			"label": "0",
			"value": 0
		},
		{
			"label": "6",
			"value": 6
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x5a611e4cc4eed8d6
// Course: 6th grade math
// Exercise: Negative numbers on the number line
// Widget key: image_2
test("number-line - Negative numbers on the number line", () => {
	const input = {
	"max": 30,
	"min": -30,
	"width": 77,
	"height": 240,
	"points": [
		{
			"color": "#0C7F99",
			"label": "?",
			"value": -30,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 6,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xacb997c47313a606
// Course: 6th grade math
// Exercise: Negative numbers on the number line
// Widget key: image_2
test("number-line - Negative numbers on the number line", () => {
	const input = {
	"max": 30,
	"min": -30,
	"width": 77,
	"height": 240,
	"points": [
		{
			"color": "#0C7F99",
			"label": "?",
			"value": -12,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 6,
	"specialTickLabels": [
		{
			"label": "-6",
			"value": -6
		},
		{
			"label": "0",
			"value": 0
		},
		{
			"label": "6",
			"value": 6
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xcf9144cd5aaae605
// Course: 6th grade math
// Exercise: Negative numbers on the number line
// Widget key: image_2
test("number-line - Negative numbers on the number line", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 77,
	"height": 240,
	"points": [
		{
			"color": "#0C7F99",
			"label": "?",
			"value": -6,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 2,
	"specialTickLabels": [
		{
			"label": "-2",
			"value": -2
		},
		{
			"label": "0",
			"value": 0
		},
		{
			"label": "2",
			"value": 2
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x0009c4c3efb4c38d
// Course: 6th grade math
// Exercise: Negative numbers on the number line
// Widget key: image_2
test("number-line - Negative numbers on the number line", () => {
	const input = {
	"max": 35,
	"min": -35,
	"width": 77,
	"height": 240,
	"points": [
		{
			"color": "#0c7f99",
			"label": "?",
			"value": -14,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 7,
	"specialTickLabels": [
		{
			"label": "",
			"value": -35
		},
		{
			"label": "",
			"value": -28
		},
		{
			"label": "",
			"value": -21
		},
		{
			"label": "",
			"value": -14
		},
		{
			"label": "-7",
			"value": -7
		},
		{
			"label": "0",
			"value": 0
		},
		{
			"label": "7",
			"value": 7
		},
		{
			"label": "",
			"value": 14
		},
		{
			"label": "",
			"value": 21
		},
		{
			"label": "",
			"value": 28
		},
		{
			"label": "",
			"value": 35
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xc45824ff7ae607c9
// Course: 6th grade math
// Exercise: Negative numbers on the number line
// Widget key: image_2
test("number-line - Negative numbers on the number line", () => {
	const input = {
	"max": 40,
	"min": -40,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#0c7f99",
			"label": "?",
			"value": -32,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 8,
	"specialTickLabels": [
		{
			"label": "",
			"value": -40
		},
		{
			"label": "",
			"value": -32
		},
		{
			"label": "",
			"value": -24
		},
		{
			"label": "",
			"value": -16
		},
		{
			"label": "-8",
			"value": -8
		},
		{
			"label": "0",
			"value": 0
		},
		{
			"label": "8",
			"value": 8
		},
		{
			"label": "",
			"value": 16
		},
		{
			"label": "",
			"value": 24
		},
		{
			"label": "",
			"value": 32
		},
		{
			"label": "",
			"value": 40
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x24a5d0824f1a3298
// Course: 6th grade math
// Exercise: Negative numbers on the number line
// Widget key: image_2
test("number-line - Negative numbers on the number line", () => {
	const input = {
	"max": 25,
	"min": -25,
	"width": 77,
	"height": 240,
	"points": [
		{
			"color": "#0C7F99",
			"label": "?",
			"value": -10,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 5,
	"specialTickLabels": [
		{
			"label": "",
			"value": -25
		},
		{
			"label": "",
			"value": -20
		},
		{
			"label": "",
			"value": -15
		},
		{
			"label": "",
			"value": -10
		},
		{
			"label": "-5",
			"value": -5
		},
		{
			"label": "0",
			"value": 0
		},
		{
			"label": "5",
			"value": 5
		},
		{
			"label": "",
			"value": 10
		},
		{
			"label": "",
			"value": 15
		},
		{
			"label": "",
			"value": 20
		},
		{
			"label": "",
			"value": 25
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x493cf88fe059a1e2
// Course: 6th grade math
// Exercise: Negative numbers on the number line
// Widget key: image_2
test("number-line - Negative numbers on the number line", () => {
	const input = {
	"max": 35,
	"min": -35,
	"width": 77,
	"height": 240,
	"points": [
		{
			"color": "#29abca",
			"label": "?",
			"value": -35,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 7,
	"specialTickLabels": [
		{
			"label": "-7",
			"value": -7
		},
		{
			"label": "0",
			"value": 0
		},
		{
			"label": "7",
			"value": 7
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x9bdd37009fb0e6e0
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: image_1
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 4,
	"min": -4,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "C",
			"value": 0,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xa7a6286b688fd181
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: image_1
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 8,
	"min": -8,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "B",
			"value": -1,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x6260dd813b89d59e
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: nl_choice_a
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 8,
	"min": -8,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": " ",
			"value": -3,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x6260dd813b89d59e
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: nl_choice_b
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 8,
	"min": -8,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": " ",
			"value": 3,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x6260dd813b89d59e
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: nl_choice_c
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 8,
	"min": -8,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": " ",
			"value": 0,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x6260dd813b89d59e
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: number_line_initial
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 8,
	"min": -8,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xbaadddeb
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: image_1
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 10,
	"min": -5,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "A",
			"value": -4,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "B",
			"value": -2,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "C",
			"value": 6,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "D",
			"value": 7,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "E",
			"value": 10,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x57b35c359b98a3f4
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: image_1
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ed",
			"label": "D",
			"value": -1.5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xc527646af4c9351b
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: image_1
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 0.6,
	"min": -0.6,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ed",
			"label": "F",
			"value": -0.4,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x4258e7f78f578111
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: number_line_initial
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 8,
	"min": -8,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ed",
			"label": "B",
			"value": -3,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x4258e7f78f578111
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: number_line_choice_a
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 8,
	"min": -8,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ed",
			"label": " ",
			"value": -3,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x4258e7f78f578111
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: number_line_choice_b
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 8,
	"min": -8,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ed",
			"label": " ",
			"value": 3,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x4258e7f78f578111
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: number_line_choice_c
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 8,
	"min": -8,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ed",
			"label": " ",
			"value": 0,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xf3d9cc00e32d464f
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: image_1
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 8,
	"min": -8,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "A",
			"value": 5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xb258023ca47a4510
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: image_1
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ed",
			"label": "D",
			"value": -1.5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xc1ac295ed6fa5fa1
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: number_line_initial
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 8,
	"min": -8,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "A",
			"value": 0,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xc1ac295ed6fa5fa1
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: number_line_choice_a
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 8,
	"min": -8,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "-A",
			"value": 0,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xc1ac295ed6fa5fa1
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: number_line_choice_b
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 8,
	"min": -8,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "-A",
			"value": -2,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xc1ac295ed6fa5fa1
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: number_line_choice_c
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 8,
	"min": -8,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "-A",
			"value": 2,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x0a5a1f1a80352070
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: number_line_initial
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 4,
	"min": -4,
	"width": 600,
	"height": 120,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x0a5a1f1a80352070
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: number_line_choice_a
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 4,
	"min": -4,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#1E90FF",
			"label": null,
			"value": -2.5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x0a5a1f1a80352070
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: number_line_choice_b
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 4,
	"min": -4,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#1E90FF",
			"label": null,
			"value": 2.5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x0a5a1f1a80352070
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: number_line_choice_c
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 4,
	"min": -4,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#1E90FF",
			"label": null,
			"value": -1.5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x5c584447c7fbea75
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: image_1
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 0.6,
	"min": -0.6,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ED",
			"label": "E",
			"value": 0.2,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xd2a3d7af
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: image_1
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 10,
	"min": -5,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "A",
			"value": -5,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "B",
			"value": -4,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "C",
			"value": -2,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "D",
			"value": 2,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "E",
			"value": 5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x8aa5a97c4a23bc52
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: number_line_choice_a
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 4,
	"min": -4,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ed",
			"label": " ",
			"value": 3.5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x8aa5a97c4a23bc52
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: number_line_choice_b
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 4,
	"min": -4,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ed",
			"label": " ",
			"value": -3.5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x8aa5a97c4a23bc52
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: number_line_choice_c
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 4,
	"min": -4,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ed",
			"label": " ",
			"value": 0,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x6d35fcf1b7fa9959
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: number_line_initial
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ED",
			"label": "C",
			"value": 0.5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x6d35fcf1b7fa9959
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: number_line_choice_a
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#FFA500",
			"label": "-C",
			"value": -0.5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x6d35fcf1b7fa9959
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: number_line_choice_b
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#FFA500",
			"label": "-C",
			"value": 0.5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x6d35fcf1b7fa9959
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: number_line_choice_c
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#FFA500",
			"label": "-C",
			"value": -1,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x71fd721566a5360d
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: image_1
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "C",
			"value": 0.5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xa3c11a88943b406b
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: number_line_initial
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ED",
			"label": "C",
			"value": 1.5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xa3c11a88943b406b
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: number_line_choice_a
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ED",
			"label": "-(-C)",
			"value": 1.5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xa3c11a88943b406b
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: number_line_choice_b
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ED",
			"label": "-(-C)",
			"value": -1.5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xa3c11a88943b406b
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: number_line_choice_c
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ED",
			"label": "-(-C)",
			"value": 0,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x8fb40438b7f86b03
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: image_1
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 8,
	"min": -8,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "A",
			"value": 6,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x39165496dae143a7
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: image_1
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ed",
			"label": "F",
			"value": -1.5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x86b2a3e066427391
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: image_1
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ed",
			"label": "C",
			"value": 1.5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x62c748d6
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: image_1
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 7,
	"min": -7,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "A",
			"value": -7,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "B",
			"value": -2,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "C",
			"value": 3,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "D",
			"value": 4,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "E",
			"value": 7,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xec3627429af7b5ea
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: image_1
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 0.6,
	"min": -0.6,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ED",
			"label": "E",
			"value": 0.6,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2d9a73dec8936789
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: number_line_choice_a
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ed",
			"label": " ",
			"value": -1.5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2d9a73dec8936789
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: number_line_choice_b
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ed",
			"label": " ",
			"value": 1.5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2d9a73dec8936789
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: number_line_choice_c
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ed",
			"label": " ",
			"value": 0,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x11099043
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: image_1
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 10,
	"min": -5,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "A",
			"value": -4,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "B",
			"value": -2,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "C",
			"value": 4,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "D",
			"value": 7,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "E",
			"value": 9,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xfee6aa630d739238
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: image_1
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 8,
	"min": -8,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ED",
			"label": "B",
			"value": -7,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xa4b45b740128203d
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: image_1
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 3,
	"min": -3,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "D",
			"value": 0,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xb6d191d1
// Course: 6th grade math
// Exercise: Negative symbol as opposite
// Widget key: image_1
test("number-line - Negative symbol as opposite", () => {
	const input = {
	"max": 10,
	"min": -5,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "A",
			"value": -5,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "B",
			"value": -4,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "C",
			"value": -2,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "D",
			"value": 2,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "E",
			"value": 5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x24f798bd
// Course: 6th grade math
// Exercise: Number opposites
// Widget key: image_1
test("number-line - Number opposites", () => {
	const input = {
	"max": 7,
	"min": -7,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "A",
			"value": -7,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "B",
			"value": -2,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "C",
			"value": 3,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "D",
			"value": 4,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "E",
			"value": 7,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x16fab81c
// Course: 6th grade math
// Exercise: Number opposites
// Widget key: image_1
test("number-line - Number opposites", () => {
	const input = {
	"max": 10,
	"min": -5,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "A",
			"value": -5,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "B",
			"value": -4,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "C",
			"value": -2,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "D",
			"value": 2,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "E",
			"value": 5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x1b944977
// Course: 6th grade math
// Exercise: Number opposites
// Widget key: image_1
test("number-line - Number opposites", () => {
	const input = {
	"max": 7,
	"min": -7,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "A",
			"value": -6,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "B",
			"value": -3,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "C",
			"value": -1,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "D",
			"value": 1,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "E",
			"value": 5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x81ab69357992c427
// Course: 6th grade math
// Exercise: Number opposites
// Widget key: number_line_initial
test("number-line - Number opposites", () => {
	const input = {
	"max": 6,
	"min": -6,
	"width": 600,
	"height": 120,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x81ab69357992c427
// Course: 6th grade math
// Exercise: Number opposites
// Widget key: number_line_choice_a
test("number-line - Number opposites", () => {
	const input = {
	"max": 6,
	"min": -6,
	"width": 500,
	"height": 100,
	"points": [
		{
			"color": "#1E90FF",
			"label": null,
			"value": -4,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x81ab69357992c427
// Course: 6th grade math
// Exercise: Number opposites
// Widget key: number_line_choice_b
test("number-line - Number opposites", () => {
	const input = {
	"max": 6,
	"min": -6,
	"width": 500,
	"height": 100,
	"points": [
		{
			"color": "#1E90FF",
			"label": null,
			"value": 4,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x81ab69357992c427
// Course: 6th grade math
// Exercise: Number opposites
// Widget key: number_line_choice_c
test("number-line - Number opposites", () => {
	const input = {
	"max": 6,
	"min": -6,
	"width": 500,
	"height": 100,
	"points": [
		{
			"color": "#1E90FF",
			"label": null,
			"value": -2,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xcb3e37851559832d
// Course: 6th grade math
// Exercise: Number opposites
// Widget key: number_line_initial
test("number-line - Number opposites", () => {
	const input = {
	"max": 1.5,
	"min": -1.5,
	"width": 600,
	"height": 120,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 0.5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xcb3e37851559832d
// Course: 6th grade math
// Exercise: Number opposites
// Widget key: number_line_choice_a
test("number-line - Number opposites", () => {
	const input = {
	"max": 1.5,
	"min": -1.5,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#1E90FF",
			"label": " ",
			"value": -1.5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xcb3e37851559832d
// Course: 6th grade math
// Exercise: Number opposites
// Widget key: number_line_choice_b
test("number-line - Number opposites", () => {
	const input = {
	"max": 1.5,
	"min": -1.5,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#1E90FF",
			"label": " ",
			"value": 1.5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xcb3e37851559832d
// Course: 6th grade math
// Exercise: Number opposites
// Widget key: number_line_choice_c
test("number-line - Number opposites", () => {
	const input = {
	"max": 1.5,
	"min": -1.5,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#1E90FF",
			"label": " ",
			"value": -1,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x7da07235
// Course: 6th grade math
// Exercise: Number opposites
// Widget key: image_1
test("number-line - Number opposites", () => {
	const input = {
	"max": 7,
	"min": -7,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "A",
			"value": -7,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "B",
			"value": -2,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "C",
			"value": 0,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "D",
			"value": 2,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "E",
			"value": 7,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xd1cb5115
// Course: 6th grade math
// Exercise: Number opposites
// Widget key: image_1
test("number-line - Number opposites", () => {
	const input = {
	"max": 10,
	"min": -5,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "A",
			"value": -5,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "B",
			"value": -2,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "C",
			"value": 0,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "D",
			"value": 2,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "E",
			"value": 7,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x10a2191e
// Course: 6th grade math
// Exercise: Number opposites
// Widget key: image_1
test("number-line - Number opposites", () => {
	const input = {
	"max": 7,
	"min": -7,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "A",
			"value": -7,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "B",
			"value": -4,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "C",
			"value": 0,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "D",
			"value": 3,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "E",
			"value": 7,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x8f7d9f5cd0ea9740
// Course: 6th grade math
// Exercise: Number opposites
// Widget key: number_line_initial
test("number-line - Number opposites", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 600,
	"height": 120,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x8f7d9f5cd0ea9740
// Course: 6th grade math
// Exercise: Number opposites
// Widget key: number_line_choice_a
test("number-line - Number opposites", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#1E90FF",
			"label": " ",
			"value": 0.4,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x8f7d9f5cd0ea9740
// Course: 6th grade math
// Exercise: Number opposites
// Widget key: number_line_choice_b
test("number-line - Number opposites", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#1E90FF",
			"label": " ",
			"value": -0.4,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x8f7d9f5cd0ea9740
// Course: 6th grade math
// Exercise: Number opposites
// Widget key: number_line_choice_c
test("number-line - Number opposites", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#1E90FF",
			"label": " ",
			"value": 0.6,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x8d5fa3f4
// Course: 6th grade math
// Exercise: Number opposites
// Widget key: image_1
test("number-line - Number opposites", () => {
	const input = {
	"max": 7,
	"min": -7,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "A",
			"value": -6,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "B",
			"value": -4,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "C",
			"value": -1,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "D",
			"value": 1,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "E",
			"value": 4,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x29aea29b0fa53f17
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: image_1
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -4,
	"min": -6,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#DAA520",
			"label": "A",
			"value": -5.85,
			"labelPosition": "above"
		},
		{
			"color": "#E53935",
			"label": "B",
			"value": -5.55,
			"labelPosition": "above"
		},
		{
			"color": "#8E44AD",
			"label": "C",
			"value": -5,
			"labelPosition": "above"
		},
		{
			"color": "#009688",
			"label": "D",
			"value": -4.35,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 9,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x29aea29b0fa53f17
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: choice_a_visual
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -4,
	"min": -6,
	"width": 500,
	"height": 100,
	"points": [
		{
			"color": "#DAA520",
			"label": "A",
			"value": -5.85,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 9,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x29aea29b0fa53f17
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: choice_b_visual
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -4,
	"min": -6,
	"width": 500,
	"height": 100,
	"points": [
		{
			"color": "#E53935",
			"label": "B",
			"value": -5.55,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 9,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x29aea29b0fa53f17
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: choice_c_visual
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -4,
	"min": -6,
	"width": 500,
	"height": 100,
	"points": [
		{
			"color": "#8E44AD",
			"label": "C",
			"value": -5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 9,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x29aea29b0fa53f17
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: choice_d_visual
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -4,
	"min": -6,
	"width": 500,
	"height": 100,
	"points": [
		{
			"color": "#009688",
			"label": "D",
			"value": -4.35,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 9,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xdbfaa778348e2ec0
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: image_1
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -0.54,
	"min": -0.66,
	"width": 86,
	"height": 240,
	"points": [
		{
			"color": "#29abca",
			"label": " ",
			"value": -0.58,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 0.01,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xd517f5fb6cdfc91a
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: image_1
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -6.9,
	"min": -8.1,
	"width": 86,
	"height": 240,
	"points": [
		{
			"color": "#29abca",
			"label": "?",
			"value": -7.4,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 0.1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xc3ebd036c8e54375
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: image_1
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -0.4,
	"min": -0.6,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#E1A158",
			"label": "A",
			"value": -0.585,
			"labelPosition": "above"
		},
		{
			"color": "#FC6255",
			"label": "B",
			"value": -0.565,
			"labelPosition": "above"
		},
		{
			"color": "#9A72AC",
			"label": "C",
			"value": -0.445,
			"labelPosition": "above"
		},
		{
			"color": "#55C1A7",
			"label": "D",
			"value": -0.415,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.05,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xc3ebd036c8e54375
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: choice_a_visual
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -0.4,
	"min": -0.6,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#E1A158",
			"label": "A",
			"value": -0.585,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.05,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xc3ebd036c8e54375
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: choice_b_visual
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -0.4,
	"min": -0.6,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#FC6255",
			"label": "B",
			"value": -0.565,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.05,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xc3ebd036c8e54375
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: choice_c_visual
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -0.4,
	"min": -0.6,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#9A72AC",
			"label": "C",
			"value": -0.445,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.05,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xc3ebd036c8e54375
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: choice_d_visual
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -0.4,
	"min": -0.6,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#55C1A7",
			"label": "D",
			"value": -0.415,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.05,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x9df6ca1b23a494bb
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: image_1
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -4.4,
	"min": -5.6,
	"width": 86.4,
	"height": 240,
	"points": [
		{
			"color": "#29abca",
			"label": "?",
			"value": -4.9,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 0.1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x1efaaf1d882f1a6a
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: image_1
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -10.4,
	"min": -11.6,
	"width": 86.4,
	"height": 240,
	"points": [
		{
			"color": "#29abca",
			"label": "?",
			"value": -11.3,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 0.3,
	"specialTickLabels": [],
	"minorTicksPerInterval": 2,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x14e9f1fac8768ced
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: image_1
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -1.9,
	"min": -3.1,
	"width": 86,
	"height": 240,
	"points": [
		{
			"color": "#29abca",
			"label": "?",
			"value": -2.1,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 0.1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x60b42de6a30dbff3
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: image_1
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -7,
	"min": -9,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#DAA520",
			"label": "A",
			"value": -8.75,
			"labelPosition": "above"
		},
		{
			"color": "#E74C3C",
			"label": "B",
			"value": -8.65,
			"labelPosition": "above"
		},
		{
			"color": "#8E44AD",
			"label": "C",
			"value": -7.55,
			"labelPosition": "above"
		},
		{
			"color": "#11ACCD",
			"label": "D",
			"value": -7.15,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 19,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x3f9fabfbe3ac2cbc
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: image_1
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": 0,
	"min": -0.2,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#e1a158",
			"label": "A",
			"value": -0.175,
			"labelPosition": "above"
		},
		{
			"color": "#fc6255",
			"label": "B",
			"value": -0.155,
			"labelPosition": "above"
		},
		{
			"color": "#9a72ac",
			"label": "C",
			"value": -0.095,
			"labelPosition": "above"
		},
		{
			"color": "#55c1a7",
			"label": "D",
			"value": -0.055,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 9,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x3f9fabfbe3ac2cbc
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: choice_a_image
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": 0,
	"min": -0.2,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#e1a158",
			"label": "A",
			"value": -0.175,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 9,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x3f9fabfbe3ac2cbc
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: choice_b_image
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": 0,
	"min": -0.2,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#fc6255",
			"label": "B",
			"value": -0.155,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 9,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x3f9fabfbe3ac2cbc
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: choice_c_image
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": 0,
	"min": -0.2,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#9a72ac",
			"label": "C",
			"value": -0.095,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 9,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x3f9fabfbe3ac2cbc
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: choice_d_image
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": 0,
	"min": -0.2,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#55c1a7",
			"label": "D",
			"value": -0.055,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 9,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x5823476d1dc18d37
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: choice_a_image
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -2,
	"min": -4,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "A",
			"value": -3.8,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 3,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x5823476d1dc18d37
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: choice_b_image
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -2,
	"min": -4,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "B",
			"value": -3.35,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 3,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x5823476d1dc18d37
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: choice_c_image
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -2,
	"min": -4,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "C",
			"value": -2.95,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 3,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x5823476d1dc18d37
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: choice_d_image
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -2,
	"min": -4,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "D",
			"value": -2.2,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 3,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x5823476d1dc18d37
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: number_line_image
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -2,
	"min": -4,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "A",
			"value": -3.8,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "B",
			"value": -3.35,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "C",
			"value": -2.95,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "D",
			"value": -2.2,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 3,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xbef9886a4ca6a4b2
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: image_1
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -7,
	"min": -9,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#e1a158",
			"label": "A",
			"value": -8.95,
			"labelPosition": "above"
		},
		{
			"color": "#fc6255",
			"label": "B",
			"value": -8.45,
			"labelPosition": "above"
		},
		{
			"color": "#9a72ac",
			"label": "C",
			"value": -8.15,
			"labelPosition": "above"
		},
		{
			"color": "#55c1a7",
			"label": "D",
			"value": -7.75,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 9,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x3084215479d28e9d
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: image_1
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -1,
	"min": -3,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#FFB800",
			"label": "A",
			"value": -2.15,
			"labelPosition": "above"
		},
		{
			"color": "#E53935",
			"label": "B",
			"value": -1.85,
			"labelPosition": "above"
		},
		{
			"color": "#8E44AD",
			"label": "C",
			"value": -1.45,
			"labelPosition": "above"
		},
		{
			"color": "#11ACCD",
			"label": "D",
			"value": -1.05,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 9,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xaf24a4f1eb7b57f3
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: image_1
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -5.9,
	"min": -7.1,
	"width": 86.4,
	"height": 240,
	"points": [
		{
			"color": "#29abca",
			"label": "?",
			"value": -6.8,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 0.5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xc5d9c30fb36fc4af
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: image_1
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -3.14,
	"min": -3.26,
	"width": 86.4,
	"height": 240,
	"points": [
		{
			"color": "#29abca",
			"label": "?",
			"value": -3.21,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 0.01,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe224445baec45957
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: image_1
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -7,
	"min": -9,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#DAA520",
			"label": "A",
			"value": -8.7,
			"labelPosition": "above"
		},
		{
			"color": "#E53935",
			"label": "B",
			"value": -8.25,
			"labelPosition": "above"
		},
		{
			"color": "#8E44AD",
			"label": "C",
			"value": -7.75,
			"labelPosition": "above"
		},
		{
			"color": "#11ACCD",
			"label": "D",
			"value": -7.55,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 9,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe224445baec45957
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: choice_a_visual
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -7,
	"min": -9,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#DAA520",
			"label": "A",
			"value": -8.7,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 9,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe224445baec45957
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: choice_b_visual
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -7,
	"min": -9,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#E53935",
			"label": "B",
			"value": -8.25,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 9,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe224445baec45957
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: choice_c_visual
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -7,
	"min": -9,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#8E44AD",
			"label": "C",
			"value": -7.75,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 9,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe224445baec45957
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: choice_d_visual
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -7,
	"min": -9,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#11ACCD",
			"label": "D",
			"value": -7.55,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 9,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2c21184e2764adc1
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: image_1
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -1.04,
	"min": -1.14,
	"width": 86,
	"height": 240,
	"points": [
		{
			"color": "#29abca",
			"label": "?",
			"value": -1.13,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 0.01,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xae3d2ea11f397d71
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: image_1
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -0.1,
	"min": -0.3,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#E1A158",
			"label": "A",
			"value": -0.265,
			"labelPosition": "above"
		},
		{
			"color": "#FC6255",
			"label": "B",
			"value": -0.215,
			"labelPosition": "above"
		},
		{
			"color": "#9A72AC",
			"label": "C",
			"value": -0.175,
			"labelPosition": "above"
		},
		{
			"color": "#55C1A7",
			"label": "D",
			"value": -0.135,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.05,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xae3d2ea11f397d71
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: choice_a_visual
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -0.1,
	"min": -0.3,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#E1A158",
			"label": "A",
			"value": -0.265,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.05,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xae3d2ea11f397d71
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: choice_b_visual
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -0.1,
	"min": -0.3,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#FC6255",
			"label": "B",
			"value": -0.215,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.05,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xae3d2ea11f397d71
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: choice_c_visual
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -0.1,
	"min": -0.3,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#9A72AC",
			"label": "C",
			"value": -0.175,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.05,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xae3d2ea11f397d71
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: choice_d_visual
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -0.1,
	"min": -0.3,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#55C1A7",
			"label": "D",
			"value": -0.135,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.05,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xde6ccea133eaaffa
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: image_1
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -0.8,
	"min": -1,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#e1a158",
			"label": "A",
			"value": -0.95,
			"labelPosition": "above"
		},
		{
			"color": "#fc6255",
			"label": "B",
			"value": -0.905,
			"labelPosition": "above"
		},
		{
			"color": "#9a72ac",
			"label": "C",
			"value": -0.88,
			"labelPosition": "above"
		},
		{
			"color": "#55c1a7",
			"label": "D",
			"value": -0.835,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.05,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xde6ccea133eaaffa
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: choice_a_image
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -0.8,
	"min": -1,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#e1a158",
			"label": "A",
			"value": -0.95,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.05,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xde6ccea133eaaffa
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: choice_b_image
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -0.8,
	"min": -1,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#fc6255",
			"label": "B",
			"value": -0.905,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.05,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xde6ccea133eaaffa
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: choice_c_image
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -0.8,
	"min": -1,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#9a72ac",
			"label": "C",
			"value": -0.88,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.05,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xde6ccea133eaaffa
// Course: 6th grade math
// Exercise: Negative decimals on the number line
// Widget key: choice_d_image
test("number-line - Negative decimals on the number line", () => {
	const input = {
	"max": -0.8,
	"min": -1,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#55c1a7",
			"label": "D",
			"value": -0.835,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.05,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xa475bef900ac39d7
// Course: 6th grade math
// Exercise: Negative fractions on the number line
// Widget key: image_1
test("number-line - Negative fractions on the number line", () => {
	const input = {
	"max": 1,
	"min": -1,
	"width": 80,
	"height": 325,
	"points": [
		{
			"color": "#11accd",
			"label": " ",
			"value": -0.6666666666666666,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 2,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe1edaad8beed5751
// Course: 6th grade math
// Exercise: Negative fractions on the number line
// Widget key: image_1
test("number-line - Negative fractions on the number line", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 380,
	"height": 80,
	"points": [
		{
			"color": "#a75a05",
			"label": "A",
			"value": -3.5,
			"labelPosition": "above"
		},
		{
			"color": "#9e034e",
			"label": "B",
			"value": -2.5,
			"labelPosition": "above"
		},
		{
			"color": "#543b78",
			"label": "C",
			"value": 2.5,
			"labelPosition": "above"
		},
		{
			"color": "#208170",
			"label": "D",
			"value": 3.5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xd7741eb9316d7cf8
// Course: 6th grade math
// Exercise: Negative fractions on the number line
// Widget key: image_1
test("number-line - Negative fractions on the number line", () => {
	const input = {
	"max": 3,
	"min": -3,
	"width": 380,
	"height": 80,
	"points": [
		{
			"color": "#a75a05",
			"label": "A",
			"value": -2.75,
			"labelPosition": "above"
		},
		{
			"color": "#9e034e",
			"label": "B",
			"value": -1.75,
			"labelPosition": "above"
		},
		{
			"color": "#543b78",
			"label": "C",
			"value": -0.25,
			"labelPosition": "above"
		},
		{
			"color": "#208170",
			"label": "D",
			"value": 1.75,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 3,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x3913551001193e80
// Course: 6th grade math
// Exercise: Negative fractions on the number line
// Widget key: image_1
test("number-line - Negative fractions on the number line", () => {
	const input = {
	"max": 3,
	"min": -3,
	"width": 380,
	"height": 80,
	"points": [
		{
			"color": "#11accd",
			"label": " ",
			"value": -1.1666666666666667,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 5,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xf3451ee9a41cff2b
// Course: 6th grade math
// Exercise: Negative fractions on the number line
// Widget key: image_1
test("number-line - Negative fractions on the number line", () => {
	const input = {
	"max": 3,
	"min": -3,
	"width": 80,
	"height": 325,
	"points": [
		{
			"color": "#000000",
			"label": "D",
			"value": -1.3333333333,
			"labelPosition": "right"
		},
		{
			"color": "#000000",
			"label": "C",
			"value": -0.6666666667,
			"labelPosition": "right"
		},
		{
			"color": "#000000",
			"label": "B",
			"value": 0.6666666667,
			"labelPosition": "right"
		},
		{
			"color": "#000000",
			"label": "A",
			"value": 1.3333333333,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 2,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x20f54014b426ee0b
// Course: 6th grade math
// Exercise: Negative fractions on the number line
// Widget key: image_1
test("number-line - Negative fractions on the number line", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 380,
	"height": 80,
	"points": [
		{
			"color": "#a75a05",
			"label": "A",
			"value": -2,
			"labelPosition": "above"
		},
		{
			"color": "#9e034e",
			"label": "B",
			"value": -1.625,
			"labelPosition": "above"
		},
		{
			"color": "#543b78",
			"label": "C",
			"value": -0.625,
			"labelPosition": "above"
		},
		{
			"color": "#208170",
			"label": "D",
			"value": 0.625,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 7,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xfad84ed676e52ee6
// Course: 6th grade math
// Exercise: Negative fractions on the number line
// Widget key: image_1
test("number-line - Negative fractions on the number line", () => {
	const input = {
	"max": 1,
	"min": -2,
	"width": 80,
	"height": 325,
	"points": [
		{
			"color": "#208170",
			"label": "D",
			"value": -1.1666666667,
			"labelPosition": "right"
		},
		{
			"color": "#543B78",
			"label": "C",
			"value": -0.1666666667,
			"labelPosition": "right"
		},
		{
			"color": "#9E034E",
			"label": "B",
			"value": 0.1666666667,
			"labelPosition": "right"
		},
		{
			"color": "#A75A05",
			"label": "A",
			"value": 0.8333333333,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 5,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x7eaab7b4893b97ab
// Course: 6th grade math
// Exercise: Negative fractions on the number line
// Widget key: image_1
test("number-line - Negative fractions on the number line", () => {
	const input = {
	"max": 1,
	"min": -4,
	"width": 380,
	"height": 100,
	"points": [
		{
			"color": "#11accd",
			"label": " ",
			"value": -3.25,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 3,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x52c875c67916e191
// Course: 6th grade math
// Exercise: Negative fractions on the number line
// Widget key: image_1
test("number-line - Negative fractions on the number line", () => {
	const input = {
	"max": 3,
	"min": -3,
	"width": 80,
	"height": 325,
	"points": [
		{
			"color": "#11accd",
			"label": " ",
			"value": -2.5,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 1,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x7b7f2a8fdad7a6ff
// Course: 6th grade math
// Exercise: Negative fractions on the number line
// Widget key: image_1
test("number-line - Negative fractions on the number line", () => {
	const input = {
	"max": 1,
	"min": -2,
	"width": 80,
	"height": 325,
	"points": [
		{
			"color": "#208170",
			"label": "D",
			"value": -1.625,
			"labelPosition": "right"
		},
		{
			"color": "#543b78",
			"label": "C",
			"value": -1.375,
			"labelPosition": "right"
		},
		{
			"color": "#9e034e",
			"label": "B",
			"value": -0.625,
			"labelPosition": "right"
		},
		{
			"color": "#a75a05",
			"label": "A",
			"value": 0.375,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 7,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x7143c04d7dd02c60
// Course: 6th grade math
// Exercise: Negative fractions on the number line
// Widget key: image_1
test("number-line - Negative fractions on the number line", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 80,
	"height": 325,
	"points": [
		{
			"color": "#11accd",
			"label": " ",
			"value": -1.25,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 3,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x43717a1e6c056612
// Course: 6th grade math
// Exercise: Compare rational numbers using a number line
// Widget key: image_1
test("number-line - Compare rational numbers using a number line", () => {
	const input = {
	"max": 1,
	"min": -7,
	"width": 320,
	"height": 90,
	"points": [
		{
			"color": "#ca337c",
			"label": " ",
			"value": -5.3,
			"labelPosition": "above"
		},
		{
			"color": "#7854ab",
			"label": " ",
			"value": -0.8,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x17a4b145738ad7fb
// Course: 6th grade math
// Exercise: Compare rational numbers using a number line
// Widget key: image_1
test("number-line - Compare rational numbers using a number line", () => {
	const input = {
	"max": 2,
	"min": -6,
	"width": 100,
	"height": 325,
	"points": [
		{
			"color": "#7854ab",
			"label": " ",
			"value": -4.4,
			"labelPosition": "right"
		},
		{
			"color": "#ca337c",
			"label": " ",
			"value": 1.2,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x3696bd8a34c4bf95
// Course: 6th grade math
// Exercise: Compare rational numbers using a number line
// Widget key: image_1
test("number-line - Compare rational numbers using a number line", () => {
	const input = {
	"max": 2,
	"min": -6,
	"width": 100,
	"height": 325,
	"points": [
		{
			"color": "#7854ab",
			"label": " ",
			"value": 0.1666667,
			"labelPosition": "right"
		},
		{
			"color": "#ca337c",
			"label": " ",
			"value": -3.75,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x2fa6108b95444f8e
// Course: 6th grade math
// Exercise: Compare rational numbers using a number line
// Widget key: image_1
test("number-line - Compare rational numbers using a number line", () => {
	const input = {
	"max": 3,
	"min": -6,
	"width": 320,
	"height": 90,
	"points": [
		{
			"color": "#ca337c",
			"label": " ",
			"value": -4.666666666666667,
			"labelPosition": "above"
		},
		{
			"color": "#7854ab",
			"label": " ",
			"value": 1.7142857142857142,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xfdede3ef5b94f114
// Course: 6th grade math
// Exercise: Compare rational numbers using a number line
// Widget key: image_1
test("number-line - Compare rational numbers using a number line", () => {
	const input = {
	"max": 1,
	"min": -7,
	"width": 100,
	"height": 325,
	"points": [
		{
			"color": "#7854ab",
			"label": " ",
			"value": -6.2,
			"labelPosition": "right"
		},
		{
			"color": "#ca337c",
			"label": " ",
			"value": -3.7,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xc768ecc77ce1221a
// Course: 6th grade math
// Exercise: Compare rational numbers using a number line
// Widget key: image_1
test("number-line - Compare rational numbers using a number line", () => {
	const input = {
	"max": 2,
	"min": -8,
	"width": 100,
	"height": 325,
	"points": [
		{
			"color": "#7854ab",
			"label": null,
			"value": -1.4,
			"labelPosition": "right"
		},
		{
			"color": "#ca337c",
			"label": null,
			"value": -6.4,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x89dccd41cb5265aa
// Course: 6th grade math
// Exercise: Compare rational numbers using a number line
// Widget key: image_1
test("number-line - Compare rational numbers using a number line", () => {
	const input = {
	"max": 2,
	"min": -8,
	"width": 100,
	"height": 325,
	"points": [
		{
			"color": "#7854ab",
			"label": " ",
			"value": -2.2222222222,
			"labelPosition": "right"
		},
		{
			"color": "#ca337c",
			"label": " ",
			"value": -6.6666666667,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x926e7a33353efa1d
// Course: 6th grade math
// Exercise: Compare rational numbers using a number line
// Widget key: image_1
test("number-line - Compare rational numbers using a number line", () => {
	const input = {
	"max": 6,
	"min": -3,
	"width": 320,
	"height": 90,
	"points": [
		{
			"color": "#7854ab",
			"label": " ",
			"value": 5.1,
			"labelPosition": "above"
		},
		{
			"color": "#ca337c",
			"label": " ",
			"value": -1.9,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xd3d944e40c0bc157
// Course: 6th grade math
// Exercise: Compare rational numbers using a number line
// Widget key: image_1
test("number-line - Compare rational numbers using a number line", () => {
	const input = {
	"max": 3,
	"min": -3,
	"width": 320,
	"height": 90,
	"points": [
		{
			"color": "#7854ab",
			"label": " ",
			"value": -1.1428571428571428,
			"labelPosition": "above"
		},
		{
			"color": "#ca337c",
			"label": " ",
			"value": 2.25,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x84388493f94ef2c8
// Course: 6th grade math
// Exercise: Compare rational numbers using a number line
// Widget key: image_1
test("number-line - Compare rational numbers using a number line", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 100,
	"height": 325,
	"points": [
		{
			"color": "#ca337c",
			"label": " ",
			"value": 3.8,
			"labelPosition": "right"
		},
		{
			"color": "#7854ab",
			"label": " ",
			"value": -2.2857142857142856,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x9b4a71e56bdb9bec
// Course: 6th grade math
// Exercise: Compare rational numbers using a number line
// Widget key: image_1
test("number-line - Compare rational numbers using a number line", () => {
	const input = {
	"max": 3,
	"min": -3,
	"width": 100,
	"height": 325,
	"points": [
		{
			"color": "#7854ab",
			"label": " ",
			"value": 2.3,
			"labelPosition": "left"
		},
		{
			"color": "#ca337c",
			"label": " ",
			"value": -2.6,
			"labelPosition": "left"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xaaa92e7ce5eeaf77
// Course: 6th grade math
// Exercise: Compare rational numbers using a number line
// Widget key: image_1
test("number-line - Compare rational numbers using a number line", () => {
	const input = {
	"max": 1,
	"min": -9,
	"width": 320,
	"height": 90,
	"points": [
		{
			"color": "#7854ab",
			"label": " ",
			"value": -7.7,
			"labelPosition": "above"
		},
		{
			"color": "#ca337c",
			"label": " ",
			"value": -3.3,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xfbb37f1cb114c9a2
// Course: 6th grade math
// Exercise: Compare rational numbers using a number line
// Widget key: image_1
test("number-line - Compare rational numbers using a number line", () => {
	const input = {
	"max": 1,
	"min": -7,
	"width": 320,
	"height": 90,
	"points": [
		{
			"color": "#7854ab",
			"label": " ",
			"value": -6.375,
			"labelPosition": "above"
		},
		{
			"color": "#ca337c",
			"label": " ",
			"value": -4.1111111111,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x0b02571c9974b1ac
// Course: 6th grade math
// Exercise: Compare rational numbers using a number line
// Widget key: image_1
test("number-line - Compare rational numbers using a number line", () => {
	const input = {
	"max": 2,
	"min": -4,
	"width": 320,
	"height": 90,
	"points": [
		{
			"color": "#7854ab",
			"label": " ",
			"value": -2.4,
			"labelPosition": "above"
		},
		{
			"color": "#ca337c",
			"label": " ",
			"value": 0.6,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x552ce2d68833781e
// Course: 6th grade math
// Exercise: Compare rational numbers using a number line
// Widget key: image_1
test("number-line - Compare rational numbers using a number line", () => {
	const input = {
	"max": 2,
	"min": -5,
	"width": 320,
	"height": 90,
	"points": [
		{
			"color": "#ca337c",
			"label": " ",
			"value": -2.5,
			"labelPosition": "above"
		},
		{
			"color": "#7854ab",
			"label": " ",
			"value": -0.6666667,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xae369f6f8763ba86
// Course: 6th grade math
// Exercise: Compare rational numbers using a number line
// Widget key: image_1
test("number-line - Compare rational numbers using a number line", () => {
	const input = {
	"max": 3,
	"min": -3,
	"width": 100,
	"height": 325,
	"points": [
		{
			"color": "#7854ab",
			"label": " ",
			"value": -2.3333333333333335,
			"labelPosition": "left"
		},
		{
			"color": "#ca337c",
			"label": " ",
			"value": -1.2,
			"labelPosition": "left"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x5db05ceda936c5d0
// Course: 6th grade math
// Exercise: Compare rational numbers
// Widget key: image_1
test("number-line - Compare rational numbers", () => {
	const input = {
	"max": 2,
	"min": -2,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "-1.3",
			"value": -1.3,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "0.7",
			"value": 0.7,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 0.5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x63be039102b62a98
// Course: 6th grade math
// Exercise: Compare rational numbers
// Widget key: image_1
test("number-line - Compare rational numbers", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x97fa696c66e0b11e
// Course: 6th grade math
// Exercise: Compare rational numbers
// Widget key: image_1
test("number-line - Compare rational numbers", () => {
	const input = {
	"max": 20,
	"min": -20,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "−12",
			"value": -12,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "18",
			"value": 18,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 10,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x7c61e7280f77ac6a
// Course: 6th grade math
// Exercise: Compare rational numbers
// Widget key: image_1
test("number-line - Compare rational numbers", () => {
	const input = {
	"max": 100,
	"min": -100,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": "-76",
			"value": -76,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": "59",
			"value": 59,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 50,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x7b49e10b83d18dbc
// Course: 6th grade math
// Exercise: Compare rational numbers
// Widget key: image_1
test("number-line - Compare rational numbers", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#000000",
			"label": ":[",
			"value": -7.5,
			"labelPosition": "above"
		},
		{
			"color": "#000000",
			"label": ":[",
			"value": 5.7,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [],
	"minorTicksPerInterval": 4,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x78e22152b5909760
// Course: 6th grade math
// Exercise: Meaning of absolute value
// Widget key: image_1
test("number-line - Meaning of absolute value", () => {
	const input = {
	"max": 100,
	"min": -100,
	"width": 380,
	"height": 120,
	"points": [
		{
			"color": "#a75a05",
			"label": "Carrie's bill",
			"value": 75,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 25,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x3439ba4c308f386b
// Course: 6th grade math
// Exercise: Meaning of absolute value
// Widget key: image_1
test("number-line - Meaning of absolute value", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 380,
	"height": 120,
	"points": [
		{
			"color": "#a75a05",
			"label": "Kendra",
			"value": -6,
			"labelPosition": "above"
		},
		{
			"color": "#0c7f99",
			"label": "Jamal",
			"value": 0,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x663e2f4f73d9ee79
// Course: 6th grade math
// Exercise: Meaning of absolute value
// Widget key: image_1
test("number-line - Meaning of absolute value", () => {
	const input = {
	"max": 10,
	"min": -10,
	"width": 144,
	"height": 325,
	"points": [
		{
			"color": "#a75a05",
			"label": "Temuco",
			"value": 7,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x4bfd696981b7127c
// Course: 6th grade math
// Exercise: Meaning of absolute value
// Widget key: image_1
test("number-line - Meaning of absolute value", () => {
	const input = {
	"max": 400,
	"min": -400,
	"width": 380,
	"height": 120,
	"points": [
		{
			"color": "#a75a05",
			"label": "Tuition bill",
			"value": -250,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 100,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x8b9e8ddc0e6e073b
// Course: 6th grade math
// Exercise: Meaning of absolute value
// Widget key: image_1
test("number-line - Meaning of absolute value", () => {
	const input = {
	"max": 4000,
	"min": -4000,
	"width": 144,
	"height": 325,
	"points": [
		{
			"color": "#a75a05",
			"label": "Lhasa",
			"value": 3656,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 1000,
	"specialTickLabels": [
		{
			"label": "sea level",
			"value": 0
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x85500f36b27c686e
// Course: 6th grade math
// Exercise: Meaning of absolute value
// Widget key: image_1
test("number-line - Meaning of absolute value", () => {
	const input = {
	"max": 8,
	"min": -8,
	"width": 380,
	"height": 120,
	"points": [
		{
			"color": "#a75a05",
			"label": "bean bag",
			"value": -4,
			"labelPosition": "above"
		},
		{
			"color": "#0c7f99",
			"label": "hole",
			"value": 0,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xfe3aa964c3345520
// Course: 6th grade math
// Exercise: Meaning of absolute value
// Widget key: image_1
test("number-line - Meaning of absolute value", () => {
	const input = {
	"max": 4,
	"min": -12,
	"width": 144,
	"height": 325,
	"points": [
		{
			"color": "#a75a05",
			"label": "Ulaanbaatar",
			"value": -8,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 2,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x0ddc47203b87395f
// Course: 6th grade math
// Exercise: Meaning of absolute value
// Widget key: image_1
test("number-line - Meaning of absolute value", () => {
	const input = {
	"max": 200,
	"min": -200,
	"width": 144,
	"height": 325,
	"points": [
		{
			"color": "#a75a05",
			"label": "Lima",
			"value": 154,
			"labelPosition": "right"
		}
	],
	"orientation": "vertical",
	"majorTickInterval": 50,
	"specialTickLabels": [
		{
			"label": "sea level",
			"value": 0
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x3fbc546d5ef83967
// Course: 6th grade math
// Exercise: Meaning of absolute value
// Widget key: image_1
test("number-line - Meaning of absolute value", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 380,
	"height": 120,
	"points": [
		{
			"color": "#0c7f99",
			"label": "Hole",
			"value": 0,
			"labelPosition": "above"
		},
		{
			"color": "#a75a05",
			"label": "Marble",
			"value": 3,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x8e87fe951d093335
// Course: 6th grade math
// Exercise: Comparing absolute values challenge
// Widget key: image_1
test("number-line - Comparing absolute values challenge", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ED",
			"label": "a",
			"value": 0,
			"labelPosition": "above"
		},
		{
			"color": "#FF00AF",
			"label": "b",
			"value": 2,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [
		{
			"label": "0",
			"value": 0
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xefd9b9156491a4fa
// Course: 6th grade math
// Exercise: Comparing absolute values challenge
// Widget key: image_1
test("number-line - Comparing absolute values challenge", () => {
	const input = {
	"max": 4,
	"min": -4,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ed",
			"label": "a",
			"value": -1,
			"labelPosition": "above"
		},
		{
			"color": "#ff00af",
			"label": "b",
			"value": 3.5,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x089a4f0a8b2040ce
// Course: 6th grade math
// Exercise: Comparing absolute values challenge
// Widget key: image_1
test("number-line - Comparing absolute values challenge", () => {
	const input = {
	"max": 4,
	"min": -4,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ed",
			"label": "a",
			"value": -3,
			"labelPosition": "above"
		},
		{
			"color": "#ff00af",
			"label": "b",
			"value": 1,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x67bae3afb4e07b89
// Course: 6th grade math
// Exercise: Comparing absolute values challenge
// Widget key: image_1
test("number-line - Comparing absolute values challenge", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ed",
			"label": "a",
			"value": -3,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [
		{
			"label": "0",
			"value": 0
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xad56e1ee6f2d38db
// Course: 6th grade math
// Exercise: Comparing absolute values challenge
// Widget key: image_1
test("number-line - Comparing absolute values challenge", () => {
	const input = {
	"max": 1,
	"min": -1,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ed",
			"label": "a",
			"value": 0.33,
			"labelPosition": "above"
		},
		{
			"color": "#ff00af",
			"label": "b",
			"value": 0.78,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [
		{
			"label": "0",
			"value": 0
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x843497a4f244d2cd
// Course: 6th grade math
// Exercise: Comparing absolute values challenge
// Widget key: image_1
test("number-line - Comparing absolute values challenge", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 80,
	"points": [
		{
			"color": "#6495ED",
			"label": "a",
			"value": 2,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 5,
	"specialTickLabels": [
		{
			"label": "",
			"value": -5
		},
		{
			"label": "0",
			"value": 0
		},
		{
			"label": "",
			"value": 5
		}
	],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: xe6ce0ba1a0c0daa5
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_c_number_line
test("number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 600,
	"height": 120,
	"points": [
		{
			"color": "#333333",
			"label": null,
			"value": -2,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x0ea6ddf2623b5753
// Course: 6th grade math
// Exercise: Graphing basic inequalities
// Widget key: choice_b_number_line
test("number-line - Graphing basic inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 600,
	"height": 100,
	"points": [
		{
			"color": "#1E90FF",
			"label": " ",
			"value": -4,
			"labelPosition": "above"
		}
	],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})


// Extracted from question: x7af81be6b2c580c8
// Course: 6th grade math
// Exercise: Plotting inequalities
// Widget key: number_line_empty
test("number-line - Plotting inequalities", () => {
	const input = {
	"max": 5,
	"min": -5,
	"width": 460,
	"height": 64,
	"points": [],
	"orientation": "horizontal",
	"majorTickInterval": 1,
	"specialTickLabels": [],
	"minorTicksPerInterval": 0,
	"type": "numberLine"
} satisfies NumberLineInput

	// Validate the input
	const parseResult = NumberLinePropsSchema.safeParse(input)
	if (!parseResult.success) {
		console.error("Schema validation failed for extracted numberLine:", parseResult.error)
		return
	}

	// Generate the widget
	const svg = generateNumberLine(parseResult.data)
	expect(svg).toMatchSnapshot()
})

