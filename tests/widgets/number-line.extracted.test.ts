// ============================================================================
// EXTRACTED TEST FILE - AUTO-GENERATED
// ============================================================================
// This file was automatically generated from database structured_json data
// Generated on: 2025-09-17T15:38:03.950Z
// Widget Type: numberLine
// Source: Real questions from database
//
// These tests use actual widget configurations extracted from production data
// to ensure comprehensive test coverage with realistic scenarios.
//
// Note: Database data may not perfectly match current widget schemas, so these
// tests rely on runtime validation within generateWidget() for flexibility.
//
// DO NOT EDIT THIS FILE DIRECTLY - it will be overwritten on regeneration
//
// Run tests: bun test tests/widgets/number-line.extracted.test.ts
// Update snapshots: bun test tests/widgets/number-line.extracted.test.ts --update-snapshots
// ============================================================================

import { describe, test, expect } from "bun:test"
import { generateWidget } from "@superbuilders/qti-assessment-item-generator/widgets/widget-generator"
import type { Widget, WidgetInput } from "@superbuilders/qti-assessment-item-generator/widgets/registry"
import * as errors from "@superbuilders/errors"
import * as logger from "@superbuilders/slog"

describe("numberLine widget tests", () => {
	// Extracted from question: xb3704fceda59f25f
	// Question: Locate a point on a vertical number line
	// Widget key: image_2
	test("number-line - [xb3704fceda59f25f] Locate a point on a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 77,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 3
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mo>?</mo>",
		      "position": -15
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xdae754517ed494ca
	// Question: Locate a point on a number line
	// Widget key: image_2
	test("number-line - [xdae754517ed494ca] Locate a point on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 3
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#29abca",
		      "style": "dot",
		      "mathml": "<mo>?</mo>",
		      "position": -12
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x34d24c3693e1d844
	// Question: Locate the point on the number line
	// Widget key: image_2
	test("number-line - [x34d24c3693e1d844] Locate the point on the number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mo>?</mo>",
		      "position": -6
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xec222d3628f91694
	// Question: Move the dot to 6 on the number line
	// Widget key: number_line_choice_a
	test("number-line - [xec222d3628f91694] Move the dot to 6 on the number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 520,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 3
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#1066CC",
		      "style": "dot",
		      "value": 6,
		      "position": 6
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xec222d3628f91694
	// Question: Move the dot to 6 on the number line
	// Widget key: number_line_choice_b
	test("number-line - [xec222d3628f91694] Move the dot to 6 on the number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 520,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 3
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#1066CC",
		      "style": "dot",
		      "value": 3,
		      "position": 3
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xec222d3628f91694
	// Question: Move the dot to 6 on the number line
	// Widget key: number_line_choice_c
	test("number-line - [xec222d3628f91694] Move the dot to 6 on the number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 520,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 3
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#1066CC",
		      "style": "dot",
		      "value": 9,
		      "position": 9
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xbe02b8fb6422f7af
	// Question: Locate a point on a vertical number line
	// Widget key: image_2
	test("number-line - [xbe02b8fb6422f7af] Locate a point on a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 25,
		  "min": -25,
		  "type": "numberLine",
		  "width": 77,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mo>?</mo>",
		      "position": -20
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xc004713f8a564a33
	// Question: Locate the question mark on the number line
	// Widget key: image_2
	test("number-line - [xc004713f8a564a33] Locate the question mark on the number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 20,
		  "min": -20,
		  "type": "numberLine",
		  "width": 77,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 4
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mo>?</mo>",
		      "position": -16
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x49b3274645e4b5aa
	// Question: Locate 12 on the number line
	// Widget key: number_line_choice_a
	test("number-line - [x49b3274645e4b5aa] Locate 12 on the number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 20,
		  "min": -20,
		  "type": "numberLine",
		  "width": 500,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 4
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mtext></mtext>",
		      "position": 12
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x49b3274645e4b5aa
	// Question: Locate 12 on the number line
	// Widget key: number_line_choice_b
	test("number-line - [x49b3274645e4b5aa] Locate 12 on the number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 20,
		  "min": -20,
		  "type": "numberLine",
		  "width": 500,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 4
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mtext></mtext>",
		      "position": 8
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x49b3274645e4b5aa
	// Question: Locate 12 on the number line
	// Widget key: number_line_choice_c
	test("number-line - [x49b3274645e4b5aa] Locate 12 on the number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 20,
		  "min": -20,
		  "type": "numberLine",
		  "width": 500,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 4
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mtext></mtext>",
		      "position": 16
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x5570936c8b2bac50
	// Question: Locate 12 on a number line
	// Widget key: nl_choice_a
	test("number-line - [x5570936c8b2bac50] Locate 12 on a number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 500,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 3
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#0066CC",
		      "style": "dot",
		      "value": 12,
		      "position": 12
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x5570936c8b2bac50
	// Question: Locate 12 on a number line
	// Widget key: nl_choice_b
	test("number-line - [x5570936c8b2bac50] Locate 12 on a number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 500,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 3
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#0066CC",
		      "style": "dot",
		      "value": 9,
		      "position": 9
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x5570936c8b2bac50
	// Question: Locate 12 on a number line
	// Widget key: nl_choice_c
	test("number-line - [x5570936c8b2bac50] Locate 12 on a number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 500,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 3
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#0066CC",
		      "style": "dot",
		      "value": 12,
		      "position": -12
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2418635b616c6379
	// Question: Identify −8 on a number line
	// Widget key: number_line_choice_a
	test("number-line - [x2418635b616c6379] Identify −8 on a number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 20,
		  "min": -20,
		  "type": "numberLine",
		  "width": 520,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 4
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#0066CC",
		      "style": "dot",
		      "mathml": "<mrow></mrow>",
		      "position": -12
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2418635b616c6379
	// Question: Identify −8 on a number line
	// Widget key: number_line_choice_b
	test("number-line - [x2418635b616c6379] Identify −8 on a number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 20,
		  "min": -20,
		  "type": "numberLine",
		  "width": 520,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 4
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#0066CC",
		      "style": "dot",
		      "mathml": "<mrow></mrow>",
		      "position": -8
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2418635b616c6379
	// Question: Identify −8 on a number line
	// Widget key: number_line_choice_c
	test("number-line - [x2418635b616c6379] Identify −8 on a number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 20,
		  "min": -20,
		  "type": "numberLine",
		  "width": 520,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 4
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#0066CC",
		      "style": "dot",
		      "mathml": "<mrow></mrow>",
		      "position": 8
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x6e0f8f4f89d5d1f9
	// Question: Select the number line with a dot at 8
	// Widget key: number_line_choice_a
	test("number-line - [x6e0f8f4f89d5d1f9] Select the number line with a dot at 8 (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 520,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#0066CC",
		      "style": "dot",
		      "value": 8,
		      "position": 8
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x6e0f8f4f89d5d1f9
	// Question: Select the number line with a dot at 8
	// Widget key: number_line_choice_b
	test("number-line - [x6e0f8f4f89d5d1f9] Select the number line with a dot at 8 (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 520,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#0066CC",
		      "style": "dot",
		      "value": 6,
		      "position": 6
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x6e0f8f4f89d5d1f9
	// Question: Select the number line with a dot at 8
	// Widget key: number_line_choice_c
	test("number-line - [x6e0f8f4f89d5d1f9] Select the number line with a dot at 8 (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 520,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#0066CC",
		      "style": "dot",
		      "value": 8,
		      "position": -8
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x316c0770a8b9704f
	// Question: Locate a point on a vertical number line
	// Widget key: image_2
	test("number-line - [x316c0770a8b9704f] Locate a point on a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 20,
		  "min": -20,
		  "type": "numberLine",
		  "width": 200,
		  "height": 480,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 4
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mo>?</mo>",
		      "position": -12
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xbe60cd481ef15a99
	// Question: Identify a point on a number line
	// Widget key: image_2
	test("number-line - [xbe60cd481ef15a99] Identify a point on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 20,
		  "min": -20,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 4
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#29abca",
		      "style": "dot",
		      "mathml": "<mo>?</mo>",
		      "position": 16
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x07b6fc89523b9a35
	// Question: Locate the question mark on a vertical number line
	// Widget key: image_2
	test("number-line - [x07b6fc89523b9a35] Locate the question mark on a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 77,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 3
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mo>?</mo>",
		      "position": -6
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x5ac3d3b0638e144a
	// Question: Locate a point on a number line
	// Widget key: image_2
	test("number-line - [x5ac3d3b0638e144a] Locate a point on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 77,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#29abca",
		      "style": "dot",
		      "mathml": "<mo>?</mo>",
		      "position": -10
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xf6648e945f6e48f6
	// Question: Locate −7 on a number line
	// Widget key: number_line_choice_a
	test("number-line - [xf6648e945f6e48f6] Locate −7 on a number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 35,
		  "min": -35,
		  "type": "numberLine",
		  "width": 520,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 7
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#0066CC",
		      "style": "dot",
		      "value": 7,
		      "position": -7
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xf6648e945f6e48f6
	// Question: Locate −7 on a number line
	// Widget key: number_line_choice_b
	test("number-line - [xf6648e945f6e48f6] Locate −7 on a number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 35,
		  "min": -35,
		  "type": "numberLine",
		  "width": 520,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 7
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#0066CC",
		      "style": "dot",
		      "value": 7,
		      "position": 7
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xf6648e945f6e48f6
	// Question: Locate −7 on a number line
	// Widget key: number_line_choice_c
	test("number-line - [xf6648e945f6e48f6] Locate −7 on a number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 35,
		  "min": -35,
		  "type": "numberLine",
		  "width": 520,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 7
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#0066CC",
		      "style": "dot",
		      "value": 14,
		      "position": -14
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x98647957d1ad2fc1
	// Question: Identify the location of a point on a number line
	// Widget key: image_2
	test("number-line - [x98647957d1ad2fc1] Identify the location of a point on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 25,
		  "min": -25,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#29abca",
		      "style": "dot",
		      "mathml": "<mtext>?</mtext>",
		      "position": -15
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xd82a2829e11b6beb
	// Question: Locate a point on a number line
	// Widget key: image_2
	test("number-line - [xd82a2829e11b6beb] Locate a point on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 30,
		  "min": -30,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 6
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mo>?</mo>",
		      "position": 18
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x5a611e4cc4eed8d6
	// Question: Identify the location of a point on a vertical number line
	// Widget key: image_2
	test("number-line - [x5a611e4cc4eed8d6] Identify the location of a point on a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 30,
		  "min": -30,
		  "type": "numberLine",
		  "width": 77,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 6
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#29abca",
		      "style": "dot",
		      "mathml": "<mo>?</mo>",
		      "position": -30
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xacb997c47313a606
	// Question: Locate a point on a vertical number line
	// Widget key: image_2
	test("number-line - [xacb997c47313a606] Locate a point on a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 30,
		  "min": -30,
		  "type": "numberLine",
		  "width": 76.8,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 6
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mo>?</mo>",
		      "position": -12
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xcf9144cd5aaae605
	// Question: Find the point on a vertical number line
	// Widget key: image_2
	test("number-line - [xcf9144cd5aaae605] Find the point on a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 77,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#29abca",
		      "style": "dot",
		      "mathml": "<mo>?</mo>",
		      "position": -6
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x0fceb27557e12bb2
	// Question: Identify the point at 10 on a number line
	// Widget key: number_line_base
	test("number-line - [x0fceb27557e12bb2] Identify the point at 10 on a number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 25,
		  "min": -25,
		  "type": "numberLine",
		  "width": 500,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x0fceb27557e12bb2
	// Question: Identify the point at 10 on a number line
	// Widget key: number_line_choice_a
	test("number-line - [x0fceb27557e12bb2] Identify the point at 10 on a number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 25,
		  "min": -25,
		  "type": "numberLine",
		  "width": 500,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#0066CC",
		      "style": "dot",
		      "mathml": "<mtext></mtext>",
		      "position": 10
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x0fceb27557e12bb2
	// Question: Identify the point at 10 on a number line
	// Widget key: number_line_choice_b
	test("number-line - [x0fceb27557e12bb2] Identify the point at 10 on a number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 25,
		  "min": -25,
		  "type": "numberLine",
		  "width": 500,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#0066CC",
		      "style": "dot",
		      "mathml": "<mtext></mtext>",
		      "position": -10
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x0fceb27557e12bb2
	// Question: Identify the point at 10 on a number line
	// Widget key: number_line_choice_c
	test("number-line - [x0fceb27557e12bb2] Identify the point at 10 on a number line (4)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 25,
		  "min": -25,
		  "type": "numberLine",
		  "width": 500,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#0066CC",
		      "style": "dot",
		      "mathml": "<mtext></mtext>",
		      "position": 15
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x0009c4c3efb4c38d
	// Question: Where is the question mark on the number line?
	// Widget key: image_2
	test("number-line - [x0009c4c3efb4c38d] Where is the question mark on the number line?", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 35,
		  "min": -35,
		  "type": "numberLine",
		  "width": 77,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 7
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#29abca",
		      "style": "dot",
		      "mathml": "<mo>?</mo>",
		      "position": -14
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xc45824ff7ae607c9
	// Question: Locate a point on a number line
	// Widget key: image_2
	test("number-line - [xc45824ff7ae607c9] Locate a point on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 40,
		  "min": -40,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 8
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mo>?</mo>",
		      "position": -32
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x904118b7cc65244f
	// Question: Locate -6 on a number line
	// Widget key: number_line_choice_a
	test("number-line - [x904118b7cc65244f] Locate -6 on a number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 520,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#0066CC",
		      "style": "dot",
		      "value": 6,
		      "position": -6
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x904118b7cc65244f
	// Question: Locate -6 on a number line
	// Widget key: number_line_choice_b
	test("number-line - [x904118b7cc65244f] Locate -6 on a number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 520,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#0066CC",
		      "style": "dot",
		      "value": 4,
		      "position": -4
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x904118b7cc65244f
	// Question: Locate -6 on a number line
	// Widget key: number_line_choice_c
	test("number-line - [x904118b7cc65244f] Locate -6 on a number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 520,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#0066CC",
		      "style": "dot",
		      "value": 6,
		      "position": 6
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x24a5d0824f1a3298
	// Question: Locate a point on a vertical number line
	// Widget key: image_2
	test("number-line - [x24a5d0824f1a3298] Locate a point on a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 25,
		  "min": -25,
		  "type": "numberLine",
		  "width": 77,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#29abca",
		      "style": "dot",
		      "mathml": "<mo>?</mo>",
		      "position": -10
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x493cf88fe059a1e2
	// Question: Locate a point on a vertical number line
	// Widget key: image_2
	test("number-line - [x493cf88fe059a1e2] Locate a point on a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 35,
		  "min": -35,
		  "type": "numberLine",
		  "width": 77,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 7
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#29abca",
		      "style": "dot",
		      "mathml": "<mo>?</mo>",
		      "position": -35
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x801e7a13b9af1d75
	// Question: Identify a point on a number line
	// Widget key: image_1
	test("number-line - [x801e7a13b9af1d75] Identify a point on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1,
		  "min": -2,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "fraction",
		      "color": "#11accd",
		      "style": "dot",
		      "position": -0.875,
		      "numerator": 7,
		      "denominator": 8
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 8
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xe1edaad8beed5751
	// Question: Identify a point on a number line
	// Widget key: image_1
	test("number-line - [xe1edaad8beed5751] Identify a point on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 5,
		  "min": -5,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#a75a05",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -3.5
		    },
		    {
		      "type": "mathml",
		      "color": "#9e034e",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -2.5
		    },
		    {
		      "type": "mathml",
		      "color": "#543b78",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": 2.5
		    },
		    {
		      "type": "mathml",
		      "color": "#208170",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": 3.5
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xd7741eb9316d7cf8
	// Question: Identify a point at -1 3/4 on a number line
	// Widget key: image_1
	test("number-line - [xd7741eb9316d7cf8] Identify a point at -1 3/4 on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 3,
		  "min": -3,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#a75a05",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -2.75
		    },
		    {
		      "type": "mathml",
		      "color": "#9e034e",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -1.75
		    },
		    {
		      "type": "mathml",
		      "color": "#543b78",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": -0.25
		    },
		    {
		      "type": "mathml",
		      "color": "#208170",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": 1.75
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 4
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x3913551001193e80
	// Question: Identify the marked point on a number line
	// Widget key: image_1
	test("number-line - [x3913551001193e80] Identify the marked point on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 3,
		  "min": -3,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<!-- no label -->",
		      "position": -1.1666666666666667
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 6
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xf3451ee9a41cff2b
	// Question: Identify a point at −1 1/3 on a number line
	// Widget key: image_1
	test("number-line - [xf3451ee9a41cff2b] Identify a point at −1 1/3 on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 3,
		  "min": -3,
		  "type": "numberLine",
		  "width": 220,
		  "height": 500,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#a75a05",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": 1.3333333333
		    },
		    {
		      "type": "mathml",
		      "color": "#9e034e",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": 0.6666666667
		    },
		    {
		      "type": "mathml",
		      "color": "#543b78",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": -0.6666666667
		    },
		    {
		      "type": "mathml",
		      "color": "#208170",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": -1.3333333333
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 3
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xa475bef900ac39d7
	// Question: Identify the marked point on a number line
	// Widget key: image_1
	test("number-line - [xa475bef900ac39d7] Identify the marked point on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1,
		  "min": -1,
		  "type": "numberLine",
		  "width": 80,
		  "height": 325,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 3
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mrow></mrow>",
		      "position": -0.6666666666666666
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x20f54014b426ee0b
	// Question: Locate a point at −5/8 on a number line
	// Widget key: image_1
	test("number-line - [x20f54014b426ee0b] Locate a point at −5/8 on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#a75a05",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -2
		    },
		    {
		      "type": "mathml",
		      "color": "#9e034e",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -1.625
		    },
		    {
		      "type": "mathml",
		      "color": "#543b78",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": -0.625
		    },
		    {
		      "type": "mathml",
		      "color": "#208170",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": 0.625
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 8
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xfad84ed676e52ee6
	// Question: Identify a point at −1/6 on a vertical number line
	// Widget key: image_1
	test("number-line - [xfad84ed676e52ee6] Identify a point at −1/6 on a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1,
		  "min": -2,
		  "type": "numberLine",
		  "width": 80,
		  "height": 325,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#208170",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": -1.1666666667
		    },
		    {
		      "type": "mathml",
		      "color": "#543b78",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": -0.1666666667
		    },
		    {
		      "type": "mathml",
		      "color": "#9e034e",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": 0.1666666667
		    },
		    {
		      "type": "mathml",
		      "color": "#a75a05",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": 0.8333333333
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 6
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x7eaab7b4893b97ab
	// Question: Identify a point on a number line divided into fourths
	// Widget key: image_1
	test("number-line - [x7eaab7b4893b97ab] Identify a point on a number line divided into fourths", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1,
		  "min": -4,
		  "type": "numberLine",
		  "width": 380,
		  "height": 100,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mrow></mrow>",
		      "position": -3.25
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 4
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x52c875c67916e191
	// Question: Identify a point on a vertical number line
	// Widget key: image_1
	test("number-line - [x52c875c67916e191] Identify a point on a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 3,
		  "min": -3,
		  "type": "numberLine",
		  "width": 80,
		  "height": 325,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mrow></mrow>",
		      "position": -2.5
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x7b7f2a8fdad7a6ff
	// Question: Locate a point at −1 3/8 on a vertical number line
	// Widget key: image_1
	test("number-line - [x7b7f2a8fdad7a6ff] Locate a point at −1 3/8 on a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1,
		  "min": -2,
		  "type": "numberLine",
		  "width": 80,
		  "height": 325,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#a75a05",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": 0.375
		    },
		    {
		      "type": "mathml",
		      "color": "#9e034e",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -0.625
		    },
		    {
		      "type": "mathml",
		      "color": "#543b78",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": -1.375
		    },
		    {
		      "type": "mathml",
		      "color": "#208170",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": -1.625
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 8
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x7143c04d7dd02c60
	// Question: Identify the point on a vertical number line
	// Widget key: image_1
	test("number-line - [x7143c04d7dd02c60] Identify the point on a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 80,
		  "height": 325,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mtext></mtext>",
		      "position": -1.25
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 4
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x8e87fe951d093335
	// Question: Determine truth values from a number line
	// Widget key: image_1
	test("number-line - [x8e87fe951d093335] Determine truth values from a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 5,
		  "min": -5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>a</mi>",
		      "position": 0
		    },
		    {
		      "type": "mathml",
		      "color": "#ff00af",
		      "style": "dot",
		      "mathml": "<mi>b</mi>",
		      "position": 3
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xefd9b9156491a4fa
	// Question: Determine truth of statements from a number line
	// Widget key: image_1
	test("number-line - [xefd9b9156491a4fa] Determine truth of statements from a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 4,
		  "min": -4,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>a</mi>",
		      "position": -1
		    },
		    {
		      "type": "mathml",
		      "color": "#FF00AF",
		      "style": "dot",
		      "mathml": "<mi>b</mi>",
		      "position": 3.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x089a4f0a8b2040ce
	// Question: Classify expressions as true or false using a number line
	// Widget key: image_1
	test("number-line - [x089a4f0a8b2040ce] Classify expressions as true or false using a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 4,
		  "min": -4,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>a</mi>",
		      "position": -3
		    },
		    {
		      "type": "mathml",
		      "color": "#ff00af",
		      "style": "dot",
		      "mathml": "<mi>b</mi>",
		      "position": 1
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x67bae3afb4e07b89
	// Question: Classify expressions as true or false using a number line
	// Widget key: image_1
	test("number-line - [x67bae3afb4e07b89] Classify expressions as true or false using a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>a</mi>",
		      "position": -5
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xad56e1ee6f2d38db
	// Question: Classify expressions as true or false using a number line
	// Widget key: image_1
	test("number-line - [xad56e1ee6f2d38db] Classify expressions as true or false using a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 5,
		  "min": -5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>a</mi>",
		      "position": 2
		    },
		    {
		      "type": "mathml",
		      "color": "#ff00af",
		      "style": "dot",
		      "mathml": "<mi>b</mi>",
		      "position": 4
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x843497a4f244d2cd
	// Question: Determine truth values from a number line
	// Widget key: image_1
	test("number-line - [x843497a4f244d2cd] Determine truth values from a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 5,
		  "min": -5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>a</mi>",
		      "position": 2.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xc66c297db9110ac2
	// Question: Determine truth values from a number line
	// Widget key: image_1
	test("number-line - [xc66c297db9110ac2] Determine truth values from a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 6,
		  "min": -6,
		  "type": "numberLine",
		  "width": 460,
		  "height": 150,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mi>a</mi>",
		      "position": -4
		    },
		    {
		      "type": "mathml",
		      "color": "#ff00af",
		      "style": "dot",
		      "mathml": "<mi>b</mi>",
		      "position": -2
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x552ce2d68833781e
	// Question: Compare fractions on a number line
	// Widget key: image_1
	test("number-line - [x552ce2d68833781e] Compare fractions on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -5,
		  "type": "numberLine",
		  "width": 320,
		  "height": 90,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "fraction",
		      "color": "#7854ab",
		      "style": "dot",
		      "position": -0.6666666666666666,
		      "numerator": 2,
		      "denominator": 3
		    },
		    {
		      "sign": "-",
		      "type": "fraction",
		      "color": "#ca337c",
		      "style": "dot",
		      "position": -2.5,
		      "numerator": 5,
		      "denominator": 2
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2fa6108b95444f8e
	// Question: Compare fractions on a number line
	// Widget key: image_1
	test("number-line - [x2fa6108b95444f8e] Compare fractions on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 3,
		  "min": -6,
		  "type": "numberLine",
		  "width": 320,
		  "height": 90,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "+",
		      "type": "fraction",
		      "color": "#7854ab",
		      "style": "dot",
		      "position": 1.7142857142857142,
		      "numerator": 12,
		      "denominator": 7
		    },
		    {
		      "sign": "-",
		      "type": "fraction",
		      "color": "#ca337c",
		      "style": "dot",
		      "position": -4.666666666666667,
		      "numerator": 14,
		      "denominator": 3
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x17a4b145738ad7fb
	// Question: Compare decimals on a vertical number line
	// Widget key: image_1
	test("number-line - [x17a4b145738ad7fb] Compare decimals on a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -6,
		  "type": "numberLine",
		  "width": 100,
		  "height": 325,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#7854ab",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mn>4.4</mn>",
		      "position": -4.4
		    },
		    {
		      "type": "mathml",
		      "color": "#ca337c",
		      "style": "dot",
		      "mathml": "<mn>1.2</mn>",
		      "position": 1.2
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x3696bd8a34c4bf95
	// Question: Compare numbers on a vertical number line
	// Widget key: image_1
	test("number-line - [x3696bd8a34c4bf95] Compare numbers on a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -6,
		  "type": "numberLine",
		  "width": 100,
		  "height": 325,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "+",
		      "type": "fraction",
		      "color": "#7854ab",
		      "style": "dot",
		      "position": 0.1666667,
		      "numerator": 1,
		      "denominator": 6
		    },
		    {
		      "sign": "-",
		      "type": "mixed",
		      "color": "#ca337c",
		      "style": "dot",
		      "whole": 3,
		      "position": -3.75,
		      "numerator": 3,
		      "denominator": 4
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x43717a1e6c056612
	// Question: Compare numbers on a horizontal number line
	// Widget key: image_1
	test("number-line - [x43717a1e6c056612] Compare numbers on a horizontal number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1,
		  "min": -7,
		  "type": "numberLine",
		  "width": 320,
		  "height": 90,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#ca337c",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mn>5.3</mn>",
		      "position": -5.3
		    },
		    {
		      "type": "mathml",
		      "color": "#7854ab",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mn>0.8</mn>",
		      "position": -0.8
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xfdede3ef5b94f114
	// Question: Compare negative decimals on a vertical number line
	// Widget key: image_1
	test("number-line - [xfdede3ef5b94f114] Compare negative decimals on a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1,
		  "min": -7,
		  "type": "numberLine",
		  "width": 100,
		  "height": 325,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#7854ab",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mn>6.2</mn>",
		      "position": -6.2
		    },
		    {
		      "type": "mathml",
		      "color": "#ca337c",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mn>3.7</mn>",
		      "position": -3.7
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xc768ecc77ce1221a
	// Question: Compare numbers on a vertical number line
	// Widget key: image_1
	test("number-line - [xc768ecc77ce1221a] Compare numbers on a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -8,
		  "type": "numberLine",
		  "width": 100,
		  "height": 325,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#7854ab",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mn>1.4</mn>",
		      "position": -1.4
		    },
		    {
		      "type": "mathml",
		      "color": "#ca337c",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mn>6.4</mn>",
		      "position": -6.4
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x89dccd41cb5265aa
	// Question: Compare rational numbers on a vertical number line
	// Widget key: image_1
	test("number-line - [x89dccd41cb5265aa] Compare rational numbers on a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -8,
		  "type": "numberLine",
		  "width": 100,
		  "height": 325,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "fraction",
		      "color": "#7854ab",
		      "style": "dot",
		      "position": -2.2222222222,
		      "numerator": 20,
		      "denominator": 9
		    },
		    {
		      "sign": "-",
		      "type": "fraction",
		      "color": "#ca337c",
		      "style": "dot",
		      "position": -6.6666666667,
		      "numerator": 20,
		      "denominator": 3
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x926e7a33353efa1d
	// Question: Compare numbers on a horizontal number line
	// Widget key: image_1
	test("number-line - [x926e7a33353efa1d] Compare numbers on a horizontal number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 6,
		  "min": -3,
		  "type": "numberLine",
		  "width": 320,
		  "height": 90,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#ca337c",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mn>1.9</mn>",
		      "position": -1.9
		    },
		    {
		      "type": "mathml",
		      "color": "#7854ab",
		      "style": "dot",
		      "mathml": "<mn>5.1</mn>",
		      "position": 5.1
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xd3d944e40c0bc157
	// Question: Compare numbers on a horizontal number line
	// Widget key: image_1
	test("number-line - [xd3d944e40c0bc157] Compare numbers on a horizontal number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 3,
		  "min": -3,
		  "type": "numberLine",
		  "width": 320,
		  "height": 90,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "mixed",
		      "color": "#7854ab",
		      "style": "dot",
		      "whole": 1,
		      "position": -1.1428571428571428,
		      "numerator": 1,
		      "denominator": 7
		    },
		    {
		      "sign": "+",
		      "type": "mixed",
		      "color": "#ca337c",
		      "style": "dot",
		      "whole": 2,
		      "position": 2.25,
		      "numerator": 1,
		      "denominator": 4
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x84388493f94ef2c8
	// Question: Compare mixed numbers on a vertical number line
	// Widget key: image_1
	test("number-line - [x84388493f94ef2c8] Compare mixed numbers on a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 5,
		  "min": -5,
		  "type": "numberLine",
		  "width": 100,
		  "height": 325,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "mixed",
		      "color": "#7854ab",
		      "style": "dot",
		      "whole": 2,
		      "position": -2.2857142857,
		      "numerator": 2,
		      "denominator": 7
		    },
		    {
		      "sign": "+",
		      "type": "mixed",
		      "color": "#ca337c",
		      "style": "dot",
		      "whole": 3,
		      "position": 3.8,
		      "numerator": 4,
		      "denominator": 5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x9b4a71e56bdb9bec
	// Question: Compare decimals on a vertical number line
	// Widget key: image_1
	test("number-line - [x9b4a71e56bdb9bec] Compare decimals on a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 3,
		  "min": -3,
		  "type": "numberLine",
		  "width": 100,
		  "height": 325,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#7854ab",
		      "style": "dot",
		      "mathml": "<mn>2.3</mn>",
		      "position": 2.3
		    },
		    {
		      "type": "mathml",
		      "color": "#ca337c",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mn>2.6</mn>",
		      "position": -2.6
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xaaa92e7ce5eeaf77
	// Question: Compare numbers on a horizontal number line
	// Widget key: image_1
	test("number-line - [xaaa92e7ce5eeaf77] Compare numbers on a horizontal number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1,
		  "min": -9,
		  "type": "numberLine",
		  "width": 320,
		  "height": 90,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#7854ab",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mn>7.7</mn>",
		      "position": -7.7
		    },
		    {
		      "type": "mathml",
		      "color": "#ca337c",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mn>3.3</mn>",
		      "position": -3.3
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xfbb37f1cb114c9a2
	// Question: Compare mixed numbers on a number line
	// Widget key: image_1
	test("number-line - [xfbb37f1cb114c9a2] Compare mixed numbers on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1,
		  "min": -7,
		  "type": "numberLine",
		  "width": 320,
		  "height": 90,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "mixed",
		      "color": "#7854ab",
		      "style": "dot",
		      "whole": 6,
		      "position": -6.375,
		      "numerator": 3,
		      "denominator": 8
		    },
		    {
		      "sign": "-",
		      "type": "mixed",
		      "color": "#ca337c",
		      "style": "dot",
		      "whole": 4,
		      "position": -4.111111,
		      "numerator": 1,
		      "denominator": 9
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x0b02571c9974b1ac
	// Question: Compare numbers on a horizontal number line
	// Widget key: image_1
	test("number-line - [x0b02571c9974b1ac] Compare numbers on a horizontal number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -4,
		  "type": "numberLine",
		  "width": 320,
		  "height": 90,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#7854ab",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mn>2.4</mn>",
		      "position": -2.4
		    },
		    {
		      "type": "mathml",
		      "color": "#ca337c",
		      "style": "dot",
		      "mathml": "<mn>0.6</mn>",
		      "position": 0.6
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xae369f6f8763ba86
	// Question: Compare fractions on a vertical number line
	// Widget key: image_1
	test("number-line - [xae369f6f8763ba86] Compare fractions on a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 3,
		  "min": -3,
		  "type": "numberLine",
		  "width": 100,
		  "height": 325,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "fraction",
		      "color": "#7854ab",
		      "style": "dot",
		      "position": -2.3333333333,
		      "numerator": 7,
		      "denominator": 3
		    },
		    {
		      "sign": "-",
		      "type": "fraction",
		      "color": "#ca337c",
		      "style": "dot",
		      "position": -1.2,
		      "numerator": 6,
		      "denominator": 5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xdbfaa778348e2ec0
	// Question: Locate the blue dot on the number line
	// Widget key: image_1
	test("number-line - [xdbfaa778348e2ec0] Locate the blue dot on the number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -0.54,
		  "min": -0.66,
		  "type": "numberLine",
		  "width": 86.4,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 0.01
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#29abca",
		      "style": "dot",
		      "mathml": "<mo>?</mo>",
		      "position": -0.58
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x29aea29b0fa53f17
	// Question: Identify a point on a number line
	// Widget key: image_1
	test("number-line - [x29aea29b0fa53f17] Identify a point on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -4,
		  "min": -6,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#E0B400",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -5.85
		    },
		    {
		      "type": "mathml",
		      "color": "#D62728",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -5.55
		    },
		    {
		      "type": "mathml",
		      "color": "#9467BD",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": -5
		    },
		    {
		      "type": "mathml",
		      "color": "#17BECF",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": -4.35
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 10
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xd517f5fb6cdfc91a
	// Question: Locate a point on a vertical number line
	// Widget key: image_1
	test("number-line - [xd517f5fb6cdfc91a] Locate a point on a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -6.9,
		  "min": -8.1,
		  "type": "numberLine",
		  "width": 86.4,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 10
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#29abca",
		      "style": "dot",
		      "mathml": "<mo>?</mo>",
		      "position": -7.4
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x1efaaf1d882f1a6a
	// Question: Locate the blue dot on a number line
	// Widget key: image_1
	test("number-line - [x1efaaf1d882f1a6a] Locate the blue dot on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -10.4,
		  "min": -11.6,
		  "type": "numberLine",
		  "width": 86.4,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 10
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#29abca",
		      "style": "dot",
		      "mathml": "<mo>?</mo>",
		      "position": -11.3
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x14e9f1fac8768ced
	// Question: Locate a point on a number line
	// Widget key: image_1
	test("number-line - [x14e9f1fac8768ced] Locate a point on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -1.9,
		  "min": -3.1,
		  "type": "numberLine",
		  "width": 86,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 10
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#29abca",
		      "style": "dot",
		      "mathml": "<mo>?</mo>",
		      "position": -2.1
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x60b42de6a30dbff3
	// Question: Identify a point on a number line
	// Widget key: choice_a_image
	test("number-line - [x60b42de6a30dbff3] Identify a point on a number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -7,
		  "min": -9,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#DAA520",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -8.75
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 10
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x60b42de6a30dbff3
	// Question: Identify a point on a number line
	// Widget key: choice_b_image
	test("number-line - [x60b42de6a30dbff3] Identify a point on a number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -7,
		  "min": -9,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#CC0000",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -8.65
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 10
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x60b42de6a30dbff3
	// Question: Identify a point on a number line
	// Widget key: choice_c_image
	test("number-line - [x60b42de6a30dbff3] Identify a point on a number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -7,
		  "min": -9,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#8E44AD",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": -7.55
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 10
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x60b42de6a30dbff3
	// Question: Identify a point on a number line
	// Widget key: choice_d_image
	test("number-line - [x60b42de6a30dbff3] Identify a point on a number line (4)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -7,
		  "min": -9,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#11ACCD",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": -7.15
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 10
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x60b42de6a30dbff3
	// Question: Identify a point on a number line
	// Widget key: number_line_image
	test("number-line - [x60b42de6a30dbff3] Identify a point on a number line (5)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -7,
		  "min": -9,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#DAA520",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -8.75
		    },
		    {
		      "type": "mathml",
		      "color": "#CC0000",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -8.65
		    },
		    {
		      "type": "mathml",
		      "color": "#8E44AD",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": -7.55
		    },
		    {
		      "type": "mathml",
		      "color": "#11ACCD",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": -7.15
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 10
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x3f9fabfbe3ac2cbc
	// Question: Identify a point at -0.095 on a number line
	// Widget key: image_1
	test("number-line - [x3f9fabfbe3ac2cbc] Identify a point at -0.095 on a number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 0,
		  "min": -0.2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 0.01
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#e1a158",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -0.175
		    },
		    {
		      "type": "mathml",
		      "color": "#fc6255",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -0.155
		    },
		    {
		      "type": "mathml",
		      "color": "#9a72ac",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": -0.095
		    },
		    {
		      "type": "mathml",
		      "color": "#55c1a7",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": -0.055
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x3f9fabfbe3ac2cbc
	// Question: Identify a point at -0.095 on a number line
	// Widget key: choice_a_image
	test("number-line - [x3f9fabfbe3ac2cbc] Identify a point at -0.095 on a number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 0,
		  "min": -0.2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 0.01
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#e1a158",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -0.175
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x3f9fabfbe3ac2cbc
	// Question: Identify a point at -0.095 on a number line
	// Widget key: choice_b_image
	test("number-line - [x3f9fabfbe3ac2cbc] Identify a point at -0.095 on a number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 0,
		  "min": -0.2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 0.01
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#fc6255",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -0.155
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x3f9fabfbe3ac2cbc
	// Question: Identify a point at -0.095 on a number line
	// Widget key: choice_c_image
	test("number-line - [x3f9fabfbe3ac2cbc] Identify a point at -0.095 on a number line (4)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 0,
		  "min": -0.2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 0.01
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#9a72ac",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": -0.095
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x3f9fabfbe3ac2cbc
	// Question: Identify a point at -0.095 on a number line
	// Widget key: choice_d_image
	test("number-line - [x3f9fabfbe3ac2cbc] Identify a point at -0.095 on a number line (5)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 0,
		  "min": -0.2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 0.01
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#55c1a7",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": -0.055
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x5823476d1dc18d37
	// Question: Identify a point on a number line
	// Widget key: choice_a_visual
	test("number-line - [x5823476d1dc18d37] Identify a point on a number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -2,
		  "min": -4,
		  "type": "numberLine",
		  "width": 500,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#DAA520",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -3.8
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 0.1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x5823476d1dc18d37
	// Question: Identify a point on a number line
	// Widget key: choice_b_visual
	test("number-line - [x5823476d1dc18d37] Identify a point on a number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -2,
		  "min": -4,
		  "type": "numberLine",
		  "width": 500,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#CC0000",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -3.35
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 0.1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x5823476d1dc18d37
	// Question: Identify a point on a number line
	// Widget key: choice_c_visual
	test("number-line - [x5823476d1dc18d37] Identify a point on a number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -2,
		  "min": -4,
		  "type": "numberLine",
		  "width": 500,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#8E44AD",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": -2.95
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 0.1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x5823476d1dc18d37
	// Question: Identify a point on a number line
	// Widget key: choice_d_visual
	test("number-line - [x5823476d1dc18d37] Identify a point on a number line (4)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -2,
		  "min": -4,
		  "type": "numberLine",
		  "width": 500,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#11ACCD",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": -2.2
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 0.1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x5823476d1dc18d37
	// Question: Identify a point on a number line
	// Widget key: number_line_image
	test("number-line - [x5823476d1dc18d37] Identify a point on a number line (5)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -2,
		  "min": -4,
		  "type": "numberLine",
		  "width": 500,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#DAA520",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -3.8
		    },
		    {
		      "type": "mathml",
		      "color": "#CC0000",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -3.35
		    },
		    {
		      "type": "mathml",
		      "color": "#8E44AD",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": -2.95
		    },
		    {
		      "type": "mathml",
		      "color": "#11ACCD",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": -2.2
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 0.1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x9df6ca1b23a494bb
	// Question: Locate a point on a vertical number line
	// Widget key: image_1
	test("number-line - [x9df6ca1b23a494bb] Locate a point on a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -4.4,
		  "min": -5.6,
		  "type": "numberLine",
		  "width": 86,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 10
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#29abca",
		      "style": "dot",
		      "mathml": "<mo>?</mo>",
		      "position": -4.9
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xbef9886a4ca6a4b2
	// Question: Identify a point on a number line
	// Widget key: image_1
	test("number-line - [xbef9886a4ca6a4b2] Identify a point on a number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -7,
		  "min": -9,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#e1a158",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -8.95
		    },
		    {
		      "type": "mathml",
		      "color": "#fc6255",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -8.45
		    },
		    {
		      "type": "mathml",
		      "color": "#9a72ac",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": -8.15
		    },
		    {
		      "type": "mathml",
		      "color": "#55c1a7",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": -7.75
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 10
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xbef9886a4ca6a4b2
	// Question: Identify a point on a number line
	// Widget key: choice_a_image
	test("number-line - [xbef9886a4ca6a4b2] Identify a point on a number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -7,
		  "min": -9,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#e1a158",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -8.95
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 10
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xbef9886a4ca6a4b2
	// Question: Identify a point on a number line
	// Widget key: choice_b_image
	test("number-line - [xbef9886a4ca6a4b2] Identify a point on a number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -7,
		  "min": -9,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#fc6255",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -8.45
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 10
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xbef9886a4ca6a4b2
	// Question: Identify a point on a number line
	// Widget key: choice_c_image
	test("number-line - [xbef9886a4ca6a4b2] Identify a point on a number line (4)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -7,
		  "min": -9,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#9a72ac",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": -8.15
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 10
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xbef9886a4ca6a4b2
	// Question: Identify a point on a number line
	// Widget key: choice_d_image
	test("number-line - [xbef9886a4ca6a4b2] Identify a point on a number line (5)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -7,
		  "min": -9,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#55c1a7",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": -7.75
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 10
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xc3ebd036c8e54375
	// Question: Identify a point on a number line (negative decimal)
	// Widget key: image_1
	test("number-line - [xc3ebd036c8e54375] Identify a point on a number line (negative decimal) (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -0.4,
		  "min": -0.6,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 0.1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#e1a158",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -0.585
		    },
		    {
		      "type": "mathml",
		      "color": "#fc6255",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -0.565
		    },
		    {
		      "type": "mathml",
		      "color": "#9a72ac",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": -0.445
		    },
		    {
		      "type": "mathml",
		      "color": "#55c1a7",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": -0.415
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 0.01
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xc3ebd036c8e54375
	// Question: Identify a point on a number line (negative decimal)
	// Widget key: choice_a_visual
	test("number-line - [xc3ebd036c8e54375] Identify a point on a number line (negative decimal) (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -0.4,
		  "min": -0.6,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 0.1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#e1a158",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -0.585
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 0.01
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xc3ebd036c8e54375
	// Question: Identify a point on a number line (negative decimal)
	// Widget key: choice_b_visual
	test("number-line - [xc3ebd036c8e54375] Identify a point on a number line (negative decimal) (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -0.4,
		  "min": -0.6,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 0.1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#fc6255",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -0.565
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 0.01
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xc3ebd036c8e54375
	// Question: Identify a point on a number line (negative decimal)
	// Widget key: choice_c_visual
	test("number-line - [xc3ebd036c8e54375] Identify a point on a number line (negative decimal) (4)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -0.4,
		  "min": -0.6,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 0.1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#9a72ac",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": -0.445
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 0.01
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xc3ebd036c8e54375
	// Question: Identify a point on a number line (negative decimal)
	// Widget key: choice_d_visual
	test("number-line - [xc3ebd036c8e54375] Identify a point on a number line (negative decimal) (5)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -0.4,
		  "min": -0.6,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 0.1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#55c1a7",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": -0.415
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 0.01
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x3084215479d28e9d
	// Question: Locate −2.15 on a number line
	// Widget key: image_1
	test("number-line - [x3084215479d28e9d] Locate −2.15 on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 0,
		  "min": -3,
		  "type": "numberLine",
		  "width": 520,
		  "height": 160,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#DAA520",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -2.15
		    },
		    {
		      "type": "mathml",
		      "color": "#D32F2F",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -1.85
		    },
		    {
		      "type": "mathml",
		      "color": "#8E44AD",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": -1.45
		    },
		    {
		      "type": "mathml",
		      "color": "#11ACCD",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": -1.05
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 20
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xaf24a4f1eb7b57f3
	// Question: Locate the blue dot on the number line
	// Widget key: image_1
	test("number-line - [xaf24a4f1eb7b57f3] Locate the blue dot on the number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -5.9,
		  "min": -7.1,
		  "type": "numberLine",
		  "width": 86,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 10
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#29abca",
		      "style": "dot",
		      "mathml": "<mo>?</mo>",
		      "position": -6.8
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xc5d9c30fb36fc4af
	// Question: Locate a point on a vertical number line
	// Widget key: image_1
	test("number-line - [xc5d9c30fb36fc4af] Locate a point on a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -3.14,
		  "min": -3.26,
		  "type": "numberLine",
		  "width": 86,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 0.01
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#29abca",
		      "style": "dot",
		      "mathml": "<mo>?</mo>",
		      "position": -3.21
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xe224445baec45957
	// Question: Identify a point on a number line
	// Widget key: choice_a_visual
	test("number-line - [xe224445baec45957] Identify a point on a number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -7,
		  "min": -9,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 10
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#E5B700",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -8.7
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xe224445baec45957
	// Question: Identify a point on a number line
	// Widget key: choice_b_visual
	test("number-line - [xe224445baec45957] Identify a point on a number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -7,
		  "min": -9,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 10
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#D62728",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -8.25
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xe224445baec45957
	// Question: Identify a point on a number line
	// Widget key: choice_c_visual
	test("number-line - [xe224445baec45957] Identify a point on a number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -7,
		  "min": -9,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 10
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6A3D9A",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": -7.75
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xe224445baec45957
	// Question: Identify a point on a number line
	// Widget key: choice_d_visual
	test("number-line - [xe224445baec45957] Identify a point on a number line (4)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -7,
		  "min": -9,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 10
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#17BECF",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": -7.55
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xe224445baec45957
	// Question: Identify a point on a number line
	// Widget key: number_line_image
	test("number-line - [xe224445baec45957] Identify a point on a number line (5)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -7,
		  "min": -9,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 10
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#E5B700",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -8.7
		    },
		    {
		      "type": "mathml",
		      "color": "#D62728",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -8.25
		    },
		    {
		      "type": "mathml",
		      "color": "#6A3D9A",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": -7.75
		    },
		    {
		      "type": "mathml",
		      "color": "#17BECF",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": -7.55
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2c21184e2764adc1
	// Question: Locate a point on a vertical number line
	// Widget key: image_1
	test("number-line - [x2c21184e2764adc1] Locate a point on a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -1.04,
		  "min": -1.14,
		  "type": "numberLine",
		  "width": 86.4,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 100
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#29abca",
		      "style": "dot",
		      "mathml": "<mo>?</mo>",
		      "position": -1.13
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xae3d2ea11f397d71
	// Question: Identify a point on a number line
	// Widget key: image_1
	test("number-line - [xae3d2ea11f397d71] Identify a point on a number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -0.1,
		  "min": -0.3,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 0.01
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#e1a158",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -0.265
		    },
		    {
		      "type": "mathml",
		      "color": "#fc6255",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -0.215
		    },
		    {
		      "type": "mathml",
		      "color": "#9a72ac",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": -0.175
		    },
		    {
		      "type": "mathml",
		      "color": "#55c1a7",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": -0.135
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xae3d2ea11f397d71
	// Question: Identify a point on a number line
	// Widget key: choice_a_visual
	test("number-line - [xae3d2ea11f397d71] Identify a point on a number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -0.1,
		  "min": -0.3,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 0.01
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#e1a158",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -0.265
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xae3d2ea11f397d71
	// Question: Identify a point on a number line
	// Widget key: choice_b_visual
	test("number-line - [xae3d2ea11f397d71] Identify a point on a number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -0.1,
		  "min": -0.3,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 0.01
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#fc6255",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -0.215
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xae3d2ea11f397d71
	// Question: Identify a point on a number line
	// Widget key: choice_c_visual
	test("number-line - [xae3d2ea11f397d71] Identify a point on a number line (4)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -0.1,
		  "min": -0.3,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 0.01
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#9a72ac",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": -0.175
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xae3d2ea11f397d71
	// Question: Identify a point on a number line
	// Widget key: choice_d_visual
	test("number-line - [xae3d2ea11f397d71] Identify a point on a number line (5)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -0.1,
		  "min": -0.3,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 0.01
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#55c1a7",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": -0.135
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xde6ccea133eaaffa
	// Question: Identify a point on a number line
	// Widget key: image_1
	test("number-line - [xde6ccea133eaaffa] Identify a point on a number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -0.8,
		  "min": -1,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 100
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#e1a158",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -0.95
		    },
		    {
		      "type": "mathml",
		      "color": "#fc6255",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -0.905
		    },
		    {
		      "type": "mathml",
		      "color": "#9a72ac",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": -0.88
		    },
		    {
		      "type": "mathml",
		      "color": "#55c1a7",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": -0.835
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xde6ccea133eaaffa
	// Question: Identify a point on a number line
	// Widget key: choice_a_visual
	test("number-line - [xde6ccea133eaaffa] Identify a point on a number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -0.8,
		  "min": -1,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 100
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#e1a158",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -0.95
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xde6ccea133eaaffa
	// Question: Identify a point on a number line
	// Widget key: choice_b_visual
	test("number-line - [xde6ccea133eaaffa] Identify a point on a number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -0.8,
		  "min": -1,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 100
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#fc6255",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -0.905
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xde6ccea133eaaffa
	// Question: Identify a point on a number line
	// Widget key: choice_c_visual
	test("number-line - [xde6ccea133eaaffa] Identify a point on a number line (4)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -0.8,
		  "min": -1,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 100
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#9a72ac",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": -0.88
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xde6ccea133eaaffa
	// Question: Identify a point on a number line
	// Widget key: choice_d_visual
	test("number-line - [xde6ccea133eaaffa] Identify a point on a number line (5)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -0.8,
		  "min": -1,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 100
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#55c1a7",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": -0.835
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xd52bf71ae9367c1d
	// Question: Sign of A + B on a number line
	// Widget key: image_1
	test("number-line - [xd52bf71ae9367c1d] Sign of A + B on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 9,
		  "min": -9,
		  "type": "numberLine",
		  "width": 460,
		  "height": 70,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -9
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -4.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xa17526c1388ca227
	// Question: Determine the sign of a sum on a number line
	// Widget key: image_1
	test("number-line - [xa17526c1388ca227] Determine the sign of a sum on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 9,
		  "min": -9,
		  "type": "numberLine",
		  "width": 460,
		  "height": 90,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -8
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": 3
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xb34bd9efe7f7b167
	// Question: Sign of the sum on a number line
	// Widget key: image_1
	test("number-line - [xb34bd9efe7f7b167] Sign of the sum on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 102,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -5
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": 9
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xb68760aaf6f3ffeb
	// Question: Sign of a sum on a vertical number line
	// Widget key: image_1
	test("number-line - [xb68760aaf6f3ffeb] Sign of a sum on a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 7,
		  "min": -8,
		  "type": "numberLine",
		  "width": 104,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -8
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": 5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2a21cc51e5afb7b3
	// Question: Determine the sign of B + A from a number line
	// Widget key: image_1
	test("number-line - [x2a21cc51e5afb7b3] Determine the sign of B + A from a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 7,
		  "min": -7,
		  "type": "numberLine",
		  "width": 460,
		  "height": 94,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": 5
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -3
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xa2d63caf8b0024eb
	// Question: Sign of a sum on a number line
	// Widget key: image_1
	test("number-line - [xa2d63caf8b0024eb] Sign of a sum on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 6,
		  "min": -5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 70,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": 3
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -4
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x6c3fca6f50a7886e
	// Question: Sign of A + B on a vertical number line
	// Widget key: image_1
	test("number-line - [x6c3fca6f50a7886e] Sign of A + B on a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 0,
		  "min": -15,
		  "type": "numberLine",
		  "width": 104,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -2
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -8.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x550561183fbc7144
	// Question: Interpret −3 + (−5) on a number line
	// Widget key: image_1
	test("number-line - [x550561183fbc7144] Interpret −3 + (−5) on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 9,
		  "min": -9,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 100
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x67e44f95cf30cb93
	// Question: Interpret 2 + 7 on a number line
	// Widget key: image_1
	test("number-line - [x67e44f95cf30cb93] Interpret 2 + 7 on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 0.5,
		  "min": -0.5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x67ac6202a9d4f087
	// Question: Interpret the expression 1 + 6
	// Widget key: image_1
	test("number-line - [x67ac6202a9d4f087] Interpret the expression 1 + 6", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 0.5,
		  "min": -0.5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x3a70b38ca758b5a2
	// Question: Interpret 5 − 8 on a number line
	// Widget key: image_1
	test("number-line - [x3a70b38ca758b5a2] Interpret 5 − 8 on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 10
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xd96eb217b7c9a450
	// Question: Interpret 4 − (−3) on a number line
	// Widget key: image_1
	test("number-line - [xd96eb217b7c9a450] Interpret 4 − (−3) on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 100
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x0e6bd1df203c7886
	// Question: Interpret subtracting a negative on a number line
	// Widget key: image_1
	test("number-line - [x0e6bd1df203c7886] Interpret subtracting a negative on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x7cc0baac5d140007
	// Question: Interpret an addition with negative numbers on a number line
	// Widget key: image_1
	test("number-line - [x7cc0baac5d140007] Interpret an addition with negative numbers on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 0.5,
		  "min": -0.5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xd973b123bab53a84
	// Question: Interpretation of 9 + (−7) on a number line
	// Widget key: image_1
	test("number-line - [xd973b123bab53a84] Interpretation of 9 + (−7) on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2436d2f7d2b2dc6d
	// Question: Interpret the expression −2 − 7 on a number line
	// Widget key: image_1
	test("number-line - [x2436d2f7d2b2dc6d] Interpret the expression −2 − 7 on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x503e4bb8de69fa12
	// Question: Interpretation of subtracting a negative on a number line
	// Widget key: image_1
	test("number-line - [x503e4bb8de69fa12] Interpretation of subtracting a negative on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 12,
		  "min": -12,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 100
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x3f9d8f1bc1edf16f
	// Question: Interpret 3 + (−7) on a number line
	// Widget key: image_1
	test("number-line - [x3f9d8f1bc1edf16f] Interpret 3 + (−7) on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x85e43baa483fc14d
	// Question: Interpret 2 − (−5) on a number line
	// Widget key: image_1
	test("number-line - [x85e43baa483fc14d] Interpret 2 − (−5) on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 8,
		  "min": -8,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xc68972fee29782e4
	// Question: True statements about 4 − (−4)
	// Widget key: image_1
	test("number-line - [xc68972fee29782e4] True statements about 4 − (−4)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 10
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xbd6be7269a700394
	// Question: Interpret 3 − (−5) on a number line
	// Widget key: image_1
	test("number-line - [xbd6be7269a700394] Interpret 3 − (−5) on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 0.5,
		  "min": -0.5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x22654291a8e6cdba
	// Question: Statements about −8 + 8
	// Widget key: image_1
	test("number-line - [x22654291a8e6cdba] Statements about −8 + 8", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 0.5,
		  "min": -0.5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xc5b1a83429ca90fc
	// Question: True statements about 5 + (-5)
	// Widget key: image_1
	test("number-line - [xc5b1a83429ca90fc] True statements about 5 + (-5)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 0.5,
		  "min": -0.5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xaf3c2b6396663afc
	// Question: True statements about -7 + 7
	// Widget key: image_1
	test("number-line - [xaf3c2b6396663afc] True statements about -7 + 7", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xbebd2f72cd59eb7f
	// Question: Interpret the expression −3 − 5 on a number line
	// Widget key: image_1
	test("number-line - [xbebd2f72cd59eb7f] Interpret the expression −3 − 5 on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xb406d46142c425d9
	// Question: Interpret -6 + 9 on a number line
	// Widget key: image_1
	test("number-line - [xb406d46142c425d9] Interpret -6 + 9 on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x45364903b6bb25c3
	// Question: Interpret −4 + 13 on a number line
	// Widget key: image_1
	test("number-line - [x45364903b6bb25c3] Interpret −4 + 13 on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 10
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x804ff13c5e0a3b0d
	// Question: Interpret the expression 3 − (−4)
	// Widget key: image_1
	test("number-line - [x804ff13c5e0a3b0d] Interpret the expression 3 − (−4)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xe88afd2e5e99c2eb
	// Question: Interpret 2 − (−7) on a number line
	// Widget key: image_1
	test("number-line - [xe88afd2e5e99c2eb] Interpret 2 − (−7) on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 0.5,
		  "min": -0.5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x8817b46c17165809
	// Question: True statements about -5 + (-5)
	// Widget key: image_1
	test("number-line - [x8817b46c17165809] True statements about -5 + (-5)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x68f5deef1c423e40
	// Question: Identify true statements about −9 + 9
	// Widget key: image_1
	test("number-line - [x68f5deef1c423e40] Identify true statements about −9 + 9", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 10
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x428f83cf6fa27a30
	// Question: True statements about the expression 6 + (−6)
	// Widget key: image_1
	test("number-line - [x428f83cf6fa27a30] True statements about the expression 6 + (−6)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 0.5,
		  "min": -0.5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x04f5bb2056f5a942
	// Question: True statements about 7 + (−7)
	// Widget key: image_1
	test("number-line - [x04f5bb2056f5a942] True statements about 7 + (−7)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 0.5,
		  "min": -0.5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xd1c7929d24a2f00f
	// Question: Interpret an expression with negatives on a number line
	// Widget key: image_1
	test("number-line - [xd1c7929d24a2f00f] Interpret an expression with negatives on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 100
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x458e3358458ed022
	// Question: True statements about −8 + (−8)
	// Widget key: image_1
	test("number-line - [x458e3358458ed022] True statements about −8 + (−8)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 0.5,
		  "min": -0.5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xf97688185047bb0e
	// Question: Interpret an expression on a number line
	// Widget key: image_1
	test("number-line - [xf97688185047bb0e] Interpret an expression on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x6c115eefe7cc2360
	// Question: True statements about 6 − (−6)
	// Widget key: image_1
	test("number-line - [x6c115eefe7cc2360] True statements about 6 − (−6)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 12,
		  "min": -12,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 6
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x873f699dbd384dfc
	// Question: Statements about the expression 7 − (−7)
	// Widget key: image_1
	test("number-line - [x873f699dbd384dfc] Statements about the expression 7 − (−7)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 0.5,
		  "min": -0.5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x0f3ecfe564a3bf27
	// Question: Match numbers to labels on a number line
	// Widget key: image_1
	test("number-line - [x0f3ecfe564a3bf27] Match numbers to labels on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 0.6,
		  "min": -0.2,
		  "type": "numberLine",
		  "width": 480,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#1F77B4",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": 0.17
		    },
		    {
		      "type": "mathml",
		      "color": "#D62728",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": 0.2222222222
		    },
		    {
		      "type": "mathml",
		      "color": "#2CA02C",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": 0.25
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x8d464b86a33a632c
	// Question: Label points on a number line
	// Widget key: number_line_image
	test("number-line - [x8d464b86a33a632c] Label points on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -1.5,
		  "min": -3,
		  "type": "numberLine",
		  "width": 360,
		  "height": 160,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mi>a</mi>",
		      "position": -2.9
		    },
		    {
		      "type": "mathml",
		      "color": "#ca337c",
		      "style": "dot",
		      "mathml": "<mi>b</mi>",
		      "position": -2.625
		    },
		    {
		      "type": "mathml",
		      "color": "#1fab54",
		      "style": "dot",
		      "mathml": "<mi>c</mi>",
		      "position": -2.3333333333
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xcca9debe191d624b
	// Question: Match the numbers to labels on a number line
	// Widget key: image_1
	test("number-line - [xcca9debe191d624b] Match the numbers to labels on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 0.16666666666666666,
		  "min": -0.5,
		  "type": "numberLine",
		  "width": 480,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 6
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#444444",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -0.46
		    },
		    {
		      "type": "mathml",
		      "color": "#444444",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -0.38
		    },
		    {
		      "type": "mathml",
		      "color": "#444444",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": -0.29
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xc401189de689f1ac
	// Question: Match numbers to labels on a number line
	// Widget key: image_1
	test("number-line - [xc401189de689f1ac] Match numbers to labels on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 0.75,
		  "min": -0.25,
		  "type": "numberLine",
		  "width": 360,
		  "height": 160,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 4
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>a</mi>",
		      "position": 0.1428571429
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>b</mi>",
		      "position": 0.2
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>c</mi>",
		      "position": 0.3333333333
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x0af75497ef49eb7f
	// Question: Change in temperature from Wednesday to Thursday
	// Widget key: image_1
	test("number-line - [x0af75497ef49eb7f] Change in temperature from Wednesday to Thursday", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 0,
		  "min": -20,
		  "type": "numberLine",
		  "width": 240,
		  "height": 325,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mrow><mtext>Friday</mtext><mo>:</mo><mo>-</mo><mn>18</mn><mo>°</mo><mi>C</mi></mrow>",
		      "position": -18
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mrow><mtext>Wednesday</mtext><mo>:</mo><mo>-</mo><mn>13</mn><mo>°</mo><mi>C</mi></mrow>",
		      "position": -13
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mrow><mtext>Tuesday</mtext><mo>:</mo><mo>-</mo><mn>11</mn><mo>°</mo><mi>C</mi></mrow>",
		      "position": -11
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mrow><mtext>Thursday</mtext><mo>:</mo><mo>-</mo><mn>8</mn><mo>°</mo><mi>C</mi></mrow>",
		      "position": -8
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mrow><mtext>Monday</mtext><mo>:</mo><mo>-</mo><mn>4</mn><mo>°</mo><mi>C</mi></mrow>",
		      "position": -4
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xf74910c2ce997c62
	// Question: Identify the city 3 °C colder than Williston
	// Widget key: image_1
	test("number-line - [xf74910c2ce997c62] Identify the city 3 °C colder than Williston", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -40,
		  "min": -50,
		  "type": "numberLine",
		  "width": 240,
		  "height": 325,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mtext>Bowman: </mtext><mo>-</mo><mn>47</mn><mo>°</mo><mi>C</mi>",
		      "position": -47
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mtext>Williston: </mtext><mo>-</mo><mn>44</mn><mo>°</mo><mi>C</mi>",
		      "position": -44
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mtext>Minot: </mtext><mo>-</mo><mn>42</mn><mo>°</mo><mi>C</mi>",
		      "position": -42
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mtext>Bismarck: </mtext><mo>-</mo><mn>41</mn><mo>°</mo><mi>C</mi>",
		      "position": -41
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mtext>Jamestown: </mtext><mo>-</mo><mn>40</mn><mo>°</mo><mi>C</mi>",
		      "position": -40
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xb242080383738fde
	// Question: Identify the city 1 °C colder than Bismarck
	// Widget key: image_1
	test("number-line - [xb242080383738fde] Identify the city 1 °C colder than Bismarck", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -40,
		  "min": -50,
		  "type": "numberLine",
		  "width": 240,
		  "height": 325,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mrow><mtext>Bowman: </mtext><mo>-</mo><mn>47</mn><mo>°</mo><mi>C</mi></mrow>",
		      "position": -47
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mrow><mtext>Williston: </mtext><mo>-</mo><mn>44</mn><mo>°</mo><mi>C</mi></mrow>",
		      "position": -44
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mrow><mtext>Minot: </mtext><mo>-</mo><mn>42</mn><mo>°</mo><mi>C</mi></mrow>",
		      "position": -42
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mrow><mtext>Bismarck: </mtext><mo>-</mo><mn>41</mn><mo>°</mo><mi>C</mi></mrow>",
		      "position": -41
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mrow><mtext>Jamestown: </mtext><mo>-</mo><mn>40</mn><mo>°</mo><mi>C</mi></mrow>",
		      "position": -40
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xc52a5aa891289c50
	// Question: Change in temperature from Monday to Wednesday
	// Widget key: image_1
	test("number-line - [xc52a5aa891289c50] Change in temperature from Monday to Wednesday", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 0,
		  "min": -20,
		  "type": "numberLine",
		  "width": 240,
		  "height": 325,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mtext>Friday: negative 18 degrees Celsius</mtext>",
		      "position": -18
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mtext>Wednesday: negative 13 degrees Celsius</mtext>",
		      "position": -13
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mtext>Tuesday: negative 11 degrees Celsius</mtext>",
		      "position": -11
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mtext>Thursday: negative 8 degrees Celsius</mtext>",
		      "position": -8
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mtext>Monday: negative 4 degrees Celsius</mtext>",
		      "position": -4
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x45dbfd7b60c55ec8
	// Question: What will the penguin find after diving deeper?
	// Widget key: image_1
	test("number-line - [x45dbfd7b60c55ec8] What will the penguin find after diving deeper?", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -90,
		  "type": "numberLine",
		  "width": 240,
		  "height": 325,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 10
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mrow><mtext>krill: negative </mtext><mn>86</mn><mtext> meters</mtext></mrow>",
		      "position": -86
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mrow><mtext>adult penguin: negative </mtext><mn>59</mn><mtext> meters</mtext></mrow>",
		      "position": -59
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mrow><mtext>sardine: negative </mtext><mn>32</mn><mtext> meters</mtext></mrow>",
		      "position": -32
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mrow><mtext>anchovy: negative </mtext><mn>22</mn><mtext> meters</mtext></mrow>",
		      "position": -22
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mrow><mtext>surface of water: </mtext><mn>0</mn><mtext> meters</mtext></mrow>",
		      "position": 0
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mrow><mtext>penguin chick: </mtext><mn>5</mn><mtext> meters</mtext></mrow>",
		      "position": 5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2823d70029fc1298
	// Question: Find an item on a vertical number line after moving up
	// Widget key: image_1
	test("number-line - [x2823d70029fc1298] Find an item on a vertical number line after moving up", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -90,
		  "type": "numberLine",
		  "width": 240,
		  "height": 325,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 10
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mtext>krill: negative 86 meters</mtext>",
		      "position": -86
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mtext>adult penguin: negative 59 meters</mtext>",
		      "position": -59
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mtext>sardine: negative 32 meters</mtext>",
		      "position": -32
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mtext>anchovy: negative 22 meters</mtext>",
		      "position": -22
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mtext>surface of water: 0 meters</mtext>",
		      "position": 0
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mtext>penguin chick: 5 meters</mtext>",
		      "position": 5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x0a03f1a2a926bffa
	// Question: Match values to their meanings on a number line
	// Widget key: image_1
	test("number-line - [x0a03f1a2a926bffa] Match values to their meanings on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 20,
		  "min": -100,
		  "type": "numberLine",
		  "width": 144,
		  "height": 325,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 20
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#a75a05",
		      "style": "dot",
		      "mathml": "<mtext>Carbon dioxide</mtext>",
		      "position": -78
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mtext>Water</mtext>",
		      "position": 0
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x3439ba4c308f386b
	// Question: True statements about positions on a number line
	// Widget key: image_1
	test("number-line - [x3439ba4c308f386b] True statements about positions on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 380,
		  "height": 120,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#a75a05",
		      "style": "dot",
		      "mathml": "<mtext>Kendra</mtext>",
		      "position": -6
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mtext>Jamal</mtext>",
		      "position": 0
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x663e2f4f73d9ee79
	// Question: Interpret a positive temperature on a number line
	// Widget key: image_1
	test("number-line - [x663e2f4f73d9ee79] Interpret a positive temperature on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 144,
		  "height": 325,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#a75a05",
		      "style": "dot",
		      "mathml": "<mtext>Temuco</mtext>",
		      "position": 7
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x4bfd696981b7127c
	// Question: Interpret a negative bill amount and absolute value
	// Widget key: image_1
	test("number-line - [x4bfd696981b7127c] Interpret a negative bill amount and absolute value", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 400,
		  "min": -400,
		  "type": "numberLine",
		  "width": 380,
		  "height": 120,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 100
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#a75a05",
		      "style": "dot",
		      "value": 250,
		      "position": -250
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x78e22152b5909760
	// Question: Interpret a number line for a gas bill
	// Widget key: image_1
	test("number-line - [x78e22152b5909760] Interpret a number line for a gas bill", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 100,
		  "min": -100,
		  "type": "numberLine",
		  "width": 380,
		  "height": 120,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 25
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#a75a05",
		      "style": "dot",
		      "mathml": "<mtext>Carrie's bill</mtext>",
		      "position": 75
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xfe3aa964c3345520
	// Question: Average temperature in Ulaanbaatar (March) on a number line
	// Widget key: image_1
	test("number-line - [xfe3aa964c3345520] Average temperature in Ulaanbaatar (March) on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 4,
		  "min": -12,
		  "type": "numberLine",
		  "width": 200,
		  "height": 500,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#a75a05",
		      "style": "dot",
		      "value": 8,
		      "position": -8
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x8b9e8ddc0e6e073b
	// Question: True statements about Lhasa's elevation
	// Widget key: image_1
	test("number-line - [x8b9e8ddc0e6e073b] True statements about Lhasa's elevation", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 4000,
		  "min": -4000,
		  "type": "numberLine",
		  "width": 144,
		  "height": 325,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1000
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mtext>sea level</mtext>",
		      "position": 0
		    },
		    {
		      "type": "mathml",
		      "color": "#a75a05",
		      "style": "dot",
		      "mathml": "<mtext>Lhasa</mtext>",
		      "position": 3656
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xfdbe9b42922b9edf
	// Question: Match values to their meanings on a vertical number line
	// Widget key: image_1
	test("number-line - [xfdbe9b42922b9edf] Match values to their meanings on a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 3,
		  "min": -3,
		  "type": "numberLine",
		  "width": 144,
		  "height": 325,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mtext>sea level</mtext>",
		      "position": 0
		    },
		    {
		      "type": "mathml",
		      "color": "#a75a05",
		      "style": "dot",
		      "mathml": "<mtext>New Orleans</mtext>",
		      "position": -2
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x85500f36b27c686e
	// Question: Bean bag location and distance on a number line
	// Widget key: image_1
	test("number-line - [x85500f36b27c686e] Bean bag location and distance on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 8,
		  "min": -8,
		  "type": "numberLine",
		  "width": 380,
		  "height": 120,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mtext>hole</mtext>",
		      "position": 0
		    },
		    {
		      "type": "mathml",
		      "color": "#a75a05",
		      "style": "dot",
		      "mathml": "<mtext>bean bag</mtext>",
		      "position": -4
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xffe2d25563b2334c
	// Question: Match values to meanings: credit card balance on a number line
	// Widget key: image_1
	test("number-line - [xffe2d25563b2334c] Match values to meanings: credit card balance on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1000,
		  "min": -200,
		  "type": "numberLine",
		  "width": 380,
		  "height": 120,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 200
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#a75a05",
		      "style": "dot",
		      "value": 600,
		      "position": 600
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x0ddc47203b87395f
	// Question: Elevation of Lima: absolute value and position relative to sea level
	// Widget key: image_1
	test("number-line - [x0ddc47203b87395f] Elevation of Lima: absolute value and position relative to sea level", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 200,
		  "min": -200,
		  "type": "numberLine",
		  "width": 144,
		  "height": 325,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 50
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#a75a05",
		      "style": "dot",
		      "mathml": "<mtext>Lima</mtext>",
		      "position": 154
		    },
		    {
		      "type": "mathml",
		      "color": "#333333",
		      "style": "dot",
		      "mathml": "<mtext>sea level</mtext>",
		      "position": 0
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x3fbc546d5ef83967
	// Question: Whose statement about the marble is true?
	// Widget key: image_1
	test("number-line - [x3fbc546d5ef83967] Whose statement about the marble is true?", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 5,
		  "min": -5,
		  "type": "numberLine",
		  "width": 380,
		  "height": 120,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mtext>Hole</mtext>",
		      "position": 0
		    },
		    {
		      "type": "mathml",
		      "color": "#a75a05",
		      "style": "dot",
		      "mathml": "<mtext>Marble</mtext>",
		      "position": 3
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xe6ce0ba1a0c0daa5
	// Question: Choose the graph of x > -2
	// Widget key: choice_c_graph
	test("number-line - [xe6ce0ba1a0c0daa5] Choose the graph of x > -2", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 5,
		  "min": -5,
		  "type": "numberLine",
		  "width": 360,
		  "height": 48,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#7854AB",
		      "style": "dot",
		      "value": 2,
		      "position": -2
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x0ea6ddf2623b5753
	// Question: Choose the graph of x > -4
	// Widget key: choice_b_nl
	test("number-line - [x0ea6ddf2623b5753] Choose the graph of x > -4", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 5,
		  "min": -5,
		  "type": "numberLine",
		  "width": 600,
		  "height": 150,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#000000",
		      "style": "dot",
		      "value": 4,
		      "position": -4
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xac4daf3ec3b769cb
	// Question: Choose the inequality that matches the number line
	// Widget key: choice_b_visual
	test("number-line - [xac4daf3ec3b769cb] Choose the inequality that matches the number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 5,
		  "min": -5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 150,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#29abca",
		      "style": "dot",
		      "value": 2,
		      "position": 2
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x6d35fcf1b7fa9959
	// Question: Locate −C on a number line
	// Widget key: number_line_choice_a
	test("number-line - [x6d35fcf1b7fa9959] Locate −C on a number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#ffa500",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mi>C</mi>",
		      "position": 0.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x6d35fcf1b7fa9959
	// Question: Locate −C on a number line
	// Widget key: number_line_choice_b
	test("number-line - [x6d35fcf1b7fa9959] Locate −C on a number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#ffa500",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mi>C</mi>",
		      "position": -0.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x6d35fcf1b7fa9959
	// Question: Locate −C on a number line
	// Widget key: number_line_choice_c
	test("number-line - [x6d35fcf1b7fa9959] Locate −C on a number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#ffa500",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mi>C</mi>",
		      "position": -1.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x9bdd37009fb0e6e0
	// Question: Value of -C on a number line
	// Widget key: image_1
	test("number-line - [x9bdd37009fb0e6e0] Value of -C on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 4,
		  "min": -4,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#1f77b4",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": 0
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x6260dd813b89d59e
	// Question: Place −A on a number line
	// Widget key: nl_choice_a
	test("number-line - [x6260dd813b89d59e] Place −A on a number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 8,
		  "min": -8,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": 3
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x6260dd813b89d59e
	// Question: Place −A on a number line
	// Widget key: nl_choice_b
	test("number-line - [x6260dd813b89d59e] Place −A on a number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 8,
		  "min": -8,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#ffa500",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mi>A</mi>",
		      "position": -3
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": 3
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x6260dd813b89d59e
	// Question: Place −A on a number line
	// Widget key: nl_choice_c
	test("number-line - [x6260dd813b89d59e] Place −A on a number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 8,
		  "min": -8,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#ffa500",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mi>A</mi>",
		      "position": 3
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xf24d398f5541a761
	// Question: Locate the opposite of a number on a number line
	// Widget key: nl_choice_a
	test("number-line - [xf24d398f5541a761] Locate the opposite of a number on a number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mi>F</mi>",
		      "position": 0.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xf24d398f5541a761
	// Question: Locate the opposite of a number on a number line
	// Widget key: nl_choice_b
	test("number-line - [xf24d398f5541a761] Locate the opposite of a number on a number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mi>F</mi>",
		      "position": -0.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xf24d398f5541a761
	// Question: Locate the opposite of a number on a number line
	// Widget key: nl_choice_c
	test("number-line - [xf24d398f5541a761] Locate the opposite of a number on a number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mi>F</mi>",
		      "position": 1.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xbaadddeb
	// Question: Point representing -(-10) on a number line
	// Widget key: image_1
	test("number-line - [xbaadddeb] Point representing -(-10) on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -4
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -2
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": 6
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": 7
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>E</mi>",
		      "position": 10
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x57b35c359b98a3f4
	// Question: Find −D on the number line
	// Widget key: image_1
	test("number-line - [x57b35c359b98a3f4] Find −D on the number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": -1.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xc527646af4c9351b
	// Question: Find the opposite on a number line
	// Widget key: image_1
	test("number-line - [xc527646af4c9351b] Find the opposite on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 0.6,
		  "min": -0.6,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 0.2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>F</mi>",
		      "position": -0.4
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x4258e7f78f578111
	// Question: Place the point for -(-B) on a number line
	// Widget key: number_line_choice_a
	test("number-line - [x4258e7f78f578111] Place the point for -(-B) on a number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 8,
		  "min": -8,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#F39C12",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mrow><mo>(</mo><mo>-</mo><mi>B</mi><mo>)</mo></mrow>",
		      "position": 3
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x4258e7f78f578111
	// Question: Place the point for -(-B) on a number line
	// Widget key: number_line_choice_b
	test("number-line - [x4258e7f78f578111] Place the point for -(-B) on a number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 8,
		  "min": -8,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#F39C12",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mrow><mo>(</mo><mo>-</mo><mi>B</mi><mo>)</mo></mrow>",
		      "position": -3
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x4258e7f78f578111
	// Question: Place the point for -(-B) on a number line
	// Widget key: number_line_choice_c
	test("number-line - [x4258e7f78f578111] Place the point for -(-B) on a number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 8,
		  "min": -8,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#F39C12",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mrow><mo>(</mo><mo>-</mo><mi>B</mi><mo>)</mo></mrow>",
		      "position": 0
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xf3d9cc00e32d464f
	// Question: Find the opposite of a labeled point on a number line
	// Widget key: image_1
	test("number-line - [xf3d9cc00e32d464f] Find the opposite of a labeled point on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 8,
		  "min": -8,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": 5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xb258023ca47a4510
	// Question: Find the value of -(-D) from the number line
	// Widget key: image_1
	test("number-line - [xb258023ca47a4510] Find the value of -(-D) from the number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": -1.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xc1ac295ed6fa5fa1
	// Question: Locate the opposite number on a number line
	// Widget key: nl_choice_a
	test("number-line - [xc1ac295ed6fa5fa1] Locate the opposite number on a number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 8,
		  "min": -8,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#E67E22",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mi>A</mi>",
		      "position": 0
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xc1ac295ed6fa5fa1
	// Question: Locate the opposite number on a number line
	// Widget key: nl_choice_b
	test("number-line - [xc1ac295ed6fa5fa1] Locate the opposite number on a number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 8,
		  "min": -8,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#0066CC",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": 0
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xc1ac295ed6fa5fa1
	// Question: Locate the opposite number on a number line
	// Widget key: nl_choice_c
	test("number-line - [xc1ac295ed6fa5fa1] Locate the opposite number on a number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 8,
		  "min": -8,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#8E44AD",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mi>A</mi>",
		      "position": 3
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x0a5a1f1a80352070
	// Question: Locate −E on a number line
	// Widget key: nl_choice_a
	test("number-line - [x0a5a1f1a80352070] Locate −E on a number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 4,
		  "min": -4,
		  "type": "numberLine",
		  "width": 460,
		  "height": 150,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#E67E22",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mi>E</mi>",
		      "position": 2.5
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x0a5a1f1a80352070
	// Question: Locate −E on a number line
	// Widget key: nl_choice_b
	test("number-line - [x0a5a1f1a80352070] Locate −E on a number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 4,
		  "min": -4,
		  "type": "numberLine",
		  "width": 460,
		  "height": 150,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#E67E22",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mi>E</mi>",
		      "position": -2.5
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x0a5a1f1a80352070
	// Question: Locate −E on a number line
	// Widget key: nl_choice_c
	test("number-line - [x0a5a1f1a80352070] Locate −E on a number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 4,
		  "min": -4,
		  "type": "numberLine",
		  "width": 460,
		  "height": 150,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#E67E22",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mi>E</mi>",
		      "position": -3
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xbccdde0de8c444be
	// Question: Select the position of -(-B) on a number line
	// Widget key: number_line_choice_a
	test("number-line - [xbccdde0de8c444be] Select the position of -(-B) on a number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#E67E22",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mrow><mo>(</mo><mo>-</mo><mi>B</mi><mo>)</mo></mrow>",
		      "position": 0
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xbccdde0de8c444be
	// Question: Select the position of -(-B) on a number line
	// Widget key: number_line_choice_b
	test("number-line - [xbccdde0de8c444be] Select the position of -(-B) on a number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#E67E22",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mrow><mo>(</mo><mo>-</mo><mi>B</mi><mo>)</mo></mrow>",
		      "position": 1
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xbccdde0de8c444be
	// Question: Select the position of -(-B) on a number line
	// Widget key: number_line_choice_c
	test("number-line - [xbccdde0de8c444be] Select the position of -(-B) on a number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#E67E22",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mrow><mo>(</mo><mo>-</mo><mi>B</mi><mo>)</mo></mrow>",
		      "position": -1
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x5c584447c7fbea75
	// Question: Find −E on the number line
	// Widget key: image_1
	test("number-line - [x5c584447c7fbea75] Find −E on the number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 0.6,
		  "min": -0.6,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 0.2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>E</mi>",
		      "position": 0.2
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xf70235524a56eeb0
	// Question: Locate -(-D) on a number line
	// Widget key: number_line_choice_a
	test("number-line - [xf70235524a56eeb0] Locate -(-D) on a number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#E67E22",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mo>(</mo><mo>-</mo><mi>D</mi><mo>)</mo>",
		      "position": -0.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xf70235524a56eeb0
	// Question: Locate -(-D) on a number line
	// Widget key: number_line_choice_b
	test("number-line - [xf70235524a56eeb0] Locate -(-D) on a number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#E67E22",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mo>(</mo><mo>-</mo><mi>D</mi><mo>)</mo>",
		      "position": 0.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xf70235524a56eeb0
	// Question: Locate -(-D) on a number line
	// Widget key: number_line_choice_c
	test("number-line - [xf70235524a56eeb0] Locate -(-D) on a number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#E67E22",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mo>(</mo><mo>-</mo><mi>D</mi><mo>)</mo>",
		      "position": 1
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xd2a3d7af
	// Question: Identify the point representing -(-2) on a number line
	// Widget key: image_1
	test("number-line - [xd2a3d7af] Identify the point representing -(-2) on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -5
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -4
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": -2
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": 2
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>E</mi>",
		      "position": 5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x8aa5a97c4a23bc52
	// Question: Locate -(-E) on a number line
	// Widget key: nl_choice_a
	test("number-line - [x8aa5a97c4a23bc52] Locate -(-E) on a number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 4,
		  "min": -4,
		  "type": "numberLine",
		  "width": 460,
		  "height": 120,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#E67E22",
		      "style": "dot",
		      "mathml": "<mo>•</mo>",
		      "position": -3.5
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x8aa5a97c4a23bc52
	// Question: Locate -(-E) on a number line
	// Widget key: nl_choice_b
	test("number-line - [x8aa5a97c4a23bc52] Locate -(-E) on a number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 4,
		  "min": -4,
		  "type": "numberLine",
		  "width": 460,
		  "height": 120,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#E67E22",
		      "style": "dot",
		      "mathml": "<mo>•</mo>",
		      "position": 3.5
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x8aa5a97c4a23bc52
	// Question: Locate -(-E) on a number line
	// Widget key: nl_choice_c
	test("number-line - [x8aa5a97c4a23bc52] Locate -(-E) on a number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 4,
		  "min": -4,
		  "type": "numberLine",
		  "width": 460,
		  "height": 120,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#E67E22",
		      "style": "dot",
		      "mathml": "<mo>•</mo>",
		      "position": 2.5
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x48c1bee527871e57
	// Question: Identify −D on the number line
	// Widget key: choice_a_number_line
	test("number-line - [x48c1bee527871e57] Identify −D on the number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#ffa500",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mi>D</mi>",
		      "position": -1.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x48c1bee527871e57
	// Question: Identify −D on the number line
	// Widget key: choice_b_number_line
	test("number-line - [x48c1bee527871e57] Identify −D on the number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#ffa500",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mi>D</mi>",
		      "position": 1.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x48c1bee527871e57
	// Question: Identify −D on the number line
	// Widget key: choice_c_number_line
	test("number-line - [x48c1bee527871e57] Identify −D on the number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#ffa500",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mi>D</mi>",
		      "position": 0.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x71fd721566a5360d
	// Question: Value of negative C on a number line
	// Widget key: image_1
	test("number-line - [x71fd721566a5360d] Value of negative C on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": 0.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x8fa3e41ee11413a6
	// Question: Locate −(−A) on a number line
	// Widget key: number_line_choice_a
	test("number-line - [x8fa3e41ee11413a6] Locate −(−A) on a number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 8,
		  "min": -8,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#E67E22",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mo>(</mo><mo>-</mo><mi>A</mi><mo>)</mo>",
		      "position": 5
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x8fa3e41ee11413a6
	// Question: Locate −(−A) on a number line
	// Widget key: number_line_choice_b
	test("number-line - [x8fa3e41ee11413a6] Locate −(−A) on a number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 8,
		  "min": -8,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#E67E22",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mo>(</mo><mo>-</mo><mi>A</mi><mo>)</mo>",
		      "position": -5
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x8fa3e41ee11413a6
	// Question: Locate −(−A) on a number line
	// Widget key: number_line_choice_c
	test("number-line - [x8fa3e41ee11413a6] Locate −(−A) on a number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 8,
		  "min": -8,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#E67E22",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mo>(</mo><mo>-</mo><mi>A</mi><mo>)</mo>",
		      "position": 0
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xa3c11a88943b406b
	// Question: Locate -(-C) on a number line
	// Widget key: number_line_choice_a
	test("number-line - [xa3c11a88943b406b] Locate -(-C) on a number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#E67E22",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mo>(</mo><mo>-</mo><mi>C</mi><mo>)</mo>",
		      "position": 1.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xa3c11a88943b406b
	// Question: Locate -(-C) on a number line
	// Widget key: number_line_choice_b
	test("number-line - [xa3c11a88943b406b] Locate -(-C) on a number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#E67E22",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mo>(</mo><mo>-</mo><mi>C</mi><mo>)</mo>",
		      "position": -1.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xa3c11a88943b406b
	// Question: Locate -(-C) on a number line
	// Widget key: number_line_choice_c
	test("number-line - [xa3c11a88943b406b] Locate -(-C) on a number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#E67E22",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mo>(</mo><mo>-</mo><mi>C</mi><mo>)</mo>",
		      "position": 0.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x8fb40438b7f86b03
	// Question: Evaluate a double negative on a number line
	// Widget key: image_1
	test("number-line - [x8fb40438b7f86b03] Evaluate a double negative on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 8,
		  "min": -8,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": 6
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x86b2a3e066427391
	// Question: Evaluate a double negative on a number line
	// Widget key: image_1
	test("number-line - [x86b2a3e066427391] Evaluate a double negative on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": 1.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x62c748d6
	// Question: Identify the point representing -(-7) on a number line
	// Widget key: image_1
	test("number-line - [x62c748d6] Identify the point representing -(-7) on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 7,
		  "min": -7,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -7
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -2
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": 3
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": 4
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>E</mi>",
		      "position": 7
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xec3627429af7b5ea
	// Question: Evaluate -(-E) from a number line
	// Widget key: image_1
	test("number-line - [xec3627429af7b5ea] Evaluate -(-E) from a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 0.6,
		  "min": -0.6,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 0.2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>E</mi>",
		      "position": 0.6
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2d9a73dec8936789
	// Question: Locate -(-F) on a number line
	// Widget key: number_line_choice_a
	test("number-line - [x2d9a73dec8936789] Locate -(-F) on a number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#E67E22",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mrow><mo>(</mo><mo>-</mo><mi>F</mi><mo>)</mo></mrow>",
		      "position": -1.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2d9a73dec8936789
	// Question: Locate -(-F) on a number line
	// Widget key: number_line_choice_b
	test("number-line - [x2d9a73dec8936789] Locate -(-F) on a number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#E67E22",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mrow><mo>(</mo><mo>-</mo><mi>F</mi><mo>)</mo></mrow>",
		      "position": 1.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2d9a73dec8936789
	// Question: Locate -(-F) on a number line
	// Widget key: number_line_choice_c
	test("number-line - [x2d9a73dec8936789] Locate -(-F) on a number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#E67E22",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mrow><mo>(</mo><mo>-</mo><mi>F</mi><mo>)</mo></mrow>",
		      "position": -0.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x11099043
	// Question: Identify the point representing the opposite of negative four
	// Widget key: image_1
	test("number-line - [x11099043] Identify the point representing the opposite of negative four", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -4
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -2
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": 4
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": 7
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>E</mi>",
		      "position": 9
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xfee6aa630d739238
	// Question: Find the value of −B on a number line
	// Widget key: image_1
	test("number-line - [xfee6aa630d739238] Find the value of −B on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 8,
		  "min": -8,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -7
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xa4b45b740128203d
	// Question: Evaluate an expression from a number line
	// Widget key: image_1
	test("number-line - [xa4b45b740128203d] Evaluate an expression from a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 3,
		  "min": -3,
		  "type": "numberLine",
		  "width": 460,
		  "height": 150,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#1f77b4",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": 0
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x00cfab218d22a88e
	// Question: Select the location of −B on a number line
	// Widget key: number_line_choice_a
	test("number-line - [x00cfab218d22a88e] Select the location of −B on a number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 8,
		  "min": -8,
		  "type": "numberLine",
		  "width": 460,
		  "height": 150,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#E67E22",
		      "style": "dot",
		      "value": 5,
		      "position": 5
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x00cfab218d22a88e
	// Question: Select the location of −B on a number line
	// Widget key: number_line_choice_b
	test("number-line - [x00cfab218d22a88e] Select the location of −B on a number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 8,
		  "min": -8,
		  "type": "numberLine",
		  "width": 460,
		  "height": 150,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#E67E22",
		      "style": "dot",
		      "value": 5,
		      "position": -5
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x00cfab218d22a88e
	// Question: Select the location of −B on a number line
	// Widget key: number_line_choice_c
	test("number-line - [x00cfab218d22a88e] Select the location of −B on a number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 8,
		  "min": -8,
		  "type": "numberLine",
		  "width": 460,
		  "height": 150,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#E67E22",
		      "style": "dot",
		      "value": 6,
		      "position": 6
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xb6d191d1
	// Question: Which point represents −(−5) on the number line?
	// Widget key: image_1
	test("number-line - [xb6d191d1] Which point represents −(−5) on the number line?", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -5
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -4
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": -2
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": 2
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>E</mi>",
		      "position": 5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x000308979d33050b
	// Question: Match expressions to descriptions using a number line
	// Widget key: image_1
	test("number-line - [x000308979d33050b] Match expressions to descriptions using a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 5,
		  "min": -5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mi>n</mi>",
		      "position": -4
		    },
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mi>q</mi>",
		      "position": -1
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x50acca2c9e26425c
	// Question: Identify which expression is negative
	// Widget key: image_1
	test("number-line - [x50acca2c9e26425c] Identify which expression is negative", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 3,
		  "min": -3,
		  "type": "numberLine",
		  "width": 460,
		  "height": 69,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mi>a</mi>",
		      "position": -1.5
		    },
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mi>b</mi>",
		      "position": 0.8
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2630a00bc0919ed9
	// Question: Order expressions using a number line
	// Widget key: image_1
	test("number-line - [x2630a00bc0919ed9] Order expressions using a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 6,
		  "min": -6,
		  "type": "numberLine",
		  "width": 460,
		  "height": 69,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mi>a</mi>",
		      "position": -1
		    },
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mi>b</mi>",
		      "position": 2
		    },
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mi>c</mi>",
		      "position": 3
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2847e4fed7c1c79f
	// Question: Identify a negative expression on a number line
	// Widget key: image_1
	test("number-line - [x2847e4fed7c1c79f] Identify a negative expression on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1.2,
		  "min": -1.8,
		  "type": "numberLine",
		  "width": 460,
		  "height": 69,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mi>a</mi>",
		      "position": -1.7
		    },
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mi>b</mi>",
		      "position": 0.8
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x7fdb16d094db88e8
	// Question: Identify a positive expression from a number line
	// Widget key: image_1
	test("number-line - [x7fdb16d094db88e8] Identify a positive expression from a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2.6,
		  "min": -3,
		  "type": "numberLine",
		  "width": 460,
		  "height": 150,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mi>a</mi>",
		      "position": -1.7
		    },
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mi>b</mi>",
		      "position": 2.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x9762a96d6ed72ffb
	// Question: Order expressions using a number line
	// Widget key: image_1
	test("number-line - [x9762a96d6ed72ffb] Order expressions using a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 3,
		  "min": -3,
		  "type": "numberLine",
		  "width": 460,
		  "height": 150,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 3
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mi>n</mi>",
		      "position": -2
		    },
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mi>q</mi>",
		      "position": 1
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x33814e542c5816f3
	// Question: Complete an inequality from a number line
	// Widget key: image_1
	test("number-line - [x33814e542c5816f3] Complete an inequality from a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 69,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>a</mi>",
		      "position": -1.2
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x0b7d83ae9dcba640
	// Question: Order expressions using a vertical number line
	// Widget key: image_1
	test("number-line - [x0b7d83ae9dcba640] Order expressions using a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1,
		  "min": -1,
		  "type": "numberLine",
		  "width": 115,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>j</mi>",
		      "position": 0.8
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>k</mi>",
		      "position": 0.4
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>l</mi>",
		      "position": -0.25
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xd874b91d7e96e8e4
	// Question: Order expressions on a vertical number line
	// Widget key: image_1
	test("number-line - [xd874b91d7e96e8e4] Order expressions on a vertical number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1,
		  "min": -1,
		  "type": "numberLine",
		  "width": 77,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mi>a</mi>",
		      "position": -1
		    },
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mi>b</mi>",
		      "position": -0.5
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x9b7d923e5f168763
	// Question: Match expressions with descriptions on a number line
	// Widget key: image_1
	test("number-line - [x9b7d923e5f168763] Match expressions with descriptions on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 8,
		  "min": -8,
		  "type": "numberLine",
		  "width": 77,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>x</mi>",
		      "position": 4.5
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>y</mi>",
		      "position": 1.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x89451955162e0708
	// Question: Match expressions to descriptions on a number line
	// Widget key: image_1
	test("number-line - [x89451955162e0708] Match expressions to descriptions on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 4,
		  "min": -4,
		  "type": "numberLine",
		  "width": 460,
		  "height": 70,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mi>r</mi>",
		      "position": -1.6
		    },
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mi>s</mi>",
		      "position": 2.6
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x98cd49b5121e21de
	// Question: Identify a negative expression from a number line
	// Widget key: image_1
	test("number-line - [x98cd49b5121e21de] Identify a negative expression from a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1.8,
		  "min": -1,
		  "type": "numberLine",
		  "width": 460,
		  "height": 150,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>a</mi>",
		      "position": -0.6
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>b</mi>",
		      "position": 1.6
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x0ef6d2b4c68519d9
	// Question: Complete an inequality using a dropdown
	// Widget key: image_1
	test("number-line - [x0ef6d2b4c68519d9] Complete an inequality using a dropdown", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 4,
		  "min": -4,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>a</mi>",
		      "position": -3.3
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>b</mi>",
		      "position": 1.6
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x702a90a60fbd1dc3
	// Question: Match expressions by value using a number line
	// Widget key: image_1
	test("number-line - [x702a90a60fbd1dc3] Match expressions by value using a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 6,
		  "min": -6,
		  "type": "numberLine",
		  "width": 460,
		  "height": 150,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 3
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>r</mi>",
		      "position": -6
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>s</mi>",
		      "position": 2.2
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xea0a8e5dd711d5b9
	// Question: Order expressions using a number line
	// Widget key: image_1
	test("number-line - [xea0a8e5dd711d5b9] Order expressions using a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 5,
		  "min": -5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 69,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>a</mi>",
		      "position": -0.8
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>b</mi>",
		      "position": 2.6
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>c</mi>",
		      "position": 4.4
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xb9bdb7b805d3221d
	// Question: Match expressions to descriptions on a number line
	// Widget key: image_1
	test("number-line - [xb9bdb7b805d3221d] Match expressions to descriptions on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 3,
		  "min": -3,
		  "type": "numberLine",
		  "width": 460,
		  "height": 69,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mi>n</mi>",
		      "position": -2
		    },
		    {
		      "type": "mathml",
		      "color": "#11accd",
		      "style": "dot",
		      "mathml": "<mi>q</mi>",
		      "position": 1
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x5207f2836aa03b35
	// Question: Complete an inequality using a number line
	// Widget key: image_1
	test("number-line - [x5207f2836aa03b35] Complete an inequality using a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1.8,
		  "min": -1.8,
		  "type": "numberLine",
		  "width": 460,
		  "height": 69,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>b</mi>",
		      "position": -1.2
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>a</mi>",
		      "position": 0.45
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xb441fe35e6768bcd
	// Question: Complete an inequality from a number line
	// Widget key: image_1
	test("number-line - [xb441fe35e6768bcd] Complete an inequality from a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1,
		  "min": -1,
		  "type": "numberLine",
		  "width": 460,
		  "height": 68.51,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>a</mi>",
		      "position": 0.9
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>b</mi>",
		      "position": -0.4
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x56a633da4f62b8b3
	// Question: Identify a positive expression from a number line
	// Widget key: image_1
	test("number-line - [x56a633da4f62b8b3] Identify a positive expression from a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 8,
		  "min": -4,
		  "type": "numberLine",
		  "width": 460,
		  "height": 150,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>a</mi>",
		      "position": 6.2
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>b</mi>",
		      "position": -3.7
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xb698e113e751422f
	// Question: Complete the inequality using a dropdown
	// Widget key: image_1
	test("number-line - [xb698e113e751422f] Complete the inequality using a dropdown", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1.5,
		  "min": -1.5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mi>c</mi>",
		      "position": -1.1
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x9122705c9e43fac7
	// Question: Determine the sign of m to the 13th times n to the 5th
	// Widget key: image_1
	test("number-line - [x9122705c9e43fac7] Determine the sign of m to the 13th times n to the 5th", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2.5,
		  "min": -1.5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 150,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>m</mi>",
		      "position": -1.3
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>n</mi>",
		      "position": -0.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x18d8a36dc05bc413
	// Question: Determine the sign of a product of cubes
	// Widget key: image_1
	test("number-line - [x18d8a36dc05bc413] Determine the sign of a product of cubes", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": 0,
		  "type": "numberLine",
		  "width": 125,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>g</mi>",
		      "position": 0.3
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>f</mi>",
		      "position": 1.8
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x14283a167a550768
	// Question: Identify the sign of a product with zero
	// Widget key: image_1
	test("number-line - [x14283a167a550768] Identify the sign of a product with zero", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 3,
		  "min": -3,
		  "type": "numberLine",
		  "width": 125,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>c</mi>",
		      "position": -2
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>d</mi>",
		      "position": 2
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x9ed54989cf2d075f
	// Question: Determine the sign of an expression
	// Widget key: image_1
	test("number-line - [x9ed54989cf2d075f] Determine the sign of an expression", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2.1,
		  "min": -1.9,
		  "type": "numberLine",
		  "width": 460,
		  "height": 69,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>m</mi>",
		      "position": -1.6
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>n</mi>",
		      "position": 0.65
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2ac97a878f373aac
	// Question: Determine the sign of an expression
	// Widget key: image_1
	test("number-line - [x2ac97a878f373aac] Determine the sign of an expression", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 125,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>c</mi>",
		      "position": -1.5
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>b</mi>",
		      "position": 0.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x06c9ebc30b95bc5c
	// Question: Determine the sign of a product
	// Widget key: image_1
	test("number-line - [x06c9ebc30b95bc5c] Determine the sign of a product", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -7,
		  "type": "numberLine",
		  "width": 460,
		  "height": 69,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>a</mi>",
		      "position": 0
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>b</mi>",
		      "position": -5.75
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x63be039102b62a98
	// Question: Select all correct inequalities
	// Widget key: image_1
	test("number-line - [x63be039102b62a98] Select all correct inequalities", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#ff00af",
		      "style": "dot",
		      "value": 5,
		      "position": -5
		    },
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#6495ed",
		      "style": "dot",
		      "value": 7,
		      "position": 7
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x61783134e6426546
	// Question: Select the correct inequalities using a number line
	// Widget key: image_1
	test("number-line - [x61783134e6426546] Select the correct inequalities using a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1,
		  "min": -1,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 8
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "fraction",
		      "color": "#0066CC",
		      "style": "dot",
		      "position": -0.75,
		      "numerator": 3,
		      "denominator": 4
		    },
		    {
		      "sign": "+",
		      "type": "fraction",
		      "color": "#E67E22",
		      "style": "dot",
		      "position": 0.375,
		      "numerator": 3,
		      "denominator": 8
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x97fa696c66e0b11e
	// Question: Select the inequalities that are true
	// Widget key: image_1
	test("number-line - [x97fa696c66e0b11e] Select the inequalities that are true", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 20,
		  "min": -20,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#000000",
		      "style": "dot",
		      "value": 12,
		      "position": -12
		    },
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#000000",
		      "style": "dot",
		      "value": 18,
		      "position": 18
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x7c61e7280f77ac6a
	// Question: Select the correct inequalities using a number line
	// Widget key: image_1
	test("number-line - [x7c61e7280f77ac6a] Select the correct inequalities using a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 100,
		  "min": -100,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 10
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#000000",
		      "style": "dot",
		      "value": 76,
		      "position": -76
		    },
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#000000",
		      "style": "dot",
		      "value": 59,
		      "position": 59
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x7b49e10b83d18dbc
	// Question: Select true inequalities using a number line
	// Widget key: image_1
	test("number-line - [x7b49e10b83d18dbc] Select true inequalities using a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mn>7.5</mn>",
		      "position": -7.5
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mn>5.7</mn>",
		      "position": 5.7
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xd8f74c2ad290d41b
	// Question: Sign of a product from a number line
	// Widget key: image_1
	test("number-line - [xd8f74c2ad290d41b] Sign of a product from a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 8,
		  "min": -8,
		  "type": "numberLine",
		  "width": 125,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>a</mi>",
		      "position": -8
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>b</mi>",
		      "position": -3.6
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x946d1c1b5794c5d0
	// Question: Determine the sign of b/c
	// Widget key: image_1
	test("number-line - [x946d1c1b5794c5d0] Determine the sign of b/c", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 125,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>b</mi>",
		      "position": 0.8
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>c</mi>",
		      "position": -1.8
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x7eb0f597fff433a8
	// Question: Determine the sign of an expression from a number line
	// Widget key: image_1
	test("number-line - [x7eb0f597fff433a8] Determine the sign of an expression from a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 9,
		  "min": -9,
		  "type": "numberLine",
		  "width": 460,
		  "height": 69,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>y</mi>",
		      "position": 1
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>x</mi>",
		      "position": -8
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xcc1780adf5a9f187
	// Question: Determine the sign of a·(−b)
	// Widget key: image_1
	test("number-line - [xcc1780adf5a9f187] Determine the sign of a·(−b)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 4,
		  "min": -4,
		  "type": "numberLine",
		  "width": 125,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>b</mi>",
		      "position": -2.5
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>a</mi>",
		      "position": 0
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xf512502ffef902d8
	// Question: Determine the sign of -xy
	// Widget key: image_1
	test("number-line - [xf512502ffef902d8] Determine the sign of -xy", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 9,
		  "min": -11,
		  "type": "numberLine",
		  "width": 460,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>x</mi>",
		      "position": -4.5
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>y</mi>",
		      "position": 8.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xf3a72fc9244f5d0b
	// Question: Determine the sign of y ÷ x from a number line
	// Widget key: image_1
	test("number-line - [xf3a72fc9244f5d0b] Determine the sign of y ÷ x from a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 150,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>x</mi>",
		      "position": -1.25
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>y</mi>",
		      "position": 0
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x46737fa249a0abbc
	// Question: Effect on mean and median after removing an outlier wage
	// Widget key: image_1
	test("number-line - [x46737fa249a0abbc] Effect on mean and median after removing an outlier wage", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 40,
		  "min": 15,
		  "type": "numberLine",
		  "width": 320,
		  "height": 130,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#11accd",
		      "style": "dot",
		      "value": 15,
		      "position": 15
		    },
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#11accd",
		      "style": "dot",
		      "value": 16,
		      "position": 16
		    },
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#11accd",
		      "style": "dot",
		      "value": 18,
		      "position": 18
		    },
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#11accd",
		      "style": "dot",
		      "value": 19,
		      "position": 19
		    },
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#11accd",
		      "style": "dot",
		      "value": 20,
		      "position": 20
		    },
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#a75a05",
		      "style": "dot",
		      "value": 40,
		      "position": 40
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xecfa7f81ad9a97f8
	// Question: Effect of removing a high outlier on mean and median
	// Widget key: image_1
	test("number-line - [xecfa7f81ad9a97f8] Effect of removing a high outlier on mean and median", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 5000,
		  "min": 1000,
		  "type": "numberLine",
		  "width": 320,
		  "height": 106,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1000
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#11accd",
		      "style": "dot",
		      "value": 1000,
		      "position": 1000
		    },
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#11accd",
		      "style": "dot",
		      "value": 1100,
		      "position": 1100
		    },
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#11accd",
		      "style": "dot",
		      "value": 1200,
		      "position": 1200
		    },
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#11accd",
		      "style": "dot",
		      "value": 1400,
		      "position": 1400
		    },
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#a75a05",
		      "style": "dot",
		      "value": 4800,
		      "position": 4800
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 100
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2793302cc08c9d81
	// Question: Identify the point closest to sqrt(97) on a number line
	// Widget key: image_1
	test("number-line - [x2793302cc08c9d81] Identify the point closest to sqrt(97) on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": 9.5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 0.1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>d</mi>",
		      "position": 9.7502
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>e</mi>",
		      "position": 9.81
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>f</mi>",
		      "position": 9.8489
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x69935621f216bc8c
	// Question: Identify a point representing a square root on a number line
	// Widget key: image_1
	test("number-line - [x69935621f216bc8c] Identify a point representing a square root on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -7,
		  "min": -7.5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 10
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>j</mi>",
		      "position": -7.311
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>k</mi>",
		      "position": -7.2628
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>l</mi>",
		      "position": -7.2111
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x414b8220aec34836
	// Question: Choose the point closest to −√77
	// Widget key: image_1
	test("number-line - [x414b8220aec34836] Choose the point closest to −√77", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -8.5,
		  "min": -9,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 10
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>m</mi>",
		      "position": -8.774
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>n</mi>",
		      "position": -8.726284
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>o</mi>",
		      "position": -8.69
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x654e1ffb45d6f82c
	// Question: Select the point representing the square root of 75 on a number line
	// Widget key: image_1
	test("number-line - [x654e1ffb45d6f82c] Select the point representing the square root of 75 on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 9,
		  "min": 8.5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 0.1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>d</mi>",
		      "position": 8.66
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>e</mi>",
		      "position": 8.72
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>f</mi>",
		      "position": 8.78
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xdf9b279db19b86d3
	// Question: Identify the point representing the square root of 27
	// Widget key: image_1
	test("number-line - [xdf9b279db19b86d3] Identify the point representing the square root of 27", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 5.5,
		  "min": 5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 10
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>a</mi>",
		      "position": 5.11
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>b</mi>",
		      "position": 5.1961
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>c</mi>",
		      "position": 5.23
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x4d8dbe91bd4466f1
	// Question: Closest point to -√8 on a number line
	// Widget key: image_1
	test("number-line - [x4d8dbe91bd4466f1] Closest point to -√8 on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -2.5,
		  "min": -3,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 10
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>j</mi>",
		      "position": -2.88502
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>k</mi>",
		      "position": -2.8284
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>l</mi>",
		      "position": -2.7489
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x75206aad4d824373
	// Question: Identify the point closest to −√84 on a number line
	// Widget key: image_1
	test("number-line - [x75206aad4d824373] Identify the point closest to −√84 on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -9,
		  "min": -9.5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 0.1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>m</mi>",
		      "position": -9.214
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>n</mi>",
		      "position": -9.1651
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>o</mi>",
		      "position": -9.090569
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x8801e1b9a9c6f18a
	// Question: Identify the point closest to sqrt(23)
	// Widget key: image_1
	test("number-line - [x8801e1b9a9c6f18a] Identify the point closest to sqrt(23)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 5,
		  "min": 4.5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 10
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>a</mi>",
		      "position": 4.68
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>b</mi>",
		      "position": 4.72
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>c</mi>",
		      "position": 4.7958
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x192b8af759e7cb42
	// Question: Expressions representing a distance on a number line
	// Widget key: image_1
	test("number-line - [x192b8af759e7cb42] Expressions representing a distance on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 460,
		  "height": 150,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#11accd",
		      "style": "dot",
		      "value": 2,
		      "position": -2
		    },
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#11accd",
		      "style": "dot",
		      "value": 1,
		      "position": 1
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xf0699745a047e2a1
	// Question: Select the number line for an absolute value difference
	// Widget key: choice_a_number_line
	test("number-line - [xf0699745a047e2a1] Select the number line for an absolute value difference (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 230,
		  "height": 100,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "+",
		      "type": "fraction",
		      "color": "#0c7f99",
		      "style": "dot",
		      "position": 0.5,
		      "numerator": 1,
		      "denominator": 2
		    },
		    {
		      "sign": "+",
		      "type": "fraction",
		      "color": "#0c7f99",
		      "style": "dot",
		      "position": 1.5,
		      "numerator": 3,
		      "denominator": 2
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xf0699745a047e2a1
	// Question: Select the number line for an absolute value difference
	// Widget key: choice_b_number_line
	test("number-line - [xf0699745a047e2a1] Select the number line for an absolute value difference (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 230,
		  "height": 100,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "fraction",
		      "color": "#0c7f99",
		      "style": "dot",
		      "position": -0.5,
		      "numerator": 1,
		      "denominator": 2
		    },
		    {
		      "sign": "+",
		      "type": "fraction",
		      "color": "#0c7f99",
		      "style": "dot",
		      "position": 1.5,
		      "numerator": 3,
		      "denominator": 2
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xf0699745a047e2a1
	// Question: Select the number line for an absolute value difference
	// Widget key: choice_c_number_line
	test("number-line - [xf0699745a047e2a1] Select the number line for an absolute value difference (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 230,
		  "height": 100,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "fraction",
		      "color": "#0c7f99",
		      "style": "dot",
		      "position": -1.5,
		      "numerator": 3,
		      "denominator": 2
		    },
		    {
		      "sign": "-",
		      "type": "fraction",
		      "color": "#0c7f99",
		      "style": "dot",
		      "position": -0.5,
		      "numerator": 1,
		      "denominator": 2
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xf0699745a047e2a1
	// Question: Select the number line for an absolute value difference
	// Widget key: choice_d_number_line
	test("number-line - [xf0699745a047e2a1] Select the number line for an absolute value difference (4)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 230,
		  "height": 100,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "fraction",
		      "color": "#0c7f99",
		      "style": "dot",
		      "position": -1.5,
		      "numerator": 3,
		      "denominator": 2
		    },
		    {
		      "sign": "+",
		      "type": "fraction",
		      "color": "#0c7f99",
		      "style": "dot",
		      "position": 0.5,
		      "numerator": 1,
		      "denominator": 2
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xbc93ac7088d522c6
	// Question: Select expressions for the distance between -3.9 and -4.7
	// Widget key: image_1
	test("number-line - [xbc93ac7088d522c6] Select expressions for the distance between -3.9 and -4.7", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -3,
		  "min": -5,
		  "type": "numberLine",
		  "width": 80,
		  "height": 325,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mn>4.7</mn>",
		      "position": -4.7
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mn>3.9</mn>",
		      "position": -3.9
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 10
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x678e737d9d948dd2
	// Question: Select the number line model for an absolute value expression
	// Widget key: choice_a_numberline
	test("number-line - [x678e737d9d948dd2] Select the number line model for an absolute value expression (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 230,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 2,
		      "position": -2
		    },
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 7,
		      "position": 7
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x678e737d9d948dd2
	// Question: Select the number line model for an absolute value expression
	// Widget key: choice_b_numberline
	test("number-line - [x678e737d9d948dd2] Select the number line model for an absolute value expression (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 230,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 2,
		      "position": 2
		    },
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 7,
		      "position": 7
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x678e737d9d948dd2
	// Question: Select the number line model for an absolute value expression
	// Widget key: choice_c_numberline
	test("number-line - [x678e737d9d948dd2] Select the number line model for an absolute value expression (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 230,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 7,
		      "position": -7
		    },
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 2,
		      "position": 2
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x678e737d9d948dd2
	// Question: Select the number line model for an absolute value expression
	// Widget key: choice_d_numberline
	test("number-line - [x678e737d9d948dd2] Select the number line model for an absolute value expression (4)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 230,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 7,
		      "position": -7
		    },
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 2,
		      "position": -2
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2e27812b11dffe14
	// Question: Match an absolute value expression to a number line model
	// Widget key: choice_a_visual
	test("number-line - [x2e27812b11dffe14] Match an absolute value expression to a number line model (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 3,
		  "min": -3,
		  "type": "numberLine",
		  "width": 230,
		  "height": 100,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "fraction",
		      "color": "#0c7f99",
		      "style": "dot",
		      "position": -1.6666666666667,
		      "numerator": 5,
		      "denominator": 3
		    },
		    {
		      "sign": "-",
		      "type": "fraction",
		      "color": "#0c7f99",
		      "style": "dot",
		      "position": -0.6666666666667,
		      "numerator": 2,
		      "denominator": 3
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 3
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2e27812b11dffe14
	// Question: Match an absolute value expression to a number line model
	// Widget key: choice_b_visual
	test("number-line - [x2e27812b11dffe14] Match an absolute value expression to a number line model (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 3,
		  "min": -3,
		  "type": "numberLine",
		  "width": 230,
		  "height": 100,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "fraction",
		      "color": "#0c7f99",
		      "style": "dot",
		      "position": -1.6666666666667,
		      "numerator": 5,
		      "denominator": 3
		    },
		    {
		      "sign": "+",
		      "type": "fraction",
		      "color": "#0c7f99",
		      "style": "dot",
		      "position": 0.6666666666667,
		      "numerator": 2,
		      "denominator": 3
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 3
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2e27812b11dffe14
	// Question: Match an absolute value expression to a number line model
	// Widget key: choice_c_visual
	test("number-line - [x2e27812b11dffe14] Match an absolute value expression to a number line model (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 3,
		  "min": -3,
		  "type": "numberLine",
		  "width": 230,
		  "height": 100,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "+",
		      "type": "fraction",
		      "color": "#0c7f99",
		      "style": "dot",
		      "position": 0.6666666666667,
		      "numerator": 2,
		      "denominator": 3
		    },
		    {
		      "sign": "+",
		      "type": "fraction",
		      "color": "#0c7f99",
		      "style": "dot",
		      "position": 1.6666666666667,
		      "numerator": 5,
		      "denominator": 3
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 3
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2e27812b11dffe14
	// Question: Match an absolute value expression to a number line model
	// Widget key: choice_d_visual
	test("number-line - [x2e27812b11dffe14] Match an absolute value expression to a number line model (4)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 3,
		  "min": -3,
		  "type": "numberLine",
		  "width": 230,
		  "height": 100,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "fraction",
		      "color": "#0c7f99",
		      "style": "dot",
		      "position": -0.6666666666667,
		      "numerator": 2,
		      "denominator": 3
		    },
		    {
		      "sign": "+",
		      "type": "fraction",
		      "color": "#0c7f99",
		      "style": "dot",
		      "position": 1.6666666666667,
		      "numerator": 5,
		      "denominator": 3
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 3
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x5afb43cd66f2bb42
	// Question: Select expressions for the distance between -2 and 1.5
	// Widget key: image_1
	test("number-line - [x5afb43cd66f2bb42] Select expressions for the distance between -2 and 1.5", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -3,
		  "type": "numberLine",
		  "width": 80,
		  "height": 325,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mn>2</mn>",
		      "position": -2
		    },
		    {
		      "type": "mathml",
		      "color": "#0c7f99",
		      "style": "dot",
		      "mathml": "<mn>1.5</mn>",
		      "position": 1.5
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xf3035ac589940ba4
	// Question: Select expressions for the distance between -1/4 and 3/4
	// Widget key: image_1
	test("number-line - [xf3035ac589940ba4] Select expressions for the distance between -1/4 and 3/4", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1,
		  "min": -0.5,
		  "type": "numberLine",
		  "width": 80,
		  "height": 325,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 4
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "fraction",
		      "color": "#11accd",
		      "style": "dot",
		      "position": -0.25,
		      "numerator": 1,
		      "denominator": 4
		    },
		    {
		      "sign": "+",
		      "type": "fraction",
		      "color": "#11accd",
		      "style": "dot",
		      "position": 0.75,
		      "numerator": 3,
		      "denominator": 4
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xa8ccf6741922eaaf
	// Question: Select expressions for the distance between −4 and 1
	// Widget key: image_1
	test("number-line - [xa8ccf6741922eaaf] Select expressions for the distance between −4 and 1", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 3,
		  "min": -6,
		  "type": "numberLine",
		  "width": 76.8,
		  "height": 240,
		  "orientation": "vertical",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#11accd",
		      "style": "dot",
		      "value": 4,
		      "position": -4
		    },
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#11accd",
		      "style": "dot",
		      "value": 1,
		      "position": 1
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x06454d11ce9a2807
	// Question: Match an expression to a number line model
	// Widget key: choice_a_numberline
	test("number-line - [x06454d11ce9a2807] Match an expression to a number line model (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 6,
		  "min": -6,
		  "type": "numberLine",
		  "width": 230,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 6,
		      "position": -6
		    },
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 3,
		      "position": -3
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x06454d11ce9a2807
	// Question: Match an expression to a number line model
	// Widget key: choice_b_numberline
	test("number-line - [x06454d11ce9a2807] Match an expression to a number line model (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 6,
		  "min": -6,
		  "type": "numberLine",
		  "width": 230,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 6,
		      "position": -6
		    },
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 3,
		      "position": 3
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x06454d11ce9a2807
	// Question: Match an expression to a number line model
	// Widget key: choice_c_numberline
	test("number-line - [x06454d11ce9a2807] Match an expression to a number line model (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 6,
		  "min": -6,
		  "type": "numberLine",
		  "width": 230,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 3,
		      "position": 3
		    },
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 6,
		      "position": 6
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x06454d11ce9a2807
	// Question: Match an expression to a number line model
	// Widget key: choice_d_numberline
	test("number-line - [x06454d11ce9a2807] Match an expression to a number line model (4)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 6,
		  "min": -6,
		  "type": "numberLine",
		  "width": 230,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 3,
		      "position": -3
		    },
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 6,
		      "position": 6
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x76d6e637a385ffc0
	// Question: Select two distance expressions between -4/3 and 1/3
	// Widget key: image_1
	test("number-line - [x76d6e637a385ffc0] Select two distance expressions between -4/3 and 1/3", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1,
		  "min": -1.6666666667,
		  "type": "numberLine",
		  "width": 460,
		  "height": 88,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 3
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "fraction",
		      "color": "#11accd",
		      "style": "dot",
		      "position": -1.3333333333,
		      "numerator": 4,
		      "denominator": 3
		    },
		    {
		      "sign": "+",
		      "type": "fraction",
		      "color": "#11accd",
		      "style": "dot",
		      "position": 0.3333333333,
		      "numerator": 1,
		      "denominator": 3
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2977b6e04d636e31
	// Question: Select the number line model for the absolute value expression
	// Widget key: choice_a_number_line
	test("number-line - [x2977b6e04d636e31] Select the number line model for the absolute value expression (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 5,
		  "min": -5,
		  "type": "numberLine",
		  "width": 230,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 4,
		      "position": -4
		    },
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 1,
		      "position": 1
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2977b6e04d636e31
	// Question: Select the number line model for the absolute value expression
	// Widget key: choice_b_number_line
	test("number-line - [x2977b6e04d636e31] Select the number line model for the absolute value expression (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 5,
		  "min": -5,
		  "type": "numberLine",
		  "width": 230,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 1,
		      "position": -1
		    },
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 4,
		      "position": 4
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2977b6e04d636e31
	// Question: Select the number line model for the absolute value expression
	// Widget key: choice_c_number_line
	test("number-line - [x2977b6e04d636e31] Select the number line model for the absolute value expression (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 5,
		  "min": -5,
		  "type": "numberLine",
		  "width": 230,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 1,
		      "position": 1
		    },
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 4,
		      "position": 4
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2977b6e04d636e31
	// Question: Select the number line model for the absolute value expression
	// Widget key: choice_d_number_line
	test("number-line - [x2977b6e04d636e31] Select the number line model for the absolute value expression (4)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 5,
		  "min": -5,
		  "type": "numberLine",
		  "width": 230,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 4,
		      "position": -4
		    },
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 1,
		      "position": -1
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x17b0d05150d27b19
	// Question: Select the number line model for |8 − 1|
	// Widget key: choice_a_nl
	test("number-line - [x17b0d05150d27b19] Select the number line model for |8 − 1| (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 230,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 1,
		      "position": -1
		    },
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 8,
		      "position": 8
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x17b0d05150d27b19
	// Question: Select the number line model for |8 − 1|
	// Widget key: choice_b_nl
	test("number-line - [x17b0d05150d27b19] Select the number line model for |8 − 1| (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": 0,
		  "type": "numberLine",
		  "width": 230,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 1,
		      "position": 1
		    },
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 8,
		      "position": 8
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x17b0d05150d27b19
	// Question: Select the number line model for |8 − 1|
	// Widget key: choice_c_nl
	test("number-line - [x17b0d05150d27b19] Select the number line model for |8 − 1| (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 0,
		  "min": -10,
		  "type": "numberLine",
		  "width": 230,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 8,
		      "position": -8
		    },
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 1,
		      "position": -1
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x17b0d05150d27b19
	// Question: Select the number line model for |8 − 1|
	// Widget key: choice_d_nl
	test("number-line - [x17b0d05150d27b19] Select the number line model for |8 − 1| (4)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -10,
		  "type": "numberLine",
		  "width": 230,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 8,
		      "position": -8
		    },
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#0c7f99",
		      "style": "dot",
		      "value": 1,
		      "position": 1
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xb846237172363521
	// Question: Match expressions to points on a number line
	// Widget key: image_1
	test("number-line - [xb846237172363521] Match expressions to points on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": 6,
		  "type": "numberLine",
		  "width": 460,
		  "height": 150,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>a</mi>",
		      "position": 7.818
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>b</mi>",
		      "position": 8.544
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ed",
		      "style": "dot",
		      "mathml": "<mi>c</mi>",
		      "position": 9.425
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 4
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x86aaf35c7a4370a9
	// Question: Match expressions to points on a number line
	// Widget key: image_1
	test("number-line - [x86aaf35c7a4370a9] Match expressions to points on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 5.8,
		  "min": 3.5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 69,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>a</mi>",
		      "position": 4
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>b</mi>",
		      "position": 5.196
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>c</mi>",
		      "position": 5.657
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x3d69b9883a6bc092
	// Question: Match expressions to points on a number line
	// Widget key: image_1
	test("number-line - [x3d69b9883a6bc092] Match expressions to points on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 11,
		  "min": 8,
		  "type": "numberLine",
		  "width": 460,
		  "height": 150,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>d</mi>",
		      "position": 8.944
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>e</mi>",
		      "position": 9.425
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>f</mi>",
		      "position": 10
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 4
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xf965b7bf99449fd1
	// Question: Match square roots to points on a number line
	// Widget key: image_1
	test("number-line - [xf965b7bf99449fd1] Match square roots to points on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 9,
		  "min": 6,
		  "type": "numberLine",
		  "width": 460,
		  "height": 69,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>m</mi>",
		      "position": 7
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>n</mi>",
		      "position": 7.211
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>o</mi>",
		      "position": 7.9375
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 4
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x6649c711f797ef18
	// Question: Match expressions to points on a number line
	// Widget key: image_1
	test("number-line - [x6649c711f797ef18] Match expressions to points on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 11,
		  "min": 8,
		  "type": "numberLine",
		  "width": 460,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>x</mi>",
		      "position": 9.425
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>y</mi>",
		      "position": 10
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>z</mi>",
		      "position": 10.198
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 4
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x9df4be33abb4177a
	// Question: Match expressions to points on a number line
	// Widget key: image_1
	test("number-line - [x9df4be33abb4177a] Match expressions to points on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 4,
		  "min": 0,
		  "type": "numberLine",
		  "width": 460,
		  "height": 70,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>p</mi>",
		      "position": 1.571
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>q</mi>",
		      "position": 2.236
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>r</mi>",
		      "position": 3.12
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 4
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x75a5893f11c1e5f8
	// Question: Match expressions to points on a number line
	// Widget key: image_1
	test("number-line - [x75a5893f11c1e5f8] Match expressions to points on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -3.5,
		  "min": -6.5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 69,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>a</mi>",
		      "position": -6.28
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>b</mi>",
		      "position": -5.667
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>c</mi>",
		      "position": -4.244
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x4667532b46f1799f
	// Question: Match expressions to points on a number line
	// Widget key: image_1
	test("number-line - [x4667532b46f1799f] Match expressions to points on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 0,
		  "min": -5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 160,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>d</mi>",
		      "position": -3.14
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>e</mi>",
		      "position": -2.8284271247461903
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>f</mi>",
		      "position": -1.5
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xb2d01f5351f1f258
	// Question: Match expressions to points on a number line
	// Widget key: image_1
	test("number-line - [xb2d01f5351f1f258] Match expressions to points on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 11,
		  "min": 6,
		  "type": "numberLine",
		  "width": 460,
		  "height": 90,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>x</mi>",
		      "position": 8.832
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>y</mi>",
		      "position": 9.425
		    },
		    {
		      "type": "mathml",
		      "color": "#6495ED",
		      "style": "dot",
		      "mathml": "<mi>z</mi>",
		      "position": 10
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xf49b38785f496f2c
	// Question: Match expressions to points on a number line
	// Widget key: image_1
	test("number-line - [xf49b38785f496f2c] Match expressions to points on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": -4,
		  "min": -9,
		  "type": "numberLine",
		  "width": 460,
		  "height": 70,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>s</mi>",
		      "position": -8.246
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>t</mi>",
		      "position": -6.283
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>u</mi>",
		      "position": -5
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 2
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x7b27f9336e6b6272
	// Question: Find the missing value on a number line
	// Widget key: image_1
	test("number-line - [x7b27f9336e6b6272] Find the missing value on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x807c968f5dc02bad
	// Question: Find the missing value on a number line
	// Widget key: image_1
	test("number-line - [x807c968f5dc02bad] Find the missing value on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x8628be484c010df2
	// Question: Find the missing value using a number line
	// Widget key: image_1
	test("number-line - [x8628be484c010df2] Find the missing value using a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x1be90394c94041d0
	// Question: Find the missing value on a number line
	// Widget key: image_1
	test("number-line - [x1be90394c94041d0] Find the missing value on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x6a9ec443b339eca6
	// Question: Find the missing value
	// Widget key: image_1
	test("number-line - [x6a9ec443b339eca6] Find the missing value", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xd5151fab9c4f9048
	// Question: Find the missing value
	// Widget key: image_1
	test("number-line - [xd5151fab9c4f9048] Find the missing value", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x681b1cd156c59e80
	// Question: Find the missing value
	// Widget key: image_1
	test("number-line - [x681b1cd156c59e80] Find the missing value", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xcc20326bcb5bf91d
	// Question: Find the missing value on a number line
	// Widget key: image_1
	test("number-line - [xcc20326bcb5bf91d] Find the missing value on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xc87eedc76dcf9888
	// Question: Find the missing value on a number line
	// Widget key: image_1
	test("number-line - [xc87eedc76dcf9888] Find the missing value on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x0120f9f9574a64c2
	// Question: Find the missing value
	// Widget key: image_1
	test("number-line - [x0120f9f9574a64c2] Find the missing value", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x160604a98b8fb38c
	// Question: Find the missing value
	// Widget key: image_1
	test("number-line - [x160604a98b8fb38c] Find the missing value", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x8e53cd0658445816
	// Question: Find the missing value using a number line
	// Widget key: image_1
	test("number-line - [x8e53cd0658445816] Find the missing value using a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x1cbe88cf7d63a3c5
	// Question: Find the missing value
	// Widget key: image_1
	test("number-line - [x1cbe88cf7d63a3c5] Find the missing value", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x7b189f09cb6c89d1
	// Question: Find the missing value on a number line
	// Widget key: image_1
	test("number-line - [x7b189f09cb6c89d1] Find the missing value on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xbd3b34293fb591a2
	// Question: Find the missing value on a number line
	// Widget key: image_1
	test("number-line - [xbd3b34293fb591a2] Find the missing value on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x146cb2ef734d4012
	// Question: Find the missing value
	// Widget key: image_1
	test("number-line - [x146cb2ef734d4012] Find the missing value", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xa88752a37218add6
	// Question: Find the missing value using a number line
	// Widget key: image_1
	test("number-line - [xa88752a37218add6] Find the missing value using a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xf77b08ed8d926109
	// Question: Find the missing value
	// Widget key: image_1
	test("number-line - [xf77b08ed8d926109] Find the missing value", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x54b1b23af62ae203
	// Question: Find the missing value on a number line
	// Widget key: image_1
	test("number-line - [x54b1b23af62ae203] Find the missing value on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x195497fa81db1cd0
	// Question: Find the missing value on a number line
	// Widget key: image_1
	test("number-line - [x195497fa81db1cd0] Find the missing value on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xfec8bc26b6ada198
	// Question: Find the missing value using a number line
	// Widget key: image_1
	test("number-line - [xfec8bc26b6ada198] Find the missing value using a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xa39914d9a6b44171
	// Question: Find the missing value using a number line
	// Widget key: image_1
	test("number-line - [xa39914d9a6b44171] Find the missing value using a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x5bf297396596bf9b
	// Question: Find the missing value
	// Widget key: image_1
	test("number-line - [x5bf297396596bf9b] Find the missing value", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x6924bf3af0c393ef
	// Question: Find the missing value
	// Widget key: image_1
	test("number-line - [x6924bf3af0c393ef] Find the missing value", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x8abaa6aa369e1766
	// Question: Find the missing value using a number line
	// Widget key: image_1
	test("number-line - [x8abaa6aa369e1766] Find the missing value using a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xda54389caa4f69ac
	// Question: Find the missing value on a number line
	// Widget key: image_1
	test("number-line - [xda54389caa4f69ac] Find the missing value on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xb8ca93d297136904
	// Question: Find the missing value
	// Widget key: image_1
	test("number-line - [xb8ca93d297136904] Find the missing value", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x57b58e02397b35fd
	// Question: Find the missing value on a number line
	// Widget key: image_1
	test("number-line - [x57b58e02397b35fd] Find the missing value on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 15,
		  "min": -15,
		  "type": "numberLine",
		  "width": 380,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x24f798bd
	// Question: Opposite of -3 on a number line
	// Widget key: image_1
	test("number-line - [x24f798bd] Opposite of -3 on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 7,
		  "min": -7,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -7
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -2
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": 3
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": 4
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>E</mi>",
		      "position": 7
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x16fab81c
	// Question: Identify the opposite on a number line
	// Widget key: image_1
	test("number-line - [x16fab81c] Identify the opposite on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -5
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -4
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": -2
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": 2
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>E</mi>",
		      "position": 5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x1b944977
	// Question: Identify the opposite of -1 on a number line
	// Widget key: image_1
	test("number-line - [x1b944977] Identify the opposite of -1 on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 7,
		  "min": -7,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -6
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -3
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": -1
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": 1
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>E</mi>",
		      "position": 5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x81ab69357992c427
	// Question: Graph the opposite of 4 on a number line
	// Widget key: choice_a_number_line
	test("number-line - [x81ab69357992c427] Graph the opposite of 4 on a number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 6,
		  "min": -6,
		  "type": "numberLine",
		  "width": 500,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#0066CC",
		      "style": "dot",
		      "value": 4,
		      "position": -4
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x81ab69357992c427
	// Question: Graph the opposite of 4 on a number line
	// Widget key: choice_b_number_line
	test("number-line - [x81ab69357992c427] Graph the opposite of 4 on a number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 6,
		  "min": -6,
		  "type": "numberLine",
		  "width": 500,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#0066CC",
		      "style": "dot",
		      "value": 4,
		      "position": 4
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x81ab69357992c427
	// Question: Graph the opposite of 4 on a number line
	// Widget key: choice_c_number_line
	test("number-line - [x81ab69357992c427] Graph the opposite of 4 on a number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 6,
		  "min": -6,
		  "type": "numberLine",
		  "width": 500,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#0066CC",
		      "style": "dot",
		      "value": 2,
		      "position": -2
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xcb3e37851559832d
	// Question: Graph the opposite of 1.5 on the number line
	// Widget key: nl_choice_a
	test("number-line - [xcb3e37851559832d] Graph the opposite of 1.5 on the number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1.5,
		  "min": -1.5,
		  "type": "numberLine",
		  "width": 500,
		  "height": 200,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 0.5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#0066CC",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mn>1.5</mn>",
		      "position": -1.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xcb3e37851559832d
	// Question: Graph the opposite of 1.5 on the number line
	// Widget key: nl_choice_b
	test("number-line - [xcb3e37851559832d] Graph the opposite of 1.5 on the number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1.5,
		  "min": -1.5,
		  "type": "numberLine",
		  "width": 500,
		  "height": 200,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 0.5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#D7263D",
		      "style": "dot",
		      "mathml": "<mn>1.5</mn>",
		      "position": 1.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xcb3e37851559832d
	// Question: Graph the opposite of 1.5 on the number line
	// Widget key: nl_choice_c
	test("number-line - [xcb3e37851559832d] Graph the opposite of 1.5 on the number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1.5,
		  "min": -1.5,
		  "type": "numberLine",
		  "width": 500,
		  "height": 200,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 0.5
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#FF9900",
		      "style": "dot",
		      "mathml": "<mo>-</mo><mn>0.5</mn>",
		      "position": -0.5
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x7da07235
	// Question: Identify the opposite of a number on a number line
	// Widget key: image_1
	test("number-line - [x7da07235] Identify the opposite of a number on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 7,
		  "min": -7,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -7
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -2
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": 0
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": 2
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>E</mi>",
		      "position": 7
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xd1cb5115
	// Question: Identify the opposite of zero on a number line
	// Widget key: image_1
	test("number-line - [xd1cb5115] Identify the opposite of zero on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 10,
		  "min": -5,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -5
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -2
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": 0
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": 2
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>E</mi>",
		      "position": 7
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x10a2191e
	// Question: Opposite of 7 on a number line
	// Widget key: image_1
	test("number-line - [x10a2191e] Opposite of 7 on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 7,
		  "min": -7,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -7
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -4
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": 0
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": 3
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>E</mi>",
		      "position": 7
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xb15dcef5fbe05c37
	// Question: Graph the opposite of a fraction on a number line
	// Widget key: nl_choice_a
	test("number-line - [xb15dcef5fbe05c37] Graph the opposite of a fraction on a number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1,
		  "min": -1,
		  "type": "numberLine",
		  "width": 500,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 3
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "fraction",
		      "color": "#0066CC",
		      "style": "dot",
		      "position": -0.6666666667,
		      "numerator": 2,
		      "denominator": 3
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 6
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xb15dcef5fbe05c37
	// Question: Graph the opposite of a fraction on a number line
	// Widget key: nl_choice_b
	test("number-line - [xb15dcef5fbe05c37] Graph the opposite of a fraction on a number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1,
		  "min": -1,
		  "type": "numberLine",
		  "width": 500,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 3
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "+",
		      "type": "fraction",
		      "color": "#0066CC",
		      "style": "dot",
		      "position": 0.6666666667,
		      "numerator": 2,
		      "denominator": 3
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 6
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xb15dcef5fbe05c37
	// Question: Graph the opposite of a fraction on a number line
	// Widget key: nl_choice_c
	test("number-line - [xb15dcef5fbe05c37] Graph the opposite of a fraction on a number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1,
		  "min": -1,
		  "type": "numberLine",
		  "width": 500,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 3
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "fraction",
		      "color": "#0066CC",
		      "style": "dot",
		      "position": -0.3333333333,
		      "numerator": 1,
		      "denominator": 3
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 6
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xb15dcef5fbe05c37
	// Question: Graph the opposite of a fraction on a number line
	// Widget key: nl_stimulus
	test("number-line - [xb15dcef5fbe05c37] Graph the opposite of a fraction on a number line (4)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1,
		  "min": -1,
		  "type": "numberLine",
		  "width": 500,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "fraction",
		    "denominator": 3
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 6
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x8f7d9f5cd0ea9740
	// Question: Graph the opposite of −0.4 on a number line
	// Widget key: choice_a_numberline
	test("number-line - [x8f7d9f5cd0ea9740] Graph the opposite of −0.4 on a number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 500,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#0066CC",
		      "style": "dot",
		      "mathml": "<mrow></mrow>",
		      "position": -0.4
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 5
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x8f7d9f5cd0ea9740
	// Question: Graph the opposite of −0.4 on a number line
	// Widget key: choice_b_numberline
	test("number-line - [x8f7d9f5cd0ea9740] Graph the opposite of −0.4 on a number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 500,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#0066CC",
		      "style": "dot",
		      "mathml": "<mrow></mrow>",
		      "position": 0.4
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 5
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x8f7d9f5cd0ea9740
	// Question: Graph the opposite of −0.4 on a number line
	// Widget key: choice_c_numberline
	test("number-line - [x8f7d9f5cd0ea9740] Graph the opposite of −0.4 on a number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 2,
		  "min": -2,
		  "type": "numberLine",
		  "width": 500,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#0066CC",
		      "style": "dot",
		      "mathml": "<mrow></mrow>",
		      "position": 0.2
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 5
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x8d5fa3f4
	// Question: Identify the opposite of 1 on a number line
	// Widget key: image_1
	test("number-line - [x8d5fa3f4] Identify the opposite of 1 on a number line", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 7,
		  "min": -7,
		  "type": "numberLine",
		  "width": 460,
		  "height": 80,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": false,
		  "highlightedPoints": [
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>A</mi>",
		      "position": -6
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>B</mi>",
		      "position": -4
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>C</mi>",
		      "position": -1
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>D</mi>",
		      "position": 1
		    },
		    {
		      "type": "mathml",
		      "color": "#000000",
		      "style": "dot",
		      "mathml": "<mi>E</mi>",
		      "position": 4
		    }
		  ],
		  "secondaryTickInterval": null
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x8544017494eef924
	// Question: Graph the opposite of -7 on the number line
	// Widget key: nl_choice_a
	test("number-line - [x8544017494eef924] Graph the opposite of -7 on the number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 9,
		  "min": -9,
		  "type": "numberLine",
		  "width": 500,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#0066CC",
		      "style": "dot",
		      "value": 7,
		      "position": 7
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x8544017494eef924
	// Question: Graph the opposite of -7 on the number line
	// Widget key: nl_choice_b
	test("number-line - [x8544017494eef924] Graph the opposite of -7 on the number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 9,
		  "min": -9,
		  "type": "numberLine",
		  "width": 500,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "whole",
		      "color": "#0066CC",
		      "style": "dot",
		      "value": 7,
		      "position": -7
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x8544017494eef924
	// Question: Graph the opposite of -7 on the number line
	// Widget key: nl_choice_c
	test("number-line - [x8544017494eef924] Graph the opposite of -7 on the number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 9,
		  "min": -9,
		  "type": "numberLine",
		  "width": 500,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "+",
		      "type": "whole",
		      "color": "#0066CC",
		      "style": "dot",
		      "value": 0,
		      "position": 0
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x8544017494eef924
	// Question: Graph the opposite of -7 on the number line
	// Widget key: number_line_initial
	test("number-line - [x8544017494eef924] Graph the opposite of -7 on the number line (4)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 9,
		  "min": -9,
		  "type": "numberLine",
		  "width": 500,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 2
		  },
		  "showTickLabels": true,
		  "highlightedPoints": null,
		  "secondaryTickInterval": {
		    "type": "whole",
		    "interval": 1
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x6f4dd57345731d22
	// Question: Opposite of -3/4 on a number line
	// Widget key: nl_choice_a
	test("number-line - [x6f4dd57345731d22] Opposite of -3/4 on a number line (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1,
		  "min": -1,
		  "type": "numberLine",
		  "width": 520,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "+",
		      "type": "fraction",
		      "color": "#0066CC",
		      "style": "dot",
		      "position": 0.75,
		      "numerator": 3,
		      "denominator": 4
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 8
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x6f4dd57345731d22
	// Question: Opposite of -3/4 on a number line
	// Widget key: nl_choice_b
	test("number-line - [x6f4dd57345731d22] Opposite of -3/4 on a number line (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1,
		  "min": -1,
		  "type": "numberLine",
		  "width": 520,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "-",
		      "type": "fraction",
		      "color": "#CC0000",
		      "style": "dot",
		      "position": -0.75,
		      "numerator": 3,
		      "denominator": 4
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 8
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x6f4dd57345731d22
	// Question: Opposite of -3/4 on a number line
	// Widget key: nl_choice_c
	test("number-line - [x6f4dd57345731d22] Opposite of -3/4 on a number line (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "max": 1,
		  "min": -1,
		  "type": "numberLine",
		  "width": 520,
		  "height": 180,
		  "orientation": "horizontal",
		  "tickInterval": {
		    "type": "whole",
		    "interval": 1
		  },
		  "showTickLabels": true,
		  "highlightedPoints": [
		    {
		      "sign": "+",
		      "type": "fraction",
		      "color": "#00AA00",
		      "style": "dot",
		      "position": 0.25,
		      "numerator": 1,
		      "denominator": 4
		    }
		  ],
		  "secondaryTickInterval": {
		    "type": "fraction",
		    "denominator": 8
		  }
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for numberLine", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

})
