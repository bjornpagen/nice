// ============================================================================
// EXTRACTED TEST FILE - AUTO-GENERATED
// ============================================================================
// This file was automatically generated from database structured_json data
// Generated on: 2025-09-17T15:38:03.955Z
// Widget Type: circleDiagram
// Source: Real questions from database
//
// These tests use actual widget configurations extracted from production data
// to ensure comprehensive test coverage with realistic scenarios.
//
// Note: Database data may not perfectly match current widget schemas, so these
// tests rely on runtime validation within generateWidget() for flexibility.
//
// DO NOT EDIT THIS FILE DIRECTLY - it will be overwritten on regeneration
//
// Run tests: bun test tests/widgets/circle-diagram.extracted.test.ts
// Update snapshots: bun test tests/widgets/circle-diagram.extracted.test.ts --update-snapshots
// ============================================================================

import { describe, test, expect } from "bun:test"
import { generateWidget } from "@superbuilders/qti-assessment-item-generator/widgets/widget-generator"
import type { Widget, WidgetInput } from "@superbuilders/qti-assessment-item-generator/widgets/registry"
import * as errors from "@superbuilders/errors"
import * as logger from "@superbuilders/slog"

describe("circleDiagram widget tests", () => {
	// Extracted from question: x668e4cac740758aa
	// Question: Find the radius and diameter from a circle diagram
	// Widget key: image_1
	test("circle-diagram - [x668e4cac740758aa] Find the radius and diameter from a circle diagram", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 170,
		  "height": 170,
		  "radius": 83.3,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 120,
		      "color": "#000000",
		      "label": "11 cm"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": false,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xa0eeb7bd2322116f
	// Question: Find the radius and diameter from a circle diagram
	// Widget key: image_1
	test("circle-diagram - [xa0eeb7bd2322116f] Find the radius and diameter from a circle diagram", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 170,
		  "height": 170,
		  "radius": 83.3,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 0,
		      "color": "#000000",
		      "label": "16 cm"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": false,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x9f9c3d6478f7df81
	// Question: Find the radius and diameter from a circle diagram
	// Widget key: image_1
	test("circle-diagram - [x9f9c3d6478f7df81] Find the radius and diameter from a circle diagram", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 170,
		  "height": 170,
		  "radius": 83.3,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "diameter",
		      "angle": 300,
		      "color": "#000000",
		      "label": "14 cm"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": false,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x2e4a863556ee3a48
	// Question: Find the radius and diameter of a circle
	// Widget key: image_1
	test("circle-diagram - [x2e4a863556ee3a48] Find the radius and diameter of a circle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 170,
		  "height": 170,
		  "radius": 83,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "diameter",
		      "angle": 330,
		      "color": "#000000",
		      "label": "13 cm"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": false,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xa783b879572143fb
	// Question: Find the radius and diameter from a circle diagram
	// Widget key: image_1
	test("circle-diagram - [xa783b879572143fb] Find the radius and diameter from a circle diagram", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 170,
		  "height": 170,
		  "radius": 83,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 315,
		      "color": "#000000",
		      "label": "5.1 cm"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#00000000",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": false,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xd7efb5192670f8fe
	// Question: Find the radius and diameter of a circle from its diagram
	// Widget key: image_1
	test("circle-diagram - [xd7efb5192670f8fe] Find the radius and diameter of a circle from its diagram", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 170,
		  "height": 170,
		  "radius": 80,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "diameter",
		      "angle": 45,
		      "color": "#000000",
		      "label": "18 cm"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": false,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x1ada60937bc4b46e
	// Question: Find the radius and diameter of a circle
	// Widget key: image_1
	test("circle-diagram - [x1ada60937bc4b46e] Find the radius and diameter of a circle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 170,
		  "height": 170,
		  "radius": 80,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 340,
		      "color": "#000000",
		      "label": "4.2 cm"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": false,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x3b0d8a88a8709bdb
	// Question: Find the radius and diameter of a circle
	// Widget key: image_1
	test("circle-diagram - [x3b0d8a88a8709bdb] Find the radius and diameter of a circle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 170,
		  "height": 170,
		  "radius": 83.3,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "diameter",
		      "angle": 0,
		      "color": "#000000",
		      "label": "24 cm"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": false,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x005621cbe172c70c
	// Question: Find the radius and diameter from a circle diagram
	// Widget key: image_1
	test("circle-diagram - [x005621cbe172c70c] Find the radius and diameter from a circle diagram", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 170,
		  "height": 170,
		  "radius": 83.3,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "diameter",
		      "angle": 90,
		      "color": "#000000",
		      "label": "7 cm"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": false,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xa48d750ab9463b6f
	// Question: Find the radius and diameter of a circle from a diagram
	// Widget key: image_1
	test("circle-diagram - [xa48d750ab9463b6f] Find the radius and diameter of a circle from a diagram", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 170,
		  "height": 170,
		  "radius": 80,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 210,
		      "color": "#000000",
		      "label": "7.5 cm"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": false,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xae55af45ef9cf0ab
	// Question: Find the area of a circle from its diameter
	// Widget key: image_1
	test("circle-diagram - [xae55af45ef9cf0ab] Find the area of a circle from its diameter", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 220,
		  "height": 220,
		  "radius": 100,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "diameter",
		      "angle": 0,
		      "color": "#008000",
		      "label": "d = 14"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": false,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xed323a04c967f420
	// Question: Find the area of a circle from its diameter
	// Widget key: image_1
	test("circle-diagram - [xed323a04c967f420] Find the area of a circle from its diameter", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 220,
		  "height": 220,
		  "radius": 100,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "diameter",
		      "angle": 0,
		      "color": "#008000",
		      "label": "d equals 8"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": false,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x05f1795ae20082e6
	// Question: Find the area of a circle from its diameter
	// Widget key: image_1
	test("circle-diagram - [x05f1795ae20082e6] Find the area of a circle from its diameter", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 220,
		  "height": 220,
		  "radius": 100,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "diameter",
		      "angle": 0,
		      "color": "#008000",
		      "label": "d = 10"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": false,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x5f572f10a5ca3338
	// Question: Find the area of a circle from its radius
	// Widget key: image_1
	test("circle-diagram - [x5f572f10a5ca3338] Find the area of a circle from its radius", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 220,
		  "height": 220,
		  "radius": 100,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 0,
		      "color": "#800080",
		      "label": "r = 5"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": false,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x73c00848ad40a6b5
	// Question: Area of a circle (radius 1)
	// Widget key: image_1
	test("circle-diagram - [x73c00848ad40a6b5] Area of a circle (radius 1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 220,
		  "height": 220,
		  "radius": 100,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 0,
		      "color": "#800080",
		      "label": "r = 1"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": true,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xca2baccc2058526e
	// Question: Area of a circle from its diameter
	// Widget key: image_1
	test("circle-diagram - [xca2baccc2058526e] Area of a circle from its diameter", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 220,
		  "height": 220,
		  "radius": 100,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "diameter",
		      "angle": 0,
		      "color": "#008000",
		      "label": "d = 2"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": false,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xc64648bad5943f42
	// Question: Area of a circle (radius 4)
	// Widget key: image_1
	test("circle-diagram - [xc64648bad5943f42] Area of a circle (radius 4)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 220,
		  "height": 220,
		  "radius": 100,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 0,
		      "color": "#800080",
		      "label": "r = 4"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": false,
		  "annulusFillColor": "#00000000"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xc1724554ffcf9c9c
	// Question: Area of a circle
	// Widget key: image_1
	test("circle-diagram - [xc1724554ffcf9c9c] Area of a circle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 220,
		  "height": 220,
		  "radius": 100,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 0,
		      "color": "#800080",
		      "label": "r = 7"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": false,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x84b0743b00f1f451
	// Question: Area of a shaded region between two circles
	// Widget key: image_1
	test("circle-diagram - [x84b0743b00f1f451] Area of a shaded region between two circles", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 360,
		  "height": 360,
		  "radius": 165,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 0,
		      "color": "#11accd",
		      "label": "11 cm"
		    },
		    {
		      "type": "innerRadius",
		      "angle": 90,
		      "color": "#1fab54",
		      "label": "9 cm"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#fdfdfd",
		  "innerRadius": 135,
		  "strokeColor": "#11accd",
		  "showCenterDot": false,
		  "annulusFillColor": "#ccfaff"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x5a00fc55a8fbaf5e
	// Question: Area of a shaded region between two circles
	// Widget key: image_1
	test("circle-diagram - [x5a00fc55a8fbaf5e] Area of a shaded region between two circles", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 300,
		  "height": 300,
		  "radius": 135,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 0,
		      "color": "#11accd",
		      "label": "9 cm"
		    },
		    {
		      "type": "innerRadius",
		      "angle": 90,
		      "color": "#1fab54",
		      "label": "6 cm"
		    }
		  ],
		  "areaLabel": null,
		  "fillColor": "#FFFFFF",
		  "innerRadius": 90,
		  "strokeColor": "#11accd",
		  "showCenterDot": false,
		  "annulusFillColor": "#ccfaff80"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x7361fa9bb84de799
	// Question: Area of a shaded annulus
	// Widget key: image_1
	test("circle-diagram - [x7361fa9bb84de799] Area of a shaded annulus", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 360,
		  "height": 360,
		  "radius": 165,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 0,
		      "color": "#11accd",
		      "label": "11 cm"
		    },
		    {
		      "type": "innerRadius",
		      "angle": 90,
		      "color": "#1fab54",
		      "label": "4 cm"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF",
		  "innerRadius": 60,
		  "strokeColor": "#11accd",
		  "showCenterDot": true,
		  "annulusFillColor": "rgba(204,250,255,0.5)"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x157245461ddce8b9
	// Question: Area of a shaded annulus
	// Widget key: image_1
	test("circle-diagram - [x157245461ddce8b9] Area of a shaded annulus", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 200,
		  "height": 200,
		  "radius": 80,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 0,
		      "color": "#11accd",
		      "label": " "
		    },
		    {
		      "type": "innerRadius",
		      "angle": 90,
		      "color": "#1fab54",
		      "label": " "
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": 40,
		  "strokeColor": "#11accd",
		  "showCenterDot": false,
		  "annulusFillColor": "rgba(204, 250, 255, 0.5)"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x7893c043131fd6d6
	// Question: Area of a shaded region: circle minus rectangle
	// Widget key: image_1
	test("circle-diagram - [x7893c043131fd6d6] Area of a shaded region: circle minus rectangle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 360,
		  "height": 360,
		  "radius": 160,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 0,
		      "color": "#11accd",
		      "label": "8 cm"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#ccfaff80",
		  "innerRadius": null,
		  "strokeColor": "#11accd",
		  "showCenterDot": false,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x16aff43aff976000
	// Question: Area of the shaded region (circle and rectangle)
	// Widget key: image_1
	test("circle-diagram - [x16aff43aff976000] Area of the shaded region (circle and rectangle)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 260,
		  "height": 260,
		  "radius": 120,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 0,
		      "color": "#11accd",
		      "label": "12 cm"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#ccfaff",
		  "innerRadius": null,
		  "strokeColor": "#11accd",
		  "showCenterDot": true,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x589d76b058f6795c
	// Question: Area of the shaded region between two circles
	// Widget key: image_1
	test("circle-diagram - [x589d76b058f6795c] Area of the shaded region between two circles", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 240,
		  "height": 240,
		  "radius": 105,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "innerRadius",
		      "angle": 90,
		      "color": "#1fab54",
		      "label": " "
		    },
		    {
		      "type": "radius",
		      "angle": 0,
		      "color": "#11accd",
		      "label": " "
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FDFDFD",
		  "innerRadius": 45,
		  "strokeColor": "#11accd",
		  "showCenterDot": false,
		  "annulusFillColor": "rgba(204,250,255,0.5)"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x8d3bac83c8697585
	// Question: Area of the shaded region between two concentric circles
	// Widget key: image_1
	test("circle-diagram - [x8d3bac83c8697585] Area of the shaded region between two concentric circles", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 360,
		  "height": 360,
		  "radius": 165,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 0,
		      "color": "#11accd",
		      "label": " "
		    },
		    {
		      "type": "innerRadius",
		      "angle": 90,
		      "color": "#1fab54",
		      "label": " "
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF",
		  "innerRadius": 45,
		  "strokeColor": "#11accd",
		  "showCenterDot": false,
		  "annulusFillColor": "#CCFAFF"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xd95b713b3e289911
	// Question: Area of a shaded annulus
	// Widget key: image_1
	test("circle-diagram - [xd95b713b3e289911] Area of a shaded annulus", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 240,
		  "height": 240,
		  "radius": 100,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 0,
		      "color": "#11accd",
		      "label": " "
		    },
		    {
		      "type": "innerRadius",
		      "angle": 90,
		      "color": "#1fab54",
		      "label": " "
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": 60,
		  "strokeColor": "#11accd",
		  "showCenterDot": false,
		  "annulusFillColor": "#ccfaff"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x99e1ab9c341689ea
	// Question: Arc length of a semicircle
	// Widget key: image_1
	test("circle-diagram - [x99e1ab9c341689ea] Arc length of a semicircle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [
		    {
		      "label": "?",
		      "endAngle": 180,
		      "startAngle": 0,
		      "strokeColor": "#29abca"
		    }
		  ],
		  "type": "circleDiagram",
		  "shape": "semicircle",
		  "width": 250,
		  "height": 163,
		  "radius": 100,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "diameter",
		      "angle": 0,
		      "color": "#e65a4c",
		      "label": "8"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#00000000",
		  "showCenterDot": true,
		  "annulusFillColor": "#00000000"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xe4a65d7f45d5c2da
	// Question: Find the arc length of a semicircle
	// Widget key: image_1
	test("circle-diagram - [xe4a65d7f45d5c2da] Find the arc length of a semicircle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [
		    {
		      "label": "?",
		      "endAngle": 270,
		      "startAngle": 90,
		      "strokeColor": "#29abca"
		    }
		  ],
		  "type": "circleDiagram",
		  "shape": "semicircle",
		  "width": 150,
		  "height": 250,
		  "radius": 100,
		  "sectors": [],
		  "rotation": 90,
		  "segments": [
		    {
		      "type": "diameter",
		      "angle": 90,
		      "color": "#000000",
		      "label": " "
		    },
		    {
		      "type": "radius",
		      "angle": 90,
		      "color": "#000000",
		      "label": "5"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": true,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xebb1e43c6e6056f7
	// Question: Find the arc length of a partial circle
	// Widget key: image_1
	test("circle-diagram - [xebb1e43c6e6056f7] Find the arc length of a partial circle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [
		    {
		      "label": "?",
		      "endAngle": 360,
		      "startAngle": 90,
		      "strokeColor": "#29abca"
		    }
		  ],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 250,
		  "height": 250,
		  "radius": 100,
		  "sectors": [
		    {
		      "label": " ",
		      "endAngle": 90,
		      "fillColor": "#FFFFFF00",
		      "startAngle": 0,
		      "showRightAngleMarker": true
		    }
		  ],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 0,
		      "color": "#000000",
		      "label": "4"
		    },
		    {
		      "type": "radius",
		      "angle": 90,
		      "color": "#e65a4c",
		      "label": " "
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#FFFFFF00",
		  "showCenterDot": true,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x4f8379d5d34b8a03
	// Question: Find the arc length of a semicircle
	// Widget key: image_1
	test("circle-diagram - [x4f8379d5d34b8a03] Find the arc length of a semicircle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [
		    {
		      "label": "?",
		      "endAngle": 270,
		      "startAngle": 90,
		      "strokeColor": "#29abca"
		    }
		  ],
		  "type": "circleDiagram",
		  "shape": "semicircle",
		  "width": 300,
		  "height": 300,
		  "radius": 120,
		  "sectors": [],
		  "rotation": 270,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 90,
		      "color": "#000000",
		      "label": "9"
		    },
		    {
		      "type": "radius",
		      "angle": 270,
		      "color": "#000000",
		      "label": " "
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": true,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xe22a7c3ae17893d1
	// Question: Find the arc length of a partial circle
	// Widget key: image_1
	test("circle-diagram - [xe22a7c3ae17893d1] Find the arc length of a partial circle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [
		    {
		      "label": "?",
		      "endAngle": 270,
		      "startAngle": 0,
		      "strokeColor": "#29abca"
		    }
		  ],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 250,
		  "height": 250,
		  "radius": 100,
		  "sectors": [
		    {
		      "label": " ",
		      "endAngle": 90,
		      "fillColor": "#FFFFFF00",
		      "startAngle": 0,
		      "showRightAngleMarker": true
		    }
		  ],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 0,
		      "color": "#e65a4c",
		      "label": "2"
		    },
		    {
		      "type": "radius",
		      "angle": 90,
		      "color": "#000000",
		      "label": " "
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#00000000",
		  "showCenterDot": true,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xb6a6310a58f2acf8
	// Question: Arc length of three-fourths of a circle
	// Widget key: image_1
	test("circle-diagram - [xb6a6310a58f2acf8] Arc length of three-fourths of a circle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [
		    {
		      "label": " ",
		      "endAngle": 360,
		      "startAngle": 180,
		      "strokeColor": "#29abca"
		    },
		    {
		      "label": "?",
		      "endAngle": 90,
		      "startAngle": 0,
		      "strokeColor": "#29abca"
		    }
		  ],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 250,
		  "height": 250,
		  "radius": 100,
		  "sectors": [
		    {
		      "label": " ",
		      "endAngle": 180,
		      "fillColor": "#FFFFFF00",
		      "startAngle": 90,
		      "showRightAngleMarker": true
		    }
		  ],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 90,
		      "color": "#000000",
		      "label": "5"
		    },
		    {
		      "type": "radius",
		      "angle": 180,
		      "color": "#e65a4c",
		      "label": " "
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#00000000",
		  "showCenterDot": true,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xc657b133d05c8969
	// Question: Arc length of a semicircle
	// Widget key: image_1
	test("circle-diagram - [xc657b133d05c8969] Arc length of a semicircle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [
		    {
		      "label": "?",
		      "endAngle": 180,
		      "startAngle": 0,
		      "strokeColor": "#29abca"
		    }
		  ],
		  "type": "circleDiagram",
		  "shape": "semicircle",
		  "width": 250,
		  "height": 163,
		  "radius": 100,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "diameter",
		      "angle": 0,
		      "color": "#e65a4c",
		      "label": "10"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF",
		  "innerRadius": null,
		  "strokeColor": "#29abca",
		  "showCenterDot": true,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x75c946a78ffe0fb0
	// Question: Find the arc length of a semicircle
	// Widget key: image_1
	test("circle-diagram - [x75c946a78ffe0fb0] Find the arc length of a semicircle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [
		    {
		      "label": "?",
		      "endAngle": 270,
		      "startAngle": 90,
		      "strokeColor": "#29abca"
		    }
		  ],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 320,
		  "height": 320,
		  "radius": 120,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "diameter",
		      "angle": 90,
		      "color": "#e65a4c",
		      "label": "12"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": true,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xbe5b87ef9dba93e2
	// Question: Arc length of a quarter circle
	// Widget key: image_1
	test("circle-diagram - [xbe5b87ef9dba93e2] Arc length of a quarter circle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [
		    {
		      "label": "?",
		      "endAngle": 90,
		      "startAngle": 0,
		      "strokeColor": "#29abca"
		    }
		  ],
		  "type": "circleDiagram",
		  "shape": "quarter-circle",
		  "width": 250,
		  "height": 250,
		  "radius": 100,
		  "sectors": [
		    {
		      "label": " ",
		      "endAngle": 90,
		      "fillColor": "#FFFFFF",
		      "startAngle": 0,
		      "showRightAngleMarker": true
		    }
		  ],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 0,
		      "color": "#000000",
		      "label": "8"
		    },
		    {
		      "type": "radius",
		      "angle": 90,
		      "color": "#000000",
		      "label": " "
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": true,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xd3cba57e7152980b
	// Question: Arc length of a partial circle
	// Widget key: image_1
	test("circle-diagram - [xd3cba57e7152980b] Arc length of a partial circle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [
		    {
		      "label": "?",
		      "endAngle": 270,
		      "startAngle": 180,
		      "strokeColor": "#29abca"
		    }
		  ],
		  "type": "circleDiagram",
		  "shape": "quarter-circle",
		  "width": 250,
		  "height": 250,
		  "radius": 100,
		  "sectors": [
		    {
		      "label": null,
		      "endAngle": 270,
		      "fillColor": "#FFFFFF00",
		      "startAngle": 180,
		      "showRightAngleMarker": true
		    }
		  ],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 180,
		      "color": "#000000",
		      "label": "7"
		    },
		    {
		      "type": "radius",
		      "angle": 270,
		      "color": "#000000",
		      "label": null
		    }
		  ],
		  "areaLabel": null,
		  "fillColor": "#FFFFFF",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": true,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xa6f9aa59e3004932
	// Question: Find the arc length of a semicircle
	// Widget key: image_1
	test("circle-diagram - [xa6f9aa59e3004932] Find the arc length of a semicircle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [
		    {
		      "label": "?",
		      "endAngle": 180,
		      "startAngle": 0,
		      "strokeColor": "#29abca"
		    }
		  ],
		  "type": "circleDiagram",
		  "shape": "semicircle",
		  "width": 250,
		  "height": 150,
		  "radius": 100,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 0,
		      "color": "#000000",
		      "label": "11"
		    },
		    {
		      "type": "radius",
		      "angle": 180,
		      "color": "#000000",
		      "label": " "
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": true,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x012ab26fce14c204
	// Question: Arc length of a quarter circle
	// Widget key: image_1
	test("circle-diagram - [x012ab26fce14c204] Arc length of a quarter circle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [
		    {
		      "label": "?",
		      "endAngle": 180,
		      "startAngle": 90,
		      "strokeColor": "#29abca"
		    }
		  ],
		  "type": "circleDiagram",
		  "shape": "quarter-circle",
		  "width": 300,
		  "height": 300,
		  "radius": 120,
		  "sectors": [
		    {
		      "label": null,
		      "endAngle": 180,
		      "fillColor": "#FFFFFF00",
		      "startAngle": 90,
		      "showRightAngleMarker": true
		    }
		  ],
		  "rotation": 90,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 180,
		      "color": "#000000",
		      "label": "1"
		    },
		    {
		      "type": "radius",
		      "angle": 90,
		      "color": "#000000",
		      "label": null
		    }
		  ],
		  "areaLabel": null,
		  "fillColor": "#FFFFFF",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": true,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xd10ccb78f63d32da
	// Question: Find the area of a semicircle (radius 4)
	// Widget key: image_1
	test("circle-diagram - [xd10ccb78f63d32da] Find the area of a semicircle (radius 4)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "semicircle",
		  "width": 250,
		  "height": 175,
		  "radius": 100,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "diameter",
		      "angle": 0,
		      "color": "#e65a4c",
		      "label": " "
		    },
		    {
		      "type": "radius",
		      "angle": 0,
		      "color": "#000000",
		      "label": "4"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": true,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xab2d6f389a133f2a
	// Question: Find the area of a semicircle
	// Widget key: image_1
	test("circle-diagram - [xab2d6f389a133f2a] Find the area of a semicircle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "semicircle",
		  "width": 250,
		  "height": 175,
		  "radius": 100,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "diameter",
		      "angle": 0,
		      "color": "#000000",
		      "label": " "
		    },
		    {
		      "type": "radius",
		      "angle": 0,
		      "color": "#e65a4c",
		      "label": "2"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": true,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x550d5723faebeb90
	// Question: Area of a quarter circle
	// Widget key: image_1
	test("circle-diagram - [x550d5723faebeb90] Area of a quarter circle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 250,
		  "height": 250,
		  "radius": 100,
		  "sectors": [
		    {
		      "label": null,
		      "endAngle": 270,
		      "fillColor": "#E8F4FD",
		      "startAngle": 180,
		      "showRightAngleMarker": true
		    }
		  ],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 270,
		      "color": "#000000",
		      "label": "10"
		    },
		    {
		      "type": "radius",
		      "angle": 180,
		      "color": "#000000",
		      "label": null
		    }
		  ],
		  "areaLabel": null,
		  "fillColor": "#FFFFFF",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": true,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x4ecc16c27339a3fe
	// Question: Find the area of a semicircle from its diameter
	// Widget key: image_1
	test("circle-diagram - [x4ecc16c27339a3fe] Find the area of a semicircle from its diameter", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "semicircle",
		  "width": 250,
		  "height": 250,
		  "radius": 100,
		  "sectors": [],
		  "rotation": 270,
		  "segments": [
		    {
		      "type": "diameter",
		      "angle": 90,
		      "color": "#e65a4c",
		      "label": "10"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": true,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xa877d3c25ea77c1c
	// Question: Find the area of a three-quarter circle
	// Widget key: image_1
	test("circle-diagram - [xa877d3c25ea77c1c] Find the area of a three-quarter circle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [
		    {
		      "label": " ",
		      "endAngle": 360,
		      "startAngle": 90,
		      "strokeColor": "#000000"
		    }
		  ],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 250,
		  "height": 250,
		  "radius": 100,
		  "sectors": [
		    {
		      "label": " ",
		      "endAngle": 90,
		      "fillColor": "#FFFFFF00",
		      "startAngle": 0,
		      "showRightAngleMarker": true
		    }
		  ],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 0,
		      "color": "#e65a4c",
		      "label": "6"
		    },
		    {
		      "type": "radius",
		      "angle": 90,
		      "color": "#000000",
		      "label": " "
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#FFFFFF00",
		  "showCenterDot": true,
		  "annulusFillColor": "#FFFFFF00"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xf5f42395039aab6b
	// Question: Area of three-fourths of a circle
	// Widget key: image_1
	test("circle-diagram - [xf5f42395039aab6b] Area of three-fourths of a circle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [
		    {
		      "label": " ",
		      "endAngle": 360,
		      "startAngle": 180,
		      "strokeColor": "#000000"
		    },
		    {
		      "label": " ",
		      "endAngle": 90,
		      "startAngle": 0,
		      "strokeColor": "#000000"
		    }
		  ],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 250,
		  "height": 250,
		  "radius": 100,
		  "sectors": [
		    {
		      "label": " ",
		      "endAngle": 360,
		      "fillColor": "#E8F4FD",
		      "startAngle": 180,
		      "showRightAngleMarker": false
		    },
		    {
		      "label": " ",
		      "endAngle": 90,
		      "fillColor": "#E8F4FD",
		      "startAngle": 0,
		      "showRightAngleMarker": false
		    },
		    {
		      "label": " ",
		      "endAngle": 180,
		      "fillColor": "#FFFFFF",
		      "startAngle": 90,
		      "showRightAngleMarker": true
		    }
		  ],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 180,
		      "color": "#e65a4c",
		      "label": "4"
		    },
		    {
		      "type": "radius",
		      "angle": 90,
		      "color": "#000000",
		      "label": "4"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": true,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x245cfd8b0a9759e0
	// Question: Find the area of a semicircle from its radius
	// Widget key: image_1
	test("circle-diagram - [x245cfd8b0a9759e0] Find the area of a semicircle from its radius", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "semicircle",
		  "width": 250,
		  "height": 250,
		  "radius": 100,
		  "sectors": [],
		  "rotation": 270,
		  "segments": [
		    {
		      "type": "diameter",
		      "angle": 90,
		      "color": "#000000",
		      "label": " "
		    },
		    {
		      "type": "radius",
		      "angle": 90,
		      "color": "#e65a4c",
		      "label": "6"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": true,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xad84dc6c9fa1c0e1
	// Question: Area of a semicircle
	// Widget key: image_1
	test("circle-diagram - [xad84dc6c9fa1c0e1] Area of a semicircle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "semicircle",
		  "width": 250,
		  "height": 250,
		  "radius": 100,
		  "sectors": [],
		  "rotation": 270,
		  "segments": [
		    {
		      "type": "diameter",
		      "angle": 180,
		      "color": "#e65a4c",
		      "label": "14"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": true,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x1de9ca02493bae17
	// Question: Find the area of a quarter circle
	// Widget key: image_1
	test("circle-diagram - [x1de9ca02493bae17] Find the area of a quarter circle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "quarter-circle",
		  "width": 250,
		  "height": 250,
		  "radius": 100,
		  "sectors": [],
		  "rotation": 90,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 180,
		      "color": "#000000",
		      "label": "8"
		    },
		    {
		      "type": "radius",
		      "angle": 90,
		      "color": "#000000",
		      "label": " "
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": true,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xcd23f9a85dfcd40b
	// Question: Find the area of three-fourths of a circle
	// Widget key: image_1
	test("circle-diagram - [xcd23f9a85dfcd40b] Find the area of three-fourths of a circle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [
		    {
		      "label": " ",
		      "endAngle": 270,
		      "startAngle": 0,
		      "strokeColor": "#000000"
		    }
		  ],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 250,
		  "height": 250,
		  "radius": 100,
		  "sectors": [
		    {
		      "label": " ",
		      "endAngle": 360,
		      "fillColor": "#FFFFFF00",
		      "startAngle": 270,
		      "showRightAngleMarker": true
		    }
		  ],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 0,
		      "color": "#e65a4c",
		      "label": "2"
		    },
		    {
		      "type": "radius",
		      "angle": 270,
		      "color": "#000000",
		      "label": " "
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": true,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x29a1937a05edd7a6
	// Question: Find the area of a semicircle from its diameter
	// Widget key: image_1
	test("circle-diagram - [x29a1937a05edd7a6] Find the area of a semicircle from its diameter", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "semicircle",
		  "width": 250,
		  "height": 175,
		  "radius": 100,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "diameter",
		      "angle": 0,
		      "color": "#e65a4c",
		      "label": "6"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": true,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x3bb3d5a926c6802c
	// Question: Area of a quarter circle
	// Widget key: image_1
	test("circle-diagram - [x3bb3d5a926c6802c] Area of a quarter circle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "quarter-circle",
		  "width": 250,
		  "height": 250,
		  "radius": 110,
		  "sectors": [
		    {
		      "label": " ",
		      "endAngle": 90,
		      "fillColor": "#FFFFFF00",
		      "startAngle": 0,
		      "showRightAngleMarker": true
		    }
		  ],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 0,
		      "color": "#e65a4c",
		      "label": "2"
		    },
		    {
		      "type": "radius",
		      "angle": 90,
		      "color": "#000000",
		      "label": " "
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": true,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xabe6ed366c0c0473
	// Question: Length of a rope around Earth
	// Widget key: image_1
	test("circle-diagram - [xabe6ed366c0c0473] Length of a rope around Earth", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 402,
		  "height": 402,
		  "radius": 150,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 0,
		      "color": "#11accd",
		      "label": "6371 km"
		    }
		  ],
		  "areaLabel": null,
		  "fillColor": "#FFFFFF",
		  "innerRadius": null,
		  "strokeColor": "#ff4d4d",
		  "showCenterDot": true,
		  "annulusFillColor": "#FFE9E9"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x4139736ff42aefd2
	// Question: Cross section of a block of butter
	// Widget key: choice_b_shape
	test("circle-diagram - [x4139736ff42aefd2] Cross section of a block of butter", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 120,
		  "height": 120,
		  "radius": 50,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [],
		  "areaLabel": null,
		  "fillColor": "#FFFFFF",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": false,
		  "annulusFillColor": ""
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x481f84d1c2ff8978
	// Question: Percent represented by the shaded area of a circle
	// Widget key: image_1
	test("circle-diagram - [x481f84d1c2ff8978] Percent represented by the shaded area of a circle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 320,
		  "height": 320,
		  "radius": 120,
		  "sectors": [
		    {
		      "label": " ",
		      "endAngle": 90,
		      "fillColor": "#87CEEB",
		      "startAngle": 0,
		      "showRightAngleMarker": false
		    },
		    {
		      "label": " ",
		      "endAngle": 180,
		      "fillColor": "#87CEEB",
		      "startAngle": 90,
		      "showRightAngleMarker": false
		    }
		  ],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 0,
		      "color": "#666666",
		      "label": " "
		    },
		    {
		      "type": "radius",
		      "angle": 90,
		      "color": "#666666",
		      "label": " "
		    },
		    {
		      "type": "radius",
		      "angle": 180,
		      "color": "#666666",
		      "label": " "
		    },
		    {
		      "type": "radius",
		      "angle": 270,
		      "color": "#666666",
		      "label": " "
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": true,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x65c4f90e3938e921
	// Question: What percent of the circle is shaded?
	// Widget key: image_1
	test("circle-diagram - [x65c4f90e3938e921] What percent of the circle is shaded?", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 126,
		  "height": 126,
		  "radius": 60,
		  "sectors": [
		    {
		      "label": null,
		      "endAngle": 90,
		      "fillColor": "#7854ab",
		      "startAngle": 0,
		      "showRightAngleMarker": false
		    }
		  ],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "diameter",
		      "angle": 0,
		      "color": "#7854ab",
		      "label": null
		    },
		    {
		      "type": "diameter",
		      "angle": 90,
		      "color": "#7854ab",
		      "label": null
		    }
		  ],
		  "areaLabel": null,
		  "fillColor": "#FFFFFF",
		  "innerRadius": null,
		  "strokeColor": "#7854ab",
		  "showCenterDot": false,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xa0183e5bd946dbda
	// Question: Scale drawing: cookie diameter
	// Widget key: choice_a_cookie
	test("circle-diagram - [xa0183e5bd946dbda] Scale drawing: cookie diameter (1)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 320,
		  "height": 320,
		  "radius": 120,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "diameter",
		      "angle": 0,
		      "color": "#000000",
		      "label": "12.5 units"
		    }
		  ],
		  "areaLabel": "cookie",
		  "fillColor": "#FFFFFF",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": true,
		  "annulusFillColor": "#FFE5B4"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xa0183e5bd946dbda
	// Question: Scale drawing: cookie diameter
	// Widget key: choice_b_cookie
	test("circle-diagram - [xa0183e5bd946dbda] Scale drawing: cookie diameter (2)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 320,
		  "height": 320,
		  "radius": 120,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "diameter",
		      "angle": 0,
		      "color": "#000000",
		      "label": "5 units"
		    }
		  ],
		  "areaLabel": "cookie",
		  "fillColor": "#FFFFFF",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": true,
		  "annulusFillColor": "#FFE5B4"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xa0183e5bd946dbda
	// Question: Scale drawing: cookie diameter
	// Widget key: choice_c_cookie
	test("circle-diagram - [xa0183e5bd946dbda] Scale drawing: cookie diameter (3)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 320,
		  "height": 320,
		  "radius": 120,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "diameter",
		      "angle": 0,
		      "color": "#000000",
		      "label": "2.5 units"
		    }
		  ],
		  "areaLabel": "cookie",
		  "fillColor": "#FFFFFF",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": true,
		  "annulusFillColor": "#FFE5B4"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x4b06d32f4a8d1e6d
	// Question: Similarity of circles via translation and dilation
	// Widget key: image_1
	test("circle-diagram - [x4b06d32f4a8d1e6d] Similarity of circles via translation and dilation", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 300,
		  "height": 300,
		  "radius": 112.5,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "innerRadius",
		      "angle": 315,
		      "color": "#0c7f99",
		      "label": "Q"
		    },
		    {
		      "type": "radius",
		      "angle": 45,
		      "color": "#ed5fa6",
		      "label": "S"
		    }
		  ],
		  "areaLabel": "R",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": 37.5,
		  "strokeColor": "#ed5fa6",
		  "showCenterDot": true,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x79fa0a69b5eafc99
	// Question: Find the circumference of a circle from its diameter
	// Widget key: image_1
	test("circle-diagram - [x79fa0a69b5eafc99] Find the circumference of a circle from its diameter", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 154,
		  "height": 154,
		  "radius": 70,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "diameter",
		      "angle": 0,
		      "color": "#1fab54",
		      "label": "9 cm"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": false,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xd4ffbcc37cacab47
	// Question: Find the circumference of a circle from its radius
	// Widget key: image_1
	test("circle-diagram - [xd4ffbcc37cacab47] Find the circumference of a circle from its radius", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 220,
		  "height": 220,
		  "radius": 100,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 0,
		      "color": "#800080",
		      "label": "r = 1"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": false,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x7da02a5b3b51cf7a
	// Question: Circumference of a circle (use 3.14 for π)
	// Widget key: image_1
	test("circle-diagram - [x7da02a5b3b51cf7a] Circumference of a circle (use 3.14 for π)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 154,
		  "height": 154,
		  "radius": 70,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "diameter",
		      "angle": 0,
		      "color": "#1fab54",
		      "label": "3 cm"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": false,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: x770c5bce6147b48a
	// Question: Circumference of a circle from its diameter
	// Widget key: image_1
	test("circle-diagram - [x770c5bce6147b48a] Circumference of a circle from its diameter", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 154,
		  "height": 154,
		  "radius": 70,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "diameter",
		      "angle": 0,
		      "color": "#1fab54",
		      "label": "4 cm"
		    }
		  ],
		  "areaLabel": null,
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": false,
		  "annulusFillColor": ""
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xa4c494e65cc6c162
	// Question: Circumference of a circle (use 3.14 for π)
	// Widget key: image_1
	test("circle-diagram - [xa4c494e65cc6c162] Circumference of a circle (use 3.14 for π)", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 220,
		  "height": 220,
		  "radius": 100,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 0,
		      "color": "#800080",
		      "label": "r = 5"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF00",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": false,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

	// Extracted from question: xf3d4405a2a083f5b
	// Question: Circumference of a circle
	// Widget key: image_1
	test("circle-diagram - [xf3d4405a2a083f5b] Circumference of a circle", async () => {
		// Note: Database data may not match current library schemas exactly
		// Using runtime validation in generateWidget for flexibility
		const input = {
		  "arcs": [],
		  "type": "circleDiagram",
		  "shape": "circle",
		  "width": 220,
		  "height": 220,
		  "radius": 100,
		  "sectors": [],
		  "rotation": 0,
		  "segments": [
		    {
		      "type": "radius",
		      "angle": 0,
		      "color": "#800080",
		      "label": "r = 7"
		    }
		  ],
		  "areaLabel": " ",
		  "fillColor": "#FFFFFF",
		  "innerRadius": null,
		  "strokeColor": "#000000",
		  "showCenterDot": false,
		  "annulusFillColor": "transparent"
		} as unknown as WidgetInput

		// Generate the widget (includes runtime validation)
		const result = await errors.try(generateWidget(input as unknown as Widget))
		if (result.error) {
			// If widget generation fails, it likely means the database data doesn't match current schema
			logger.error("widget generation failed for circleDiagram", { 
				error: result.error,
				inputData: input
			})
			throw errors.wrap(result.error, "widget generation")
		}
		
		const svg = result.data
		expect(svg).toMatchSnapshot()
	})

})
